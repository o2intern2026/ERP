# Merge log

Written by seat C at every checkpoint merge (COLLAB_PLAN.md §6).

| Checkpoint | Date | Block branch | Tables added | Events added | Contract changes | Tests | Rebase needed by |
|---|---|---|---|---|---|---|---|
| M0 | 2026-09-07 | block/c0-bootstrap | users, password_reset_tokens, sessions, cache, cache_locks, queue_jobs (Laravel queue table, renamed from `jobs`), job_batches, failed_jobs — framework only | — (20 events defined in contracts/events.md, none emitted yet) | initial contracts/ (8 files); CHANGE_REQUESTS #1–#16 | 23 passed (104 assertions) | @X1 @X2 rebase `main` after merge |
| M1 | 2026-09-07 | block/c1-platform | clients, suppliers, carriers; roles, permissions, model_has_roles, model_has_permissions, role_has_permissions; users +client_id/is_active/last_login_at; jobs, exceptions, documents, outbox_events, consumed_events | — (outbox + consumer API live; no business events emitted yet) | services.md: JobService real, ExceptionService::hasActiveHold; events.md: consumer API; CHANGE_REQUESTS #17–#21 | 60 passed | @X1 @X2 rebase `main` after merge, then start block/x1-oms-min and block/x2-tms |
| M2 | 2026-09-07 | block/c2-wms-core | warehouses, locations, asns, containers, asn_lines, asn_imports, stock_units, stock_ledger, stock_reservations, warehouse_tasks, warehouse_task_lines | emits asn.putaway_completed, task.completed, stock.reserved, stock.reservation_failed, stock.released; consumes order.confirmed, order.cancelled, order.reduced | db-schema.md §4 additive columns; services.md §1 StockService real; CHANGE_REQUESTS #23–#26 | 80 passed | @X1 @X2 rebase `main` after merge (X1: reserve replies now real; X2: real `packages` still M4) |
| M6-prep (platform-rest) | 2026-09-07 | block/c5-platform-rest | approvals, webhook_endpoints, webhook_deliveries, activity_log | none new; wildcard consumer pushes every event to webhooks | services.md §8 (ApprovalService, SearchRegistry, DocumentDownloader, ExceptionService assign/start), events.md webhooks, CHANGE_REQUESTS #28–#30 | 88 passed | merged ahead of M6 by agreement (no seat depends on its order); @X1 @X2 rebase; X1: serve portal downloads through DocumentDownloader, register orders in SearchRegistry |
| M3 | 2026-09-07 | block/x1-oms-min | orders, order_lines, declared_packages, fulfilments, fulfilment_lines, client_addresses, order_events, order_imports | emits order.confirmed; consumes stock.reserved, stock.reservation_failed, asn.putaway_completed, delivery.pod_captured | none new (CHANGE_REQUESTS #9 client_addresses ownership) | 106 passed | A3 / A17 / A4 / A7 by X1; A14 / A16 / A11b / A15 by C under HANDOFF.md (X1 quota). @X2 rebase `main` after merge — real orders for TMS; @C start block/c3 second half (B2c uses the real OrderService) |
| M4 | 2026-09-07 | block/c3-wms-outbound | stock_snapshots, stocktakes, stocktake_lines, scan_records, waves, packages, outbound_dispatches, return_receipts, return_receipt_lines; warehouse_tasks +wave_id, warehouse_task_lines +order_line_id | emits stock.daily_snapshot_taken, snapshot.weekly, outbound.packed, outbound.dispatched (new, additive), return.received, return.inspected; consumes nothing new | events.md outbound.dispatched + original_order_id on return.*; db-schema.md / enums.md additive columns and ※ names; services.md OrderService + ManifestParser real (Fakes retired, X1 provider guard removed); CHANGE_REQUESTS #35–#37 (#31–#34 reserved by block/c4-billing) | 125 passed (988 assertions) | @X1 @X2 rebase `main` after merge. X1: consume outbound.packed / outbound.dispatched / return.inspected for order status (HANDOFF.md). X2: `packages` + `outbound_dispatches` are real; fill `shipment_id` at handover from your booking |
| M5 | 2026-09-08 | block/x2-tms | carrier_services, shipments, transport_quotes, delivery_runs, run_stops, pods, tracking_events, carrier_costs, carrier_invoices, carrier_invoice_lines | emits shipment.quote_confirmed, shipment.booked, delivery.pod_captured, delivery.failed, delivery.extra_charge; consumes order.confirmed, outbound.packed, outbound.dispatched | none (all existing contracts followed; no CHANGE_REQUESTS rows) | 170 passed (1383 assertions) | B5–B9b by X2. Manual / own-fleet carrier paths only; Transdirect adapter interface-compatible but not live (account deferred by project lead). Follow-ups for C before M6: retire `FakeTransportOptionService` (provider still guards on USE_FAKE_SERVICES), move `TRANSDIRECT_*` env reads into config/services.php, Platform consumers for shipment.booked / delivery.pod_captured → Job cost (X2's HANDOFF note). X1 / X2 out of quota — no rebase notice sent |
| M6 | 2026-09-08 | block/c4-billing | charge_codes, charge_rules, rate_cards, rate_items, charges, invoices, invoice_jobs, invoice_lines, credit_notes, credit_note_lines, payments, customer_quotes, customer_quote_lines; clients.standard_rate_card_id FK | consumes task.completed, asn.putaway_completed, outbound.packed, shipment.quote_confirmed, delivery.extra_charge | services.md §9, enums.md §6, db-schema §6 additive, CHANGE_REQUESTS #31–#33 | see PR | built on block/c3-wms-outbound; merge at M6 after M4/M5 as planned Merged 2026-09-08 after main (M4, M5, M5 integration) was merged in: every Fake retired (CHANGE_REQUESTS #47), 184 tests. Follow-ups: CHANGE_REQUESTS #5 decided "adds" by project lead 2026-09-08 (rule change in a follow-up PR); rate card figures checked against the source workbook |
| M6b | 2026-09-08 | block/x1-portal-reports | orders +original_order_id / return_inspected_at / return_decision / return_decided_by / return_decided_at / return_decision_note, order_lines +original_order_line_id, order_api_tokens, order_api_idempotency_keys | emits order.cancelled, order.reduced, return.requested, return.financial_decision; consumes task.completed (pick), outbound.packed, outbound.dispatched, return.inspected | CHANGE_REQUESTS #38–#42 (X1 build), #41 fixed by C in PR #14 (ClientScope before SubstituteBindings), routes.md / db-schema.md rows for A4b / A11 | 214 passed (1891 assertions) | Block 2 part 1 built for X1 by C's delegated agent under HANDOFF (X1 quota exhausted); A7b / A21 / A22 remain for X1. Project lead approved merge 2026-09-08 ("m6b也可以同时合入"). @X1 @X2 out of quota — no rebase notice sent; both branches are fully merged |
| M7 | 2026-09-08 | block/x1-quotes-reports | report_deliveries; orders index (2026_09_08_700020); customer_quotes rows via Billing QuoteService | consumes invoice.issued (→ billing_status billed); emits nothing new (shipment.quote_confirmed via QuoteSelectionService from the portal) | routes.md rows for the new Orders / Portal / Reports routes; services.md QuoteService pre-priced line (#68); CHANGE_REQUESTS #50–#54, #70–#75 | 238 passed (2293 assertions) | A7b, A21, A22 + portal quote confirmation / invoices / stock + invoice.issued consumer, built for X1 by C's delegated agents under HANDOFF (X1 quota exhausted); adversarial review before merge fixed 3 defects (#75). Project lead approved 2026-09-08 ("合入"). Every X1 task row is now ✅ |
| c14 | 2026-09-08 | block/c14-goods-receipt | goods_receipts, goods_receipt_lines; stock_units +goods_receipt_id; asns +receiving_completed_at (additive, applied to the live trial DB with `migrate`) | — | routes.md (+7 Warehouse routes: receiving worklist, unplanned receiving, receipts index/show/pdf/complete), enums.md (goods_receipts.status open\|completed, documents.type +goods_receipt, ASN = 预报单 / 入库单 = goods receipt), db-schema.md §4, CHANGE_REQUESTS #90–#92; lang-only renames in X1 files (orders/portal/reports: 入库单号 → 预报单号 (ASN)) | 260 passed, 1 skipped (Karrio live) | X1 (lang keys only; optional portal link to download 入库单 documents) |
| audit-1 | 2026-09-10 | fix/audit-c-2026-09-10 + fix/audit-x-2026-09-10 | — (no schema change) | — | CHANGE_REQUESTS #96–#104 (lang/zh/validation.php app-wide; role-gated buttons; input retention; precondition messages; Orders/Portal/Transport/Reports fixes as integrator — see HANDOFF notes), #105 immediate outbox dispatch | 324 passed, 1 skipped (Karrio live) | X1 / X2 (their zones edited by C as integrator; file list in HANDOFF.md) |
| i18n-1 | 2026-09-10 | i18n/zh-sweep-c + i18n/zh-sweep-x | — | — | CHANGE_REQUESTS #107–#110: every user-visible message in Chinese (service refusals via RuleViolation + lang keys, abort texts, flashes, Blade literals, JS strings), Chinese framework error pages 404/405/409/413/419/429/500/503, lang/zh/{auth,pagination,passwords}.php, uncaught RuleViolation safety net in bootstrap/app.php; third-party documents (invoice PDF, consignment note, labels) deliberately unchanged | 347 passed, 1 skipped | X1 / X2 (their zones edited by C as integrator; file lists in HANDOFF.md) |
| day-2026-09-10 | 2026-09-10 | main (direct, integrator) | goods_receipts / goods_receipt_lines (c14, earlier row); no other schema change | — | CHANGE_REQUESTS #93 (403 → Chinese no-permission page naming allowed roles), #94 (手动填写入库单), #95 (作业登记 rework), #105 (immediate outbox dispatch, 10 s schedule), #106 (缺货 visibility, Chinese outbound refusals, release filters), #111 (client cancellation by stage), #112 (客户请求 inbox); UI: dl.kv grids, kv cards, StatusBadge everywhere; role wording 客服 / 协调员; all status labels Chinese | 349 passed, 1 skipped | X1 / X2 (files listed per CR in HANDOFF.md) |
| pdf-en | 2026-09-11 | feat/pdf-english | — | — | CHANGE_REQUESTS #114: client-facing PDFs in English via `lang/en/pdf.php` (goods receipt, tax invoice, unit / location labels, consignment note, own-fleet label, POD); AGENTS.md i18n rule amended | 356 passed, 1 skipped (Karrio live) | X2 (three Transport PDF templates edited — see HANDOFF 2026-09-11) |
| asn-delivery | 2026-09-11 | feat/asn-line-delivery | — | — | CHANGE_REQUESTS #115: 编辑收件信息 per ASN goods line (+2 Warehouse routes, `AsnService::updateLineDelivery`), blocked-list links, add-line form with the full address | 358 passed, 1 skipped (Karrio live) | — |
| portal-asn | 2026-09-11 | feat/portal-asn | asns +client_confirmed_at / client_confirmed_by (additive); portal_asn_submissions (new, Portal) | — | CHANGE_REQUESTS #116: 客户自助预报入库 (portal form + API push), 待客服确认 flag / confirm route / badges; routes.md + db-schema.md rows | 361 passed, 1 skipped (Karrio live) | X1 (Portal module files added — see HANDOFF 2026-09-11) |
| orders-inbound | 2026-09-11 | feat/orders-inbound | — (no schema change; portal_asn_submissions left unused) | — | CHANGE_REQUESTS #117: 从订单生成预报单 (InboundService contract, JobService::cancelIfEmpty, Orders 待建预报 page), portal 预报入库 read-only, client ASN creation + API removed | 362 passed, 1 skipped (Karrio live) | X1 (Orders + Portal files — see HANDOFF 2026-09-11 #117) |
| client-transport-choice | 2026-09-11 | feat/client-transport-choice | orders +transport_preference (json, additive) | — | CHANGE_REQUESTS #118: client picks the transport option with the 估价; Transport selects / confirms it automatically within tolerance; `TransportOptionService::estimate()` contract method | 365 passed, 1 skipped (Karrio live) | X1 (Orders + Portal files), X2 (Transport files) — see HANDOFF 2026-09-11 #118 |
| asn-import-orders | 2026-09-11 | feat/asn-import-orders | — | — | CHANGE_REQUESTS #119: 从订单导入货物行 on the ASN page (InboundService::addOrderLinesToAsn, OrderService::awaitingAsn / attachOrdersToAsn), manual add-line collapsed, 来源订单 column JobService::moveOrder (new, contracts/services.md §5): the merged order takes its shipment / quotes / holds / tasks / documents into the ASN Job so the emptied per-order Job can be cancelled. | 370 passed, 1 skipped (Karrio live) | X1 (Orders files — see HANDOFF 2026-09-11 #119); X2 (`shipments.job_id` / `carrier_costs.job_id` re-homed by `JobService::moveOrder` — HANDOFF #119 Transport note) |
| pickup-billing | 2026-09-14 | feat/pickup-deliver-billing | — (charge_codes / charge_rules rows via BillingSeeder — run once on the trial server) | shipment.quote_confirmed +order_type / is_urgent / lines / pallet_count / carton_count / label_count (additive) | CHANGE_REQUESTS #120: 提货直送 billed with the from_stock handling codes at final quote confirmation; estimate shows them; TR-PICKUP manual code | 381 passed, 1 skipped (KarrioLiveTest, needs KARRIO env), 4882 assertions; pint clean; BillingSeeder re-run ×2 on local erp DB: 53 rules, 0 duplicate (trigger_event, charge_code_id), 45 codes, 34 Edward items | X1, X2 — see HANDOFF 2026-09-14 #120 |
| partial-pallet-pick | 2026-09-14 | fix/partial-pallet-pick | — | — (outbound.packed unit_type semantics clarified, CHANGE_REQUESTS #121) | partial pallet picks billed and estimated as carton picks; only a pallet that leaves whole is a pallet pick | 372 passed, 1 skipped (Karrio live) | X1 (OrderEstimateService::pickUnit — see CHANGE_REQUESTS #121) |
| confirm-wording | 2026-09-14 | fix/confirm-wording | — | — | — (wording only): the order page confirm button, flash and timeline note follow the order type — 提货直送 reads 生成运输方案 instead of 检查库存 / 等待 WMS 锁定库存 (lead feedback 2026-09-14; HANDOFF 2026-09-14 确认按钮) | 386 passed, 1 skipped (Karrio live), 4916 assertions; pint clean | X1 (Orders lang / show view / OrderController) |
| weight-autocalc | 2026-09-14 | fix/confirm-wording | — | — | — (form JS + lang only): goods lines carry 单件重量 ⇄ 整行合计 via 箱数; declared packages mirror the goods lines while 按货物明细自动生成申报包裹 is ticked; live totals under both tables (lead feedback 2026-09-14 第 13 点; HANDOFF 2026-09-14 重量自动换算) | 388 passed, 1 skipped (Karrio live), 4947 assertions; pint clean; browser-checked JS | X1 (Orders partials goods-lines / goods-line-row / declared-packages, lang) |
| physical-containers | 2026-09-14 | feat/physical-containers | physical_containers (new); containers +physical_container_id / devanning_basis / devanning_basis_qty / devanning_share / devanning_basis_provisional; warehouse_tasks job_id / client_id nullable (NULL only for source_type physical_container) +physical_container_id; charge_codes +TR-SIDELOADER and charge_rules changes via BillingSeeder — run once on the trial server after `migrate` | physical_container.arrived (new); task.completed +box block (job / client null, physical_container, members[] with share, allocation_basis, basis_provisional, activity_version — additive) | CHANGE_REQUESTS #122: 拼柜 / LCL 物理柜 — coordinator linking screen, box-level devanning task, shares computed by Warehouse (provisional until receiving completes, 重算分摊 → version + 1), ChargeEngine `allocated` quantity source (per-member client / Job / unpack_mode, whole-box reversal on a higher version), real RateService enforces max_line_count / max_gross_weight_kg (Fake aligned), cartage + TR-SIDELOADER on the arrival event (CR #7 / #34 superseded, dormant rules kept), portal unchanged (negative test); db-schema / enums / events / charge-codes / services / routes updated, ERP_PLAN §0.2 / §4.2 / §4.8 / §6.4 | 396 passed, 1 skipped (KarrioLiveTest, needs KARRIO env), 5119 assertions; pint clean | — (no X1 / X2 file touched; HANDOFF 2026-09-14 #122 is informational: events.md additions, CR #7 / #34 superseded) |
| portal-inbound-csv | 2026-09-14 | feat/portal-inbound-csv | — (no schema change; inbound context in `order_imports.errors.context.inbound`, `order_imports.source` +`portal` value only) | — | CHANGE_REQUESTS #123: 客户门户 入库清单 CSV / Excel 提交 — upload → preview (every mapped field per 唛头, row + column Chinese errors) → the client's own orders (source portal, one loose Job per submission) → 待建预报 shows the submitted 柜号 / ETA with 选中并填入; `SpreadsheetManifestParser` accuracy hardening (format sniffing, UTF-8 BOM / UTF-16 / GB18030, delimiter, full-width, header aliases, cm / mm + 单件重量 × 箱数, phone / postcode / state normalisation, `label` on entries — services.md §8 additive); routes.md Portal row, enums.md | 417 passed, 1 skipped (KarrioLiveTest, needs KARRIO env), 5417 assertions; pint clean | X1 (Orders + Portal files — see HANDOFF 2026-09-14 #123: keep the parser tables / `convertRow`, the `OrderImportService` context keys and lazy Job, the `portal.asns.imports.*` routes) |
| asn-collection | 2026-09-14 | feat/asn-collection | asns +inbound_transport / collection_address / collection_ready_date / collection_packages / collection_notes / collection_requested_at / collection_requested_by / collection_version / collection_shipment_id / collection_status / collection_plan; shipments order_id nullable +asn_id +asn_activity_version — migrations only, no seeder change | asn.collection_requested, asn.collection_cancelled (new, Warehouse → Transport); shipment.quote_confirmed / shipment.booked / delivery.pod_captured / delivery.failed +asn_id +shipment_type (additive; order_id nullable) | CHANGE_REQUESTS #124: 预报单 到仓方式 我方上门提货 — the ASN carries the pickup request, Transport runs an `inbound_collection` shipment (no order, receiver = our warehouse) through quote → confirm → book → run → POD, the POD marks the ASN arrived, Billing bills TR-* on the ASN's Job (events.md, db-schema.md, enums.md, routes.md, services.md, charge-codes.md; HANDOFF 2026-09-14 预报单我方上门提货) | 406 passed, 1 skipped (Karrio live), 5361 assertions — 8 new tests (Warehouse 5, Transport 2, Portal 1); pint clean | X2 (Transport: enums, status machine, intake, request factory, quote selection, booking / POD / tracking payloads, run / redelivery type checks, manifest, consignment note, controllers + views, lang; tests B5d / B7 / B9a) · X1 (Orders DeliveryPodCapturedConsumer guard; Portal asns/show include + new partial + lang) |
| portal-collection-request | 2026-09-15 | feat/portal-collection-request | asns +collection_preference (json) / collection_requested_via / collection_import_id (indexed, no FK) — one migration, no seeder | — (none new; `asn.collection_requested` +client_preference +requested_via, additive) | CHANGE_REQUESTS #125: 客户门户申请上门提货 + 自选提货方案, 客服审核 — the portal 入库清单 carries 需要我们上门提货 (pickup / contact / ready date / warehouse), the preview prices the collection plans (客户价) from the list rows through the same request builder Transport uses, confirm stores the customer snapshot; 待建预报 flags and pre-fills it and generating the ASN calls the new `InboundService::requestCollection` in the same transaction; Transport confirms the client's option within tolerance (`clientPreference` reads `asns.collection_preference`), else the client re-confirms on the new `portal.asns.collection.quotes.confirm`; ASN page shows the client's plan / origin and warns on 从订单导入货物行 rows (`awaitingAsn` +`collection_requested`); shared `CollectionTailgate` / `collectionSender` / `collectionRequest` (events.md, db-schema.md, enums.md, services.md, routes.md; HANDOFF 2026-09-15 #125) | 440 passed, 1 skipped (KarrioLiveTest, needs KARRIO env), 5978 assertions — 15 new tests (Portal 4, Orders 4, Transport 6, Warehouse 1; incl. the same-day review fixes: collection re-request reference, staff-requested collection 404, import / order subset, no_items refusal, Warehouse preference whitelist); pint clean | X1 (Orders OrderImportService / OrderInboundService / OrderInboundController / inbound view / lang; Portal estimate / quotes services, import / ASN / quote controllers, routes, views, lang) · X2 (Transport CollectionTailgate extracted, request builder statics, clientPreference asn branch) — see HANDOFF 2026-09-15 #125 |
| location-storage-tier | 2026-09-15 | feat/location-storage-tier | locations +storage_tier +rack_level (index warehouse_id, storage_tier); order_lines +storage_tier +storage_tier_source; asn_lines +storage_tier +storage_tier_source; stock_units +required_storage_tier +storage_tier_override_reason; stock_snapshots +location_storage_tier +required_storage_tier; rate_items +warehouse_id — four migrations; charge_codes / charge_rules / standard-card rate_items row via BillingSeeder (run once by hand on the trial server, idempotent) | — (snapshot.weekly units[] +location_storage_tier / required_storage_tier / warehouse_id documented, additive) | CHANGE_REQUESTS #126: 底层库位存储等级 — locations carry tier + rack level (bulk set, logged, English labels); the declared tier travels portal / staff sheet (存储等级, value pre-fill from `tier_value_threshold_cents`) → order_lines → asn_lines → stock units; putaway refuses a declared-bottom good pallet outside a bottom storage location unless a reason is given, hints the first free bottom location; stock list 底层库位剩货托盘; `WH-STORAGE-TIER-PLT-WK` = percent of the pallet's base weekly storage (standard 10 %, band 10–20 %, warehouse rows beat the all-warehouse row for their own warehouse and never match a lookup without one, missing / POA base → needs review, never $0); ERP_PLAN §3 Q3 / §4.1.10 / §4.3 rule 3 / §6.2 amended on the lead's instruction (db-schema, enums, charge-codes §3 §7, services §4 §8, events, routes; HANDOFF 2026-09-15 底层库位存储等级) | 458 passed, 1 skipped (KarrioLiveTest, needs KARRIO env), 6232 assertions (after merging main / CR #125 and the 2026-09-15 review fixes) — 18 new tests (Warehouse 9, Billing 6, Orders 3); pint clean | X1 (Orders: order_lines migration, OrderLine, OrderCreationService whitelist, OrderImportService storageTiers, SpreadsheetManifestParser aliases / convertRow, OrderInboundService linePayload, imports/show, lang · Portal: PortalInboundImportController template + tierSurcharge, PortalStockController, asns/imports create + show, stock/index, lang — see HANDOFF 2026-09-15; main (CR #125) merged into the branch, six conflicts resolved by union) |
| portal-manual-inbound | 2026-09-17 | feat/portal-manual-inbound | — (no schema change; `order_imports.status` +`draft` value only, typed rows / ticks in `order_imports.errors.context.manual`, attached orders in `errors.result.attached`, `document_id` null for a manual list) | — | CHANGE_REQUESTS #128: 门户手工建立入库清单 — rows typed on the portal page (`ManifestParser::fromRows`, additive to services.md §8: the same normalisation / validation as a sheet, Chinese row + column errors back on the form) and / or the client's existing orders awaiting an ASN ticked into the same submission (以订单为准: read from the ORDER, never modified, recorded by id only), same preview → confirm as the CSV, collection estimate over typed rows + attached lines, drafts; `OrderImport::orderIds()` (created ∪ attached) read by 待建预报 / `collectionRequests` / the collection subset rule / the portal pages (routes.md Portal row, enums.md, db-schema.md; HANDOFF 2026-09-17 #128) | 466 passed, 1 skipped (KarrioLiveTest, needs KARRIO env), 6555 assertions — 8 new tests (Portal 5, Orders 3); pint clean | X1 (Orders: parser `fromRows` / `carryDown`, `OrderImportService` sixth constructor argument + `record()` / `previewRows` / `saveDraft` / `attachableOrders` / `confirm` attached ids, `OrderImport` helpers, `OrderInboundService` two lines, `OrderInboundController::portalSubmissions`, `OrderImportController::show`, three views, lang; Portal: three routes, `PortalInboundImportController` manual actions + shared rules, `PortalTransportEstimate` `label`, new `manual` view + four partials (`create` now includes two of them), `show` / `index` / `asns/index`, lang — see HANDOFF 2026-09-17 #128) |
| order-storage-tier | 2026-09-17 | feat/order-storage-tier | — (no migration: `order_lines.storage_tier` / `storage_tier_source` exist since #126; no seeder) | — (none) | CHANGE_REQUESTS #129: 下单时选择存储等级 — the client chooses the storage TIER (标准 / 底层) per goods line when placing an order: portal form (select per row after 包装类型 + one hint line with the client's own 底层 percent per warehouse from the new `PortalStorageTier`, source `client`), the staff manual form (same partial, source `staff`; the form re-rendered after a validation error prices the hint for the selected client server side), the draft line add / edit (source `staff`) and the order API (`lines[].storage_tier`, source `client`; absent = no tier); staff order page shows badge + source (客户选择 / 员工设置 / 按单价预选), portal order page badge only; a tier, never a bin — #126 putaway check / weekly surcharge / order line → ASN line → stock unit chain unchanged (routes.md one row; enums / db-schema / services unchanged; HANDOFF 2026-09-17 下单时选择存储等级) | 464 passed, 1 skipped (KarrioLiveTest, needs KARRIO env), 6334 assertions — 6 new tests (Portal 3, Orders 3), 102 assertions; pint clean | X1 (Orders: `partials/goods-line-row`, `partials/goods-lines`, new `partials/storage-tier-select`, `OrderFormRows`, `OrderController`, `OrderLineController`, `OrderApiController`, `views/show`, `views/api-tokens/index`, lang · Portal: new `Services/PortalStorageTier`, `PortalOrderController`, `views/orders/show`, lang — see HANDOFF 2026-09-17; the CR #128 files are untouched and every lang / contracts / docs change is appended at the end of its block, so the #128 merge stays mechanical) |
| date-input-lang | 2026-09-17 | fix/date-input-lang | — (no migration, no seeder) | — (none) | none in `contracts/`; AGENTS.md rule (Server-rendered Blade only): date / time inputs carry `lang="en-AU"` right after `type="…"` — lead request 2026-09-17: the native picker read 「yyyy/mm/日」 because `<html lang="zh">` localises it, and Chromium honours `lang` on the input itself; 40 inputs in 29 Blade views across Billing / MasterData / Orders / Platform / Portal / Reports / Transport / Warehouse now read dd/mm/yyyy in Chrome / Edge while the page stays zh (Firefox / Safari follow the browser language; docs/demo-guide.zh.md §8 FAQ row; HANDOFF 2026-09-17 日期输入框 lang="en-AU") | 474 passed, 1 skipped (KarrioLiveTest, needs KARRIO env), 6667 assertions — 2 new tests (Platform `DateInputLangTest`: source scan of `app/**` + `resources/views` failing with file:line, plus rendered `<html lang="zh">` / input `lang="en-AU"` check), 10 assertions; pint clean | X1 (Orders 5 views, Portal 5 views, Reports `partials/filters`) · X2 (Transport 4 views) — one attribute per date / time input, no logic change, see HANDOFF 2026-09-17 |
| date-field-component | 2026-09-17 | fix/date-field-component | — (no migration, no seeder) | — (none) | none in `contracts/`; AGENTS.md rule (Server-rendered Blade only) rewritten: date fields use the shared `<x-date-field name="…" :value="…" />` component (`resources/views/components/date-field.blade.php`, frozen zone) — dd/mm/yyyy shown in a text box in EVERY browser, ISO `yyyy-mm-dd` posted from its hidden input (the only named one, so controllers / validation / `old()` are untouched), the native date input off-screen as the calendar picker behind a button, one delegated script in `layouts/app.blade.php`; supersedes the `lang="en-AU"` attribute of 2026-09-17 (verified ineffective in a real Chromium — it formats by the browser UI language); 39 fields in 29 views (Billing 11, Platform 2, Warehouse 7, Orders 6, Portal 6, Reports 2, Transport 5 — the run ETA `datetime-local` = component + native time box, hidden keeps `Y-m-d\TH:i`), the client dispatch cut-off `type="time"` stays native; prefill scripts (待建预报 选中并填入, 未开票 period presets, 收件信息同上一行) set the hidden value and dispatch `change`; `lang/zh/platform.php` +`date_field.*`, `public/css/app.css` 99 / 100 lines; docs/demo-guide.zh.md §8 FAQ row; HANDOFF 2026-09-17 日期字段组件 | 476 passed, 1 skipped (KarrioLiveTest, needs KARRIO env), 6703 assertions — Platform `DateFieldTest` (4 tests, 45 assertions; replaces `DateInputLangTest`): view scan failing with file:line on any raw date / datetime-local input or `lang="en-AU"`, > 30 component uses, component markup (ISO / Carbon / null / `time` / `aria-invalid`), 仓库 收货单 + 门户 手工建立入库清单 rendered, portal order `requested_date` ISO still validates while dd/mm/yyyy is refused; `PortalAuditFixesTest` one assertion moved to the hidden input + native picker; pint clean; typing / normalising / invalid date / prefill / `showPicker()` verified in the desktop Chromium pane | X1 (Orders 5 views + the 选中并填入 script, Portal 6 views incl. `manual-rows` script, Reports `partials/filters`) · X2 (Transport 4 views) — markup swap only, field names / values unchanged, see HANDOFF 2026-09-17 |
| driver-permissions | 2026-09-17 | fix/driver-permissions | — (no migration, no seeder) | — (none) | `contracts/routes.md`: one Transport row (roles per route); `contracts/CHANGE_REQUESTS.md` #130 — lead decision 2026-09-17 (Jimmy): drivers (`transport_operator`) no longer plan their own work — the dispatcher plans, the driver executes. Removed from the driver: `runs.create` / `store`, `runs.stops.store` / `reorder`, every `carrier-invoices.*` route, the shipment board (`transport.index`), `shipments.show` and every shipment action (label, book, pod, extra charges, manual quote, redelivery, quote select, own-fleet cost), `orders.margin`; kept: `/driver/**` and `runs.index` / `runs.show` limited to the driver's own runs (another driver's run → 404, read-only page). Everything else stays with admin \| customer_service \| dispatcher (finance keeps its reconciliation / cost / margin access; warehouse roles keep reading the board). Enforced in the controllers via `RequiredRoles`; nav hides 运输 and 承运商对账 for drivers (HANDOFF 2026-09-17 司机权限收紧) | 478 passed, 1 skipped (KarrioLiveTest, needs KARRIO env), 6771 assertions — 4 new tests (Transport `DriverPermissionsTest`: driver 403 on every removed GET / POST, own-runs-only list + foreign run 404 + read-only run page, driver page deliver / fail end to end with the nav, planners / finance / warehouse readers unchanged), 104 assertions; 9 existing Transport tests re-cast their planner from transport_operator to dispatcher; pint clean | X2 (Transport: 15 controllers, `runs/index`, `runs/show`, `shipments/show`, nav include; 10 tests) · X1 (Orders `views/show` — one role token on the 收付毛利 link) — see HANDOFF 2026-09-17 |
| client-signup-only | 2026-09-22 | fix/audit-client-signup-only | — (no migration, no seeder change; BillingSeeder back-fill kept) | — (none) | `contracts/routes.md`: one MasterData row — `GET /admin/clients/create` + `POST /admin/clients` (`masterdata.clients.create` / `.store`) removed, `POST /admin/clients/{client}/bind-standard-card` (`masterdata.clients.bind_standard_card`, admin only) added; `contracts/CHANGE_REQUESTS.md` #134 — lead decision 2026-09-22 (Jimmy), audit A13 / GAP-01: staff never create clients; a company registers at `/register` (pending, standard rate card bound), staff approve / edit; `Client::creating` binds the active standard card on every remaining create path; clients index hint follows `erp.allow_signup` (default true); edit page shows 标准价目表 name · version and 专属价目表 有 / 无 with an admin 修复 when the standard card is null (also on the index row) | 485 passed, 1 skipped (6882 assertions): `ClientCrudTest` rewritten (10 tests — registration → approve → edit, old routes 405, hint follows allow_signup, `Client::create` omitted / null → active standard card, draft standard ignored, explicit binding kept, pre-card client back-filled by BillingSeeder, edit-page cards, 修复 admin-only and refused without an active card), `ValidationLocaleTest` client check moved to the edit form, `CreatesUsers::client()` explicit-null contract kept; pint clean | none — C zones only (MasterData, Billing `Models/RateCard`, Platform `RegistrationController`, `tests/Support/CreatesUsers.php` unchanged contract); X1 / X2 `git rebase main` as usual after merge |
| audit-transport | 2026-09-22 | fix/audit-transport | — (no migration, no seeder) | — (none; nothing new is published — a stop leaving a run informs no other module, HANDOFF 2026-09-22) | `contracts/routes.md`: one Transport row (+`transport.runs.update` PATCH /transport/runs/{run}, `transport.runs.stops.destroy` DELETE /transport/runs/{run}/stops/{stop}, `transport.runs.cancel` POST /transport/runs/{run}/cancel, `transport.shipments.requote` POST /transport/{shipment}/requote — planner roles per #130); `contracts/CHANGE_REQUESTS.md` #133 — audit blockers A7 TMS-01 (a delivery run could not be corrected: no stop removal, no date / driver / vehicle change, no cancel, driver limited to a run dated exactly today) and A8 TMS-02 (quotes die after 24 h with no way to re-quote and read 待选择 when dead), lead approval 2026-09-22. Rules: 移出班次 for a pending stop of an open run (shipment stays `booked`, no run, re-plannable; stops renumbered; a dispatched run with nothing open completes), 修改班次 / 取消班次 only while no stop is delivered (failed stops stay), the driver finishes any planned / dispatched run dated on or before today; 重新报价 for quoting \| quoted \| quote_confirmed-not-booked incl. inbound collections — expired rows `expired`, a confirmed selection `requoted`, then `TransportOptionService::quote()` at the latest stage re-runs the client-preference / variance rule; quotes table shows 有效至 and 已过期. Enums / events / services.md untouched (`cancelled`, `expired`, `requoted` existed). Activity log `delivery_run` / `shipment` (HANDOFF 2026-09-22 审计阻断项 A7–A8) | 489 passed, 1 skipped (KarrioLiveTest, needs KARRIO env), 6989 assertions — 9 new tests (Transport `RunEditingTest` 6: remove / re-add, edit with run number kept, cancel releases stops, refused after a delivery while a pending stop still leaves, driver finishes yesterday's run and not tomorrow's, driver + finance / warehouse 403; `RequoteTest` 3: expired rows and fresh selectable quotes with activity log, client-chosen collection re-confirmed within tolerance and waiting beyond it, planner-only / booked refused / no-option refusal then quotes), 182 assertions; pint clean | X2 (Transport: `routes.php`, `DeliveryRunService`, `DriverPodService`, `DeliveryRunController`, `RunStopController`, `DriverController`, `DeliveryRun` model, new `ShipmentRequoteService` + `ShipmentRequoteController`, views `runs/show`, `driver`, `shipments/show`, `lang/zh/transport.php`) — see HANDOFF 2026-09-22; X1 nothing |
| audit-b-portal | 2026-09-22 | fix/audit-b-portal | — (no migration, no seeder) | — (none) | `contracts/services.md` §8: one additive bullet — Row +`deliver_to_address_type` (null without a 地址类型 / Address type column or for an empty cell; 住宅 / residential / home → residential, 商业 / 公司 / business → business, FBA / 亚马逊 → fba, else row error `invalid_address_type`; carried down under the mark, part of the consistency pre-check; `fromRows` field), every `errors` entry +`consignment_mark`; `contracts/routes.md`: one Portal row (`portal.asns.imports.confirm` and `orders.imports.confirm` require `skip_acknowledged=1` whenever any row of the list would be skipped, button 只提交可生成的 N 张（跳过 M 行） + required tick; template column 地址类型 after 邮编 with FBA / 商业 / 住宅 samples; manual `rows[i][deliver_to_address_type]`); `contracts/CHANGE_REQUESTS.md` #136 — audit 2026-09-22 Part B, PORTAL-05 (a 唛头 with a refused row formed a "ready" group → an order silently short of a goods line; now `blocked` with the row numbers, `rows_not_read`) and PORTAL-07 (no address type on the list → a residential consignee never reached the OMS-13 tailgate rule; now an optional column / per-row select, no new business rule), lead approval 2026-09-22 | 1 skipped, 499 passed (7201 assertions) — +5 new (`tests/Feature/Portal/PortalInboundBlockedMarkTest.php` 3, `tests/Feature/Portal/PortalInboundAddressTypeTest.php` 2), 2 updated (`PortalInboundImportTest` template case: 3 sample rows + address types; `OrderImportTest`: 3 staff confirm posts +`skip_acknowledged`) | @X1 rebase `main` after merge (Orders: `Services/SpreadsheetManifestParser.php`, `Services/OrderImportService.php`, `Http/Controllers/OrderImportController.php`, `views/imports/show.blade.php`, `lang/zh/orders.php`; Portal: `Http/Controllers/PortalInboundImportController.php`, `views/asns/imports/{show,create}.blade.php`, `views/asns/imports/partials/{manual-row,manual-rows}.blade.php`, `lang/zh/portal.php`) — see HANDOFF 2026-09-22 fix/audit-b-portal; X2 nothing |
