# Handoff register

One branch, one seat at a time. Register before covering another seat's block branch (COLLAB_PLAN.md §5.2 rule 4).

| Date | Branch | From seat | To seat | Starting at task | Expected return | Returned |
|---|---|---|---|---|---|---|
| 2026-09-07 | block/x1-oms-min | X1 (quota exhausted) | C | A14 (A3, A17, A4, A7 done by X1) | when M3 is merged — X1 resumes with block 2 (`block/x1-portal-reports`) | 2026-09-07 — A14 / A16 / A11b / A15 delivered, PR "M3: OMS minimal block" opened; see Notes |
| 2026-09-08 | block/x1-portal-reports | X1 (quota exhausted) | C (delegated build agent, C reviews) | block 2 from task 1: outbound / return event consumers → order status, A13, A11, A9-p, A12, A4b | when X1's quota returns — A7b / A21 / A22 stay with X1 (need M6 RateService / data) | |
| 2026-09-08 | block/c8-karrio-adapter (Transport zone) | X2 (quota exhausted) | C | Karrio carrier adapter + `karrio` source (project lead: open-source API instead of Transdirect) | single task; X2 resumes Transport afterwards | 2026-09-08 — adapter, seeds, docs, tests (CHANGE_REQUESTS #49) |
| 2026-09-08 | block/x1-quotes-reports | X1 (quota exhausted) | C (delegated build agent, C reviews) | block 2 part 2: A7b customer quotes + estimate, A21 reports (boss / client), A22 scheduled client reports | when X1's quota returns |  2026-09-08 — A7b / A21 / A22 delivered, draft PR "M7 (draft): OMS block 2 part 2" opened; see Notes |
| 2026-09-08 | block/x1-feedback-1 | X1 (quota exhausted) | C (delegated build agent, C reviews) | tester feedback items 1, 2, 3b, 5, 6, 7 (portal stock filter, order info layout, address suggestions, package type select, tailgate checkbox, line dimensions) | when X1's quota returns | |
| 2026-09-08 | main (tester feedback round 2, direct) | X1 (quota exhausted) | C | tester feedback #10: portal two-step order (估价 → 确认提交), Portal zone | — (C's own change, CHANGE_REQUESTS #88) | ✔ 2026-09-08 |

## Notes

### 2026-09-07 · block/x1-oms-min · C covered A14 / A16 / A11b / A15 for X1
Paste-ready context for X1's Codex when it resumes (block 2, `block/x1-portal-reports`):

- **Done by C on your branch, in your conventions** (final classes, `orders::` views, `lang/zh/orders.php`, tests in `tests/Feature/Orders`, PSR-4 under `app/Modules/Orders`):
  - `Services/TailgateRule.php` (A16) — `evaluate()` / `apply()`; threshold from `RateService::thresholds(client, 'TR-TAILGATE')['tailgate_weight_kg']` (default 25). **Assumption:** a line's `actual_weight_kg` is the line total, per-piece = total / `carton_qty`; declared packages count as pieces. Applied in `OrderCreationService::create()` and again in `OrderStatusService::transitionOperational('confirmed')` unless `tailgate_reason = manual`. Override: `POST /orders/{order}/tailgate` (`OrderController::tailgate`, reason → `order_events.note`).
  - A11b — `OrderCreationService::create()` accepts a missing `job_id` and opens a Job through `JobService` (`transport_only` for `pickup_deliver`, else `loose`); `lines` optional for `pickup_deliver`, pickup address + `declared_packages` required (see `OrderController::validated()`); `form.blade.php` shows the pickup fieldset by order type; `show.blade.php` renders pickup + packages.
  - `Services/OrderBatchService.php` + `Http/Controllers/BatchController.php` + `views/batches/index.blade.php` (A14) — `GET /orders/batches?ref=<container_no|asn_no>`; read-only joins on Warehouse's `asns` / `containers` / `asn_lines`; revenue shows "pending" until Billing (M6) adds `charges`. ASN chips on order lines via `asnRefsFor()`.
  - `Services/OrderHoldService.php` + `Http/Controllers/HoldController.php` + `Http/Controllers/QueueController.php` + `views/queue/index.blade.php` (A15) — presets `pending_confirm | short | financial_hold | exceptions | today`; holds are Platform exceptions written through `ExceptionService::raise('hold', 'orders', …)` / `resolve()`, read via `DB::table('exceptions')` (read-only). Financial holds: `admin|finance` only. This covers most of **A13** already; in block 2 only add what is missing (list highlighting, approval flow if Finance wants one).
  - `OrdersServiceProvider` registers orders in `SearchRegistry` (A30). Nav include gained 调度队列 / 入库批次.
  - New lang sections: `orders.pickup`, `orders.tailgate`, `orders.holds`, `orders.queue`, `orders.batches`.
  - Tests: `TailgateAndTransportOrderTest`, `QueueAndBatchTest` (8 tests). Full suite 106 green on the branch.
- **Branch state:** `origin/main` (M2 + platform-rest) merged into the branch as a merge commit (no rebase, nothing to force-push). PR "M3: OMS minimal block" opened by C.
- **From M4 (C, 2026-09-07):** Warehouse now calls `OrderService::createFromAsn` from the ASN page and writes back `asn_lines.order_line_id`; it emits `outbound.packed`, `outbound.dispatched` (new, additive — see events.md), `return.received`, `return.inspected` (both now also carry `original_order_id`). Orders has no consumer for the outbound / return events yet: block 2 should move the order to `packed` / `dispatched` / `returned` from them (Orders owns order status, §4.3 rule 6). The `OrderService` / `ManifestParser` Fakes are retired and `OrdersServiceProvider` binds the real services unconditionally (CHANGE_REQUESTS #37).
- **When you resume:** `git fetch origin && git checkout block/x1-portal-reports` (create from `main` after M3 merges); read `AGENTS.md`, `CODEX_PLAN.md §3` 版块 2, `contracts/`. Portal downloads must go through `App\Support\Documents\DocumentDownloader` (services.md §8). `order.cancelled` / `order.reduced` are still to be emitted (A11); `customer_quotes` come from Billing's `QuoteService` (A7b shares it, services.md §9).

### 2026-09-07 · B9a · X2 order-page integration handoff

- Transport owns the cost data and exposes the staff-only order transport margin page as `transport.orders.margin` (`/transport/orders/{orderId}/margin`). `ShipmentMarginService::order()` returns per-shipment and aggregate “收 − 付 = 毛利”; client-scope callers receive revenue only and `CarrierCost` itself is hidden by a data-layer global scope.
- `app/Modules/Orders/views/show.blade.php` belongs to X1, so X2 did not edit it. When X1 next changes the order detail page, add a staff-only link or panel to `route('transport.orders.margin', $order->id)`; do not expose any cost or margin fields in Portal/client requests.
- Platform owns `jobs` and already receives the exact `shipment.booked` event. At M5 integration, C should make `JobService::summarize()` derive/cache estimated and actual cost from `carrier_costs`; X2 does not write the shared `jobs` table directly.

### 2026-09-08 · block/x1-portal-reports · built for X1 by C's agent

Block 2 tasks 1–6 delivered in X1's conventions (final classes, `orders::` / `portal::` views on `layouts.app`, `lang/zh/{orders,portal}.php`, Feature tests with `RefreshDatabase` + `tests/Support` helpers). Suite: **194 tests green** (170 → 194), `pint` clean, `migrate:fresh --seed` clean. Draft PR "M6 (draft): OMS block 2 part 1" opened; nothing merged.

- **Task 1 · event consumers → order status** (`app/Modules/Orders/Consumers/`): `TaskCompletedConsumer` (pick → `picking`), `OutboundPackedConsumer`, `OutboundDispatchedConsumer` (stores `fulfilments.shipment_id`), `ReturnInspectedConsumer`; `DeliveryPodCapturedConsumer` now also handles pure transport orders (no batch → confirmed → dispatched → delivered). Logic lives in `FulfilmentService::markPicking/markPacked/markDispatched/markDelivered/resyncOperationalStatus`: the batch status is monotonic, the **order follows its slowest batch** (picking when any batch is picked; packed / dispatched only when every batch is; delivered when every carton is), steps are walked one by one through `OrderStatusService::transitionOperational` (system actor) so the timeline shows each stage even when events arrive out of order; terminal orders are never resurrected; replays are no-ops. `qty_shipped` now counts dispatched-or-delivered batches. **Changed expectation:** `FulfilmentAllocationTest` — F1 delivered while F2 is still allocated leaves the order at `picking` (was `dispatched`).
- **A13 financial lock** (`OrderStatusService`, `OrderHoldService`, `HoldController`, `Order::hasActiveFinancialHold()`): `transitionOperational(…, 'dispatched')` throws `App\Modules\Orders\Exceptions\OrderRuleViolation` while `ExceptionService::hasActiveHold('financial', client, order)` is true; place = admin / finance, release = admin / finance / dispatcher, reason required both ways, actor in the timeline; releasing a financial hold calls `FulfilmentService::resyncOperationalStatus()` so a dispatch Warehouse already reported goes through; order list shows a 财务锁 badge. Until Warehouse / Transport gate the action (CHANGE_REQUESTS #40) the consumer records the batch as dispatched, leaves the order `packed` and writes the refusal into the timeline.
- **A11 change / cancel + returns** (`OrderChangeService`, `ReturnRequestService`, `OrderChangeController`, `ReturnController`, events `OrderCancelled`, `OrderReduced`, `ReturnRequested`, `ReturnFinancialDecision`): received / confirmed / allocated → admin / customer_service / dispatcher, free; picking / packed → admin / warehouse_supervisor + reason; dispatched / delivered → only a return. Cancel emits `order.cancelled` (map gained picking / packed → cancelled); reduce (decrease only) shrinks fulfilment lines from the newest batch and emits `order.reduced` for lines with `asn_line_id`. Return = order with `order_type = return`, `original_order_id`, same Job, `confirmed` at request time **without** `order.confirmed` (no reservation for returns), `pickup_address` = original deliver-to; `return.requested` emitted; `return.inspected` → return order `returned` + `return_inspected_at`, original gets a timeline note (with `return_order_id` null an unambiguous open return on the original is matched); Finance (admin / finance) records credit / no_credit once → `return.financial_decision` (credit_lines default to every return line, `amount_cents` null for Billing to price). Migration `2026_09_08_700000` (CHANGE_REQUESTS #38).
- **A9-p portal** (`app/Modules/Portal/Http/Controllers/Portal{Order,Return,Document}Controller`, `views/orders/{index,show,create}`, `lang/zh/portal.php`, nav): list with search (order no / reference / mark), customer status and date range; detail with lines, batches, Transport tracking (`shipments` / `tracking_events` / `pods` read-only through X2's models, client-scoped) and status steps only (no internal notes, no costs, no margin link); create (`OrderCreationService`, source `portal`, `client_id` from the user, A17 prefill); return request (source `portal`); `GET /portal/documents/{id}` through `DocumentDownloader`. **Gotcha:** implicit route-model binding resolves before `client.scope` sets the tenant → portal actions call `Model::query()->findOrFail()` themselves (CHANGE_REQUESTS #41). Placeholder `IndexController` / `views/index.blade.php` removed; `portal.index` is the list.
- **A12 PDF / e-mail → draft** (`DraftOrderTextExtractor`, `DraftOrderParser`, `DraftOrderService`, `DraftOrderController`, `OrderLineController`, `views/drafts/create.blade.php`): `pdftotext` when on PATH else a naive Tj/TJ reader, `.eml` (quoted-printable / base64 / html) and `.txt`; heuristic zh/en fields → order with `source = pdf`, `received`, placeholders (`orders.drafts.placeholder`) where nothing matched, file attached as `packing_list` via `DocumentService`, `order_imports` row (`source = pdf`, `errors` json holds matched fields + text). While `received`, coordinators edit / add lines and link them to the client's ASN goods lines (read-only `asn_lines` lookup) so the A7 confirm rule can pass.
- **A4b API stub** (`OrderApiController`, `ApiTokenController`, `OrderApiTokenService`, models `OrderApiToken`, `OrderApiIdempotencyKey`, migration `2026_09_08_700010`, CHANGE_REQUESTS #39): `POST /orders/api/orders` with `Authorization: Bearer <token>` (SHA-256 hash stored, revocable) and optional `Idempotency-Key` (replay → 200 with `replayed: true`); 401 / 422 / 201 JSON; creation only through `OrderCreationService` (source `api`). Admin page `/orders/api-tokens` issues tokens (plain shown once) and revokes them.
- **Assumptions to review** (also CHANGE_REQUESTS #42): return orders share the original Job; `.eml` uploads use source `pdf`; supervisor stage = `admin | warehouse_supervisor`; cancel always needs a reason (payload field); a POD without `fulfilment_id` on a from_stock order is ignored.
- **Left for X1** (need M6 RateService / real data): **A7b** customer quotes (Billing `QuoteService` + `TransportOptionService`), **A21** reports, **A22** scheduled client reports. Suggested next: add a staff-only link to `route('transport.orders.margin', $order->id)` on `orders::show` (X2 handoff above); after C's #40 lands, drop the "packed while locked" branch from `FulfilmentService::stepOperational` if wanted.

### 2026-09-08 · block/x1-quotes-reports · built for X1 by C's agent

Block 2 part 2 (A7b, A21, A22) delivered in X1's conventions (final classes, `orders::` / `portal::` / `reports::` views on `layouts.app`, `lang/zh/{orders,portal,reports}.php`, Feature tests with `RefreshDatabase` + `tests/Support` helpers). Suite: **224 tests green** (214 → 224, 1 skipped as before), `pint` clean, `migrate:fresh --seed` clean. Draft PR "M7 (draft): OMS block 2 part 2 — A7b customer quotes, A21 reports, A22 scheduled client reports" opened; nothing merged. Assumptions and cross-module needs: CHANGE_REQUESTS #50–#54.

- **A7b customer quotes + estimate** (`app/Modules/Orders/Services/OrderEstimateService.php`, `Http/Controllers/OrderEstimateController.php`, `views/partials/estimate.blade.php`; Portal `Http/Controllers/PortalEstimateController.php`; `POST /orders/{order}/estimate` (admin / customer_service / dispatcher / finance), `POST /portal/orders/{order}/estimate` (client user, own order); lang `orders.estimate`, `portal.estimate`): the "估价" panel on `orders::show` and `portal::orders.show` prices the order's expected warehouse services through Billing's `RateService` and persists them with `QuoteService::create` (stage `preliminary`, `orders.customer_quote_id`; re-estimate = new quote, previous `expired` via `setStatus`, timeline note). Lines: `WH-ORDER-DESPATCH` (+ `WH-ORDER-DESPATCH-URGENT` for same-day orders entered after the client cut-off), picks per unit (`WH-PICK-PLT` from pallet stock units or a pallet-like package_type; carton bands probed through `RateService` with `weight_kg`), `WH-LABEL-OUT` (pieces), `WH-LOAD-PLT` (pallet picks). Freight = Transport's preliminary quote read-only from `shipments` / `transport_quotes` (selected > recommended > cheapest; customer price only, cost never selected) or "待运输报价". POA / missing rate → "待报价", never $0 (page, totals and timeline). The freight is **not** a stored quote line — `QuoteService` cannot take a pre-priced amount (#50); the chosen `transport_quote_id` + price go into `customer_quotes.notes`. Portal renders the same partial with `$staff = false` (no Billing link, `assumptions` JSON never rendered). Order form carries a hint only (the estimate needs a saved order).
- **A21 reports** (`app/Modules/Reports/Services/{ReportService,ReportPeriod,ReportFormat,ReportCsv}.php`, `Http/Controllers/{BossReportController,ClientReportController}.php`, Portal `Http/Controllers/PortalReportController.php`, views `reports::{index,client,partials.filters,partials.table}`, nav includes `layouts/nav/{reports,portal}.blade.php`, `lang/zh/reports.php` rewritten; `IndexController` + placeholder view removed — `ReportsPlaceholderTest` still passes on the boss page): seven tables (orders by status / by client, on-time rate + overdue-open, inbound, outbound, revenue vs cost vs margin, open exceptions) as grouped `DB::table()` SQL, current month by default (`from` / `to`, ≤ 366 days), CSV export per table (UTF-8 BOM, translated headers, totals row). Boss = admin | finance at `/reports`; staff client view = admin | finance | customer_service | dispatcher at `/reports/client?client_id=`; client users at `/portal/reports` (ClientScope, #52). Client view: explicit `client_id` predicate on every query, cost / margin / estimated_jobs never selected (#53), exceptions limited to discrepancy / pick_short / delivery_failed. Migration `2026_09_08_700020` adds report indexes on X1 tables.
- **A22 scheduled client reports** (`Services/ClientReportMailer.php`, `Mail/ClientReportMail.php`, `views/mail/client-report.blade.php`, `Console/SendClient{Weekly,Monthly}ReportsCommand.php`, `Models/ReportDelivery.php`, migration `2026_09_08_710000`, `ReportsServiceProvider`): `reports:client-weekly [--week= --client= --force]` (last ISO week) and `reports:client-monthly [--month= --client= --force]` (last month), scheduled Monday 07:00 / 1st 07:30 Australia/Melbourne with `callAfterResolving(Schedule::class)`; recipient `clients.billing_email` → `contact_email`, no address → skipped (recorded once), inactive clients ignored; idempotent per (client, frequency, period_start) unless `--force`; the mail is the A21 client view (customer figures only) with one CSV per table attached; every send / skip / failure is a `report_deliveries` row (#54). `MAIL_MAILER=log` locally.
- **Tests:** `tests/Feature/Orders/CustomerQuoteEstimateTest.php` (5: mixed pallet + carton lines, urgent code, stock-unit pallets + freight without cost + re-estimate, portal customer prices + isolation, missing rates never $0), `tests/Feature/Reports/ReportsTest.php` (3: boss margin + roles + export, client user isolation without cost / margin, staff client filter), `tests/Feature/Reports/ClientReportMailTest.php` (2: one mail per addressed client with only its own figures, idempotency / --force / --week; monthly + schedule expressions).
- **Left for X1 / C:** decide #50 and then store the freight as a quote line (drop the `notes` snapshot); confirm #51 / #53; routes.md + db-schema.md rows (#52, #54). Still open from part 1: staff-only link to `route('transport.orders.margin', $order->id)` on `orders::show` (X2 handoff); after C's #40 lands, drop the "packed while locked" branch from `FulfilmentService::stepOperational` if wanted. Possible follow-up if clients ask: a PDF version of the mailed report (dompdf is available).

### 2026-09-08 · block/x1-quotes-reports part 2 · portal quote confirmation, invoices, stock, invoice.issued

Acceptance-audit follow-ups owned by X1 (docs/acceptance/2026-09-08 gaps #2, #5 and the Orders half of #7) plus the A7b freight line (#50 → #68) and the block 2 part 1 leftovers, built for X1 by C's agent in X1's conventions (final classes, `portal::` / `orders::` views on `layouts.app`, `lang/zh/{orders,portal}.php`, Feature tests with `RefreshDatabase` + `tests/Support` helpers). `origin/main` (PRs #17, #19) merged first. Suite: **238 passed, 1 skipped** (232 → 238), `pint` clean, `migrate:fresh --seed` clean. Draft PR #18 updated; nothing merged. Assumptions: CHANGE_REQUESTS #70–#74.

- **Portal final quote confirmation** (`app/Modules/Portal/Services/PortalTransportQuotes.php`, `Http/Controllers/PortalQuoteController.php`, panel in `views/orders/show.blade.php`, lang `portal.quotes`; `POST /portal/orders/{order}/quotes/{quote}/confirm`): the order page lists every outbound batch shipment (`fulfilment_id` set — one per packed batch), else the preliminary one, with its `final` quotes: carrier, service level, ETA, 客户价, Recommended / Cheapest / Fastest, expiry, a confirm button per `quoted` quote while the shipment is `quoted`; afterwards the confirmed choice (`selected`, `selected_by`) is shown. Confirmation = Transport's `QuoteSelectionService::select($shipment, $quote, 'client', userId)` (actor validated there, shipment → `quote_confirmed`, `shipment.quote_confirmed` emitted — Billing bills the freight from it, Orders writes no charge). `TransportQuote` is resolved through `whereHas('shipment')` (client-scoped + explicit client id) so a foreign quote is a 404; Transport's `DomainException`s surface as form errors. `cost_cents` / `markup_percent` are never selected (#70).
- **Portal invoices + stock** (`PortalInvoiceController`, `PortalStockController`, `views/invoices/index`, `views/stock/index`, nav 我的账单 / 我的库存, lang `portal.invoices` / `portal.stock`): `GET /portal/invoices` — the client's non-draft invoices with GST figures, status / overdue badge, outstanding total and `GET /portal/invoices/{invoice}/download` through `DocumentDownloader` (draft / foreign / no PDF → 404); `GET /portal/stock` — `stock_units` → `asn_lines` → `asns` (+ `locations.type`) grouped by goods line × location type × condition × put-away flag with pallets / on hand / reserved / available (= on hand − reserved for put-away good stock only) / inbound and totals, search on mark / description / ASN no / FBA ref. Read-only `DB::table()` with the user's `client_id` as an explicit predicate (A21 pattern) (#71).
- **`invoice.issued` consumer** (`app/Modules/Orders/Consumers/InvoiceIssuedConsumer.php`, registered in `OrdersServiceProvider`, lang `orders.billing`): `order_ids` → `OrderStatusService::transitionBilling(…, 'billed')` from `unbilled` / `partially_billed`, system actor, timeline note "计费模块已开出发票 INV-… (服务发票)"; `billed` / `credited` skipped (replay-safe); `job_ids` ignored; unknown / foreign order id throws → retry → dead + integration exception. Customer status becomes `invoiced` (#72).
- **Freight as a real quote line** (`OrderEstimateService::freightLine`, `current()` rewritten, partial `orders::partials.estimate`, lang `orders.estimate`): Transport's preliminary quote (final for pickup_deliver) is stored on the customer quote as `TR-DELIVERY-BASE` with `amount_cents` + `transport_quote_id` (QuoteService pre-priced path, `assumptions.calculation.pre_priced`); notes only carry 待运输报价 when nothing was quoted. The panel reads the freight back from the line (amount) and the transport quote (carrier / ETA / flags) and shows services subtotal / freight / total ex GST / GST / total inc GST; `subtotal_cents` / `gst_cents` on the quote now include the freight (#73). `CustomerQuoteEstimateTest` updated accordingly.
- **Leftovers from part 1**: staff-only link to `route('transport.orders.margin', $order->id)` on `orders::show` (role-gated to the roles X2's `OrderMarginController` accepts; `PortalOrdersTest` keeps asserting it never appears in the portal); the interim "packed while locked → re-sync on release" branch is gone from `FulfilmentService::stepOperational` and `OrderHoldService::release` (Warehouse refuses the handover under a hold, #40 / #46; `FinancialLockTest` asserts the new behaviour) (#74).
- **Tests added:** `tests/Feature/Portal/PortalQuoteConfirmationTest.php` (2: customer prices + confirm / re-select + confirmed view + idempotent + refused after confirmation; foreign quote 404, staff 403), `tests/Feature/Portal/PortalInvoicesAndStockTest.php` (2: own issued invoices only, draft hidden, PDF download, foreign 404; own stock with reserved / available, search, isolation), `tests/Feature/Orders/InvoiceIssuedConsumerTest.php` (2: TestEvent path with skip / replay / foreign-order failure; real path pack → charges → `InvoiceService::issue` → billed).
- **Left for C / X1:** routes.md rows for the four new portal routes (#70, #71); confirm #72 / #73 at M7; ERP_PLAN has no task row for the portal invoice / stock / quote pages (PLT-3 note under §2.3 says they reuse module client views) — nothing new to tick. Possible follow-ups if wanted: a per-quote "why recommended" hint in the portal, PDF of the mailed client report (dompdf available).

### 2026-09-08 · block/x1-feedback-1 · tester feedback items 1, 2, 3b, 5, 6, 7

Project lead's manual-testing feedback (Word doc with screenshots) for the X1 zone, built for X1 by C's agent in X1's conventions (final classes, `orders::` / `portal::` views on `layouts.app`, `lang/zh/{orders,portal}.php`, Feature tests with `RefreshDatabase` + `tests/Support` helpers). Branch cut from `origin/main` (M7 + nav redesign). Suite: **246 passed, 1 skipped** (238 → 246), `pint` clean, `migrate:fresh --seed` clean. Draft PR "Tester feedback (X1 zone): …" opened; nothing merged. Assumptions and requests: CHANGE_REQUESTS #76–#80. One commit per item (`x1/feedback: …`).

- **Item 1 · 我的库存 filters** (`PortalStockController`, `portal::stock.index`, lang `portal.stock.filters`): `condition` = good \| abnormal (quarantine + damaged; predicate on `stock_units.condition`) and `availability` = available \| none (applied to the grouped rows; available = on hand − reserved for put-away good stock), both combining with the free-text search; one flex row — search grows, the two selects and 筛选 stay fixed, wrapping only below 640 px.
- **Item 2 · portal order page layout** (`portal::orders.show`): every block is a `dl.kv` grid (muted 9rem label left, value right, one field per row); 订单信息 uses `kv-2` (two pairs per row ≥ 768 px) and now also shows 尾板车; the delivery / pickup / return blocks list name / phone / address / address type / 送货备注 as rows. CSS inline on the page (#80).
- **Item 3b · address suggestions** (`Services/AddressSuggestionService`, `Http/Controllers/AddressSuggestionController`, Portal `PortalAddressSuggestionController`, `orders::partials.address-suggest`, routes `orders.addresses.suggest` / `portal.addresses.suggest`; #78 / #79): dropdown under 收件地址 from 2 characters (250 ms debounce, same-origin fetch, abortable, keyboard ↑ ↓ Enter Esc); entries = address book first, then distinct past `deliver_to_*` snapshots (most recent first), max 8, deduplicated; choosing one fills name / phone / address / suburb / state / postcode / address type and fires `change` so the tailgate helper re-evaluates. No external provider: a brand-new address gets no suggestions.
- **Item 5 · package type dropdown** (`OrderEnums::PACKAGE_TYPES`, `OrderEnums::packageTypeLabel()`, `orders::partials.package-type-select`, lang `orders.package_types`; #76): staff + portal goods lines and declared packages, `Rule::in` in `OrderController::validated()` and `PortalOrderController::store()`; the edit / add-line forms on `orders::show` use the same select (their controller keeps `string|max:30` so a legacy value such as `box` can be re-saved unchanged); both order pages render unknown values raw. `Http/OrderFormRows::prune()` drops spare form rows whose only content is the dropdown default before validation (a select always submits a value) — this replaced the portal's submit-time row disabling.
- **Item 6 · tailgate checkbox** (`orders::partials.tailgate`, `OrderController::create()/validated()`, `PortalOrderController::create()/store()`, `OrderCreationService::create()`, lang `orders.tailgate.form_*` / `timeline.manual_*_entry`, `portal.tailgate`; #77): hint 按单件重量超过 :kg kg 自动勾选,可取消 with the threshold from `RateService::thresholds(client, 'TR-TAILGATE')` (staff form: per client through `data-tailgate-kg` on the client option, so the hint and the rule follow the selected client; portal: the session client); the JS re-evaluates on every input / change (line weight ÷ cartons, declared package weight, residential address type) unless the person touched the box (`tailgate_manual = 1`); the server stores a hand-made decision with `tailgate_reason = manual` plus a timeline note and `TailgateRule::apply()` leaves it alone at entry and confirmation. Untouched box → automatic rule exactly as before.
- **Item 7 · goods-line rows** (`orders::partials.{goods-lines,goods-line-row,declared-packages,declared-package-row}`, lang `orders.lines`, `orders.actions.add_line/add_package/remove_row`): one compact `table.form-rows` row per line — 中文品名 / 英文品名 / 包装类型 / 箱数 / (件数) / 重量（整行合计）/ 长 / 宽 / 高 / (CBM) / ×, header row instead of labels; rows re-render from `old()` after a failed submit; 新增货物行 clones `<template id="goods-line-template">` (`__INDEX__` → next free index); removing the only row clears it; the first row keeps `goods-required` for the pure-transport toggle. Weight semantics unchanged: a line's `actual_weight_kg` is the line total (TailgateRule divides by cartons) — the header and hint say so.
- **Tests added / extended:** `tests/Feature/Orders/PackageTypeAndDimensionsTest.php` (3), `tests/Feature/Orders/AddressSuggestionTest.php` (2), `TailgateAndTransportOrderTest` (+1: heavy line auto / hand-unticked stays off with `manual` through confirmation and the event / light order hand-ticked on / portal form + page), `tests/Feature/Portal/PortalOrdersTest` (+1 layout grids), `PortalInvoicesAndStockTest` (+1 filters). The inline JS (add / remove rows, auto-tick + touched flag, per-client threshold, suggestion dropdown with a mocked fetch) was smoke-tested in a browser on a standalone render of the partials — Feature tests cannot run it.
- **Not done / left for X1 / C:** external address autocomplete (#78); routes.md rows (#79); enums.md row for the package list (#76); shared CSS (#80). `OrderApiController`, Excel import and PDF drafts still accept free-text package types on purpose. Item 4 of the lead's document was not part of this brief.

### 2026-09-08 · main · tester feedback round 2 (#8 #9 #10) · C, covering X1 for the Portal edits

- **#8 客户注册** (Platform + MasterData, C's zones): `RegistrationController` (`GET/POST /register`), `platform::auth.register`, `ClientController::approve()` + pending-first list, `Enums::CLIENT_STATUSES`, `LoginController` "审核中" message, `config('erp.allow_signup')`. Tests `tests/Feature/Platform/RegistrationTest.php` (3).
- **#9 "$" on every amount** (Billing views; one line in X1's `orders::batches.index` + its assertion in `QueueAndBatchTest`): all through `Money::cents()->format()`.
- **#10 先估价再提交** (Portal zone, X1): `PortalOrderController::preview()` + `rules()` + `formData()`, `OrderEstimateService::preview()` (Orders zone, additive), `portal::orders.create` estimate block + two buttons + invalidate-on-edit JS, lang `portal.actions.get_estimate/confirm_submit`, `portal.estimate.preview_*`. Test `PortalOrdersTest::test_client_gets_an_estimate_before_confirming_and_the_preview_creates_no_order`.
- **Left for X1 (when back):** the staff order form (`/orders/create`) still submits in one step — if the same two-step flow is wanted there, reuse `OrderEstimateService::preview()`.

### 2026-09-08 · block/c14-goods-receipt · tester feedback round 3 item 2 (入库单)

Lead decisions (binding, CHANGE_REQUESTS #90–#92): **ASN = 预报单 (ASN), 入库单 = goods receipt document, never mixed**; all three readings built (printable 入库单 PDF, one-screen 无预报收货, explicit 入库完成); numbering `receipt_no = {asn_no}-R{batch_no}` — every receiving batch of an ASN is its own 入库单 and all batches roll up to the ASN.

- **Warehouse (C):** `goods_receipts` / `goods_receipt_lines` (+ `stock_units.goods_receipt_id`, `asns.receiving_completed_at`), `GoodsReceiptService` (`openFor` / `recordLine` / `complete` / `pdf` / `receiveUnplanned` / `rollup` / `nextReceiptNo`), hooked into `ReceivingService::receiveLine()` (new optional trailing `?int $actorId`; return type unchanged). Pages: 收货 worklist `/warehouse/receiving`, 无预报收货 `/warehouse/receiving/unplanned`, 入库单 `/warehouse/receipts` (+ page, live PDF, 入库完成), ASN page 入库单 section / roll-up / badge, receiving-form hint; nav entries 收货 · 无预报收货 · 入库单; global search finds `receipt_no`. `Enums::DOCUMENT_TYPES` + `platform.documents.types` gain `goods_receipt` (入库单).
- **PDF font:** dompdf's bundled DejaVu Sans has no CJK glyphs (the invoice PDF is English-only, so this never showed). The 入库单 PDF loads a CJK TrueType font from `config('erp.pdf_cjk_font')` (default `storage/fonts/cjk.ttf`, env `PDF_CJK_FONT`; must live inside the project because of dompdf's chroot) when the file exists and falls back to DejaVu Sans otherwise (Chinese then renders blank — Latin / numbers still fine). `storage/fonts/` is git-ignored, and `storage_path()` is per checkout — **in every checkout / deployment copy a CJK font there** (this Mac: `cp "/System/Library/Fonts/Supplemental/Arial Unicode.ttf" storage/fonts/cjk.ttf` — `/Users/jimmy/Desktop/erp-repo` has no `storage/fonts/` yet, so the merge alone will not carry it; a Linux server: Noto Sans CJK SC). No open-licensed CJK font is available offline on this Mac (Arial Unicode is Apple-proprietary and 23 MB, so it is not committed); a subsetted Noto Sans SC at a tracked path is the follow-up that would make the branch self-contained. Because the stored 入库单 PDF is a client-visible document, `GoodsReceiptService::complete()` (and therefore 无预报收货 and `DemoFlowSeeder`) **refuses with the zh message `warehouse.receipts.errors.no_cjk_font` while the font is missing** — nothing blank is filed silently; the live preview route still renders with the DejaVu fallback and `cjkFont()` logs a warning once per process. `GoodsReceiptTest` asserts the embedded font unconditionally, so the suite fails loudly on a checkout without it. **Pre-merge step for C:** `cp storage/fonts/cjk.ttf /Users/jimmy/Desktop/erp-repo/storage/fonts/cjk.ttf` (or set `PDF_CJK_FONT` in its `.env`) *before* `migrate:fresh --seed` / the trial's first 入库完成. Font subsetting is on (a one-line 入库单 PDF is ~25 KB, the 135-line Edward one ~110 KB); the PDF is A4 landscape. Because php-font-lib parses the whole font in memory (~65 MB per render, freed by an explicit `gc_collect_cycles()`), `GoodsReceiptService::pdf()` raises `memory_limit` to 512M for the render when a CJK font is configured, and `phpunit.xml` now sets `memory_limit = 512M` for the test process (the suite peaks at ~180 MB with the font present; without the font it stays where it was).
- **X1 zone — please read:** C edited three of your lang files as integrator (X1 out of quota), lang-only, listed key by key in CHANGE_REQUESTS #92: `orders.batches.title` / `orders.batches.asn`, `portal.stock.search` / `portal.stock.fields.asn_no`, `reports` inbound hint / `asns` count label. The portal stock page therefore now says **预报单号 (ASN)** where it said 入库单号. The completed 入库单 PDF is a client-visible `goods_receipt` document on the ASN (`related_type = asn`), downloadable through `portal.documents.download` (by id only); a portal link / list of 入库单 documents (e.g. on 我的库存 or a 我的单据 page) is a possible X1 follow-up — nothing in the portal links to them yet, and the demo guide / `receipts.hint` say so (单据中心 only).
- **Demo:** `DemoFlowSeeder` completes the Edward container batch and each loose-truck batch (`edward_receipt_no` in the output); `docs/demo-guide.zh.md` §4 / §5 / §7 / §8 updated (收货 → 入库完成 → PDF → 上架; 无预报收货 sub-step; FAQ on ASN vs 入库单 and why customer_service has no 收货 button).
- **Review fixes (same day):** `openFor()` reads the ASN's receipts `FOR UPDATE` after the ASN lock — under REPEATABLE READ a plain SELECT after waiting on the lock returned the pre-commit snapshot, so the second operator built the same `batch_no` and failed on the unique index instead of joining the batch; `complete()` re-reads the receipt `FOR UPDATE` inside its transaction and runs the open / has-lines / font guards on that row, so a double-submitted 入库完成 is refused instead of filing a second `goods_receipt` document (the two forms also disable their button on submit); the 待收货 worklist honours an explicit 仓库 = 全部 (`$request->has('warehouse_id')`) and the dropdown shows the warehouse actually applied.
- **Tests:** `tests/Feature/Warehouse/GoodsReceiptTest.php` (9), `DemoFlowTest` asserts the completed 入库单 + document. Out of scope, unchanged: partial per-line receiving across batches, new ASN statuses, undoing receipts, portal ASN creation, tester items 1 / 3 / 4.


### 2026-09-10 · main · no-permission page (CR #93) · C touched X1/X2 controllers

- Mechanical replacement only: `abort_unless(<user>->hasAnyRole(ROLES), 403[, MSG])` → `RequiredRoles::requireAny(ROLES[, MSG])` (`App\Support\Auth\RequiredRoles`) in Orders / Reports / Transport controllers so `errors/403` can list the allowed roles. Same 403 status, same messages. New code-level role checks should call the helper.

### 2026-09-10 · main · 作业登记 (CR #95) · C added one link in X1's `orders::show`

- `app/Modules/Orders/views/show.blade.php`: a warehouse-roles-only button 登记缠膜 / 人工时 → `warehouse.tasks.create?order_id=…&task_type=wrap` (lang `orders.actions.register_vas`). Nothing else in the Orders zone changed.

### 2026-09-10 · main · immediate outbox dispatch (CR #105) · C touched two X1 tests

- `tests/Feature/Orders/OrderManagementTest.php`: the order.confirmed row is delivered immediately now, so the test no longer asserts `status = pending`. `tests/Feature/Orders/QueueAndBatchTest.php`: the queue-preset test disables `erp.outbox_dispatch_now` because it pins statuses by hand. No production code in the Orders zone changed.
### 2026-09-10 · fix/audit-x-2026-09-10 · audit lane B (Orders / Portal / Transport / Reports) · C as integrator, X1 / X2 zones

Fixes for the confirmed 2026-09-10 audit findings in the X1 / X2 zones. Every file touched is listed per package so X1 / X2 can rebase and review.

- **B1 · blockers.** `GET /orders/batches` (nav link 入库批次, no query string) 500ed with `Undefined array key "ref"` — `(string) $validated['ref'] ?? ''` cast before the coalesce and `validate()` omits an absent key. Coalesce now happens before the cast; `?ref=` and `?ref=<value>` behave as before. Files: `app/Modules/Orders/Http/Controllers/BatchController.php`, `tests/Feature/Orders/QueueAndBatchTest.php` (+1 `test_batch_page_opens_without_a_reference`).
- **B2 · Orders majors.** (1) *Un-confirmable from_stock order*: the ASN goods-line picker is now always rendered on a received from_stock order (each 修改此行 form **and** the 新增货物行 form, new partial `orders::partials.asn-line-select` with a 唛头 / 品名 / ASN 号 filter box, inline JS in `orders::show`); when the client has no ASN lines yet the select is empty and the hint `orders.drafts.no_asn_lines_yet` says what to do; the page warns above 确认 (`orders.drafts.unlinked_warning`) and `OrderController::confirm()` now names the unlinked lines (`orders.validation.unlinked_stock_lines`, old key `unlinked_stock` kept). Not done: switching the order type of a PDF draft / confirming without line-level ASN links — both would be rule changes (A7), raise as an open question if wanted. (2) *Add-line 包装类型*: `OrderLineController::store()` array union reversed — the submitted type wins, carton is the fallback. (3) *Job of another client*: `OrderController::validated()` and `OrderImportController::preview()` throw a `job_id` `ValidationException` (redirect back with old input) instead of `abort(422)`; the manual form's Job select is filtered by client (`data-client-id`, same JS as drafts.create); the import form shows `JOB — 客户名`, is filtered the same way and keeps `service_level` via `old()`. Job stays mandatory on the import (the import service needs one). (4) *Hidden 提货 fieldset*: `toggleType()` also sets `pickup.disabled` so nothing in it is submitted; `OrderFormRows::prune()` drops `declared_packages` whenever `order_type !== 'pickup_deliver'` (also covers the portal form, which shares the helper). (5) *箱数-only row vanished*: `carton_qty` is now line content in `OrderFormRows::LINE_CONTENT` and `orders::partials.goods-lines` no longer pre-fills 箱数 = 1, so the row reaches validation and fails on 品名 instead of disappearing; a row carrying only the dropdown default is still pruned. (6) *English validation with dotted keys*: new `App\Modules\Orders\Http\OrderValidation::{messages,attributes}()` (arrays in `lang/zh/orders.php` → `validation.attributes` / `validation.messages`, wildcard keys + `:position` → "第 2 行货物的箱数必填。") passed as the 2nd / 3rd argument by every Orders validator (`OrderController` store + update, `OrderLineController`, `OrderImportController`, `DraftOrderController`, `ClientAddressController`). `lang/zh/validation.php` is **not** created here (lane A / seat C owns it) — scalar-field rules still fall back to Laravel's English sentence until it lands, but with the Chinese attribute name. (7) *API 500 without package_type*: `OrderCreationService::create()` defaults a missing / null line `package_type` to carton (the column is NOT NULL); the usage example on 订单 API 接入 now includes `"package_type":"carton"`. Files: `app/Modules/Orders/Http/OrderValidation.php` (new), `app/Modules/Orders/Http/OrderFormRows.php`, `app/Modules/Orders/Http/Controllers/{OrderController,OrderLineController,OrderImportController,DraftOrderController,ClientAddressController}.php`, `app/Modules/Orders/Services/OrderCreationService.php`, `app/Modules/Orders/views/{show,form}.blade.php`, `app/Modules/Orders/views/imports/create.blade.php`, `app/Modules/Orders/views/api-tokens/index.blade.php`, `app/Modules/Orders/views/partials/goods-lines.blade.php`, `app/Modules/Orders/views/partials/asn-line-select.blade.php` (new), `lang/zh/orders.php`, `tests/Feature/Orders/OrderFormAuditFixesTest.php` (new, 7 tests). Orders suite 60 green, Portal 12 green.
- **B3 · Portal.** (1) *Hidden 提货包裹 rows blocked 获取估价 / 确认提交* (major): `toggleType()` in `portal::orders.create` now also sets `pickup.disabled`, so a hidden 提货 fieldset submits nothing; a stale row that still reaches the server is dropped by `OrderFormRows::prune()` (B2) — covered by a Portal test on preview + store. (2) *申请退货 offered on line-less 纯运输 orders* (major): `PortalOrderController::show` passes `canRequestReturn = acceptsReturnRequest() && lines->isNotEmpty()`; the page shows `portal.returns.pure_transport` (请联系客服) instead of a form with no quantity rows. Staff twin: `OrderController::show` gets the same gate and `orders::show` prints `orders.returns.messages.pure_transport`. `PortalReturnController` / `ReturnController` make `quantities` optional (`nullable|array`), so an empty selection is answered by the service's Chinese 请至少选择一行货物 rather than an English "quantities field is required". Not done: returning declared packages (would need `ReturnRequestService` to build return lines from `declared_packages` — a rule change, raise in ERP_PLAN if wanted). (3) *Expired final quote still had a 确认此方案 button* (minor): `PortalTransportQuotes::forOrder` sets `expired` / `can_confirm` per quote row (same `expires_at->isFuture()` guard as the staff shipment page) plus `all_expired` per shipment; the portal shows an 已过期 badge, no button, and `portal.quotes.expired_hint` (请联系客服重新报价) when every quote is stale. (4) *箱数-only row pruned with an internal `lines` error* (minor): fixed in B2 (`carton_qty` is row content, no pre-filled 1); the portal validators now pass `PortalValidation::messages()/attributes()` (Orders row-aware set + `portal.validation.*`), so the row fails on 第 1 行货物请填写中文或英文品名 and the typed 箱数 survives in `old()`. (5) *Added rows had no `required`* (minor): `orders::partials.goods-line-row` marks 包装类型 / 箱数 `goods-required` + `required` on every row including the `<template>` row; both forms (`portal::orders.create`, `orders::form`) re-run `toggleType()` on 添加货物行 so pure-transport orders still get the flags cleared on new rows. `requested_date` also carries `min=today`. (6) *Rejected 申请退货 wiped the panel* (minor): `withInput()` on the `OrderRuleViolation` branch (portal and staff), `<details open>` when `$errors->has('return')`, `old('reason')` / `old('quantities.<id>')` on the inputs. (7) *English validation, dotted keys, list printed twice* (minor): new `App\Modules\Portal\Http\PortalValidation` (merges `OrderValidation` with `lang/zh/portal.php` → `validation.attributes` / `validation.messages`: 退货原因, 退回箱数, 收件地址 etc., `reason.required`, `requested_date.after_or_equal` …) passed by `PortalOrderController` (store + preview) and `PortalReturnController`; the duplicate error list in `portal::orders.create` is gone (the layout flash renders `$errors` once). `lang/zh/validation.php` still belongs to lane A — untranslated scalar rules keep Laravel's English sentence with the Chinese attribute name until it lands. Files: `app/Modules/Portal/Http/PortalValidation.php` (new), `app/Modules/Portal/Http/Controllers/{PortalOrderController,PortalReturnController}.php`, `app/Modules/Portal/Services/PortalTransportQuotes.php`, `app/Modules/Portal/views/orders/{create,show}.blade.php`, `app/Modules/Orders/Http/Controllers/{OrderController,ReturnController}.php`, `app/Modules/Orders/views/{form,show}.blade.php`, `app/Modules/Orders/views/partials/goods-line-row.blade.php`, `lang/zh/{portal,orders}.php`, `tests/Feature/Portal/PortalAuditFixesTest.php` (new, 7 tests). Portal 19 green, Orders 60 green.
- **B4 · Transport (X2 zone).** (1) *Nav dead links* (minor ×2): `layouts/nav/transport` gates 班次 `@role('admin|customer_service|dispatcher|transport_operator')`, 承运商对账 `@role('admin|transport_operator|finance')`, 司机任务 `@role('transport_operator')` — the same role lists as `DeliveryRunController` / `CarrierInvoiceController` / `DriverController`; 运输 and 运输异常 stay open to every staff role (no server check). `DriverController` keeps transport_operator-only (the page lists the signed-in driver's runs; an admin would see an empty page) — now via `RequiredRoles::requireAny` so the 403 page names the role. (2) *Shipment page offered forms the server 403s* (major): every action block in `transport::shipments.show` (预订 / 人工报价 / 选择·确认方案 / 重派 / 承运商 POD / 额外费用) is wrapped in `@role('admin|customer_service|dispatcher|transport_operator')`, 自有车队成本 in `@role('admin|dispatcher|transport_operator|finance')`, matching its controller; the remaining `abort_unless(...hasAnyRole..., 403)` calls in `ExtraChargeController`, `CarrierPodController`, `RedeliveryController`, `RunStopController`, `DeliveryRunController`, `ShipmentLabelController` were converted to `RequiredRoles::requireAny` (CR #93 follow-through). (3) *打印运单 dead-ended on a 422 page* (major): new `ShipmentLabelService::available()` (final + selected quote, and own fleet / archived waybill / booked gateway whose adapter declares `label`); `ShipmentController::show` passes `canPrintLabel`, the page shows the button only then and otherwise a hint (`transport.labels.manual_unavailable` for a booked manual carrier, `transport.labels.not_ready` for a preliminary / unbooked selection); `ShipmentLabelController` redirects to the shipment page with `errors.label` instead of `abort(422)` (B6 test updated accordingly). (4) *人工报价 500 above 999.99 % markup* (minor): `ManualQuoteService::MAX_MARKUP_PERCENT` guard → `DomainException` `transport.manual_quote.markup_too_high`; the controller keeps the input (`withInput()`). No schema change. (5) *人工报价 form offered when the booking request cannot be built* (major): `ShipmentController::show` computes `manualQuoteStages` (stages for which `ShipmentQuoteRequestFactory::build()` is non-null, only while quoting / quoted); the form offers only those stages (+ `stage_unavailable_hint`), or prints `manual_quote.details_unavailable_hint` (仓库打包交接后 / 取货地址 + 申报包裹) instead of a form that always fails. (6) *Run stop reorder refused the single-number edit* (minor): `positions.*` no longer `distinct`; `RunStopController::orderedStopIds()` — distinct numbers are sorted as before, exactly one changed number is treated as "move this stop to that position" (insert semantics, others keep order), several changed stops on one number → `transport.runs.duplicate_positions` (Chinese, input kept via `withInput()` + `old('positions.<id>')`); page carries `runs.reorder_hint`. (7) *Driver POD silent submit without a signature* (minor): the message is rendered in a `<p role="alert" data-signature-error hidden>` next to the canvas (shown on submit, hidden on the next stroke) instead of `setCustomValidity` on a hidden input; the inert `required` was dropped; the server rule is unchanged and now reads 请先在签名框中签名。. (8) *Quote selection flashed under `success`* (minor): `QuoteSelectionController` uses `status` (B5d test updated). (9) *English validation / dotted keys / lost input* (minor): new `App\Modules\Transport\Http\TransportValidation::{messages,attributes}()` (arrays in `lang/zh/transport.php` → `validation.attributes` / `validation.messages`, incl. `photos.*` / `positions.*` with `:position` and a module-scoped set of generic per-rule sentences so Transport never prints Laravel's English default) passed by every Transport validator (`ManualQuoteController`, `ShipmentBookingController`, `CarrierPodController`, `ExtraChargeController`, `OwnFleetCostController`, `RunStopController` store + reorder, `DriverController` deliver + fail, `DeliveryRunController::store`, `CarrierInvoiceController::store`); `withInput()` added to the business-rule branches of `ManualQuoteController`, `ShipmentBookingController`, `OwnFleetCostController`, `RunStopController` (both), `DriverController::fail`; `old()` on the run page's shipment select and the extra-charge selects. Files: `resources/views/layouts/nav/transport.blade.php`, `app/Modules/Transport/Http/TransportValidation.php` (new), `app/Modules/Transport/Http/Controllers/{ShipmentController,ShipmentLabelController,ManualQuoteController,QuoteSelectionController,ShipmentBookingController,RedeliveryController,CarrierPodController,ExtraChargeController,OwnFleetCostController,RunStopController,DriverController,DeliveryRunController,CarrierInvoiceController}.php`, `app/Modules/Transport/Services/{ShipmentLabelService,ManualQuoteService}.php`, `app/Modules/Transport/views/shipments/show.blade.php`, `app/Modules/Transport/views/runs/show.blade.php`, `app/Modules/Transport/views/driver.blade.php`, `lang/zh/transport.php`, `tests/Feature/Transport/TransportAuditFixesTest.php` (new, 7 tests), `tests/Feature/Transport/{B5dQuoteSelectionTest,B6ShipmentLabelTest}.php` (assertions follow the new flash key / redirect). Transport 59 green (Karrio live skipped).

- **B5 · Orders / Reports minors (+ one Transport controller).** (1) *确认订单 for every staff role* (minor ×2, static + dynamic): `orders::show` renders the confirm form only for `admin | customer_service | dispatcher` (the list in `OrderController::authorizeOrderEntry()`), other staff read `orders.actions.confirm_needs_role` instead of clicking into the 403 page. (2) *毛利 link 404s without a shipment* (minor): kept the link in `orders::show`; `OrderMarginController` (X2 zone) now renders `transport::orders.margin` with an empty state (`transport.costs.order_no_shipments`, no table / formula) for an order that has no shipment yet or never gets one (退货), and 404s only when neither an `orders` row nor a shipment exists (B9a builds shipments against a bare `order_id`, so that path must keep working). (3) *Import / draft / API-token forms lose input* (minor): `orders::drafts.create` 服务等级 `@selected(old('service_level','standard'))` (the import form was already fixed in B2), `orders::api-tokens.index` `@selected(old('client_id'))` + `value="{{ old('name') }}"` + `maxlength=100`; `ApiTokenController::store` passes `OrderValidation::messages()/attributes()` (new attribute `name` → 令牌名称). (4) *Reports filter loses every input* (minor): `reports::partials.filters` reads `old('client_id' | 'from' | 'to', <resolved value>)`. (5) *Reports validation in English with raw keys* (minor): new `App\Modules\Reports\Http\ReportValidation` (arrays in `lang/zh/reports.php` → `validation.attributes` 开始日期 / 结束日期 / 客户, `validation.messages` incl. `to.after_or_equal`, `client_id.required|exists` and a module-scoped generic set) passed by `BossReportController`, `ClientReportController` and `PortalReportController` (index + export each). `lang/zh/validation.php` still belongs to lane A. (6) *366-day range silently truncated* (minor): new rule `App\Modules\Reports\Rules\MaxPeriodSpan` (DataAwareRule) appended to `ReportPeriod::rules()['to']` — a longer span is a `to` error `reports.validation.messages.max_days` (names the latest allowed 到 date); the clamp in `fromInput()` is kept only for programmatic callers (mail commands); the filter bar prints `reports.filters.span_hint`. Files: `app/Modules/Orders/views/{show,drafts/create,api-tokens/index}.blade.php`, `app/Modules/Orders/Http/Controllers/ApiTokenController.php`, `app/Modules/Transport/Http/Controllers/OrderMarginController.php`, `app/Modules/Transport/views/orders/margin.blade.php`, `app/Modules/Reports/Http/ReportValidation.php` (new), `app/Modules/Reports/Rules/MaxPeriodSpan.php` (new), `app/Modules/Reports/Services/ReportPeriod.php`, `app/Modules/Reports/Http/Controllers/{BossReportController,ClientReportController}.php`, `app/Modules/Portal/Http/Controllers/PortalReportController.php`, `app/Modules/Reports/views/partials/filters.blade.php`, `lang/zh/{orders,transport,reports}.php`, `tests/Feature/Orders/OrderFormAuditFixesTest.php` (+2), `tests/Feature/Transport/TransportAuditFixesTest.php` (+1), `tests/Feature/Reports/ReportsAuditFixesTest.php` (new, 2). Full suite 295 green, 1 skipped (Karrio live). CHANGE_REQUESTS #100–#104 summarise the lane per module.

#### Notes — every X1 / X2-zone file touched by lane B (B1–B5), for rebase and review

Orders (X1): `app/Modules/Orders/Http/Controllers/{ApiTokenController,BatchController,ClientAddressController,DraftOrderController,OrderController,OrderImportController,OrderLineController,ReturnController}.php`, `app/Modules/Orders/Http/{OrderFormRows,OrderValidation}.php` (the latter new), `app/Modules/Orders/Services/OrderCreationService.php`, `app/Modules/Orders/views/{show,form}.blade.php`, `app/Modules/Orders/views/{api-tokens/index,drafts/create,imports/create}.blade.php`, `app/Modules/Orders/views/partials/{asn-line-select (new),goods-line-row,goods-lines}.blade.php`, `lang/zh/orders.php`, `tests/Feature/Orders/{OrderFormAuditFixesTest (new),QueueAndBatchTest}.php`.
Portal (X1): `app/Modules/Portal/Http/Controllers/{PortalOrderController,PortalReportController,PortalReturnController}.php`, `app/Modules/Portal/Http/PortalValidation.php` (new), `app/Modules/Portal/Services/PortalTransportQuotes.php`, `app/Modules/Portal/views/orders/{create,show}.blade.php`, `lang/zh/portal.php`, `tests/Feature/Portal/PortalAuditFixesTest.php` (new).
Reports (X1): `app/Modules/Reports/Http/Controllers/{BossReportController,ClientReportController}.php`, `app/Modules/Reports/Http/ReportValidation.php` (new), `app/Modules/Reports/Rules/MaxPeriodSpan.php` (new), `app/Modules/Reports/Services/ReportPeriod.php`, `app/Modules/Reports/views/partials/filters.blade.php`, `lang/zh/reports.php`, `tests/Feature/Reports/ReportsAuditFixesTest.php` (new).
Transport (X2): `resources/views/layouts/nav/transport.blade.php`, `app/Modules/Transport/Http/Controllers/{CarrierInvoiceController,CarrierPodController,DeliveryRunController,DriverController,ExtraChargeController,ManualQuoteController,OrderMarginController,OwnFleetCostController,QuoteSelectionController,RedeliveryController,RunStopController,ShipmentBookingController,ShipmentController,ShipmentLabelController}.php`, `app/Modules/Transport/Http/TransportValidation.php` (new), `app/Modules/Transport/Services/{ManualQuoteService,ShipmentLabelService}.php`, `app/Modules/Transport/views/{driver,orders/margin,runs/show,shipments/show}.blade.php`, `lang/zh/transport.php`, `tests/Feature/Transport/{TransportAuditFixesTest (new),B5dQuoteSelectionTest,B6ShipmentLabelTest}.php`.
Not touched: `contracts/*.md` (only `CHANGE_REQUESTS.md` rows #100–#104 appended), `app/Support/**`, `lang/zh/validation.php` (lane A), routes, migrations, seeders, lane A modules.

#### Notes — lane B polish (reviewer residuals after B1–B5)

- **Reports · 366-day cap without `from`** (residual of the silent-truncation finding): `MaxPeriodSpan` used to return early when `from` was absent, so `GET /reports?to=2028-12-31` (hand-edited URL / bookmark, page and CSV export, boss / client / portal alike) passed validation and `ReportPeriod::fromInput()` clamped `to` against its own default with no message. The rule now takes an absent `from` as the same default `fromInput()` uses (`MaxPeriodSpan::defaultFrom()` = `ReportPeriod::currentMonth()->from`, one source), so the span check covers every request the clamp would otherwise shorten; the clamp itself stays for the mail commands. Sibling case left as is (not in the finding): `?to=<date before the current month>` without `from` still becomes a one-day period `from = to = <month start>` — raise as a follow-up if wanted. Files: `app/Modules/Reports/Rules/MaxPeriodSpan.php`, `tests/Feature/Reports/ReportsAuditFixesTest.php` (+1 `test_cap_applies_when_from_is_omitted_from_the_url`).
- **Transport · `markup_too_high` wording**: the guard in `ManualQuoteService` is strict (`> 999.99 %`), so exactly 11 × cost (1000.00 %) is refused; the sentence said 不能超过 11 倍, which reads as 11 × allowed. Now 必须低于内部成本的 11 倍（加价率上限 999.99%）. Files: `lang/zh/transport.php` (text only; the test asserts through `__()`).

### 2026-09-10 · main · ASN goods-line picker (X1 zone, integrator)

- `app/Modules/Orders/views/partials/asn-line-select.blade.php` + the inline JS in `orders::show`: the search box is no longer a form control (`form="asn-filter-none"`) and swallows Enter, so it cannot submit the line form; the picker sits in its own full-width labelled block with a match count and auto-select when one row remains; lang `orders.drafts.asn_picker_title / asn_filter_matches / asn_filter_placeholder / asn_link_hint / no_asn_line` reworded.
- `orders::show` goods table: the line editor and the add-line form moved to full-width rows with labelled fields; 保存修改 / 添加货物行 buttons are inline (`width:auto`). Tester feedback 2026-09-10.

### 2026-09-10 · main · item 4 B/C/D (CR #106) · C touched Orders zone

- `app/Modules/Orders/Services/FulfilmentService.php` (resolveOpen('stock_shortage') once no line is backordered), `app/Modules/Orders/Http/Controllers/OrderController.php` (`shortages` for the order page), `app/Modules/Orders/views/show.blade.php` (缺货 banner), `lang/zh/orders.php` (`fulfilments.shortage_*`).
- `app/Modules/Transport/views/shipments/show.blade.php` header + `ShipmentController::show` (`orderNo`) + lang `transport.shipments.margin_link` (X2 zone, integrator): title with status badge, two-column dl.kv card, order number linked to the order page. Tester feedback 2026-09-10.
- Detail-page facts as two-column cards (tester feedback 2026-09-10): `orders::show` 订单指令 + 收货信息, `transport::runs.show`, `transport::carrier-invoices.show`, `transport::shipments.show` use `article.kv-card` + `dl.kv-2`; app.css now styles every `<dl>` as the grid by default (C, frozen zone).

### 2026-09-10 · main · client cancellation (CR #111) · C touched Orders + Portal zones

- Orders: `Services/OrderChangeService.php` (cancelByClient / requestCancel / rejectCancelRequest / openCancelRequest / latestCancelRequest; `cancel()` now resolves open requests; ExceptionService injected), `Http/Controllers/OrderChangeController.php` (rejectCancelRequest), `Http/Controllers/OrderController.php` (`cancelRequest`), `routes.php`, `views/show.blade.php` (banner), `lang/zh/orders.php` (`cancel_request.*`, `changes.messages.client_*`, `changes.timeline.cancelled_by_client`).
- Portal: `Http/Controllers/PortalOrderCancelController.php` (new), `Http/Controllers/PortalOrderController.php` (`canCancel` / `canRequestCancel` / `cancelRequest`), `routes.php`, `views/orders/show.blade.php` (可用操作 block, `#return-request`), `lang/zh/portal.php` (`cancel.*`).

### 2026-09-10 · main · 客户请求 inbox (CR #112) · C touched Orders zone

- New: `app/Modules/Orders/Services/ClientRequestService.php`, `app/Modules/Orders/Http/Controllers/ClientRequestController.php`, `app/Modules/Orders/views/requests/index.blade.php`; edited `app/Modules/Orders/routes.php`, `resources/views/layouts/nav/orders.blade.php` (客户请求 + badge; queue link unchanged), `lang/zh/orders.php` (`requests.*`, `queue.title` spacing).

### 2026-09-10 · i18n/zh-sweep-x · i18n sweep lane B (Orders / Portal / Reports / Transport) · C as integrator, X1 / X2 zones

Wording sweep for the lead's 2026-09-10 rule (整个系统的每一处报错都用中文): every message a person can see in the X1 / X2 modules is Chinese. Behaviour is unchanged except that carrier booking failures now carry a Chinese status label with the raw carrier status and the gateway's own message appended. CHANGE_REQUESTS rows #109 (Orders / Portal / Reports) and #110 (Transport). Lane A owns `lang/zh/validation.php` and the framework error pages — untouched here.

- **B1 · Orders.** `orders.customer_statuses.*` → Chinese labels (enum keys unchanged); `OrderHoldService::release()` stale-hold refusal → new key `orders.holds.messages.not_active` (flashed by `HoldController::release()`).
- **B2 · Portal / Reports.** `portal::orders.create` estimate preview renders the uom through the **pre-existing** `orders.estimate.uoms.*` keys (view change only, no new lang key); `PortalOrderController::index()` / `PortalStockController::index()` pass the existing `PortalValidation::messages()/attributes()` to their filter `validate()` calls, and `lang/zh/portal.php` `validation.attributes` gained the six filter names (`q`, `status`, `from`, `to`, `condition`, `availability`) — `PortalValidation.php` itself is unchanged; `portal.messages.client_only` reworded to cover every portal page; the four `fba_reference` labels (`orders.fields`, `orders.validation.attributes`, `orders.imports.fields`, `portal.fields`) = FBA 货件编号（Shipment ID）. Reports: no production change.
- **B3 · Transport.** `ManualCarrierAdapter::book()` missing-reference refusal → new key `transport.booking.manual_reference_required`; `ShipmentStatusMachine::transition()` `DomainException` message → the **existing** key `transport.tracking.invalid_transition` (production change, one line; `B5ShipmentTest` gained one `expectExceptionMessage` assertion); `ShipmentBookingService` builds the booking-failure reason as Chinese label（raw status）+ gateway detail (new keys `booking.carrier_status_reason`, `booking.carrier_detail`, `booking.carrier_statuses.{new,pending_payment,pending_review,request_failed,cancelled}`, `booking.carrier_status_other`).
- **B4 · polish (reviewer residuals).** `orders.fulfilments.fields.backordered` Backorder → 缺货箱数 and `orders.fulfilments.batches_title` → 分批履约与缺货; FBA 引用 → FBA 货件编号 in `orders.imports.hint`, `orders.imports.errors.inconsistent_group`, `portal.stock.search`; the hardcoded `箱` in `orders::partials.asn-line-select` → new key `orders.drafts.asn_option_cartons` (`:count 箱`); `transport.booking.hold_hint` Finance → 财务, `transport.labels.{print_waybill,waybill_unavailable,manual_unavailable}` Waybill → 运单, `transport.extra_charges.reported` Billing → 计费模块. `ShipmentBookingService`: a non-failure carrier status without a booking reference now reads the new key `transport.booking.missing_reference` (承运商未返回预订参考号（:status）) instead of the "not booked" label; `carrierDetail()` looks up `message`, then `detail`, then `error` at each level before recursing (Karrio bodies carry the machine code in `error` next to the sentence in `message`), still ≤ 200 chars. Behaviour otherwise identical.

#### Notes — every X1 / X2-zone file touched by the i18n sweep (B1–B4), for rebase and review

Orders (X1): `app/Modules/Orders/Services/OrderHoldService.php` (one message), `app/Modules/Orders/views/partials/asn-line-select.blade.php` (one option label), `lang/zh/orders.php` (`customer_statuses.*`, `holds.messages.not_active` new, `fields.fba_reference`, `validation.attributes.fba_reference`, `imports.fields.fba_reference`, `imports.hint`, `imports.errors.inconsistent_group`, `fulfilments.fields.backordered`, `fulfilments.batches_title`, `drafts.asn_option_cartons` new), `tests/Feature/Orders/FinancialLockTest.php`, `tests/Feature/Orders/OrderManagementTest.php`, `tests/Feature/Orders/FulfilmentAllocationTest.php`, `tests/Feature/Orders/OrderImportTest.php`.
Portal (X1): `app/Modules/Portal/Http/Controllers/PortalOrderController.php` (one `validate()` call), `app/Modules/Portal/Http/Controllers/PortalStockController.php` (one `validate()` call + import), `app/Modules/Portal/views/orders/create.blade.php` (uom cell), `lang/zh/portal.php` (`fields.fba_reference`, `validation.attributes.{q,status,from,to,condition,availability}` new, `messages.client_only`, `stock.search`), `tests/Feature/Portal/PortalInvoicesAndStockTest.php`, `tests/Feature/Portal/PortalQuoteConfirmationTest.php`.
Reports (X1): `tests/Feature/Reports/ReportsTest.php` only (asserts the Chinese reversed-range message on the page; no production file changed — Reports was already through `lang/zh`).
Transport (X2): `app/Modules/Transport/Adapters/ManualCarrierAdapter.php` (one message), `app/Modules/Transport/Services/ShipmentStatusMachine.php` (one message), `app/Modules/Transport/Services/ShipmentBookingService.php` (`NOT_BOOKED_STATUSES` / `DETAIL_KEYS` constants, private `carrierReason()` / `carrierDetail()`), `lang/zh/transport.php` (`booking.manual_reference_required`, `booking.missing_reference`, `booking.carrier_status_reason`, `booking.carrier_detail`, `booking.carrier_statuses.*`, `booking.carrier_status_other` new; `booking.hold_hint`, `labels.print_waybill`, `labels.waybill_unavailable`, `labels.manual_unavailable`, `extra_charges.reported` reworded), `tests/Feature/Transport/B5ShipmentTest.php` (+1 assertion), `tests/Feature/Transport/B9aCarrierCostMarginTest.php` (+1 test with three refusal scenarios).
Left English on purpose: consignment note / own-fleet label / waybill PDFs (third-party documents: `transport.labels.{own_fleet,ship_to,shipment,package,weight,dimensions}` on the own-fleet label PDF), Transport / Orders model enum guards and consumer guards no browser user reaches, `SyncTrackingCommand` description, `ClientReportMailer` frequency guard, console summary keys, carrier tracking status text and gateway `message` text (third-party data), `Log::*`, unit symbols (kg / mm / CBM placeholders), acronyms and proper nouns (ASN / API / PDF / GST / Excel / Transdirect / EIZ / Karrio), the system-wide terms `Job` and `Coordinator` (used identically in lane A's `platform.php` / `warehouse.php` / `billing.php` — a rename is a cross-lane decision), and `token` on the admin API page (technical term next to `Bearer token`).
Not touched: `contracts/*.md` other than `CHANGE_REQUESTS.md` rows #109–#110, `app/Support/**`, `lang/zh/validation.php`, routes, migrations, seeders, lane A modules.
**For lane A:** align `lang/zh/validation.php:257` `attributes.fba_reference` ('FBA Shipment ID' → 'FBA 货件编号（Shipment ID）') and `lang/zh/warehouse.php:116` `blocked_reasons.inconsistent_delivery_or_fba` ('FBA 引用' → 'FBA 货件编号') with the Orders / Portal term.

### 2026-09-11 · main · English PDFs (CR #114) · C touched X2's three Transport PDF templates

- **Why.** Lead request: every PDF a client or carrier receives prints in English; the screens stay Chinese.
- **Transport files (X2 zone), Blade only — no PHP changed:** `app/Modules/Transport/views/shipments/consignment-note.blade.php`, `app/Modules/Transport/views/shipments/label.blade.php`, `app/Modules/Transport/views/driver/pod-pdf.blade.php`. Every `__('transport.…')` became `__('pdf.…')` from the new `lang/en/pdf.php`; `<html lang="en">`; the consignment note prints package types / service levels in English and the generated-at / delivered-at times as `d M Y H:i`. `lang/zh/transport.php` is untouched — `transport.consignment_note.*`, `transport.labels.*`, `transport.driver.*` are still used by the screens, error messages and tests.
- **Tests.** None of X2's tests changed (`B5ShipmentTest` asserts label codes only; `B6` / `B7` assert PDF bytes). New `tests/Feature/Platform/PdfEnglishTest.php` scans the seven templates. On rebase keep the `pdf.*` keys; a new word on a PDF goes into `lang/en/pdf.php` (C's file — append a CHANGE_REQUESTS row).
- **Formatting only (X1 zone):** `./vendor/bin/pint` re-ordered the `use` lines in `app/Modules/Orders/routes.php` and `app/Modules/Portal/routes.php` (pre-existing `ordered_imports` drift on main that made `pint --test` red; no code change).

### 2026-09-11 · main · 客户自助预报入库 (CR #116) · C built in X1's Portal zone as integrator

- **Why.** Lead request: clients submit their own ASN (packing list + container + ETA) in the portal, or push it through an API; customer service confirms. Staff flows unchanged.
- **Portal files (X1 zone), all new unless noted:** `app/Modules/Portal/migrations/2026_09_11_100010_create_portal_asn_submissions_table.php`, `Models/PortalAsnSubmission.php`, `Services/PortalAsnService.php`, `Http/Controllers/PortalAsnController.php`, `Http/Controllers/PortalAsnApiController.php`, `views/asns/{index,create,show}.blade.php`; edited `routes.php` (+7 routes), `resources/views/layouts/nav/portal.blade.php` (预报入库 link), `lang/zh/portal.php` (`asns.*` section, validation attributes / messages). The API reuses Orders' `OrderApiTokenService` (read-only call) — no Orders file changed.
- **Warehouse side (C's own zone):** `asns.client_confirmed_at/by`, `AsnService::confirmClientSubmission / clearDraftLines / pendingClientConfirmationCount`, `warehouse.asns.confirm_client`, list filter, banner, nav badge.
- **Tests.** New `tests/Feature/Portal/PortalAsnTest.php` (form submit with the real workbook, detail + 入库单 download, 补传, isolation, 客服 confirm, API push + replay + 401 / 422). On rebase keep the `portal.asns.*` names and the `pending` filter; a new portal ASN string goes into `lang/zh/portal.php` `asns.*`.

### 2026-09-11 · main · 从订单生成预报单 (CR #117) · C built in X1's Orders + Portal zones as integrator

- **Why.** Lead decision: ASNs follow the client's orders; staff generate them (main flow), the manual ASN stays (exception), clients never create ASNs — the client-side half of CR #116 is reverted.
- **Orders files (X1 zone), new unless noted:** `app/Modules/Orders/Services/OrderInboundService.php`, `Http/Controllers/OrderInboundController.php`, `views/inbound/index.blade.php`; edited `routes.php` (+`orders.inbound.index/store`), `views/show.blade.php` (one 生成预报单 button block, gated on eligibility + roles), `resources/views/layouts/nav/orders.blade.php` (待建预报 link + count badge), `lang/zh/orders.php` (`inbound.*`, four `validation.attributes`). The service depends on the new `App\Support\Contracts\InboundService` (Warehouse) and `JobService::cancelIfEmpty` (Platform) — both C's.
- **Portal files (X1 zone):** deleted `Http/Controllers/PortalAsnApiController.php`, `Services/PortalAsnService.php`, `Models/PortalAsnSubmission.php`, `views/asns/create.blade.php`; `PortalAsnController` reduced to index / show; `views/asns/{index,show}.blade.php` rewritten read-only; `routes.php` down to two asns routes (no API); `lang/zh/portal.php` `asns.*` trimmed and the #116 validation strings removed. The migration `2026_09_11_100010_create_portal_asn_submissions_table.php` stays (merged; table unused).
- **Tests.** `tests/Feature/Orders/OrderInboundTest.php` (new, end to end through receive → putaway → allocation), `tests/Feature/Portal/PortalAsnTest.php` rewritten. On rebase keep `orders.inbound.*` and the read-only portal routes; new Orders strings go into `lang/zh/orders.php` `inbound.*`.

### 2026-09-11 · main · 客户自选运输方案 (CR #118) · C edited X2's Transport and X1's Orders / Portal zones as integrator

- **Why.** Lead request: the client decides the transport plan, with the options' estimates shown next to the warehouse 估价 at order time; the dispatcher executes.
- **Transport (X2):** `Services/TransportOptionService.php` rewritten in place — `estimate(int $clientId, array $request)` (new contract method), `candidates()` / `markCandidates()` / `thirdPartyPrice()` now keyed by client id (no Shipment needed), preliminary stage `applyClientPreference()`, final stage `resolveFinalVariance()` confirms the **same option** as the reference (selected preliminary quote, else `orders.transport_preference`) instead of the recommended quote, carrying the client actor. `Services/ShipmentQuoteRequestFactory.php`: sender for unpacked from_stock shipments (goods' warehouse → first active), parcels from order lines when no declared packages, public static `itemsFromLines()` / `partyForWarehouse()` / `completeParty()`. `Http/Controllers/ShipmentController.php` (+`clientPreference`), `views/shipments/show.blade.php` (one kv row), `lang/zh/transport.php` (`quotes.client_preference*`). Constructor and `quote()` signature unchanged; B5c/B5d tests untouched.
- **Orders (X1):** migration `2026_09_11_100030_add_transport_preference_to_orders.php`, `Models/Order.php` (fillable + array cast), `Services/OrderEstimateService.php` (`freightFromPreference()`, `freightLine()` null-safe quote id, description key), `views/show.blade.php` (one line), `lang/zh/orders.php`.
- **Portal (X1):** `Services/PortalTransportEstimate.php` (new), `Http/Controllers/PortalOrderController.php` (preview / store / rules), `views/orders/create.blade.php` (options table inside `#estimate-preview`, totals follow the choice, `invalidate()` ignores the preview), `views/orders/show.blade.php` (choice line + 您的选择 badge), `lang/zh/portal.php`.
- **Tests.** `tests/Feature/Portal/PortalTransportChoiceTest.php` (new): estimate → choice → confirm → auto-select → packed → auto-confirm; out-of-tolerance path; no-dimensions path. On rebase keep `transport_choice` / `transport_preference` names and the same-option rule.

### 2026-09-11 · main · 从订单导入货物行 (CR #119) · C edited X1's Orders zone as integrator

- **Why.** Lead request: an ASN generated from orders must not need its goods lines typed by hand — on the ASN page (booked / arrived / receiving) staff tick the client's pending orders and every goods line (consignee, phone, address, suburb, state, postcode, FBA, mark, description, cartons) comes from the order; the manual add-line form becomes the collapsed exception and each line names its source order.
- **Contract methods (C):** `App\Support\Contracts\OrderService::awaitingAsn(int $clientId): array` (the client's from_stock orders in received / confirmed with ≥ 1 goods line whose `asn_line_id` is null, oldest first) and `attachOrdersToAsn(int $asnId, array $orderIds, ?int $actorId): array{orders, lines, merged, cancelled, job_no}`; `App\Support\Contracts\InboundService::addOrderLinesToAsn(int $asnId, array $lines): array{asn_id, asn_no, lines}` (Warehouse, implemented by `AsnService`; refuses past receiving with `warehouse.asns.errors.import_orders_closed`). Signatures in `contracts/services.md` §2 / §10.
- **Orders files (X1 zone), edited:** `app/Modules/Orders/Services/OrderInboundService.php` (Job merge + `order_lines.asn_line_id` write-back + quiet backorder reused for the attach path), `app/Modules/Orders/Services/AsnOrderService.php` (`awaitingAsn` / `attachOrdersToAsn` — the OrderService implementation; refuses another client's order with `orders.inbound.errors.asn_other_client` and a Job that already carries an ASN with `job_has_asn`), `lang/zh/orders.php` (`inbound.errors.asn_other_client`). The Fake OrderService in `app/Support` gained the same two methods (C's zone).
- **Warehouse (C's own):** `routes.php` (`POST /warehouse/asns/{asn}/import-orders` → `warehouse.asns.import_orders`, roles admin / customer_service / warehouse_supervisor), `AsnController::importOrders`, `warehouse::asns.show` (import block, `<details>` around the manual form, 来源订单 column, 由订单生成 badge), `lang/zh/warehouse.php` `asns.*`.
- **Tests.** `tests/Feature/Warehouse/AsnImportOrdersTest.php` (new): import → links / Job merge / backorder → receive → putaway → allocation; guards for other client, Job-with-ASN, put-away ASN, roles. On rebase keep the method names above and the `import_orders` error bag key; new Orders strings go into `lang/zh/orders.php` `inbound.errors.*`.
- **Platform (C's own), integration fix:** `App\Support\Contracts\JobService::moveOrder(int $orderId, int $toJobId): int` (new; `contracts/services.md` §5) implemented by `App\Modules\Platform\Services\JobService` + `FakeJobService`. `OrderInboundService::mergeIntoJob` now calls it instead of updating `orders.job_id` directly, so the shipment Transport opened at `order.confirmed` (CR #118), its carrier costs, customer quotes, exceptions, packages, dispatches, warehouse tasks and order documents follow the order into the ASN's Job and `cancelIfEmpty` can close the emptied per-order Job (before, a confirmed order's Job stayed open with a stranded shipment). Charges / invoices / ledger / events never move. X1: on rebase keep the `moveOrder` call in `mergeIntoJob`.
- **Transport (X2) — read this too:** `moveOrder` rewrites `shipments.job_id` and `carrier_costs.job_id` for the shipment Transport opened at `order.confirmed` when that order is merged into an ASN Job (`contracts/db-schema.md` ownership note, `services.md` §5 explicit table list). No Transport code changes; on rebase, treat `job_id` on those rows as re-homable by that one Platform call (e.g. never cache the Job of a shipment across requests, resolve it from the row). `JobService::cancelIfEmpty` now also waits for pending / failed `outbox_events` of the Job, so a consumer retried by cron never writes under a cancelled Job.
- **Review round (same day, C):** `OrderService::attachOrdersToAsn` gained an optional 4th parameter `?string $containerNo = null` (X1: keep it on rebase — the ASN page's `container_no` select, required when the ASN has > 1 container); `OrderInboundService::mergeIntoJob` refuses an order whose old Job still has pending / failed `outbox_events` (`lang/zh/orders.php` `inbound.errors.events_pending`); `attachToAsn` locks the `asns` row (read-only, `lockForUpdate`) and no longer reads `containers` — Warehouse owns the container default / requirement (`AsnService::addOrderLinesToAsn`, `warehouse.asns.errors.import_orders_pick_container`); `PutawayService::completeIfDone` locks the ASN row before deciding to flip it. Warehouse view: import card directly under the 货物行 table with a header button, rows unticked by default + 全选, rows whose Job already carries an ASN disabled, manual `<details>` re-opens on its own validation errors, 由订单生成 badge only when the order line predates the ASN line.

### 2026-09-14 · main · 提货直送计费 (CR #120) · C edited X2 Transport and X1 Orders / Portal zones as integrator

- **Why.** Lead decision 2026-09-14 (CHANGE_REQUESTS #120, "按默认"): a 提货直送 (pickup_deliver) order — collected at the client's pickup address and delivered, never in stock, never packed, no `outbound.packed` — is billed the SAME handling standards as a from_stock outbound order, at the moment the final transport plan is confirmed (`shipment.quote_confirmed`, the event that already bills TR-DELIVERY-BASE / TR-TAILGATE / TR-FUEL and on which `PerJobInvoiceConsumer` drafts the per_job service invoice), from the client's DECLARED packages (`declared_packages`: package_type, qty, weight_kg PER PIECE). Codes and rates as for stock orders: WH-ORDER-DESPATCH ×1 (+ WH-ORDER-DESPATCH-URGENT when requested for the confirmation day and confirmed after `clients.dispatch_cutoff_time`), WH-PICK-PLT per declared pallet piece, WH-PICK-CTN-LT22 / 22-45 / GE45 per carton piece banded on its declared weight (null → 0 → LT22), WH-LABEL-OUT per piece (Σ qty), WH-LOAD-PLT per pallet. Unique key `order:{order_id}`, version 1 — a reconfirmation never re-bills. The estimate (portal 获取估价, order page) shows the same lines next to freight. New code TR-PICKUP (上门提货附加费, transport / delivery, 'Collection at sender (surcharge)') with a manual-trigger rule only — never auto-billed until priced, no Edward rate row.
- **Shared helpers (C, `app/Support`) — use them after rebase, never re-implement:** `App\Support\PackageUnits::unitType(?string $packageType): string` ('pallet' when the type is pallet or skid, case-insensitive, else 'carton') and `App\Support\UrgentDespatch::isUrgent(?string $requestedDate, ?string $cutoffTime, \DateTimeInterface $at): bool` (true only when both inputs are non-blank, `$requestedDate` (Y-m-d) is `$at`'s date and `$at`'s time H:i:s is later than the cut-off H:i / H:i:s). Same semantics as `OrderEstimateService::isUrgent()` (requested today && now() past the client's cut-off): Orders and Transport both call the helpers so the estimate and the charge can never disagree.
- **Transport (X2), edited:** `app/Modules/Transport/Services/QuoteSelectionService.php` — the `shipment.quote_confirmed` payload gains (additive, `contracts/events.md`): `order_type` for EVERY shipment (read from `orders`: from_stock / pickup_deliver / return); for pickup_deliver shipments only `is_urgent` (bool), `lines` = [{package_type, unit_type ('pallet' | 'carton' via PackageUnits), qty, unit_weight_kg (declared per-piece weight or 0)}], `pallet_count`, `carton_count`, `label_count` (= Σ qty). from_stock shipments carry none of the handling keys — their handling comes from outbound.packed. `tests/Feature/Transport/B5dQuoteSelectionTest.php` (payload assertions). On rebase keep these key names exactly; Billing's rules read them.
- **Orders (X1), edited:** `app/Modules/Orders/Services/OrderEstimateService.php` — `lines()` returns the handling lines for pickup_deliver from the declared packages (order processing, urgent, pick per pallet / carton band by declared weight_kg, labels per piece, load per pallet) instead of `[]`; `app/Modules/Orders/Http/Controllers/OrderController.php` and `app/Modules/Orders/Http/Controllers/OrderApiController.php` — `declared_packages.*.weight_kg` required for pickup_deliver; `lang/zh/orders.php`; Orders tests (`tests/Feature/Orders/`).
- **Portal (X1), edited:** `app/Modules/Portal/Http/Controllers/PortalOrderController.php` (rules: `declared_packages.*.weight_kg` required for pickup_deliver, so 获取估价 / 确认提交 always carry a per-piece weight; `preview()` now assembles `pickup_address` from the pickup_* fields through the shared `pickupAddress()` helper `store()` uses, so a 提货直送 获取估价 shows the freight options next to the handling lines instead of `no_address`), `lang/zh/portal.php`, `tests/Feature/Portal/PortalOrdersTest.php`.
- **Billing (C's own):** `app/Modules/Billing/Seeders/BillingSeeder.php` — TR-PICKUP code + manual rule; `shipment.quote_confirmed` rules for the seven WH codes with condition `order_type = pickup_deliver` (URGENT also `is_urgent = true`), unique-key template `order:{order_id}`; `ChargeEngine` conditions compare as strings, so an event without `order_type` (recorded before #120, fixtures) never matches. Existing `outbound.packed` rules and behaviour byte-identical. Run once on the trial server: `php artisan db:seed --class="App\Modules\Billing\Seeders\BillingSeeder"`.
- **Tests.** `tests/Feature/Billing/PickupDeliverBillingTest.php` (new): portal 估价 → order → confirm → final quote → client confirms → six handling charges + freight / tailgate / fuel → per_job draft → idempotent replay; from_stock end to end gets no handling charge at quote confirmation; urgent + from_stock + legacy payloads through the engine; TR-PICKUP manual only.

### 2026-09-14 · main · 确认按钮按订单类型措辞 · C edited the X1 Orders zone as integrator

- **Why.** Lead feedback 2026-09-14: the dispatcher's order page showed "确认订单并检查库存" on a 提货直送 order, and after the click the flash / timeline said the order was waiting for WMS to lock stock. A pickup_deliver order never touches stock (`ShipmentIntakeService` opens it at the final quote stage), so the wording was wrong for that type.
- **Orders (X1), edited:** `lang/zh/orders.php` — new keys `actions.confirm_pickup_deliver`, `fulfilments.timeline.confirmed_pickup_deliver`, `messages.confirmed_pickup_deliver` (the `from_stock` texts are unchanged). `views/show.blade.php` — the confirm button picks the key by `order_type`. `Http/Controllers/OrderController.php::confirm()` — the timeline note and flash use the `_pickup_deliver` keys for that type. `return` orders are confirmed inside `ReturnRequestService` and never show the button, so they need no text.
- **Tests.** `tests/Feature/Orders/ConfirmButtonWordingTest.php` (new): a dispatcher sees "确认订单并生成运输方案" and no "检查库存" on a pickup_deliver order, confirms it, and gets the transport wording in the flash and the timeline; a from_stock order keeps the original button text. No contract, migration or event change.

### 2026-09-14 · main · 货物明细 / 申报包裹重量自动换算与对应 · C edited the X1 Orders zone (shared form partials) as integrator

- **Why.** Lead feedback 2026-09-14 (客户新建订单第 13 点): the order forms asked for the same weights twice in different units — 申报包裹 wants 单件重量, 货物明细 wants 整行合计 — and nothing tied the two tables together. Behaviour is JS-only on the shared partials; no rule, route, validation or contract change.
- **Orders (X1), edited:** `views/partials/goods-line-row.blade.php` — a 单件重量 helper input per row (`class="unit-weight"`, NO name → never submitted); the line total gains `class="line-weight"` (name unchanged). `views/partials/goods-lines.blade.php` — header column, `<tfoot>` live totals (`#goods-lines-total`), and the JS: 单件重量 ⇄ 整行合计 converted through 箱数 whichever one is typed (箱数 change re-derives the total from the unit weight); `mirror()` rebuilds the declared packages from the goods lines (one package row per goods line: type, qty = 箱数, 单件重量, dims; inputs read-only, remove / add buttons hidden) while `#packages-follow-lines` is ticked; after each line edit it dispatches a `change` on the tbody so the tailgate helper re-reads the total it just set. `views/partials/declared-packages.blade.php` — the switch (`packages_follow_lines`, hidden 0 + checkbox 1, default on; not validated, only carried through old()), its hint and `<tfoot>` totals (`#declared-packages-total`). `lang/zh/orders.php` — `lines.hint` reworded, `lines.unit_weight`, `lines.totals`, `pickup.follow_lines`, `pickup.follow_hint`, `pickup.totals`.
- **Server side unchanged:** `OrderFormRows::prune` still drops empty rows; mirrored rows are read-only (not disabled), so they submit; the from_stock branch still disables the whole 提货 fieldset. Both the staff form (`orders::form`, extended columns) and the portal form get the behaviour through the shared partials.
- **Tests.** `tests/Feature/Orders/WeightAutoCalcFormTest.php` (new): both forms carry the helper, totals, switch (checked by default) and the mirror JS; the switch state survives a failed submit. Browser check (local static render, 2026-09-14): 4 × 50 kg → line total 200, package row 4 / 50 / dims, totals 4 箱 · 200 kg both sides, tailgate auto-ticked; a second line typed as a 30 kg total → 15 kg per piece; qty 2→3 → total 45; removing the line drops its package row; unticking unlocks the rows and the add button; from_stock disables the fieldset.


### 2026-09-14 · feat/physical-containers · 拼柜 / LCL 物理柜 (CR #122) · C, own zones only — no X1 / X2 file touched

- **Why.** Lead approved the FCL / LCL design brief with all seven defaults ("7个点都先按默认执行"): several clients' ASNs share one physical container; the coordinator links them on a new 物理柜 screen, the box carries the ONE devanning task, Warehouse computes each member's share and Billing splits devanning (and cartage / sideloader) over the members' Jobs.
- **Contracts that touch you (read, no code owed).** `contracts/events.md`: `task.completed` gained an additive box block (envelope `job_id` / `client_id` **null** for source_type `physical_container`, `members[]` with `share`, `activity_version`) — X1's `TaskCompletedConsumer` / `FulfilmentService::markPicking` already ignores non-pick tasks and was verified against a null-job envelope; new event `physical_container.arrived` (Billing only). `contracts/charge-codes.md` §1: cartage TR-CARTAGE-20/40 now arises on `physical_container.arrived` — **CR #7 / #34 are superseded**: X2 owes no cartage shipment flow; the dormant `shipment.quote_confirmed` cartage rules stay seeded, `cartage_container_size` may stay unset. `contracts/enums.md`: `warehouse_tasks.source_type` +`physical_container`, `charge_rules.quantity_source` +`allocated`, `charge_rules.trigger_event` +`physical_container.arrived`.
- **Schema.** `physical_containers` (new), `containers` +5 columns, `warehouse_tasks.job_id` / `client_id` nullable (NULL only for the box task) +`physical_container_id`. If your code joins `warehouse_tasks` by `job_id`, treat NULL as "no Job" (box-level task); nothing of yours does today.
- **Portal (X1).** Untouched by design (F7): `portal::asns.show` keeps printing the ASN's own container rows; `tests/Feature/Warehouse/PhysicalContainerTest` has the negative test (another member's asn_no / client name / LCL badge never in the portal response). Never eager-load `containers.physicalContainer.members` from a portal route.
- **Trial server, once after merge:** `php artisan migrate` then `php artisan db:seed --class="App\Modules\Billing\Seeders\BillingSeeder"` (TR-SIDELOADER code, WH-DEVAN-* rules → allocated, cartage / sideloader rules on the arrival event).

### 2026-09-14 · feat/portal-inbound-csv · 客户门户入库清单 CSV 提交 (CR #123) · C edited X1 Orders / Portal zones as integrator

- **Why.** Lead request 2026-09-14: 「帮我加回入库提交csv功能，要保证能够准确的转换客户订单信息。在后台做」. CR #117 stands (clients never create ASNs): the client's upload becomes the client's ORDERS (source `portal`, status received) through the staff 批量导入 machinery; customer service builds the ASN in 待建预报 with the 柜号 / ETA the client submitted.
- **Portal files (X1 zone).** New: `app/Modules/Portal/Http/Controllers/PortalInboundImportController.php`, `views/asns/imports/{index,create,show}.blade.php`. Edited: `routes.php` (+6 routes `portal.asns.imports.*`, placed before `GET /asns/{asn}`), `views/asns/index.blade.php` (header with 上传清单 / 已提交清单 buttons), `views/orders/index.blade.php` (second header button), `lang/zh/portal.php` (`inbound.*` section, validation attributes `manifest / container_no / container_size / expected_date / reference / notes`, messages `manifest.* / expected_date.date / container_size.in / container_no.max`). The controller reads the Warehouse `Asn` model (client-scoped, read-only) only to show "预报单 once generated" — same precedent as `PortalAsnController`.
- **Orders files (X1 zone).** `Services/SpreadsheetManifestParser.php` rewritten in place — keep on rebase: `HEADERS` / `CARTON_FALLBACKS` / `STATE_ALIASES` / `PACKAGE_TYPES` / `SERVICE_LEVELS` tables, `read()` byte sniffing, `readCsv()` + `decode()` + `sniffDelimiter()`, `convertRow()` (all normalisation), `entry()` (adds `label`), `locateHeaders()` fallback pass, and the `orders.imports.errors.*` messages now taking `:row` / `:field` / `:value`. `Services/OrderImportService.php`: constructor gained `JobService`; `preview()` context keys `job_id` (nullable), `source`, `client_visible`, `inbound`; the duplicate-file warning is now FIRST in `warnings`; `groups()` skips the ASN-match check without a Job and carries `requested_date` / `service_level` per group; `confirm()` opens ONE loose Job (reference = 参考号 / 柜号, notes = `orders.imports.job_note`) with the first order when the context has none, writes it back to `errors.context.job_id`, uses the group's 要求送达日 / 服务等级, creates orders with source `portal` for a portal import, and declared packages now carry `weight_kg = line total ÷ 箱数` (was the line total — over-flagged 尾板车). `Http/Controllers/OrderImportController.php` (errors CSV prints `label`, explicit fputcsv escape). `Http/Controllers/OrderInboundController.php` (+`portalSubmissions()`, view vars `submissions` / `importByOrder`). `views/inbound/index.blade.php` (客户提交的入库信息 card per client group, 门户清单 #id under the order no, `data-select-import` buttons + the pre-fill JS at the end of the existing script). `views/imports/index.blade.php` (来源 badge, file name), `views/imports/show.blade.php` (source / file line, inbound card + `portal_note` for portal source, `label` in the issues table, checkboxes / confirm button hidden for portal source). `lang/zh/orders.php` (`imports.fields.file_name / source`, `imports.columns.*`, `imports.warnings.*`, `imports.errors.*` rewritten row-aware + `xls_unsupported / integer / invalid_postcode / phone_scientific / invalid_date / invalid_service_level`, `imports.inbound.*`, `imports.portal_note`, `imports.job_note`).
- **Data.** No migration. The inbound context is `order_imports.errors.context.inbound` (`container_no, container_size, expected_date, reference, notes, uploaded_at`); `errors.context.source / client_visible / job_id` were added; orders ↔ import through `errors.result.created[]` as before. `order_imports.source` gained `portal` (enums.md). The upload's Document is `client_visible = true` for portal submissions (staff imports stay false).
- **Behaviour changes X1 should know.** (1) Every parser message is now `第 N 行「column」…`; the old `required` message printed a raw lang key (`orders.fields.deliver_to_state`) — fixed. (2) `No.` / `Qty` / `数量` / `件数` are carton counts only when the sheet has no 箱数-type column. (3) Package words are mapped to `OrderEnums::PACKAGE_TYPES` codes (`纸箱` → `carton`), unknown words kept as typed. (4) Phones lose spaces / dashes on import (`0400 000 001` → `0400000001`). (5) Import declared packages carry per-piece weight. (6) `OrderImportService::preview()` still accepts the old 4-key context; new keys are optional.
- **Tests.** New `tests/Feature/Portal/PortalInboundImportTest.php` (upload → preview shows every field → confirm → exact order columns / units, one Job, 待建预报 context + pre-fill, staff list read-only, tenancy 404s, template BOM + round trip, hard errors block confirm, legacy XLS message, duplicate file), `tests/Feature/Orders/ManifestParserAccuracyTest.php` (one test per rule) with fixtures in `tests/Fixtures/imports/` (generated once; `gbk.csv` is GB18030, `utf16*.csv` are UTF-16 — do not re-save them in an editor). Existing `OrderImportTest`, `SpreadsheetManifestParserTest`, `PortalAsnTest`, `OrderInboundTest`, Warehouse B2b import tests unchanged and green.
- **Trial server, after merge:** deploy runs `migrate --force` (nothing to migrate); no seeder needed.
### 2026-09-14 · feat/asn-collection · 预报单我方上门提货 (CR #124) · C edited X2 Transport and X1 Orders / Portal zones as integrator

- **Why.** Lead decision 2026-09-14 ("按这个方式改"): a client who wants goods brought INTO our warehouse must not use 现场提货直送 (that is a customer delivery order). The 预报单 gains a 到仓方式 — 客户自送 (default, today's behaviour) or 我方上门提货: pickup address + contact + ready date + declared packages → Transport opens an `inbound_collection` shipment (sender = pickup, receiver = our warehouse, **no order**), quotes it with the existing option service at the final stage, the dispatcher / customer service confirms and books, the driver collects, and the POD at our dock marks the ASN arrived. Billing: TR-DELIVERY-BASE / TR-FUEL / TR-TAILGATE / TR-REMOTE through the existing `shipment.quote_confirmed` rules on the ASN's Job; no outbound handling (no `order_type`). Containers stay on `physical_containers` (#122).
- **Contracts that touch you (read).** `contracts/events.md`: new `asn.collection_requested` / `asn.collection_cancelled` (Warehouse → Transport); `shipment.quote_confirmed`, `shipment.booked`, `delivery.pod_captured`, `delivery.failed` gained `asn_id` (nullable) + `shipment_type`, `order_id` documented nullable, Warehouse consumes all four. `db-schema.md`: `shipments.order_id` nullable, +`asn_id`, +`asn_activity_version`; `asns` +11 collection columns. `enums.md`: `shipments.shipment_type` +`inbound_collection` (same statuses as outbound), `asns.inbound_transport`, `asns.collection_status`. `services.md` §3 (intake) / §10 (`AsnService::setCollection` …). `charge-codes.md` §6 (collection bills TR-* only).
- **Transport (X2), edited — keep on rebase:** migration `2026_09_14_300010_add_asn_collection_to_shipments.php` (order_id nullable, asn_id, asn_activity_version). `Support/TransportEnums.php` (+`inbound_collection`, `DELIVERY_TYPES`, `isDelivery()`; `shipmentStatuses('inbound_collection')` = outbound). `Services/ShipmentStatusMachine.php` (`DELIVERY_TRANSITIONS` shared by both types). `Models/Shipment.php` (+asn_id / asn_activity_version fillable, `isCollection()`). `Consumers/AsnCollectionRequestedConsumer.php` / `AsnCollectionCancelledConsumer.php` (new) + `TransportServiceProvider` registry. `Services/ShipmentIntakeService.php`: `fromAsnCollection()` (ONE shipment per asn_id, shipment_no `SHP-{asn_no}`, final quote at once, tailgate at pickup from `RateService::thresholds(client, 'TR-TAILGATE')`, a higher `activity_version` re-quotes the same shipment — status back to `quoting`, quotes `requoted`, selection cleared; equal / lower = replay; booked = refuse + log), `cancelAsnCollection()` (unbooked → `booking_cancelled` with its quotes); constructor gained an optional third `?RateService $rates = null` (B5d's `new ShipmentIntakeService(a, b)` still works). `Services/ShipmentQuoteRequestFactory.php`: `build()` branches to `buildCollection()` when `asn_id` is set (reads `asns` / `warehouses` / `asn_lines` by query builder), public static `collectionItems()`. `Services/TransportOptionService.php`: `_quote_request` now also stores `sender`; `clientPreference()` returns null without an order. `Services/QuoteSelectionService.php`: type check via `isDelivery()`, payload +`asn_id`, `orderContext()` skips the orders read when `order_id` is null (order_type null, no handling keys). `Services/ShipmentBookingService.php`, `DriverPodService.php`, `CarrierPodService.php`, `TrackingSyncService.php`: payloads +`asn_id` +`shipment_type`; type checks / `syncActive()` query via `DELIVERY_TYPES`. `Services/ShipmentProgressService.php`, `DeliveryRunService.php`, `RedeliveryService.php` (copies asn_id / type), `Http/Controllers/DeliveryRunController.php` (eligible query): `isDelivery()` / `DELIVERY_TYPES` instead of `=== 'outbound'`. `Services/PackageManifest.php`: `forCollection()` — one row per declared piece, `carton_label = {shipment_no}-{n}` (consignment note + own-fleet labels). `Services/ConsignmentNotePdf.php` + `views/shipments/consignment-note.blade.php`: ASN row and a Collect from / Deliver to block for collections (English keys `pdf.consignment_note.asn|collect_from|deliver_to`; `pdf.package_types` +tube / flat_pack / skid). `Http/Controllers/ShipmentController.php` (null-safe order lookups, `asnNo`, `collectionParties`), `IndexController.php` (`asnNos`), `views/shipments/show.blade.php` (预报单 row + 提货地址 / 送达仓库 instead of the order row; margin link only with an order; 客户下单时选择 row says the dispatcher confirms), `views/index.blade.php` (ASN number next to the type), `views/driver.blade.php` (提货地址 + 送达仓库 for a collection stop), `lang/zh/transport.php` (`shipment_types.inbound_collection`, `shipments.asn|collection_header|collection_from|collection_to`, `quotes.client_preference_collection`, `driver.pickup_from|deliver_to_warehouse`). `delivery.extra_charge` payload unchanged (B8 asserts it exactly). Tests: `tests/Feature/Transport/InboundCollectionShipmentTest.php` (new); B5d / B9a key lists and B7's exact payloads gained `asn_id` / `shipment_type`.
- **Orders (X1), ONE guard:** `Consumers/DeliveryPodCapturedConsumer.php` returns early when `payload.order_id` is null (a collection POD would otherwise `findOrFail(0)` and dead-letter). Nothing else in Orders touched.
- **Portal (X1):** `views/asns/partials/collection.blade.php` (new, read-only: 到仓方式, pickup address, ready date, packages, status, confirmed plan + **client price only** — `asns.collection_plan` never holds cost / markup) included from `views/asns/show.blade.php` with one `@include`; `lang/zh/portal.php` new `asns.collection` block. `PortalAsnController::index` / `asns/index.blade.php` untouched. Test `tests/Feature/Portal/PortalAsnCollectionTest.php` (new).
- **Warehouse / Platform / Support (C's own):** migration `2026_09_14_300000_add_collection_to_asns.php`, `Asn` model, `Events\AsnCollectionRequested` / `AsnCollectionCancelled`, `AsnService::setCollection / clearCollection / recordCollectionProgress`, `Consumers\AsnCollectionProgressConsumer` (the four Transport events), `AsnController` (create + `updateCollection` / `destroyCollection`), `routes.php` (`PUT` / `DELETE /warehouse/asns/{asn}/collection`), `views/asns/create` (到仓方式 radio), `asns/show` (one include), `asns/partials/collection-fields` + `collection-card`, `lang/zh/warehouse.php` `asns.collection.*`; `Platform\Consumers\JobCostConsumer` counts both delivery types; `App\Support\Enums` + `StatusBadge`. Test `tests/Feature/Warehouse/AsnCollectionTest.php`.
- **v1 boundaries.** No client choice of the collection plan (portal card read-only; the dispatcher / customer service confirms on the shipment page — `applyClientPreference` is skipped without an order). Edits / 改为客户自送 after booking are refused on the ASN page (Chinese message) and belong to the dispatcher on the shipment page — no cancel-booking button exists yet. `collection_status` `quoted` / `collected` are reserved (no event sets them). Loose goods / pallets only; FCL / LCL cartage stays on `physical_containers`.
- **Trial server after merge:** deploy runs `migrate --force` (two migrations). No Billing seeder run needed — the existing `shipment.quote_confirmed` TR-* rules bill a collection on the ASN's Job.

### 2026-09-15 · 客户门户申请上门提货 (CR #125) · C edited X1 Orders / Portal and X2 Transport zones as integrator

- **Why.** Lead decision 2026-09-15 (Jimmy): 「保留客服审核，帮我按建议补」. The client asks for a collection on its portal 入库清单 and ticks a plan with client prices; customer service reviews it in 待建预报 and generating the ASN opens the collection (#124 chain); Transport confirms the client's option within tolerance, otherwise the client re-confirms in the portal. Nothing reaches Transport before customer service generates the ASN; clients still never create ASNs (#117).
- **Contracts that touch you (read).** `events.md`: `asn.collection_requested` +`client_preference` (customer snapshot or null) +`requested_via` (client \| staff). `db-schema.md`: `asns` +`collection_preference` / `collection_requested_via` / `collection_import_id`. `services.md`: §2 `awaitingAsn` rows +`collection_requested` (bool), §3 collection client choice + the shared helpers below, §10 `InboundService::requestCollection`. `routes.md`: +`portal.asns.collection.quotes.confirm`, new fields on `portal.asns.imports.store` / `.confirm` and `orders.inbound.store`. `enums.md`: `asns.collection_requested_via`.
- **Transport (X2) — keep on rebase:** `app/Modules/Transport/Support/CollectionTailgate.php` (new) is THE pickup-tailgate rule (`required(int $clientId, array $packages, array $lines, ?RateService $rates = null)`), moved verbatim out of `ShipmentIntakeService` (private `collectionNeedsTailgate` and `DEFAULT_TAILGATE_KG` removed; `fromAsnCollection` calls the helper with `$this->rates`). `ShipmentQuoteRequestFactory`: `buildCollection` now goes through the new public static `collectionSender(array $address)` and `collectionRequest(int $clientId, array $sender, ?array $receiver, array $items, bool $tailgatePickup, ?string $readyDate, string $description)` — same output as before (existing InboundCollectionShipmentTest unchanged and green); the portal estimate calls the same two methods, so do not inline them back or change one side only. `TransportOptionService::clientPreference()`: for a shipment without `order_id` but with `asn_id` it reads `asns.collection_preference` by query builder (guarded by `Schema::hasColumn`) — `preferenceActor` / `matching` / `awaitReconfirmation` unchanged; `resolveFinalVariance` see the review bullet below. No other Transport behaviour changed; Transport tests untouched except the new `tests/Feature/Transport/ClientChosenCollectionPlanTest.php`.
- **Orders (X1) — keep on rebase:** `Services/OrderImportService.php` +`recordInboundCollection(OrderImport $import, array $collection)` (writes only `errors.context.inbound.collection.preference` / `.packages`, row-locked, pending portal imports with a collection request; pass the returned model to `confirm()` — `confirm()` writes its own audit copy). `Services/OrderInboundService.php`: `generate()` header keys `inbound_transport`, `collection`, `collection_ready_date`, `collection_notes`, `collection_import_id` → `InboundService::requestCollection` after `linkLines` INSIDE the transaction (private `collectionRequest()` reads the preference server side and refuses another client's / non-portal / not-imported import with `orders.inbound.errors.collection_import_invalid`); `awaitingAsn()` rows +`collection_requested` via the new public `collectionRequests(int $clientId)`; `linePayload()` uses the new public static `asnLineDescription()` (the portal estimate describes parcels with it — keep the two in step). `Services/AsnOrderService.php` (docblock only). `Http/Controllers/OrderInboundController.php`: store rules for the 到仓方式 fields (`exclude_unless:inbound_transport,we_collect` + `required_if`; no `after_or_equal` on purpose — Warehouse's RuleViolation rolls the generation back), `portalSubmissions()` +`collection`. `views/inbound/index.blade.php`: error block, 到仓方式 column / badge, data-* attributes on 选中并填入 (`data-import-id`, `data-collection`, `data-warehouse-id`, `data-collection-{name,phone,address,suburb,state,postcode,type,ready-date,notes}`), the 到仓方式 fieldsets (collection fieldset hidden + disabled unless we_collect) and the JS. `lang/zh/orders.php`: `validation.attributes` / `messages` for `collection*`, `imports.errors.no_collection_request`, `imports.inbound.{transport,client_delivers,collection_badge,ready_date,chosen_plan,no_plan}`, `inbound.errors.collection_import_invalid`, `inbound.collection.*`. Test `tests/Feature/Orders/InboundCollectionReviewTest.php` (new).
- **Portal (X1) — keep on rebase:** `Services/PortalTransportEstimate.php`: `SNAPSHOT` is public (Warehouse's `AsnService::PREFERENCE_KEYS` = SNAPSHOT + chosen_at / chosen_by, asserted in the portal test), new `collectionOptions(int $clientId, array $collection, array $rows)` and public static `snapshot()`, `defaultWarehouse()` now public static (`options()` / `choose()` behave as before). `Services/PortalTransportQuotes.php` +`forAsnCollection(Asn $asn)` (customer columns only). `Http/Controllers/PortalInboundImportController.php`: `create` (warehouses, default warehouse), `store` (到仓方式 rules, `inbound.collection`), `show` (collection card data + live plans while pending), `confirm` (`collection_choice` required when plans exist, re-priced, only the key trusted, `recordInboundCollection` before `confirm`). `Http/Controllers/PortalAsnController::show` (+`collectionQuotes`), `PortalQuoteController::confirmCollection` (new, 404 isolation like `confirm`). `routes.php` (+`POST /asns/{asn}/collection/quotes/{quote}/confirm` after `asns.show`). Views: `asns/imports/create` (到仓方式 card + toggle JS), `asns/imports/show` (error block, 上门提货申请 card, plans table inside the confirm form), `asns/imports/index` (申请上门提货 badge), `asns/partials/collection` (您在门户选择的方案, 确认方, 提货运费有变化 re-confirm table), `asns/show` (status line + passes `collectionQuotes`). `lang/zh/portal.php`: `validation.attributes` / `messages` for the new fields, `inbound.collection.*`, `asns.collection.{fields.confirmed_by, fields.client_choice, reconfirm_*, your_choice, confirm, expired*, confirmed, confirmed_by}`. Test `tests/Feature/Portal/PortalCollectionRequestTest.php` (new). The portal never renders or stores cost / markup: the plans come from `TransportOptionService::estimate()` (customer fields), the stored snapshot is whitelisted, the quote rows are read with `CUSTOMER_COLUMNS`.
- **Warehouse / Support (C's own):** migration `2026_09_15_100000_add_collection_preference_to_asns.php`, `Asn`, `AsnService` (`PREFERENCE_KEYS`, `requestCollection`, `collectionOrigin`, payload keys, `clearCollection`), `Contracts\InboundService::requestCollection`, `Enums::ASN_COLLECTION_REQUESTED_VIA`, ASN page collection card + import-card warning, `lang/zh/warehouse.php`. Shared test helpers `tests/Support/StubCarrierAdapter.php`, `tests/Support/PortalCollectionFlow.php` (new).
- **Review fixes (same day, same branch) — keep on rebase.** *Transport (X2):* `TransportOptionService::resolveFinalVariance` calls the new private `collectionReference()` for a collection shipment (no `order_id`, `asn_id` set) that has a client preference: earlier final selections are replayed oldest first, a same-option selection within tolerance of the current reference leaves it (automatic confirmations never creep the price), any other selection becomes the reference in its maker's name (client via `preferenceActor` on `selected_by_user_id`, else system); the tolerance check moved to the private `withinTolerance()` (same rule, order shipments unchanged). *Portal (X1):* `PortalQuoteController::confirmCollection` 404s unless the ASN is client-requested with a preference (the view's condition); `PortalInboundImportController::confirm` refuses a collection request with reason `no_items` (`portal.inbound.collection.errors.no_items`) and `asns/imports/show` shows that message without the confirm button; `lang/zh/portal.php` `inbound.collection.{hint, no_plan.no_items, no_plan_hint, errors.no_items}`. *Orders (X1):* `OrderInboundService::collectionRequest($header, $clientId, $orderIds)` refuses orders the import did not create (`orders.inbound.errors.collection_import_orders`) and carries the preference only when the ASN takes all of the import's orders; `views/inbound/index` JS drops the import link when another order is ticked and swaps the note for `inbound.collection.partial_import` on a partial selection (`inbound.collection.import_cleared`). *Tests:* ClientChosenCollectionPlanTest +3 (explicit re-choice kept, accepted price kept / no creep, staff-requested collection 404) and a third-party carrier whose client price differs from its cost on the re-confirm page; InboundCollectionReviewTest +1 (foreign orders refused, partial generation without the plan); PortalCollectionRequestTest no-plan test now covers the `no_items` refusal and the `none` path through generation; AsnCollectionTest +1 (Warehouse whitelist); `PortalCollectionFlow::bindStubCarriers` is idempotent.
- **Trial server after merge:** deploy runs `migrate --force` (one migration). No seeder needed.

### 2026-09-15 · 底层库位存储等级 (CR #126) · C edited X1 Orders / Portal zones as integrator

- **Why.** Lead request 2026-09-15 (Jimmy): expensive goods go on the bottom rack level; storage there costs 10 %–20 % more (Melbourne / Sydney price standards); the supervisor still chooses the putaway location; the client side declares the tier. Two tiers (`standard` / `bottom`), pallets only, surcharge only when the pallet is declared bottom AND sits in a bottom location at the last snapshot of the week; declared goods value may pre-fill bottom (per-client threshold) but never prices anything; no reserved locations; no check on moves. Branch `feat/location-storage-tier`.
- **Contracts that touch you (read).** `db-schema.md`: `order_lines` +`storage_tier` (nullable) +`storage_tier_source`; `asn_lines` +`storage_tier` (default standard) +`storage_tier_source`; `stock_units` +`required_storage_tier` +`storage_tier_override_reason`; `stock_snapshots` +`location_storage_tier` +`required_storage_tier`; `locations` +`storage_tier` +`rack_level`; `rate_items` +`warehouse_id`. `enums.md` §4 (tier, tier source). `services.md` §4 (context `warehouse_id` / `storage_tier`, warehouse specificity +16) and §8 (Row `storage_tier`, `storage_tier_declared`). `charge-codes.md` §3 `WH-STORAGE-TIER-PLT-WK`, §7 `min_percent` / `max_percent` / `tier_value_threshold_cents`. `events.md` `snapshot.weekly` units[] documentation.
- **Orders (X1), edited — keep on rebase:** migration `app/Modules/Orders/migrations/2026_09_15_100020_add_storage_tier_to_order_lines.php`. `Models/OrderLine.php` (+2 fillable). `Services/OrderCreationService.php` (line whitelist +`storage_tier`, `storage_tier_source`). `Services/OrderImportService.php` (constructor +`RateService $rates`; `preview()` calls the new private `storageTiers()` right after `parse()` — declared cell → source `client` (portal) / `staff`, else the pre-fill from `tier_value_threshold_cents` → `bottom` / `value_rule` + warning). `Services/SpreadsheetManifestParser.php` (`HEADERS` +4 aliases at the end, new `STORAGE_TIER_VALUES`, one block in `convertRow()` before the error check, two row keys). `Services/OrderInboundService.php` (`linePayload()` +2 keys). `views/imports/show.blade.php` (one 存储等级 column in the groups table). `lang/zh/orders.php` (`imports.fields.storage_tier`, `imports.columns.storage_tier`, `imports.tier_bottom_rows|tier_standard|tier_prefilled`, `imports.warnings.tier_value_prefill`, `imports.errors.invalid_storage_tier`).
- **Portal (X1), edited — keep on rebase:** `Http/Controllers/PortalInboundImportController.php` (`TEMPLATE_HEADERS` +`存储等级`, the two sample rows +`标准` / `底层`, `show()` +`tierSurcharge` view key, new private `tierSurcharge()` reading the client's own percent per warehouse through `RateService::price` — never cost). `Http/Controllers/PortalStockController.php` (`l.storage_tier` in groupBy + select). `views/asns/imports/create.blade.php` (one hint line), `views/asns/imports/show.blade.php` (one 存储等级 column + one surcharge paragraph), `views/stock/index.blade.php` (one column, tfoot colspan 5 → 6). `lang/zh/portal.php` (`stock.fields.storage_tier`, `stock.storage_tiers`, `inbound.fields.storage_tier`, `inbound.storage_tier_hint|tier_prefilled|tier_surcharge`).
- **CR #125 overlap — merged, not mechanical.** CR #125 reached `main` (b89bdb5) first; `main` was merged into this branch on 2026-09-15 and six files conflicted, all resolved by union: `Orders/Services/OrderImportService.php` (both `use` lines: `RateService`, `RuleViolation`); `Portal/Http/Controllers/PortalInboundImportController.php` (#125's `Warehouse` / `Collection` imports, collection view keys and `confirm(…, PortalTransportEstimate $estimate)` kept; #126's `tierSurcharge` view key kept and the private `tierSurcharge()` moved to the END of the class, now reading `Warehouse::query()` instead of `DB::table`); `contracts/db-schema.md` (#125 `asns` row + #126 `locations` row); `contracts/routes.md` (#125's two Portal rows untouched + a separate #126 Portal row for the template column / preview tier column / stock tier); `docs/demo-guide.zh.md` (#125's 客户门户 row + the 我的库存 tier note); this file (#125 section first). `CHANGE_REQUESTS.md` / `MERGELOG.md` rows reordered #125 → #126. Other shared files (`views/asns/imports/{create,show}`, lang, `AsnService`, `OrderInboundService`, contracts) auto-merged. X1: rebase onto the branch's merge, not onto the old 7a4d004 base.
- **Review fixes 2026-09-15 (same branch).** `RateService::matchItem`: a warehouse row matches only a context naming that warehouse (before, a context without `warehouse_id` matched it first, so a MEL / SYD row without bands or without `tier_value_threshold_cents` hid the all-warehouse row from `thresholds()` — pallet-class suggestion at receiving and the value pre-fill in `OrderImportService`). `StorageBillingService` passes `warehouse_id` on rental, carton / CBM and pickface lines too. Putaway page: after a tier refusal the code field keeps focus (text selected, the next scan replaces it) and the reason input is neither `required` nor focused. Tests tightened (portal preview cell markup, location table cells) and added (staff manifest import tier, returned unit tier, warehouse rows vs thresholds / rental / pickface / carton).
- **Warehouse / Billing / Support (C's own):** see CHANGE_REQUESTS #126 for the full list (locations bulk + labels, putaway rule + hint + reason, stock-list 底层库位剩货托盘, snapshot tier columns, ASN line tier edit, rate-card warehouse select + percent band, `StorageBillingService` surcharge, `BillingSeeder` tier line).
- **Tests.** New: `tests/Feature/Warehouse/LocationTierTest.php`, `tests/Feature/Warehouse/PutawayTierTest.php`, `tests/Feature/Billing/StorageTierBillingTest.php`, `tests/Feature/Orders/DeclaredStorageTierTest.php`. Changed: `tests/Feature/Billing/BillingPagesTest.php` (a new standard-card version copies 35 items: 34 Edward + the surcharge line).
- **Trial server after merge:** deploy runs `migrate --force` (four migrations); run `php artisan db:seed --class="App\Modules\Billing\Seeders\BillingSeeder"` once by hand (adds the code, the documentation rule and the 10 % line to the live standard card; idempotent); the supervisor marks the bottom locations of MEL and SYD in 库位配置 → 批量设置层位 / 存储等级; Finance adds MEL / SYD rows in a new card version if the two warehouses' percents differ.


### 2026-09-17 · 门户手工建立入库清单 (CR #128) · C edited X1 Orders / Portal zones as integrator

- **Why.** Lead request 2026-09-17 (Jimmy): 「只建立了CSV自动导入, 不能从客户订单界面手动导入」. The portal 入库清单 can now be typed on the page (same fields as the CSV template) and can bring in the client's existing orders that still wait for an ASN — **以订单为准**: the ORDER's consignee / goods data is the truth, the list only adds the inbound context and the optional 上门提货 request. Same preview → confirm as the CSV (#123 / #125); one 待建预报 card per submission. Drafts save and continue later. Branch `feat/portal-manual-inbound`.
- **Contracts that touch you (read).** `services.md` §8 `ManifestParser::fromRows` (additive; `App\Support\Contracts\ManifestParser` + `FakeManifestParser` gained it). `routes.md` Portal row (`portal.asns.imports.manual.{create,store,edit}`, `action`). `enums.md` `order_imports.status` +`draft`. `db-schema.md` `order_imports.errors` audit keys `context.manual` / `result.attached`.
- **Orders (X1), edited — keep on rebase:** `Services/SpreadsheetManifestParser.php` (public const `FORM_FIELDS`; public `fromRows()`; the per-row carry-down of `normalise()` moved verbatim into the private `carryDown()` used by both paths; private `scalar()`). `Services/OrderImportService.php` (constructor +`OrderInboundService $inbound` — sixth argument; `preview()` now ends in the shared private `record()` (storage tiers, optional file warning first, groups, failed rows, audit, counts — behaviour unchanged, `PortalInboundImportTest` / `OrderImportTest` green); new public `previewRows(rows, attachedOrderIds, context, actorId, ?draft)`, `saveDraft(...)`, `attachableOrders(clientId, ?exceptImportId)`; private `manualImport`, `checkAttachable` (RuleViolation `orders.imports.errors.order_unknown` / `order_not_attachable`), `lockAttached`, `takenOrderIds`; `confirm()` locks + re-checks the attached ids inside the transaction, writes `result.attached` for manual lists and sets `imported` when created ∪ attached ≠ ∅; the Job note falls back to 手工录入 without a file name). `Models/OrderImport.php` (+`orderIds()`, `isManual()`, `manualAttachedIds()`). `Services/OrderInboundService.php` (`collectionRequests()` and `collectionRequest()`'s whole-list rule read `orderIds()` — two lines). `Http/Controllers/OrderInboundController.php` (`portalSubmissions()` reads `orderIds()`, +`manual`, `attached_nos`). `Http/Controllers/OrderImportController.php` (`show()` +`attachedOrders`; +`Order` / `Collection` imports). Views: `inbound/index` (来源 / 文件名 column, 带入的已有订单 line under the order list), `imports/index` (手工录入 for a manual list), `imports/show` (`$manual`: typed rows card from `context.manual.rows` + attached orders card; draft note). `lang/zh/orders.php` (`imports.statuses.draft`, `imports.errors.order_unknown|order_not_attachable`, `imports.inbound.source|manual|attached`, `imports.manual_entry`, `imports.draft_note`, `imports.manual.*`).
- **Portal (X1), edited — keep on rebase:** `routes.php` (+3, before `{import}`: `GET|POST /asns/imports/manual`, `GET /asns/imports/{import}/edit`). `Http/Controllers/PortalInboundImportController.php` — `manualCreate` / `manualEdit` / `manualStore` (+`ManifestParser` injection; `Validator::make` + `ValidationException::redirectTo($formUrl)` so errors always return to the right form; row errors keyed `rows.{i}.{column}`), `store()` now uses the shared private `inboundRules()` / `collectionRules(bool $draft)` / `submissionContext()` / `collectionContext()` (null-safe; same stored shape as before), `show()` (`View|RedirectResponse`: a draft redirects to the edit form; +`manual`, `attachedOrders`, the estimate rows include the attached orders' unlinked lines via `estimateRows()`), `confirm()` (proceeds when ready groups OR attached orders exist; catches the service's refusal into `attached_order_ids`; manual flash counts both), private `manualForm`, `manualDefaults`, `draft`, `manualRows`, `attachedOrders`, `defaultWarehouseId`. `Services/PortalTransportEstimate.php` (`collectionOptions()`: a row `label` is copied to its package and used in `unpriced_rows` instead of the row number — only present for attached orders' lines, so stored `packages` of CSV lists are unchanged). Views: new `asns/imports/manual.blade.php`, `asns/imports/partials/context-fields.blade.php` and `collection-fields.blade.php` (extracted verbatim from `create` — old() first, then `$defaults` from a draft; the toggle script lives in the collection partial), `manual-rows.blade.php` + `manual-row.blade.php` (rows table, add / remove, renumbering, 单件重量 helper); `create` includes the two partials (no behaviour change); `show` (手工录入 header, attached-orders block, `label` in the packages table, `unpriced_mixed` wording when an order line is unpriced, result lists attached orders, the "again" link goes to the manual form for a manual list); `index` (来源 / 文件名, 草稿 + 继续编辑, attached count in 订单); `asns/index` (手工建立入库清单 button). `lang/zh/portal.php` (`inbound.fields.source`, `inbound.statuses.draft`, `inbound.manual.*`, `inbound.collection.unpriced_mixed|row_ref|order_ref`, validation attributes `action / draft_id / rows / rows.* / rows.*.* / attached_order_ids / attached_order_ids.*`).
- **Data.** No migration, no seeder: `draft` is a new value of `order_imports.status`; the typed rows and ticks live in `order_imports.errors.context.manual`, the attached orders in `errors.result.attached`. `document_id` is null for a manual list.
- **Behaviour X1 should know.** (1) `OrderImport::orderIds()` (created ∪ attached) is THE way to read a submission's orders — `collectionRequests`, the collection subset rule, 待建预报, portal index / show all use it. (2) `attachableOrders` = `OrderInboundService::candidates($clientId)` minus orders in any other portal submission (created / attached by an imported one, ticked on a draft / pending manual one); CSV-created orders therefore never show up as attachable. (3) `confirm()` re-checks attached ids under a row lock — an ASN generated meanwhile, or another list that took the order, rolls the whole confirm back (typed rows not created either) with a Chinese message on the preview page. (4) The collection estimate / stored packages of a manual list include the attached orders' unlinked lines (`label` = order no). (5) `collectionRules(true)` (drafts) requires nothing — a half-filled pickup request can be saved.
- **Tests.** New `tests/Feature/Portal/PortalManualInboundTest.php` (5 tests: attachable list + row errors + old() + upload regression; 以订单为准 with tampered consignee fields and a same-mark typed row — order and lines byte-identical before / after preview and confirm; drafts reopen / tenancy 404s / same row reused; collection estimate over attached lines + plan confirm; an order taken meanwhile refuses confirm with nothing created) and `tests/Feature/Orders/InboundManualSubmissionReviewTest.php` (3 tests: one card with created + attached, 选中并填入 ids, ASN from all with the plan and the ORDER's consignee on the attached line; partial generation without the plan + foreign order refused; 客户自送 list). Existing suites unchanged and green.
- **Trial server, after merge:** deploy runs `migrate --force` (nothing to migrate); no seeder needed.
### 2026-09-17 · 下单时选择存储等级 (CR #129) · C edited X1 Orders / Portal zones as integrator

- **Why.** Lead request 2026-09-17 (Jimmy): clients cannot choose where their goods are stored when they place an order. Approved proposal: the client chooses the storage TIER (标准 / 底层) per goods line when placing an order — a tier, never a physical bin; the supervisor still picks the location at putaway (#126 hint / refusal unchanged). The same field is on the staff manual order form, the order API and the draft line add / edit; the order pages show it. Nothing else in the #126 chain changes (order line → ASN line → stock unit → putaway check → weekly surcharge already works once `order_lines.storage_tier` is set). Branch `feat/order-storage-tier` (from main 2932326). No migration, no seeder.
- **Contracts that touch you (read).** `routes.md`: one #129 row (portal preview / store, staff store, draft line add / edit, API accept `storage_tier`; no new route). `enums.md` / `db-schema.md` / `services.md` unchanged (values and columns exist since #126).
- **Orders (X1), edited — keep on rebase:** `views/partials/goods-line-row.blade.php` (one cell after 包装类型: `@include('orders::partials.storage-tier-select')`, name `lines[i][storage_tier]`, default standard, `old()` kept), `views/partials/goods-lines.blade.php` (`$tierSurcharge` / `$tierPercent` in the `@php` block, one header cell, tfoot colspan `$extended ? 13 : 11`, one `<p id="storage-tier-hint">` under the table — the JS is untouched), new `views/partials/storage-tier-select.blade.php` (mirrors `package-type-select`). `Http/OrderFormRows.php` (new public static `withStorageTierSource(array $data, string $source)`; `LINE_CONTENT` deliberately without `storage_tier`). `Http/Controllers/OrderController.php` (`use PortalStorageTier`; `create()` +`tierSurcharge` for `old('client_id')`; `store()` wraps `validated()` in `withStorageTierSource(…, 'staff')`; `validated()` +rule `lines.*.storage_tier`). `Http/Controllers/OrderLineController.php` (`use Enums`; `validated()` +rule `storage_tier`, sets source `staff` when posted, unsets the key when absent). `Http/Controllers/OrderApiController.php` (`use OrderFormRows`; `withStorageTierSource(…, 'client')` on the validated body; +rule `lines.*.storage_tier`). `views/show.blade.php` (goods table: one header cell + one cell after 包装类型 — badge + source small text; line-edit row colspan 7 → 8; the tier select in the edit form and in the add form). `views/api-tokens/index.blade.php` (JSON example +`"storage_tier":"standard"`, one hint line). `lang/zh/orders.php` — appended only: `lines.storage_tier`, `lines.storage_tiers`, `lines.storage_tier_sources`, `lines.storage_tier_hint`, `lines.storage_tier_surcharge` (end of the `lines` block); `validation.attributes` `lines.*.storage_tier`, `storage_tier` (end of block); `validation.messages` `lines.*.storage_tier.in`, `storage_tier.in` (end of block); `api.storage_tier_hint` (end of the `api` block).
- **Portal (X1), edited — keep on rebase:** new `Services/PortalStorageTier.php` (`surchargeByWarehouse(int $clientId): array<string, string>` warehouse code → "10%", customer price only through `RateService::price` with a synthetic base, percent mode only, missing / POA / min-charge rows skipped; static `summary(array): ?string` → "10%" or "MEL 10% · SYD 20%"). `Http/Controllers/PortalOrderController.php` (`use PortalStorageTier`; `formData()` +`tierSurcharge`; `store()` and `preview()` call `OrderFormRows::withStorageTierSource($data, 'client')` right after `validate()`; `rules()` +`lines.*.storage_tier`). `views/orders/show.blade.php` (goods table: one header cell + one badge-only cell after 包装类型; the declared-package rows get a `—` cell). `lang/zh/portal.php` — appended only: `fields.storage_tier` (end of the `fields` block).
- **CR #128 overlap (built in parallel on another branch).** This CR does NOT touch `PortalInboundImportController`, `views/asns/imports/*`, `OrderImportService`, `OrderInboundService`, `OrderInboundController`, `views/inbound/index`, `SpreadsheetManifestParser`; every lang / contracts / docs change is appended at the END of its block or table so the later merge is mechanical (`CHANGE_REQUESTS` row 129 after 126 — reorder if 128 lands first; `routes.md` row after the last #126 row; this section after #126; MERGELOG row; demo-guide FAQ row; ERP_PLAN §3 Q3 sentence). `PortalInboundImportController::tierSurcharge()` now duplicates `PortalStorageTier::surchargeByWarehouse()` — switch that controller to the service in a later CR, not here.
- **Where the tier comes from, per entry point.** Portal form (`portal.orders.preview` / `store`): every line the client submits carries the select's value → source `client` (the default 标准 included — the client saw and left it, as an explicit 标准 cell on the inbound list). Staff form (`orders.store`): same select → source `staff`. Draft line add / edit (`orders.lines.store` / `update`): the select on the order page → source `staff`; a request without the field leaves the line as it was. API (`orders.api.orders.store`): `lines[].storage_tier` optional → source `client`; absent → no tier, no source (`AsnService::addLines` makes it standard). Then unchanged #126: `OrderInboundService::linePayload` → `AsnService::appendOrderLines` → `asn_lines.storage_tier` / `_source` → `ReceivingService` → `stock_units.required_storage_tier` → putaway check → weekly surcharge.
- **Tests.** New: `tests/Feature/Portal/PortalOrderStorageTierTest.php` (3 tests: form select + per-warehouse percent hint / no hint without a priced card; Chinese row error + spare-row pruning; store → portal page badge → 待建预报 → ASN line → received unit), `tests/Feature/Orders/OrderStorageTierTest.php` (3 tests: staff form + re-rendered hint + order page badge / source; API + token page example; draft line add / edit). No existing test changed.
- **Trial server after merge:** deploy only — no migration, no seeder, no manual step (the #126 seeder / bottom-location marking must already have been done for the surcharge to price).

### 2026-09-17 · main · 手工建立入库清单 版式 (lead feedback: UI too cramped) · C edited the X1 Portal zone as integrator

- **Portal (X1), edited:** `views/asns/imports/partials/manual-rows.blade.php` + `manual-row.blade.php` — the 20-column table became one `<article class="manual-row">` card per goods line with visible labels, fields grouped 货物 / 重量与尺寸 / 收件信息 (Pico `.grid`, stacks on phones); JS selectors moved from `tr` / `tbody` to `.manual-row` / `#manual-rows`; new 收件信息同上一行 button copies the consignee block (and an empty 唛头) from the card above; the row number lives in `.row-title` (data-template `第 :n 行`) and a hidden `.row-no`. Field names, attribute order and the `rows.i.field` aria-invalid rule are unchanged (PortalManualInboundTest untouched, green). `lang/zh/portal.php` `inbound.manual` gains `row_title`, `row_hint`, `groups.*`, `actions.copy_prev`. No controller, service or contract change.


### 2026-09-17 · main · 日期输入框 lang="en-AU" · C edited X1 (Orders, Portal, Reports) and X2 (Transport) views as integrator

- **Why.** Lead request 2026-09-17 (Jimmy): the native date pickers read 「yyyy/mm/日」. `<html lang="zh">` makes Chromium localise every `<input type="date|datetime-local|month|time|week">`; Chromium honours `lang` on the input itself, so every such input now carries `lang="en-AU"` (dd/mm/yyyy, the business locale) while the page stays zh. No route, contract, lang, seeder or schema change; the attribute sits right after the `type="…"` token, so the `name="…" value="…"` strings tests assert keep matching. Posted values are unchanged (`Y-m-d` / `H:i`).
- **Rule (AGENTS.md, Server-rendered Blade only):** every date / time input carries `lang="en-AU"` right after its type. `tests/Feature/Platform/DateInputLangTest.php` scans `app/**` and `resources/views` (Blade and PHP strings) and fails with file:line for any that lacks it, and guards `<html lang="zh">` on the layout. Add the attribute to every new date / time input.
- **Orders (X1), edited — keep on rebase (6 inputs):** `views/form.blade.php` (1), `views/imports/create.blade.php` (1), `views/inbound/index.blade.php` (2), `views/index.blade.php` (1), `views/show.blade.php` (1).
- **Portal (X1), edited — keep on rebase (6 inputs):** `views/asns/imports/partials/collection-fields.blade.php` (1), `views/asns/imports/partials/context-fields.blade.php` (1), `views/asns/imports/partials/manual-row.blade.php` (1), `views/orders/create.blade.php` (1), `views/orders/index.blade.php` (2).
- **Reports (X1), edited — keep on rebase (2 inputs):** `views/partials/filters.blade.php` (2).
- **Transport (X2), edited — keep on rebase (5 inputs):** `views/carrier-invoices/index.blade.php` (2), `views/runs/create.blade.php` (1), `views/runs/show.blade.php` (1, the `datetime-local` ETA), `views/shipments/show.blade.php` (1).
- **C's own zones, for the record (21 inputs):** Billing 11 (`charges/index` 2, `invoices/show` 1, `invoices/unbilled` 5, `quotes/create` 1, `rate_cards/index` 1, `rate_cards/show` 1), MasterData 1 (`clients/form`, the `time` dispatch cut-off), Platform 2 (`activity/index`), Warehouse 7 (`asns/create` 1, `asns/partials/collection-fields` 1, `outbound/index` 1, `physical_containers/create` 1, `receipts/index` 2, `snapshots/index` 1).
- **Behaviour.** Chrome / Edge show dd/mm/yyyy; Firefox and Safari ignore the input `lang` and follow the browser / OS language (docs/demo-guide.zh.md §8 has the FAQ row).
- **Trial server after merge:** deploy only — no migration, no seeder, no manual step.

### 2026-09-17 · main · 日期字段组件 <x-date-field> · C edited X1 (Orders, Portal, Reports) and X2 (Transport) views as integrator

- **Why.** Lead request 2026-09-17 (Jimmy): every date picker must READ dd/mm/yyyy, never 「yyyy/mm/日」. The earlier `lang="en-AU"` attribute (f985295, section above) was verified in a real Chromium to be ineffective — Chromium formats a native `<input type="date">` by the browser's own UI language and ignores `lang` on the element — so the display is now ours. Supersedes the section above; every `lang="en-AU"` is gone.
- **The component (frozen zone, C):** `resources/views/components/date-field.blade.php` — `<x-date-field name="expected_date" :value="old('expected_date')" required min="…" max="…" aria-label="…" />` renders one `<span class="date-field">` with (a) a text box `class="date-text"` showing dd/mm/yyyy (`inputmode="numeric"`, `pattern`, `autocomplete="off"`; `required` / `placeholder` / `autofocus` / `title` / `aria-*` land here), (b) `<input type="hidden" name="…" value="yyyy-mm-dd">` — the ONLY named input, so controllers, validation (`date`, `after_or_equal:today`), `old()` and every `name="…" value="yyyy-mm-dd"` assertion are untouched (`id` / `data-*` land here), (c) the native date input off-screen (`class="date-native"`, `tabindex="-1"`, `aria-hidden`; `min` / `max` land here) used only as the picker, (d) the calendar button `class="date-pick"` (`platform.date_field.pick`). `:value` takes ISO, a Carbon / DateTime or null. `time` adds a native `<input type="time">` beside the date and the hidden value becomes `Y-m-d\TH:i` (the Transport run ETA). `class` goes to the wrapper.
- **The script (frozen zone, C):** one `<script>` in `resources/views/layouts/app.blade.php` `<head>`, delegated on `document` so rows cloned from a `<template>` work: typing dd/mm/yyyy → hidden ISO (d/m/yyyy, dd-mm-yyyy, dd.mm.yyyy and ddmmyyyy normalised on blur; invalid / incomplete / outside min–max → hidden empty, `aria-invalid="true"` and a `setCustomValidity` message `platform.date_field.invalid`); the button → `native.showPicker()` (else `native.click()`) with the picker pre-set from the hidden value; native change → text + hidden; a `change` event dispatched on the hidden input → text (and time box) refreshed. **Prefill scripts set the HIDDEN input's value and dispatch `new Event('change', {bubbles: true})`.** `public/css/app.css` +2 lines (99 / 100).
- **Rule (AGENTS.md, Server-rendered Blade only):** never a raw `<input type="date">` / `datetime-local` in a view; `tests/Feature/Platform/DateFieldTest.php` (replaces `DateInputLangTest`) scans `app/Modules/*/views` + `resources/views` and fails with file:line, requires > 30 component uses, renders 仓库 → 收货单 and 门户 → 手工建立入库清单, and proves the posted ISO still validates on the portal order form. Plain `type="time"` inputs stay native (client dispatch cut-off, `lang` attribute dropped).
- **Orders (X1), edited — keep on rebase (6 fields, 5 views):** `views/form.blade.php` (1), `views/imports/create.blade.php` (1), `views/inbound/index.blade.php` (2 + the 待建预报 选中并填入 script now sets `expected_date` / `collection_ready_date` through a `setDate()` helper that dispatches `change`), `views/index.blade.php` (1), `views/show.blade.php` (1).
- **Portal (X1), edited — keep on rebase (6 fields, 6 views):** `views/asns/imports/partials/collection-fields.blade.php` (1), `views/asns/imports/partials/context-fields.blade.php` (1), `views/asns/imports/partials/manual-row.blade.php` (1 — `:name` / `:value` / `:aria-label` / `:aria-invalid` from the row helpers; works inside the `__INDEX__` `<template>`), `views/asns/imports/partials/manual-rows.blade.php` (no field; 收件信息同上一行 dispatches `change` after copying so the date text follows), `views/orders/create.blade.php` (1), `views/orders/index.blade.php` (2). `tests/Feature/Portal/PortalAuditFixesTest.php` line 136: the `min` assertion now reads the hidden input + the native picker.
- **Reports (X1), edited — keep on rebase (2 fields):** `views/partials/filters.blade.php` (2).
- **Transport (X2), edited — keep on rebase (5 fields, 4 views):** `views/carrier-invoices/index.blade.php` (2), `views/runs/create.blade.php` (1), `views/runs/show.blade.php` (1 — the `datetime-local` ETA is now `<x-date-field name="eta" … time />`; the posted `eta` value is unchanged), `views/shipments/show.blade.php` (1).
- **C's own zones, for the record (20 fields):** Billing 11 (`charges/index` 2, `invoices/show` 1, `invoices/unbilled` 5 + the preset buttons dispatch `change`, `quotes/create` 1, `rate_cards/index` 1, `rate_cards/show` 1), Platform 2 (`activity/index`), Warehouse 7 (`asns/create` 1, `asns/partials/collection-fields` 1, `outbound/index` 1, `physical_containers/create` 1, `receipts/index` 2, `snapshots/index` 1); MasterData `clients/form` keeps its native `type="time"`. `lang/zh/platform.php` +`date_field.pick / time / invalid`.
- **Trial server after merge:** deploy only — no migration, no seeder, no manual step (`php artisan view:clear` if compiled views are cached).
### 2026-09-17 · fix/driver-permissions · 司机权限收紧 (CR #130) · C edited the X2 Transport zone as integrator

- **Why.** Lead decision 2026-09-17 (Jimmy): drivers (`transport_operator`) must no longer plan their own work — they could create runs for any driver, add / reorder stops, see every run, open 承运商对账 and the shipment pages with carrier cost and quotes. Industry practice: the dispatcher plans, the driver executes. No migration, seeder, enum, event, service or lang change; enforced in the controllers with `RequiredRoles` (the Transport routes file carries no role middleware, so none was added).
- **Transport (X2), edited — keep on rebase.** `Http/Controllers/DeliveryRunController.php` (`PLANNER_ROLES` admin | customer_service | dispatcher for create / store; `READER_ROLES` + `driverOnly()` — a transport_operator without a planner role lists only its own runs, another driver's run is a 404, and `eligibleShipments` is empty for it), `RunStopController.php` (PLANNER_ROLES), `CarrierInvoiceController.php` (admin | finance), `IndexController.php`, `ShipmentController.php` (new `VIEWER_ROLES` = every staff role except the driver, used by the board, the shipment page and the 托运清单), `ConsignmentNoteController.php`, `ShipmentLabelController.php` (`ROLES` without the driver), `CarrierPodController.php`, `ExtraChargeController.php`, `ShipmentBookingController.php`, `ManualQuoteController.php`, `RedeliveryController.php`, `QuoteSelectionController.php` (admin | customer_service | dispatcher), `OwnFleetCostController.php` (admin | dispatcher | finance), `OrderMarginController.php` (admin | customer_service | dispatcher | finance). `DriverController` untouched.
- **Transport views (X2), edited — keep on rebase; wrapper lines only, no reflow.** `views/runs/index.blade.php` (新建班次 inside `@role('admin|customer_service|dispatcher')`), `views/runs/show.blade.php` (add-stop block, reorder `<form>` open / close, sequence inputs → `{{ $stop->seq }}`, save button and the shipment links inside the same `@role`; drivers get a read-only list), `views/shipments/show.blade.php` (transport_operator removed from the existing `@role(...)` / `hasAnyRole([...])` lists — the page itself is now 403 for the driver). `resources/views/layouts/nav/transport.blade.php`: 运输 inside `@unlessrole('transport_operator')`, 承运商对账 `@role('admin|finance')`, 班次 and 司机任务 unchanged.
- **Outside Transport (one token each).** Orders (X1) `views/show.blade.php` — the 收付毛利 link's `hasAnyRole` list drops transport_operator; Platform `views/jobs/show.blade.php` — `$canOpenTransport` (same roles as `ShipmentController::VIEWER_ROLES`) turns the shipment link into text for the driver, like the existing `$canOpenWarehouse`.
- **Tests.** New `tests/Feature/Transport/DriverPermissionsTest.php` (driver 403 on every removed GET / POST, own-runs-only list, other driver's run 404, read-only run page, driver page deliver + fail end to end with the nav, dispatcher / admin / finance / customer_service / warehouse unchanged, dispatcher-who-also-drives still plans). Existing tests that planned runs or acted on shipments as a transport_operator now use a dispatcher (`B5ShipmentTest`, `B5bDeliveryRunTest`, `B5dQuoteSelectionTest`, `B6ShipmentLabelTest`, `B8DeliveryTrackingTest` two POSTs, `B9aCarrierCostMarginTest`, `M4TransportIntegrationTest`, `TransportAuditFixesTest` incl. the nav test, `TransportPlaceholderTest` /transport); the run's driver stays a transport_operator everywhere.
- **Docs.** `contracts/routes.md` Transport row (roles per route), `contracts/CHANGE_REQUESTS.md` #130, `MERGELOG.md`, `docs/demo-guide.zh.md` (role table rows 调度 / 司机, page map 运输 row, FAQ 司机为什么建不了班次).
- **Left as is (see CR #130 decision).** Warehouse roles keep reading the board / shipment page; 承运商对账 stays admin | finance (no dispatcher); `lang/zh/transport.php` unchanged (no new message needed — the 404 for a foreign run is Laravel's, the 403 page lists the roles).
- **Trial server after merge:** deploy only — no migration, no seeder, no manual step. Existing driver accounts lose the planning menus on their next page load.

### 2026-09-22 · fix/audit-client-signup-only · 客户只能自助注册 (A13, CR #134) · C own zones only

- **Why.** Audit item A13 (GAP-01): a client created by staff on `/admin/clients/create` was saved with `standard_rate_card_id` null, so every charge for it fell to Missing Rate; only the self-registration path and the BillingSeeder back-fill bound the standard card. Lead decision 2026-09-22 (Jimmy): remove the staff "new client" feature altogether — clients are created ONLY through self-registration (`/register`), staff approve, edit and manage them afterwards. No migration, no seeder change, no enum / event / service-contract change; nothing outside C's zones was touched (X1 / X2: nothing to rebase around except `tests/Support/CreatesUsers.php`, whose contract is unchanged).
- **MasterData.** `routes.php` drops `clients.create` / `clients.store` and adds `POST /admin/clients/{client}/bind-standard-card` (`clients.bind_standard_card`, `role:admin`). `ClientController`: `create()` / `store()` removed; `index()` passes `signupOpen` (config `erp.allow_signup`); `edit()` passes the standard card (relation) and the active own card; new `bindStandardCard()` (修复 — binds `RateCard::activeStandard()`, refuses with `masterdata.clients.no_active_standard_card` while none is active). `views/clients/index`: the 新建客户 button is replaced by a hint — signup open: "clients register themselves … appear here as 待审核" + link to `/register`; signup closed: says so and how to reopen; pending count unchanged; rows with a null standard card show a 未绑定标准价目表 badge and (admin) the 修复 button. `views/clients/form`: edit-only (PUT), a read-only block above the form — 标准价目表 name · v<version> (linked to the Billing card page for admin / finance) or the warning + admin 修复, and 专属价目表 有(name · v) / 无. `Models/Client`: `creating` hook fills a null `standard_rate_card_id` with `RateCard::activeStandardId()`; `standardRateCard()` BelongsTo; `activeOwnRateCard()`.
- **Billing / Platform.** `Models/RateCard::activeStandard()` / `activeStandardId()` (newest active standard version); `RegistrationController` uses the helper (behaviour unchanged). `BillingSeeder` back-fill kept as is (MasterDataSeeder still runs before BillingSeeder, so the demo clients rely on it).
- **lang.** `lang/zh/masterdata.php` clients: `create` removed; `signup_hint`, `signup_link`, `signup_closed_hint`, `standard_card`, `own_card`, `own_card_yes` / `_no`, `standard_card_missing`, `no_standard_card`, `bind_standard_card`, `standard_card_bound`, `no_active_standard_card`; `standard_card_hint` reworded.
- **Tests.** `tests/Feature/MasterData/ClientCrudTest.php` rewritten around registration → approve → edit (old routes 405, index hint follows `allow_signup`, payment-terms validation via PUT, `Client::create` with the field omitted or null gets the active standard card while a draft standard version is ignored and an explicit binding is kept, a client created before any standard card is back-filled by BillingSeeder, edit page shows both cards, 修复 is admin-only on the index and the edit page and refuses while no standard card is active). `tests/Feature/Platform/ValidationLocaleTest.php`: the Chinese-message check moved from the create form to the client edit form (same messages). `tests/Support/CreatesUsers::client()`: the standard card now comes from the hook; an explicit `['standard_rate_card_id' => null]` still yields an unpriced client (unbound after create) so the Missing Rate tests in Billing / Orders are untouched. `RoutesContractTest` did not list the removed routes.
- **Docs.** `contracts/routes.md` MasterData row (removed routes + 修复 route, decision noted), `contracts/CHANGE_REQUESTS.md` #134, `MERGELOG.md`, `docs/demo-guide.zh.md` (§4 registration paragraph, §5 主数据 row, FAQ "管理员怎么新建客户").
- **Left as is.** Suppliers / carriers keep their staff create forms (no rate card). `docs/acceptance/2026-09-10-blocking-bug-audit.md` keeps its historical `/admin/clients/create` reproduction step.
- **Trial server after merge:** deploy only — no migration, no seeder re-run, no manual step. `ALLOW_SIGNUP` is on by default (`config/erp.php`: `env('ALLOW_SIGNUP', true)`); if the trial `.env` sets it to false, staff cannot onboard a client until it is set back to true. Any pre-existing client with a null standard card shows the badge on `/admin/clients`; an admin clicks 修复 once.
### 2026-09-22 · fix/audit-b-platform · 审计 B 类平台项 (CR #137) · C own zones + ONE include line in X1 Orders

- **Why.** Audit Part B platform / admin items, lead approval 2026-09-22 — fixed exactly as the report suggested: ADMIN-08 + CRAWL-05 + TMS-15 (driver / floor permissions on the shared Platform pages, extending CR #130), ADMIN-12 (nobody is told when cron / the outbox stalls), ADMIN-13 (unthrottled login, no self-service password, admin self-lock-out), ADMIN-10 (documents centre asks for database ids), GAP-03 (a deactivated client's portal logins keep working). No migration, no seeder, no enum / event / service-contract change.
- **Platform (C).** `routes.php`: `GET /` → `LoginController::home` (lands like login: guest → /jobs, client → /portal, driver-only → /driver, other staff → /jobs); new `GET/PUT /account/password` (`platform.password.edit` / `.update`, `auth` + `client.scope`); `jobs.create` / `jobs.store` behind `role:admin|customer_service|dispatcher|warehouse_supervisor`; `documents.store` / `documents.visibility` behind `role:admin|customer_service|finance|warehouse_supervisor`. `LoginController`: `landingUrl()` / `isDriverOnly()`, `RateLimiter` 5 / minute per email + IP (`throttleKey`), `refusalFor()` → `platform.auth.inactive` for a deactivated login (or a client-user login whose client is inactive — checked after `Auth::attempt` too, for users left `is_active` by older data). New `PasswordController` + `views/auth/password`. `JobService`: `COST_ROLES` + `canSeeCost(?User)`; `summarize()` strips cost / margin for a signed-in user outside the list (a system caller keeps them). `UserController::update`: `guardAdminLockout()` — yourself: no 停用 / role change; the last active admin: no 停用 / demotion (`platform.users.errors.*`). `DocumentController`: `store()` takes `document_no` (+ optional `back=1`), resolves through new `Services/DocumentNumberResolver` (JOB- / ASN- / ORD- / SHP- prefix → `jobs` / `asns` / `orders` / `shipments` by number column, else `invoices.invoice_no`; job_id from the row or, for an invoice, from its lines when they agree), refuses an unknown number (`platform.documents.number_not_found`); `index()` gains `job_no=`, `canEdit`, translated related type + Job link; `EDITOR_ROLES`. New `Services/HealthCheck` (`facts()`, `staleBanner()` cached 60 s under `erp.health.banner`, `oldestPendingMinutes()` on `created_at` of `status = pending`), new `Console/HealthCommand` `erp:health [--json]` (exit 1 when stale or dead > 0), registered in `PlatformServiceProvider`. Views: `jobs/show` (invoices panel in `@role('admin|finance|customer_service|dispatcher')`, 打开单据中心 passes `job_no`, `@include('platform::documents.upload-details')`), `jobs/index` (新建 Job in `@role`), `documents/index` (单号 box, `job_no` filter, actions column only for editors), new `documents/upload-details` (renders nothing outside `EDITOR_ROLES`; lists what is attached to the number; posts `documents.store` with `back=1`).
- **Frozen zone (C).** `config/erp.php` +`outbox_stale_minutes` (env `OUTBOX_STALE_MINUTES`, default 10). `bootstrap/app.php` `redirectUsersTo` → `LoginController::landingUrl`. `app/Support/Tenancy/ClientScope.php`: allow-list +`account/*`; a client user whose client is not `active` is logged out (session invalidated) and redirected to /login with `platform.auth.inactive`. `resources/views/layouts/app.blade.php` includes new `layouts/partials/health.blade.php` before the flash; `layouts/nav.blade.php`: the user name is now a link to 修改密码.
- **MasterData (C).** `ClientController::update` in a transaction: status → `inactive` sets every user of the client `is_active = false` (symmetrical to the `active` branch / approve), flash `masterdata.clients.deactivated`; `views/clients/form` hint `masterdata.clients.status_hint` under the status select.
- **Orders (X1) — ONE line, keep on rebase.** `app/Modules/Orders/views/show.blade.php`: `@include('platform::documents.upload-details', ['documentNo' => $order->order_no])` just above the 时间线 `<h2>`. Nothing else in Orders / Portal / Reports was touched; the partial is Platform's and decides by role what to render.
- **lang.** `lang/zh/platform.php`: `auth.failed` reworded (邮箱或密码不正确。), +`auth.inactive`, +`auth.throttled`; +`users.errors.self_lockout` / `last_admin`; new `password.*`; new `health.*`; `documents`: +`uploaded_to`, `upload_for`, `number`, `number_hint`, `number_not_found`, `client_hint` reworded (`related_id` key kept for the list column, unused by the form). `lang/zh/masterdata.php`: +`clients.deactivated`, `clients.status_hint`.
- **Tests.** New `tests/Feature/Platform/DriverPlatformAccessTest.php` (5), `HealthCheckTest.php` (3), `AccountSecurityTest.php` (3), `DocumentNumberUploadTest.php` (3), `tests/Feature/MasterData/ClientDeactivationTest.php` (2). Updated: `RoutesContractTest` (+`account` prefix), `DocumentCentreTest` (number form), `Audit20260910MinorsTest::test_client_visible_documents_always_have_a_client` (number form; the no-client document is created through the model as modules do).
- **Docs.** `contracts/routes.md` (Platform row, +Platform #137 row, +MasterData #137 row), `contracts/CHANGE_REQUESTS.md` #137, `MERGELOG.md`, `docs/demo-guide.zh.md` (§4 role table + password line, §5 page map, §6, §8 FAQ ×3).
- **Left as is (not in the approved list).** Approvals page still lists every request for every staff role (audit ADMIN-08 d); `ExceptionController::sourceUrl` 'shipment' branch (another agent edits that controller); 司机任务 ordering in the X2 运输 nav include; users-list filters; forgot-password mail flow (needs SMTP on the host).
- **Trial server after merge:** deploy only — no migration, no seeder, no manual step (`deploy/server/deploy.sh` rebuilds config / route / view caches). Optional: `OUTBOX_STALE_MINUTES=10` in `.env` to tune the banner; optional external monitor line in root's crontab, e.g. `*/5 * * * * cd /var/www/erp && php artisan erp:health --json >> storage/logs/health.log 2>&1` (exit code 1 = stale or dead events). Users of a client deactivated before this fix are refused at login on the client's status even though `users.is_active` is still true; re-saving the client as 停用 once flips their flags.

### 2026-09-22 · fix/audit-transport · 审计阻断项 A7–A8 (CR #133) · C edited the X2 Transport zone as integrator

- **Why.** Audit 2026-09-22 (lead approval 2026-09-22), blockers A7 TMS-01 and A8 TMS-02. A7: a delivery run could not be corrected — no stop removal, no date / driver / vehicle change, no cancel, and the driver could act only on a run dated exactly today, so a wrong shipment on the truck, a sick driver, a wrong-date run or two drops left at midnight locked those shipments forever. A8: quotes die after 24 h, were produced only by the event consumers, and an expired one still read 待选择 with no way to re-quote — an own-fleet shipment packed on Friday could not be confirmed on Monday. No migration, no seeder, no enum / event / service-contract change (`cancelled`, `expired` and `requoted` already exist).
- **Transport (X2), edited — keep on rebase.** `routes.php` (+`PATCH /transport/runs/{run}` `runs.update`, `POST /transport/runs/{run}/cancel` `runs.cancel`, `DELETE /transport/runs/{run}/stops/{stop}` `runs.stops.destroy`, `POST /transport/{shipment}/requote` `shipments.requote`); `Services/DeliveryRunService.php` (`update` / `removeStop` / `cancel`, `OPEN_STATUSES` planned | dispatched, `OPEN_STOP_STATUSES` pending | arrived, private `lockOpenRun` — refuses a closed run and any run with a delivered stop —, `release` (run_stop deleted, `shipments.delivery_run_id` → null, shipment stays `booked`), `renumber` 1..n, a dispatched run with nothing open → `completed`, `activeDriver` shared with `create`, activity log `delivery_run` with old / new attributes and the released shipment numbers); new `Services/ShipmentRequoteService.php` (`REQUOTABLE_STATUSES` quoting | quoted | quote_confirmed; expired `quoted` rows → `expired`, a confirmed final selection → `requoted` and the shipment back to `quoted` in one transaction, then `TransportOptionService::quote($id, latestStage)` OUTSIDE it as the consumers do; `latestStage`: final once a final quote exists / collection / packed, else preliminary, no quote at all → pickup_deliver final, from_stock preliminary; activity log `shipment` 重新报价); `Services/DriverPodService.php` (`lockDriverStop`: `run_date` on or before today instead of `isSameDay`); `Http/Controllers/DeliveryRunController.php` (`update`, `cancel`, `show` passes `editable` / `open` / `drivers`, private `drivers()` shared with `create`), `RunStopController.php` (`destroy`, 404 for a stop of another run), `DriverController.php` (`index`: `run_date <= today`, planned | dispatched, oldest first), new `Http/Controllers/ShipmentRequoteController.php` (planner roles via `DeliveryRunController::PLANNER_ROLES`; 0 quotes → `requote_none` error); `Models/DeliveryRun.php` (`isOpen()`, `isEditable()`).
- **Transport views (X2), edited — keep on rebase.** `views/runs/show.blade.php` (planners, while `$editable`: 修改班次 `<details>` with `<x-date-field name="run_date">`, driver select, vehicle, and the 取消班次 form with `confirm()`; while `$open`: an 操作 column whose 移出班次 button uses the `form="remove-stop-{id}"` attribute to post a hidden per-stop DELETE form rendered AFTER the reorder form — a form cannot nest inside the reorder form; the driver's read-only page is unchanged), `views/driver.blade.php` (a run dated before today carries a warn badge 配送日期 … 尚未完成), `views/shipments/show.blade.php` (重新报价 form above the quotes table for planners while the shipment is requotable; 有效至 column `d/m H:i`; a `quoted` row past `expires_at` renders the 已过期 badge instead of 待选择 — the select button was already hidden). `lang/zh/transport.php`: `driver.no_runs` reworded, +`driver.run_date`, `driver.stop_unavailable` reworded; +`quotes.expires_at / requote / requote_hint / requoted / requote_none / requote_invalid_status / log.requoted`; +`runs.edit / edit_hint / save_changes / updated / cancel / cancel_confirm / cancelled / actions / remove_stop / remove_stop_confirm / stop_removed / not_editable / delivered_stop / stop_not_pending / stop_not_in_run / log.*`.
- **Tests.** New `tests/Feature/Transport/RunEditingTest.php` (6: remove stop → shipment free, renumbered, re-added to another run, foreign stop 404, activity log; edit date / driver / vehicle with the run number kept, invalid driver / validation in Chinese; cancel releases both stops and the run is closed to planning; after a delivery edit / cancel / removing the delivered stop are refused while the pending stop still leaves and the run completes; the driver finishes yesterday's run today, tomorrow's stop is refused, the flag disappears once done; the driver and finance / warehouse get 403 on all three routes) and `tests/Feature/Transport/RequoteTest.php` (3: expired rows read 已过期 with 有效至, selecting stays refused, requote marks them `expired` and produces fresh selectable rows with the new validity + activity log; a client-chosen collection re-confirms in the client's name within tolerance and waits in `quoted` beyond it, `shipment.quote_confirmed` published again with `asn_id`; driver / finance / warehouse / client 403, a booked shipment refused with quotes untouched, a `quoting` shipment with no carrier gets the Chinese refusal + manual_transport exception and its quotes once a carrier answers). Every existing suite untouched and green (`DriverPermissionsTest`, `B7DriverPodTest` — a run dated tomorrow is still hidden — included).
- **Docs.** `contracts/routes.md` Transport row (#133), `contracts/CHANGE_REQUESTS.md` #133, `MERGELOG.md`, `docs/demo-guide.zh.md` (§7 steps 4 / 5, §8 FAQ 班次建错了 / 报价过期了).
- **Left as is / for X2 and X1 to know.** (1) A stop leaving a run (移出班次 / 取消班次) publishes nothing: `shipment.booked` was published with `delivery_run_id` when the stop was added and there is no event for "un-planned" in `contracts/events.md` — Orders' tracking still reads the booking; if Orders ever shows the run, it must read `shipments.delivery_run_id` (Transport table) through a service, not the stale payload. Not invented here; raise a CR if it is needed. (2) A run's `run_no` keeps the date it was created with after 修改班次 (identifier, not a fact). (3) A failed stop is never released — its shipment is `failed` and goes through 重新派送 (new shipment) as before. (4) Requoting a `quote_confirmed` shipment re-publishes `shipment.quote_confirmed` on re-confirmation; Billing's `shipment:{id}` key means the freight is not billed twice — same as the packed re-quote path already in `main`. (5) `arrived` stop status is treated as open (nothing sets it today).
- **Trial server after merge:** deploy only — no migration, no seeder, no manual step. `php artisan view:clear` is part of the deploy script (rebuild caches); nothing else.
### 2026-09-22 · fix/audit-warehouse · 审计阻断项 A1–A6 (lead approval 2026-09-22) · C, own zones (one X1 test adapted)

- **Why.** Audit 2026-09-22 (docs in the audit scratchpad; CHANGE_REQUESTS #131 / #138): six blockers in the warehouse flows — unscannable labels (INBOUND-01 / -14), double receipt of a goods line (INBOUND-02), an ASN with a 0-received line never completing (INBOUND-03), 登记拆柜 without its container = no devanning fee (INBOUND-08), a six-row pack form (OUTBOUND-01), cancelled orders still picked / packed / dispatched (OUTBOUND-02). No migration, no seeder, no enum / event change; one new route.
- **A1 labels + scan (Warehouse, contracts):** `Services/ScanCodes.php` (new: `U<id>` / `L<id>` tokens, `whereUnit` / `whereLocation`), `Models/StockUnit.php` + `Models/Location.php` (`scanCode()` scopes), `Services/LabelService.php` (barcode = token, `barcodeWidthPx()`, `PRINTABLE_WIDTH_PX`, `printableDescription()` — CJK description omitted), `views/labels/units.blade.php` (English only: mark + cartons largest, boxed BOTTOM LEVEL, "Received at" small print, full label_code kept), `views/labels/locations.blade.php` (116 mm + 20 mm padding = one page), `lang/en/pdf.php` (`unit_label.received_at`), `Services/ScanResolver.php`, `Http/Controllers/{Putaway,Stocktake,Stock}Controller.php` (every scan input takes the token or the full code, any case). `contracts/services.md` §12, `contracts/routes.md` Warehouse row. Old labels (full code in the barcode) keep scanning.
- **A2 double receipt:** `Services/ReceivingService.php` (`receiveLine` locks the goods line FOR UPDATE, locking reads for "received already?", refuses `already_received` / `not_receivable` as `RuleViolation`; also calls `PutawayService::completeIfDone` when the line has no units), `Http/Controllers/ReceivingController.php` (`form` redirects a received line / a closed ASN to the ASN page with the reason; `store` catches the refusal the same way), `views/receiving/{form,bulk,unplanned}.blade.php` (submit button locks on submit).
- **A3 ASN completion:** `Services/PutawayService.php` (`completeIfDone` public, returns bool; the 入库单 rule — a line with a receipt line or stock units counts as received; guards `putaway` / `closed` against a second event), `Services/GoodsReceiptService.php` (`complete()` calls it — the verifier's gap: the 0-line received after the last putaway), `Console/ReevaluateAsnPutawayCommand.php` (new, `asn:reevaluate-putaway [--apply]`), `WarehouseServiceProvider.php`. **Server step after merge (run once, on the trial server only):** `php artisan asn:reevaluate-putaway` lists ASNs stuck in `receiving` with everything received and put away; `php artisan asn:reevaluate-putaway --apply` completes them (status `putaway`, one `asn.putaway_completed` each → Billing raises WH-PUTAWAY-PLT / WH-LABEL-IN / VAS-PALLET-PURCHASE for those ASNs, so tell Finance which ASN numbers it printed). Not run anywhere by C. Note: an ASN whose every line was received as 0 also completes now (event with `pallet_count` 0 → the engine skips zero quantities, no charge).
- **A4 devanning container:** `Http/Controllers/TaskController.php` (`create` renders the opening ASN's containers server side and preselects its only unlinked one; `store` refuses a devanning task without `container_id` when the ASN has containers — `warehouse.tasks.container_required`), `views/tasks/create.blade.php` (options + `linked` flag in the data attribute; the script keeps the choice / preselects), `views/asns/show.blade.php` (登记拆柜 per unlinked container row with `container_id`; the task number once registered), `lang/zh/warehouse.php` (`tasks.container_required`, `asns.register_devanning`). Devanning tasks completed before the fix without a container earned no fee and are not re-billed — Finance adds a manual charge.
- **A5 pack form:** `views/outbound/pack.blade.php` (3 rows + `<template>` + 添加包裹, `old()` rows re-rendered), `views/outbound/_package-row.blade.php` (new; 件数 column), `Http/Controllers/OutboundController.php` (`packages.*.qty` 1–200), `Services/OutboundService.php` (`MAX_PACKAGES = 200`, `expandPackages()` — labels PKG-n-01… run over the expanded list; `too_many_packages`), `lang/zh/warehouse.php` (`outbound.qty`, `add_package`, `packages_hint`, `errors.too_many_packages`).
- **A6 cancelled orders:** `Services/OutboundService.php` (`releaseWave` and the board's 待释放 skip `operational_status = cancelled`; `confirmPick` / `pack` / `dispatch` refuse `warehouse.outbound.errors.order_cancelled`; new `closeCancelledTask()`, `cancelUnstartedPickTasks()`, `cancelledOrderIds()`; `closeWaveIfDone` counts `cancelled` as closed; `StockService` injected for the release), `Consumers/OrderCancelledConsumer.php` (cancels the order's not-yet-started pick tasks first, then releases as before), `Http/Controllers/OutboundController.php` (`closeTask`, `cancelledOrders` for the board and the wave page), `routes.php` (`POST /warehouse/outbound/tasks/{task}/close`, admin | warehouse_supervisor), `views/outbound/wave.blade.php` (card: 订单已取消 — 已拣货物请放回原库位 + 关闭任务 for admin / supervisor, confirm forms hidden), `views/outbound/index.blade.php` (danger badge instead of 打包 / 发运交接 for a cancelled order), `lang/zh/warehouse.php` (`outbound.order_cancelled_badge`, `cancelled_card`, `cancelled_card_hint`, `close_task`, `task_closed`, `errors.order_cancelled` / `order_not_cancelled` / `close_pick_only`). **Not done, by the rules:** no order note is written back to Orders — `contracts/services.md` exposes no note hook and Warehouse never writes `order_events`; if X1 wants one, an `order.pick_task_closed` event or an `OrderService::note` contract is the way. **Open question for the lead:** 关闭任务 does NOT put the already-picked cartons back on hand (their `pick` movement stands; the card tells the operator to return them to the location, and a stocktake adjustment corrects on-hand). If the goods should return to stock automatically, closing can write an `adjust` movement per confirmed line — say so and it is a few lines.
- **Tests.** New (16): `tests/Feature/Warehouse/LabelScanCodeTest.php` (2), `ReceivingDoubleReceiptTest.php` (3), `AsnPutawayCompletionTest.php` (3, incl. the command), `DevanningContainerTest.php` (2, incl. the WH-DEVAN-40-PLT charge), `PackQuantityTest.php` (2), `CancelledOrderOutboundTest.php` (4, incl. operator 403 on the close route). Adapted: `tests/Feature/Warehouse/Audit20260910MinorsTest.php` (two spots received a new line on an ASN that had already completed — now a second ASN), `tests/Feature/Warehouse/BulkReceivingTest.php` (the form of a received line now redirects). **X1 file touched — keep on rebase:** `tests/Feature/Orders/FulfilmentAllocationTest.php` — `stockOrder()` gained `$pendingQty` and returns the pending unit; the replenishment test puts that second unit away instead of receiving the same goods line twice (a goods line is received once; the backorder retry still keys on the line's `asn_line_id` in `asn.putaway_completed`). No Orders / Portal / Transport / Billing code touched.
- **Docs.** `contracts/CHANGE_REQUESTS.md` #131 / #138, `contracts/routes.md` (two Warehouse rows), `contracts/services.md` §12, `docs/demo-guide.zh.md` §8 (five FAQ rows), `MERGELOG.md`.
- **Trial server after merge:** deploy, `php artisan view:clear` if compiled views are cached, then the one-off `asn:reevaluate-putaway` above. No migration, no seeder.

### 2026-09-22 · fix/audit-billing · 审计阻断项 A9–A12 · C own zones (Billing, Platform, config, contracts); Platform now depends on one Orders service
Audit 2026-09-22 blockers FIN-01 + GAP-04 (A9), FIN-02 (A10), FIN-03 (A11, CR #132), ADMIN-01 (A12); lead approval 2026-09-22. No migration, no seeder. Warehouse and Transport code untouched.
- **A9 seller block (CR #139).** `config/erp.php` `company` (+ `.env.example` `COMPANY_*`, placeholder defaults — replace before go-live), `lang/en/pdf.php` invoice keys (abn / phone / email / invoice_no / period / reference / rate / asn / container / mark / shipment / less_credit_notes / paid / balance_due / how_to_pay / bank / bsb / account_no / account_name / payment_reference, footer reworded), `app/Modules/Billing/views/invoices/pdf.blade.php` rewritten (seller top-left, TAX INVOICE + Bill to right, meta strip, Rate column, source document per line, Paid / Balance due, How to pay), `InvoiceService` (`issue()` refuses without `company.abn` — zh `billing.errors.company_abn_missing`; `pdfData()` public for tests; `sourceRefs()` read-only lookups). No credit-note PDF exists — nothing added.
- **A10 credit-note GST.** `Invoice::creditedCents()` (Σ amount + gst of issued notes) used by `Invoice::outstandingCents()`, `InvoiceService::recordPayment`, `CreditNoteService::issue`; the portal and receivables pages read the model. `InvoiceController::creditCapError()` caps each credit-note line at line amount − draft / issued credit already on it (zh `billing.credit_notes.errors.exceeds_line`). `BillingPagesTest` pinned total − 5000 → total − 5500.
- **A11 missing rate (CR #132).** `ChargeEngine::charge` writes the `needs_review` $0 placeholder with `{missing_rate, suggested_cents, exception_id}`, re-prices it in place on a same-version replay, `reverse()` marks a placeholder reversed without a twin, `review()` closes the linked exception; `isUnpricedMissingRate()` public static for the views. `ChargeController::review` `?client_id=`, `reverse` message fix; `InvoiceController::unbilled` per-client counts + attention list, `show` → `unpricedFor()` warning data; views `charges/review`, `invoices/unbilled`, `invoices/show`; `lang/zh/billing.php` (review_hint rewritten, badges, warning, exception notes). `app/Support/Contracts/ExceptionService::resolve(int, ?int $resolvedBy, ?string)` + Platform impl (system close; a non-hold's note is appended to `message`).
- **A12 hold release from the centre.** `app/Modules/Platform/Http/Controllers/ExceptionController` — `assign` / `start` / `resolve` call `RequiredRoles::requireAny(OrderHoldService::rolesFor($hold_type, true))` for holds; `resolve` of a hold with an `order_id` goes through **Orders' `OrderHoldService::release()`** (X1 zone, called not copied) so the order timeline gets the same 放行 line as the order page; a client-wide hold (no order) is resolved directly — no timeline exists for it. `views/exceptions/index.blade.php` shows the forms only to `canRelease()` roles, others get 「只能由 … 放行」 + 「到订单页放行」; `lang/zh/platform.php` `exceptions.hold_roles / hold_roles_hint / to_order_release`. **X1: `OrderHoldService::rolesFor(string, bool): array` and `release(Order, int, string, int): void` are now called from Platform — keep the signatures (add them to `contracts/services.md` §2 at the next checkpoint if you prefer a formal contract).**
- **Tests.** New `tests/Feature/Billing/AuditBillingBlockersTest.php` (5), `tests/Feature/Platform/ExceptionCentreTest.php` +1. Re-pinned: `ChargeEngineTest` (placeholder), `DevanningAllocationTest` (placeholders counted; pending-only where the old assertion listed cards / keys), `StorageTierBillingTest` (missing base rate = placeholder). Suite: 486 passed, 1 skipped, 7024 assertions; pint clean.
- **Docs.** CHANGE_REQUESTS #132 / #139 (#131 left free for the parallel audit branch), services.md §4 / §6 / §9, charge-codes.md §6, MERGELOG, demo-guide FAQ (公司信息占位、缺费率去哪了、异常中心放不了财务锁).
- **Trial server after merge:** deploy only — the `COMPANY_*` values are built-in defaults, nothing to configure; replace them in `.env` before go-live. Existing open missing-rate exceptions from before the merge have no placeholder row (only events processed after the merge get one) — price them as manual charges or re-run the event from the integration monitor.
### 2026-09-22 · fix/audit-b-portal · 审计 B 类门户项 (CR #136) · C edited X1 Orders / Portal zones as integrator

- **Why.** Audit 2026-09-22 Part B (lead approval 2026-09-22: fix exactly as the report suggested), portal items PORTAL-05 and PORTAL-07, both `major`. PORTAL-05: `SpreadsheetManifestParser` dropped a row with a hard error and `OrderImportService::groups()` grouped only the rows it read, so the other rows of the same 唛头 formed a "ready" group and one 确认提交 produced an order silently short of a goods line (then a short ASN and a receiving discrepancy); the button never changed and asked nothing. PORTAL-07: the list (CSV and manual) had no address type — `deliver_to_address_type` came from the address book, else `fba` for an FBA reference, else `business` — so a residential consignee ordered through the list was never `residential` and the OMS-13 tailgate rule could not fire. No new business rule: the existing `TailgateRule` is simply reached.
- **Orders (X1), edited — keep on rebase.** `Services/SpreadsheetManifestParser.php` (HEADERS +`addresstype` / `deliveryaddresstype` / `consigneeaddresstype` / `consigneetype` / `residential` / `residentialaddress` / 地址类型 / 收件类型 / 收件地址类型 / 收货地址类型 / 收件人类型 → `deliver_to_address_type`; new `ADDRESS_TYPE_VALUES`; `CARRIED` + `FORM_FIELDS` (after `deliver_to_postcode`) + `deliverySignature()` + the Row key `deliver_to_address_type`; `convertRow` error `invalid_address_type`; every error entry now also carries `consignment_mark` = the refused row's mark after carry-down, null when the mark is missing). `Services/OrderImportService.php` (`groups(array $rows, array $errors, array $context, string $sha256)` — builds mark → refused rows from `$errors`, a mark with any refused row is `blocked` FIRST with `rows_not_read` and `error_rows` on the group; group `deliver_to_address_type` = `$first['deliver_to_address_type'] ?? address book ?? (FBA ? fba : business)`; `signature()` includes the address type; `record()` passes `$parsed['errors']`). `Http/Controllers/OrderImportController.php` (`show` passes `skippedRows`; `confirm` validates `skip_acknowledged` boolean and, while pending with skipped rows > 0 and no acknowledgement, redirects back with `orders.imports.errors.skip_unacknowledged` and creates nothing; private `skippedRows()`). `views/imports/show.blade.php` (an `$errors` block; address type after the destination in the 收件 cell via `orders.address_types.*`; for a non-portal pending import the button reads `orders.imports.actions.confirm_partial` with a required checkbox `skip_acknowledged` when `skippedRows > 0`, else the plain 确认生成订单). `lang/zh/orders.php` (`imports.actions.confirm_partial` / `skip_acknowledge`, `imports.columns.deliver_to_address_type`, `imports.errors.invalid_address_type` / `rows_not_read` / `skip_unacknowledged`; `inconsistent_group` wording unchanged).
- **Portal (X1), edited — keep on rebase.** `Http/Controllers/PortalInboundImportController.php` (`TEMPLATE_HEADERS` +地址类型 after 邮编; `template()` three sample rows — EDW-001 FBA, EDW-002 商业, EDW-003 住宅 (Ms Li, 2 × 3 kg desk lamps, Box Hill VIC 3128); `show` passes `skippedRows`; `confirm` refuses without `skip_acknowledged` when rows are skipped (after the "nothing to confirm" check, before the collection plan check) with `portal.inbound.errors.skip_unacknowledged`; `manualForm` passes `addressTypes` = `OrderEnums::ADDRESS_TYPES`; private `skippedRows()`). Views: `asns/imports/show.blade.php` (地址类型 column after 邮编 — `portal.inbound.address_types.*`, a residential mark shows the badge `portal.inbound.residential_tailgate`; when `skippedRows > 0` the button reads `portal.inbound.actions.confirm_partial` (`:ready` = ready groups, `:skipped` = skipped rows) with the required checkbox `skip_acknowledged` labelled `portal.inbound.skip_acknowledge`), `asns/imports/create.blade.php` (one hint line `portal.inbound.address_type_hint`), `asns/imports/partials/manual-row.blade.php` (select `rows[i][deliver_to_address_type]`: empty = `portal.inbound.manual.address_type_auto`, then business / fba / residential), `asns/imports/partials/manual-rows.blade.php` (收件信息同上一行 copies the select too). `lang/zh/portal.php` (`inbound.address_type_hint`, `inbound.address_types` (3), `inbound.residential_tailgate`, `inbound.skip_acknowledge`, `inbound.fields.address_type`, `inbound.actions.confirm_partial`, `inbound.errors.skip_unacknowledged`, `inbound.manual.columns.deliver_to_address_type`, `inbound.manual.address_type_auto`).
- **Support / contracts (C).** `Contracts\ManifestParser` docblock (Row +`deliver_to_address_type`, errors +`consignment_mark`), `Fakes\FakeManifestParser` (`deliver_to_address_type` null on both rows), `contracts/services.md` §8 (one additive bullet), `contracts/routes.md` (one Portal row), `contracts/CHANGE_REQUESTS.md` #136.
- **Tests.** New `tests/Feature/Portal/PortalInboundBlockedMarkTest.php` (3: CSV — MK-A with a refused third row is blocked with the row named, the preview button reads 只提交可生成的 1 张（跳过 3 行） with the required tick, confirm without the tick is refused in Chinese and creates nothing, with it MK-B only and MK-A stays blocked, a clean list keeps 确认提交 with no tick; manual — the form still refuses a broken row before any preview (#128) and `OrderImportService::previewRows` with the same rows blocks MK-A, the portal confirm needs the tick; staff — the same list on `/orders/imports`, 只生成可生成的 1 张（跳过 2 行） + tick, refusal without it, the errors CSV names the blocked mark). New `tests/Feature/Portal/PortalInboundAddressTypeTest.php` (2: CSV 住宅 / empty+FBA / 商业 / empty / 火星 → residential / fba / business / business + one Chinese row error naming 地址类型, carry-down under the mark, the residential badge once, staff page shows the type, orders created with the types, `tailgate_required` + `residential_address` on the residential order at entry and after `OrderStatusService::transitionOperational(…, 'confirmed')`, business / FBA orders without, English header `Address type` with home / Amazon FBA / Company straight through the parser; manual — the select per row, residential / FBA / business rows → orders with the types and the tailgate on the residential one). Updated: `PortalInboundImportTest` template case (header string with 地址类型, three sample rows, three ready groups fba / business / residential), `OrderImportTest` (three staff confirm posts pass `skip_acknowledged` — 5 refused rows in the real workbook, a blocked mark + a refused row, a duplicate mark).
- **Docs.** `contracts/services.md` §8, `contracts/routes.md` Portal row, `contracts/CHANGE_REQUESTS.md` #136, `MERGELOG.md`, `docs/demo-guide.zh.md` §8 FAQ (清单里一行出错 / 住宅收件人).
- **Left as is / for X1 to know.** (1) The manual form keeps refusing a broken row before any preview (#128), so on the page the PORTAL-05 block only ever shows for uploads; the grouping rule is shared and tested through `previewRows`. (2) `inconsistent_group` wording is unchanged although the address type is now part of the signature (a 住宅 and a 商业 row under one mark block the group). (3) `OrderController::update` still cannot change `deliver_to_address_type` on an existing order (audit note under PORTAL-07) — not in scope, the manual tailgate flag remains the backstop. (4) `AsnImportService` (Warehouse) ignores the new row key; ASN lines have no address type. (5) No migration (`orders.deliver_to_address_type` is a 20-char string with all three values already in use), no seeder, no event change.
- **Trial server after merge:** deploy only — no migration, no seeder, no manual step. `php artisan view:clear` is part of the deploy script; nothing else.

### 2026-09-22 · fix/audit-b-transport · 审计 B 类运输项 (CR #135) · C edited the X2 Transport zone as integrator

- **Why.** Audit 2026-09-22 Part B (lead approval 2026-09-22, "fix exactly as the report suggested"): TMS-03 / CS-19 money typed in cents, TMS-11 extra charges leaving no record, TMS-07 full-resolution driver photos + lost signature on rejection, TMS-09 failed stop with no state and a second fail / a delivery still accepted, TMS-10 redelivery clickable twice + `Str::limit` dropping the last digit, TMS-12 carrier POD stamped with the upload time and PDF only, GAP-05 consignment note without parties and with the database id as the order. No enum / event / service-contract change; three additive Transport migrations.
- **Transport (X2), edited — keep on rebase.** `migrations/2026_09_22_300000_create_shipment_extra_charges_table.php`, `…300010_add_failure_note_to_pods.php`, `…300020_add_redelivery_link_to_shipments.php`; new `Models/ShipmentExtraCharge.php`; `Models/Shipment.php` (+`redelivery_of_shipment_id` fillable, `extraCharges()`, `redeliveryOf()`, `redelivery()`), `Models/Pod.php` (+`failure_note`); `Services/ExtraChargeService.php` (`REPORTABLE_STATUSES` booked → failed, `reportable()`, `report(…, bool $confirmRepeat = false): ShipmentExtraCharge` — row + `delivery.extra_charge` in ONE transaction, the row carries the event's uuid; same type again without the tick → `extra_charges.already_reported`; before booking → `extra_charges.not_booked`), `Services/RedeliveryService.php` (refuses when `shipments.redelivery_of_shipment_id = failed.id` exists — `redelivery.already_exists` naming it; `nextNumber()`: root = number without `-R\d+`, next = max existing `-R<n>` + 1 → `SHP-ORD-20260921-0009-R1`, `-R2` after R1 fails; `_redelivery_of` in raw_response kept), `Services/CarrierPodService.php` (`capture(…, ?CarbonInterface $deliveredAt = null)` — the 实际签收时间 is `pods.delivered_at`, `shipments.delivered_at` and the event's `delivered_at`; refuses a future time and one before the shipment existed; `pdf` / `jpg` / `png` detected by content — `%PDF-`, `FF D8 FF`, PNG signature — stored as `<no>-carrier-pod.<ext>` with the real mime; `invalid_pdf` → `invalid_file`), `Services/DriverPodService.php` (`assertStopOpen()` in both `deliver` and `fail`: a `failed` stop → `driver.already_failed` (reason + time), a delivered one → `already_delivered`; `fail(…, ?string $note = null, array $photos = [])` — note required by the service for `other` (`failure_note_required`, checked AFTER the stop lock so `stop_unavailable` keeps precedence), photos through the shared `storePhotos()` on the POD path as `photo` documents named `<no>-failure-photo-<n>`, ids in `photo_document_ids`; the note is appended to the `delivery_failed` exception message (`exceptions.driver_failed_note`), payload unchanged), `Services/ShipmentQuoteRequestFactory.php` (+`parties(Shipment)`: sender / receiver / order_no / instructions for any delivery shipment without the completeness gate — collection: pickup → ASN warehouse; order: warehouse (or pickup address) → consignee + `orders.delivery_instructions`), `Services/ConsignmentNotePdf.php` (constructor takes the factory; `data()` exposed — live parties first, the quote snapshot fills gaps; `orderNo`, `instructions`), controllers `ManualQuoteController` (`cost` / `customer_price`), `OwnFleetCostController` (`actual_cost`), `ExtraChargeController` (`carrier_cost`, `confirm_repeat`, flash `reported_detail`), `CarrierInvoiceController` (`total`), `CarrierPodController` (`delivered_at` required date ≤ now, `pod_file` mimes pdf,jpg,jpeg,png), `DriverController::fail` (`failure_note`, `photos[]` 0–5), `RedeliveryController` (flash `redelivery_hint` = original id), `ShipmentController::show` (loads `extraCharges.reportedBy`, `redeliveryOf`, `redelivery`; `redeliveryHintShipment`). All dollar fields: `numeric`, `decimal:0,2`, converted with `Money::fromDecimal`, flash echoes `$185.50`.
- **Transport views (X2), edited — keep on rebase.** `views/shipments/show.blade.php` (dollar boxes `step="0.01"` + `transport.money_hint`; 原失败运输单 / 重派运输单 header rows; redelivery button → link once one exists, `create_hint`; the `redelivery_hint` flash with a link to the ORIGINAL's `#extra-charges` (the new shipment is not booked yet, the original is); carrier POD `<x-date-field name="delivered_at" time>` default now + `accept="application/pdf,image/jpeg,image/png"`; `<details id="extra-charges">` — form only while reportable else `not_booked_hint`, `confirm_repeat` checkbox once a report exists, table 上报时间 · 类型 · 数量 · 承运商成本 · 说明 · 上报人), `views/driver.blade.php` (stop status badge on every card; failed card = badge + `failed_state` reason / time + note + photo count + `failed_next`, no forms; hidden `stop_id` in both forms so `old()` fills only the posted stop; `signature_data` re-rendered from `old()` and redrawn on the canvas; failure form +`failure_note` textarea +`photos[]`; `capture` dropped; inline script: picks accumulate up to 5 with an n/5 count, on submit each photo → `createImageBitmap` → canvas max 1600 px → `toBlob('image/jpeg', 0.8)` → `input.files` rebuilt via `DataTransfer` → native `form.submit()`; browsers without the APIs post the originals), `views/carrier-invoices/index.blade.php` (`total` in dollars), `views/shipments/consignment-note.blade.php` (From / Deliver to table for every shipment — name, address line, phone, instructions, tailgate + flag; Order = order number; Tracking no.; "Received in good order — Name / Signature / Date" box; tolerates a render without `parties` / `orderNo`). `lang/zh/transport.php`: +`money_hint`; `driver.photos_hint / photos_selected / failure_note / failure_photos / failure_note_required / failed_state / failed_note_label / failed_photos_label / failed_next / already_failed`; `exceptions.driver_failed_note`; `manual_quote.cost / customer_price / saved_amounts` (−`cost_cents / customer_price_cents`); `costs.actual_cost / saved_amount` (−`actual_cost_cents`); `reconciliation.total_input / imported_total` (−`total_cents`); `carrier_pod.file` reworded, +`delivered_at / delivered_at_hint / invalid_file / delivered_in_future / delivered_before_shipment` (−`invalid_pdf`); `extra_charges.carrier_cost / cost / reported_detail / cost_suffix / not_booked / not_booked_hint / already_reported / confirm_repeat / reported_list / none_reported / reported_by / reported_at` (−`cost_cents`); `redelivery.create_hint / already_exists / existing / origin / charge_hint / charge_link`; validation attributes `cost / customer_price / actual_cost / carrier_cost / total / confirm_repeat / delivered_at / failure_note` (−the three `*_cents`), messages `decimal`, `boolean`, `pod_file.mimes` reworded, `delivered_at.before_or_equal`. `lang/en/pdf.php` `consignment_note.*` additions (PdfEnglishTest green).
- **Tests.** New `tests/Feature/Transport/AuditPartBTransportTest.php` (7): dollars → cents + flash echo on all four forms, 3-decimal refusal, saved cost shown back as `83.50`; extra-charge row + event uuid, listing, repeat refused / accepted with the tick, other type free, refused before booking (form hidden); driver page carries the compression script and no `capture`, an uncompressed post still works, a rejected post keeps name + signature for that stop only; failed stop state, `other` needs a note, note + photo stored and in the exception message, repeat fail / deliver-after-fail refused in Chinese with one pod row and one `delivery.failed`; redelivery `-R1` (last digit kept), links both ways, hint with `#extra-charges` link, second click refused, `-R2` after R1 fails; carrier POD `delivered_at` everywhere incl. the payload, JPG archived as `image/jpeg`, future time / non-image refused; consignment note From / Deliver to / instructions / tailgate flag / order number / tracking / receipt box, no CJK, PDF streams. Existing tests moved to the dollar fields: `B8DeliveryTrackingTest` (POD `delivered_at`, extra charge on a booked shipment with `carrier_cost`), `B9aCarrierCostMarginTest` (`actual_cost`), `B9bCarrierInvoiceTest` (`total`), `M4TransportIntegrationTest` (`cost` / `customer_price`), `TransportAuditFixesTest` (manual quote in dollars; extra-charge form hidden on quote_confirmed, shown on booked). `PdfEnglishTest`, `DateFieldTest`, `B7DriverPodTest`, `RunEditingTest`, `DriverPermissionsTest`, `InboundCollectionShipmentTest`, `DemoFlowTest` untouched and green.
- **Docs.** `contracts/routes.md` Transport row (#135), `contracts/db-schema.md` (§5: `shipment_extra_charges`, `pods.failure_note`, `shipments.redelivery_of_shipment_id`), `contracts/CHANGE_REQUESTS.md` #135, `MERGELOG.md`, `docs/demo-guide.zh.md` (§7 steps 4 / 5, §8 FAQ), new `deploy/README.md` (upload limits — recommendation only).
- **Left as is / for X2 to know.** (1) `shipment_extra_charges` is Transport's record of what was REPORTED; the client charge is still Billing's `charges` row from the event — Billing untouched, its key `extra:{shipment_id}:{charge_type}:{occurred_at}` unchanged. (2) The carrier CSV still reads `billed_cents` (TMS-14, not in this batch). (3) `warehouses` has no phone column, so the From block prints a phone only when the party carries one (the quote snapshot's `phone`); the receiver's phone comes from the order. (4) POD photos stay required (至少一张) — unchanged rule; the compression is client side, the server accepts plain uploads as before. (5) `SESSION_LIFETIME` (120 min) not changed; the redrawn signature covers the 419 case. (6) The PHP / nginx limits are NOT applied by this branch — `deploy/README.md` documents the recommended values for the lead to set on the trial box.
- **Trial server after merge:** deploy (`bash deploy/deploy-trial.sh`) runs the three additive migrations (`shipment_extra_charges`, `pods.failure_note`, `shipments.redelivery_of_shipment_id`); no seeder, no manual data step. Separately, and only if the lead agrees: set `upload_max_filesize = 8M`, `post_max_size = 40M` in the PHP-FPM ini and `client_max_body_size 40m;` in the nginx server block, then reload both (see `deploy/README.md`).

### 2026-09-22 · fix/audit-b-billing · 审计 B 类财务项 (CR #140) · C own zones (Billing, Platform approvals / Job page, contracts)
Audit 2026-09-22 Part B billing items FIN-16 / FIN-13 / FIN-08 / FIN-09 / FIN-10, lead approval 2026-09-22 ("fix exactly as the report suggested"). One additive migration (`payments` +`note`, +`void_of_payment_id`), no seeder, no enum / event change. Warehouse, Transport, Orders, Portal code untouched.
- **FIN-16 冲销 vs credit note.** `charges/index`: 发票 column (draft no. + 草稿中 / issued no. / —); an invoiced row links 去开冲减单 → `invoices/{invoice}?credit_line={line}#credit-note` (form opens, that line's remaining amount pre-filled — `InvoiceController::creditedByLine`, also the per-line `max`); a drafted row shows a hint; 冲销 only for pooled rows, and `ChargeController::reverse` refuses `invoice_line_id` / `invoiced` rows in Chinese. `ChargeEngine::reverse` itself is unchanged (version supersedes still need the negative twin).
- **FIN-13 payments.** `InvoiceService::recordPayment` refuses `amount > outstandingCents()`; `voidPayment(Payment, reason, ?by)` writes an offsetting negative row (`void_of_payment_id`, `note`), never deletes; `applyPaymentTotals()` recomputes `paid_amount_cents` / status (`paid` → `part_paid` → `issued`) / `is_overdue` from Σ rows. Route `POST /billing/payments/{payment}/void` (`billing.payments.void`). `invoices/show`: amount box pre-filled with and capped at the balance, payments as a table with 作废收款 (reason required), 已作废 badge, 「作废收款 #id · reason」 on the twin. `Payment` model: `voidOf()`, `voidedBy()`, `isVoidRow()`.
- **FIN-08 rate-card lock + link + diff.** `RateCardService::lockedForApproval` (pending **or approved** `rate_card_change`) refuses `updateItem` / `addItem` (`billing.rate_cards.errors.locked_for_approval`); `previousVersion` / `diffAgainstPrevious` (match on code + class + band + zone + level + warehouse + carrier; rate / min / POA / mode / percent / thresholds) → `rate_cards/show` summary line, badges, old → new cells, 只看改动 checkbox (inline JS on `data-diff`), removed-rows table; edit / add forms hidden while locked. Platform `ApprovalController::subjectLinks` → the 对象 column links rate cards to their page and credit notes to their invoice (`platform.approvals.subjects.*`). **Bug found by the diff and fixed:** `newVersion` copied `getAttributes()` through the `array` cast → `threshold_json` double-encoded on every copied version (read back as a string: pick bands / container caps / pallet sizes silently lost on v2+). Now `replicate()`, and `RateItem::threshold_json` uses `App\Modules\Billing\Casts\ThresholdJson` (decodes a double-encoded row, writes back clean) — rows already broken on the trial server heal on read, no data step.
- **FIN-09 manual charge.** Blank first code option (`billing.charges.select_code`, required), `<x-date-field name="charge_date">` default today / `max` today → `ChargeEngine::manual(..., ?CarbonInterface $chargeDate)` (8th optional parameter; every existing caller unchanged); `?job_id=&client_id=` prefill (`ChargeController::manual(Request)`); Job page 费用与利润 header gets 手工加费 for admin / finance, also on a Job with no charge yet.
- **FIN-10 draft merge + counts.** `InvoiceService::appendJobCharges(draft, jobId): int` (+ private `addLines` / `recomputeTotals` shared with `draft()`), route `POST /billing/invoices/{invoice}/append-job/{job}` (`billing.invoices.append_job`); `invoices/unbilled` row of a Job on an open non-storage draft: 「已有草稿 DR-… ($)」 + 并入该草稿 (`InvoiceController::openDraftsByJob`); `invoices/show` (draft): corrected hint + 「JOB 另有 N 条新服务费未并入」 + 并入本草稿 (`newChargesByJob`); `/billing` header 待开票 = pool rows exactly (`poolCount`), plus 草稿中 (`draftedCount`); `Charge::invoiceLine()` relation.
- **Tests.** New `tests/Feature/Billing/AuditPartBBillingTest.php` (5 tests, one per item; FIN-16 also covers the credit-note approval link, FIN-08 the threshold copy + legacy heal). No existing assertion re-pinned.
- **Docs.** CHANGE_REQUESTS #140, routes.md (Billing row: two routes + behaviour changes), services.md §9 (ChargeEngine::manual, InvoiceService, RateCardService), db-schema.md §6 (`payments`), MERGELOG, demo-guide FAQ (5 lines).
- **Trial server after merge:** deploy runs `migrate --force` (one migration). Nothing to seed or configure. X1 / X2: nothing to rebase for beyond `main`.

### 2026-09-22 · fix/audit-b-warehouse · 审计 B 类仓库项 (CR #141) · C, own zones only

- **Why.** Audit 2026-09-22 Part B (lead approval 2026-09-22): ten warehouse items — INBOUND-04 / -05 / -07 / -13 / -16, OUTBOUND-08 / -09 / -12 / -03, CRAWL-01. Fixed as the report suggested. No migration, no seeder, no enum / event / services.md change; five new routes (all Warehouse). No Billing / Transport / Orders / Portal code touched; the Billing `DevanningAllocationTest` still posts `physical_containers.arrive` directly and passes because that POST is unchanged.
- **INBOUND-04 per-line receiving:** `Http/Controllers/ReceivingController.php` (`store` → `Validator::make` + `after`: Σ `units[*][carton_qty]` = `received_cartons` → `warehouse.receiving.units_sum_mismatch`; variance reason required with the bulk rule → `bulk.reason_required`), `views/receiving/form.blade.php` (rows from `<template>` — the fixed 3-row cap is gone — one row mirrors 实收, several rows show 单元合计 X / 实收 Y and lock the submit), new `views/receiving/_unit-row.blade.php`.
- **INBOUND-05 pallet source + unit correction:** `views/receiving/bulk.blade.php`, `views/receiving/unplanned.blade.php` + `_unplanned-row.blade.php` (托盘来源 select per row, default client_own, 全部设为 type / source), `ReceivingController::bulkStore` + `UnplannedReceivingController::store` (`rows.*.pallet_source`), new `Services/UnitCorrectionService.php` (`correct()` — source / dims / weight / optional class, reason required, class re-suggested by `RateService::suggestPalletClass`, `activity('stock_unit')…->log('unit_corrected')` with old / attributes / reason), `StockController::update` + route `PATCH /warehouse/stock/{unit}` (admin \| warehouse_supervisor), `views/stock/show.blade.php` (修改单元信息 card; null class → 未分类(按标准托计费)), `views/snapshots/index.blade.php` (same label), `Models/StockUnit.php` (logOnly + dims / weight / override reason). **Guard decision (report asked which):** the edit is allowed at any time — `StorageBillingService::billWeek` bills from the week's LAST snapshot, so the corrected class / source reaches the next daily snapshot and the next weekly run on its own; a week already billed is not re-run (the card says so). A hard guard would be one read of `charges.source_activity_id = "unit:{id}:week:{week}"` — not built.
- **INBOUND-07 VAS quantity:** `Http/Controllers/TaskController.php` (`COMPLETION_RULES`, `completionError()` per type — wrap / waste `billable_qty` > 0, labour / vas_other hours > 0, scanning count or serials, devanning defaults 1 container — used by `complete` and by `store` with `complete_now`: create + complete in one `DB::transaction`, box devanning included), `views/tasks/index.blade.php` (inputs `required` / min), `views/tasks/create.blade.php` (现在完成 fieldset, fields switch with the type), `lang/zh/warehouse.php` (`tasks.quantity_required.*`, `complete_now*`, `serials`, `created_completed`). Not done: the default VAS-only list filter + 含系统记录 link (not in the approved item).
- **INBOUND-13 ASN Job / client:** `AsnController::store` (`Rule::exists('jobs','id')->where('client_id', …)`, message `asns.job_not_of_client`), `AsnController::create` (`currentWarehouseId`), `views/asns/create.blade.php` (`data-client` on every Job option + `syncJobs()`; warehouse defaults to the session warehouse). Not done: add-row containers on the create form (not in the approved item).
- **INBOUND-16 physical container:** `Services/PhysicalContainerService.php` (`update()` before arrival / emission, warehouse only without members, duplicate rules as create; `delete()` for an empty shell — no members, no non-cancelled task, nothing emitted; `arrivalPreview()` — the codes the arrival raises (TR-CARTAGE-{size} when 我方拖车, TR-SIDELOADER when 侧卸车) per member share, priced read-only via `RateService::price`, 22.5 t flag), `Http/Controllers/PhysicalContainerController.php` (`edit`, `update`, `destroy`, `arriveConfirm`), `routes.php` (`GET …/{box}/edit`, `PUT …/{box}`, `DELETE …/{box}`, `GET …/{box}/arrive`), new `views/physical_containers/edit.blade.php` + `arrive.blade.php`, `views/physical_containers/show.blade.php` (登记到港 → link to the confirmation; 修改物理柜 / 删除物理柜), `Models/PhysicalContainer.php` (logOnly + container_no / warehouse_id / size / unpack_mode / eta_date).
- **OUTBOUND-08 short pick:** `Services/OutboundService.php` (`SHORT_REASONS`; `confirmPick(…, ?string $shortReason = null, ?string $shortNote = null)` — signature extended with optional params, every existing caller unchanged; message from `warehouse.outbound.pick_short_message`; a service caller without a reason is recorded as 其他), `OutboundController::pick` (reason required when picked < required → `errors.short_reason_required`; flash `pick_short_recorded`), `views/outbound/wave.blade.php` (reason select + note shown when 实拣 < 应拣, confirm() naming the shortfall, 少拣 N badge on the row, ":lines 行少拣" badge on the card footer next to 打包). **Kept:** the shortfall is still released to available stock (`release` movement). **Pending lead decision:** should 找不到 keep the quantity unavailable and open a stocktake line for the location instead? Say so and it is a small change in `confirmPick`.
- **OUTBOUND-09 pack rows:** `OutboundController::pack` (`after`: a weighed row needs `package_type` and all three dims → `errors.row_type_required` / `row_dims_required` with the row number), `views/outbound/pack.blade.php` (row errors above the table, type copied from the row above when a weight is typed, Enter moves focus instead of submitting, live 本次打包: 共 N 件 · 总重 · 托). Existing tests `PackQuantityTest` (satchel row) and `CancelledOrderOutboundTest` (pack row) gained dims. Not done: supervisor 重新打包 (larger item, needs a contract note — `outbound.packed` has fed Billing / TMS).
- **OUTBOUND-12 wave selection:** `OutboundController::release` (`order_ids` required \| array \| min:1 → `errors.select_orders`), `OutboundController::index` (箱数 per row = Σ `fulfilment_lines.qty`, `today`), `views/outbound/index.blade.php` (header 全选, 释放波次 (N) live count + disabled at 0, client / date filters tick the matching rows via `data-client` / `data-date`, 箱数 column, 今天 / 已过期 badge, visible labels on client / date / warehouse). Existing tests `StockShortageTest` and `Audit20260910MinorsTest` post `order_ids => [999999]` with their impossible client filter (same `no_candidates` refusal).
- **OUTBOUND-03 dispatch ↔ shipment:** `OutboundController::shipmentsFor()` (read-only `shipments` ⟕ `carriers` ⟕ `transport_quotes` by `fulfilment_id`; Transport exposes no shipment read in services.md — same DB::table precedent as this board's orders / fulfilments reads; **X2: nothing to change, Warehouse only reads `shipments.id / shipment_no / status / fulfilment_id / carrier_id / selected_quote_id`, `carriers.name`, `transport_quotes.source`**), `views/outbound/index.blade.php` (shipment no · carrier · status badge per 待发运 row; `booked` → hidden `shipment_id` + 交接给 defaulted from the quote source (own_fleet → 自有司机, else 承运商); otherwise 未订舱 and 客户自提 preselected; 手动填运单 ID `<details>` override), `OutboundController::dispatch` (a given `shipment_id` must have `fulfilment_id` = this batch → `errors.shipment_not_of_fulfilment`).
- **CRAWL-01 labels:** `Services/LabelService.php` (`barcode()` = one PNG `<img>` via `BarcodeGeneratorPNG` + GD, `BATCH_SIZE = 40`, `batch()`, `batches()`), `Http/Controllers/LabelController.php` (`?batch=n` on all three routes; a set over 40 without `batch` → `warehouse::labels.batches` HTML page with one link per batch; `locations` filters `type` / `zone` / `aisle_from` / `aisle_to`), new `views/labels/batches.blade.php`, `views/locations/index.blade.php` (按区 / 巷道 / 类型打印 filter per warehouse), `Models/Location.php` (`codeBetween()` — the aisle / bin range compare, now shared with `LocationController::bulk`). Measured (tinker, dev DB units repeated): 40 labels 0.50 s / 58 MB peak, 80 labels 0.56 s / 64 MB, 200 labels 1.61 s / 76 MB (audit: 1.9 s / 100 MB, fatal, 13.7 s / 282 MB) — no `memory_limit` override needed. Old labels keep scanning (the token did not change).
- **Tests.** New (10 files, 14 tests): `ReceivingUnitReconciliationTest` (1), `PalletSourceCorrectionTest` (2), `VasCompletionQuantityTest` (2), `AsnJobClientTest` (1), `PhysicalContainerEditTest` (2), `PickShortReasonTest` (1), `PackValidationTest` (1), `WaveSelectionTest` (1), `DispatchShipmentLinkTest` (1), `LabelBatchTest` (2). Adapted: `PackQuantityTest`, `CancelledOrderOutboundTest`, `StockShortageTest`, `Audit20260910MinorsTest`, `LabelScanCodeTest` — all Warehouse. No X1 / X2 test touched.
- **Docs.** `contracts/CHANGE_REQUESTS.md` #141, `contracts/routes.md` (one Warehouse row), `docs/demo-guide.zh.md` §8 (FAQ rows), `MERGELOG.md`.
- **Trial server after merge:** deploy, `php artisan view:clear` if compiled views are cached. No migration, no seeder, no one-off command. GD must be loaded in the server's PHP (it is on every standard build; `php -m | grep gd`) — without it label PDFs throw at the first barcode.

### 2026-09-22 · fix/lead-decisions-142 · 负责人决策 1–5 (CR #142) · C own zones + ONE X1 Portal controller edit as integrator

- **Why.** Lead decisions 2026-09-22 on the open questions left by CR #132 / #140 / #141 — five items, implemented as CR #142 (row quotes each decision). One additive migration (`stock_units.qty_frozen`, `stocktakes.kind`), no seeder, no event / payload change, one new route, two route middleware changes. Transport, Orders and Reports code untouched.
- **X1 zone touched (integrator, read-shaped):** `app/Modules/Portal/Http/Controllers/PortalStockController.php` — the stock aggregation gained `coalesce(sum(u.qty_frozen), 0) as qty_frozen` and `qty_available` subtracts it (the portal 可用 figure follows `StockUnit::availableQty()`). Two lines, no view change, no new column shown. X1: nothing to do beyond `git rebase main`; the column exists after the deploy's `migrate --force`.
- **(1) 找不到 → frozen + 差异盘点.** `Warehouse/migrations/2026_09_22_400000_add_qty_frozen_to_stock_units_and_kind_to_stocktakes.php`; `Models/StockUnit.php` (`qty_frozen` fillable / cast / logged, `availableQty()` = on_hand − reserved − frozen, `scopeHasAvailable()`), `Models/Stocktake.php` (`KIND_FULL` / `KIND_DISCREPANCY`, `isDiscrepancy()`), `Services/StockService.php` (`onHand` subtracts frozen inside the unchanged three keys; `reserve` uses `hasAvailable()`), `Services/OutboundService.php` (`confirmPick`: every reason still consumes the reservation and writes the `release` movement; `not_found` also increments `qty_frozen` and calls `StocktakeService::addDiscrepancyLine`; the pick_short exception then has `source_type = stocktake` / `source_id` = the stocktake — the exception centre's source link leads there — and `pick_short_frozen_suffix` in the message; constructor gained `StocktakeService`), `Services/StocktakeService.php` (`addDiscrepancyLine(StockUnit, ?int $userId): Stocktake` — the warehouse's open `discrepancy` stocktake, locked, created on first use, one line per unit with expected = on hand, an existing line reset to uncounted; `count()` releases the frozen cartons when counted ≥ expected; `close()` adjusts as before and clears any remaining frozen), `Http/Controllers/OutboundController.php` (flash `pick_short_frozen_recorded` names the stocktake), `Http/Controllers/StockController.php` (只看可用 via `hasAvailable()`), views `outbound/wave` (confirm() variant for 找不到 via `data-confirm-frozen`), `stock/show` + `stock/index` (冻结 badge / column), `stocktakes/show` + `stocktakes/index` (kind badge, hint, 冻结 N per line, kind column). 缺货 / 其他 unchanged. **破损 unchanged:** the only quarantine path (`QuarantineService`) flips a whole unit's condition and refuses reserved units — a partial-carton quarantine would need a unit split the plan does not describe; not invented.
- **(2) Unit correction.** No guard (lead). `lang/zh/warehouse.php` `stock.correct.hint` now states: takes effect from the next daily snapshot = the next weekly storage run; billed weeks are never re-run; a billed week is corrected by manual charge / credit note.
- **(3) Unpriced items block 开出发票.** `Billing/Services/InvoiceService.php` (`issue(Invoice, bool $unpricedOverride = false, ?int $by = null)`: refuses `billing.errors.unpriced_block` while `unpricedItems()` is non-empty unless the override; the override appends `billing.invoices.unpriced_override_note` (who / when / charge + exception ids) to `invoices.notes` and logs `invoice` / `issued_with_unpriced_override` with the ids; an empty draft is refused `billing.errors.empty_draft`; `unpricedItems(Invoice)` moved here from the controller, public), `Http/Controllers/InvoiceController.php` (`show` reads the service + the override log for the badge; `issue(Request …)` validates `unpriced_override`), `views/invoices/show.blade.php` (checkbox 仍然开票(未定价费用留到下期) inside the issue form only when items exist, hint text now says it blocks, badge 开票时有未定价费用 + 备注 block once issued — the client PDF never prints notes). `Platform/Services/DemoStory.php` and `database/seeders/DemoFlowSeeder.php` issue through the override when the demo client's card lacks a rate and say so (`demo.steps.invoiced_override`).
- **(4) 移出草稿.** Route `DELETE /billing/invoices/{invoice}/lines/{line}` (`billing.invoices.lines.destroy`, admin | finance) → `InvoiceController::removeLine` (404 for a line of another invoice) → `InvoiceService::removeLine(Invoice, InvoiceLine): Charge` (`invoice_line_id` null — the charge's status was never changed by drafting, so it is back in the pool as it was; line deleted; Job detached when it has no line left; totals recomputed; refused on a non-draft `billing.errors.only_drafts_remove_line`). Draft page: 操作 column with 移出草稿 + confirm(); an emptied draft shows `empty_draft_hint` and a disabled 开出发票.
- **(5) Approvals visibility.** `Platform/routes.php`: `approvals.index` + `approvals.cancel` moved under `role:admin|finance` (approve / reject already were); `resources/views/layouts/nav/platform.blade.php` (Platform's own include): the 审批 entry inside `@role('admin|finance')`. Requester status on the item pages was already there (rate card: 审批中 / 已批准 / locked hint → centre; invoice: credit note 审批中 → 审批中心 / 已批准) — verified by test, nothing added.
- **Tests.** New: `tests/Feature/Warehouse/PickShortNotFoundTest.php` (3), `tests/Feature/Billing/LeadDecisions142BillingTest.php` (3), `tests/Feature/Platform/ApprovalsVisibilityTest.php` (2). Re-pinned: `tests/Feature/Warehouse/PickShortReasonTest.php` (找不到 → 5 available, flash names the stocktake). No X1 / X2 test touched.
- **Docs.** `contracts/CHANGE_REQUESTS.md` #142, `db-schema.md` §4 (`stock_units.qty_frozen`; `stocktakes` / `stocktake_lines` rows rewritten to the built columns + `kind`), `enums.md` (`stocktakes.kind`; `stocktakes.status` aligned to the built `counting | closed`), `services.md` §1 (qty_available) + §9 (InvoiceService issue / unpricedItems / removeLine), `routes.md` (Warehouse, Billing, Platform rows), `docs/demo-guide.zh.md` §8 FAQ, `MERGELOG.md`.
- **Trial server after merge:** deploy runs `migrate --force` (one additive migration). No seeder, no config, no one-off command. `php artisan view:clear` if compiled views are cached.

### 2026-09-22 · feat/import-consolidation-format · 拼箱清单格式直接导入 (CR #143) · C edited X1 Orders / Portal zones as integrator

- **Why.** The lead's real order is a COSCO consolidation packing list (English header row under a Chinese group-header row, one row per carton, 286 rows / 174 recipients); it must upload as is so the client can test the whole chain by hand.
- **Orders (X1), edited:** `Services/SpreadsheetManifestParser.php` — the alias table above (services.md §8), first-column-wins duplicate rule, `carton_qty = 1` per row plus a row-0 warning when the sheet has no 箱数 column, CJK-aware Commodity mapping. `Services/OrderImportService.php` — `GROUP_BY` / `ADDRESS_TYPE_DEFAULTS` constants, `group_by = recipient` groups by recipient + postcode + address (mark = the first waybill root, waybill kept in each line description), `address_type_default` applied to rows without a 地址类型 column; both recorded in `errors.context`. `Http/Controllers/OrderImportController.php` + `views/imports/{create,show}.blade.php` — the same two options and the rule / order count on the preview. `lang/zh/orders.php` appended keys.
- **Portal (X1), edited:** `Http/Controllers/PortalInboundImportController.php` (options validated and passed for the upload and the manual list; preview shows them), `views/asns/imports/{create,manual,show}.blade.php`, new partial `views/asns/imports/partials/import-options.blade.php`, `lang/zh/portal.php` (`inbound.options.*`, `consolidation_hint`).
- **Tests.** `tests/Feature/Portal/PortalConsolidationImportTest.php` + synthetic fixture `tests/Fixtures/imports/consolidation_dsf.xlsx` (fictional data). The real sample stays outside the repo (`~/Desktop/ERP/samples/`, personal data).
- **Keep on rebase:** the option partial is included by both portal forms; the staff import form carries the same two selects; nothing else in the import pipeline changed shape.

### 2026-09-23 · feat/import-order-type · 批量导入 from 新建订单 for both order types (CR #144) · C edited X1 Orders / Portal zones as integrator

- **Why.** The lead wants every client able to import a CSV / Excel list (the real consolidation format of #143) for BOTH order types from 新建订单 — 库存出库配送 and 现场提货直送 — not only the inbound list under 预报入库.
- **Orders (X1), edited:** `Services/OrderImportService.php` — `ORDER_TYPES`, `order_type` in `withOptions()`, no tier pre-fill for a 提货直送 list, `confirm()` creates `pickup_deliver` orders with `pickup_address` from `context.pickup` and a `transport_only` Job (private `pickupAddress()`); `Models/OrderImport.php` — `orderType()`.
- **Portal (X1), edited:** `Http/Controllers/PortalInboundImportController.php` (`order_type` from the query / form, `pickupRules()`, `submissionContext()` per type, preview data `orderType` / `pickup`, confirm message per type), `views/asns/imports/create.blade.php` (order-type radios; the inbound blocks and the new pickup block sit in fieldsets that are hidden AND disabled by the toggle, so only the chosen block submits), new partial `views/asns/imports/partials/pickup-fields.blade.php`, `views/asns/imports/{show,index}.blade.php` (type-aware title / breadcrumb / context card / tier column / result text), `views/orders/create.blade.php` (批量导入 card). `lang/zh/portal.php` (`inbound.title` now 批量导入订单, `inbound.order_type*`, `inbound.pickup.*`, `inbound.show_title_pickup`, `after_confirm_pickup`, `messages.confirmed_pickup`, `create.import_*`, validation attributes / messages for `pickup.*`).
- **Tests.** `tests/Feature/Portal/PortalOrderTypeImportTest.php` (5 tests) on the synthetic #143 fixture; the real sample stays outside the repo.
- **Keep on rebase:** one upload form for both types — keep `order_type` first and the two fieldsets toggled hidden+disabled; 手工建立入库清单 and the staff import never post `order_type` and stay from_stock.
### 2026-09-23 · feat/auto-import · 自动导入 (CR #145) · C edited X1 Orders / Portal zones as integrator

- **Why.** The lead wants lists imported without a person on the page — the client's system pushing them and a per-client inbox folder — in the real consolidation format, with automatic confirmation only when a list is clean.
- **Orders (X1), added:** `Services/AutoImportService.php` (the one entry for both channels: options → client defaults → OrderImportService, replay by sha256, the auto-confirm rule, summary / result text, inbox file handling), `Http/Controllers/OrderImportApiController.php` (`POST /orders/api/imports`, `GET /orders/api/imports/{id}`), `Console/ImportInboxCommand.php` (`imports:inbox`), `Mail/ImportResultMail.php`. **Edited:** `routes.php` (two API routes), `OrdersServiceProvider.php` (command registration), `Services/OrderImportService.php` (constants reference `Enums`, `orders.source` mapping, `takenOrderIds` over `AutoImportService::CLIENT_SOURCES`, and the `confirm()` by-reference fix — group statuses / order ids now persist), `Services/OrderInboundService.php` + `Http/Controllers/OrderInboundController.php` (client-source lists), `Http/Controllers/ApiTokenController.php` + `views/api-tokens/index.blade.php` (the import endpoint documented), `views/imports/{show,index}.blade.php` (api / inbox treated as the client's submission).
- **Portal (X1), edited:** `Http/Controllers/PortalInboundImportController.php` (`index()` / `own()` over the client sources), `views/asns/imports/{index,show}.blade.php` (source badge). `lang/zh/orders.php` (`sources.inbox`, `api.import_*`, `api.errors.*`, `imports.auto.*`), `lang/zh/portal.php` (`inbound.sources`).
- **C's own:** `app/Support/Enums.php` (`IMPORT_GROUP_BYS`, `IMPORT_ADDRESS_TYPE_DEFAULTS`, `ORDER_IMPORT_SOURCES`), MasterData (`clients.import_defaults` migration, `Client::importDefaults()` / `importDefaultsFrom()`, the client form fieldset, `ClientController` rules), `routes/console.php` (`imports:inbox` every five minutes), `lang/zh/masterdata.php`.
- **Tests.** `tests/Feature/Orders/OrderImportApiTest.php`, `tests/Feature/Orders/ImportInboxCommandTest.php`, `tests/Feature/MasterData/ClientImportDefaultsTest.php` — all on the synthetic #143 fixture.
- **Keep on rebase:** every "the client's own submission" query must use `AutoImportService::CLIENT_SOURCES`, never `source = 'portal'` alone; the auto-confirm rule lives in `AutoImportService::canAutoConfirm()` only.
### 2026-09-24 · feat/template-consolidation · 模板一比一改成拼箱清单格式 (CR #146) · C edited X1 Portal zone as integrator

- **Why.** The lead wants the client's downloadable template to be exactly the real consolidation list — every column, unread ones included — so a client can fill it or upload its own sheet unchanged.
- **Portal (X1), edited:** `Http/Controllers/PortalInboundImportController.php` (`TEMPLATE_GROUP_ROW` / `TEMPLATE_HEADER_ROW` / `TEMPLATE_SAMPLE_ROWS`, `template()` writes them as CSV, new `templateXlsx()` streams `data/inbound-list-template.xlsx`, `create()` passes `templateColumns`), `routes.php` (`asns.imports.template_xlsx`), `views/asns/imports/create.blade.php` (two template links, 表头说明 disclosure). `lang/zh/portal.php` (`inbound.template_xlsx`, `template_hint`, `template_columns_title`, `template_columns`).
- **C's own:** `data/inbound-list-template.xlsx` (built from the real workbook: data rows deleted, properties scrubbed, sample rows added — never regenerate from the real file without re-checking for identity; the build script lives outside the repo).
- **Tests.** `PortalInboundImportTest` (CSV round trip rewritten, new xlsx test), `DeclaredStorageTierTest` (no 存储等级 column in the template any more).
- **Keep on rebase:** the header row must stay byte-identical to the client's sheet (empty E header, full-width bracket in V); the Chinese aliases stay so older filled templates still upload.
### 2026-09-24 · feat/inbound-select-all · 待建预报 全选 (CR #147) · C edited X1 Orders zone as integrator

- **Why.** Testing with the 175-order list showed the page had no way to tick a whole client group at once.
- **Orders (X1), edited:** `views/inbound/index.blade.php` — a `data-select-all` checkbox in each client group's 选择 header; the script's `limit()` now ends with `syncSelectAll()` so the header mirrors the rows (checked / indeterminate / disabled) after every tick, the 选中并填入 button and page load; a header tick sets its group's rows, then runs `limit()` + `checkImport()` like a row tick. `lang/zh/orders.php` — `inbound.fields.select_all`.
- **Tests.** `PortalInboundImportTest` (the control is on the page with the group's count).
- **Keep on rebase:** keep `syncSelectAll()` inside `limit()`; the one-client-per-ASN rule and the import-link check are unchanged.
### 2026-09-24 · feat/orders-bulk-confirm · 一键确认订单 (CR #153) · C edited X1 Orders zone as integrator

- **Why.** Testing with 175 orders: confirming them one page at a time was not workable.
- **Orders (X1), edited:** `routes.php` (`POST /confirm-bulk`, declared before the `{order}` routes), `Http/Controllers/OrderController.php` (`confirmBulk()`; the checks of `confirm()` moved into a private `confirmRefusal()` both use — behaviour of the single button unchanged), `views/index.blade.php` (checkbox column for the order-entry roles, the 一键确认 form, inline script), `lang/zh/orders.php` (`bulk_confirm.*`).
- **Tests.** `tests/Feature/Orders/OrderBulkConfirmTest.php`.
- **Keep on rebase:** keep `confirmRefusal()` as the single place for "can this order be confirmed"; the bulk route must stay above `POST /{order}/confirm`.
### 2026-09-25 · feat/container-seq-putaway-select · 自动柜号 (CR #158) · C edited X1 Orders / Portal zones as integrator

- **Why.** The lead does not want clients typing a 柜号; the system numbers each inbound list in sequence, customer service may replace it with the shipping line's number when it builds the ASN.
- **Orders (X1), edited:** `migrations/2026_09_25_100000_add_container_no_to_order_imports.php` (nullable indexed string 20), `Models/OrderImport.php` (fillable), `Services/OrderImportService.php` (`withContainerNumber()` called by `preview()` and `previewRows()` — DocumentNumbers over `order_imports.container_no`, prefix CTN; a given number is kept).
- **Portal (X1), edited:** `views/asns/imports/partials/context-fields.blade.php` (disabled 系统自动生成 box + hint instead of the input), `lang/zh/portal.php` (`inbound.context_hint`, `container_auto`, `container_auto_hint`).
- **Tests.** `tests/Feature/Portal/PortalContainerNumberTest.php`; existing tests that post `container_no` still pass (the param is accepted, e.g. from the API).
- **Keep on rebase:** the number is taken inside `withContainerNumber()` only — never generate it in a controller; keep `container_no` accepted by the API.
### 2026-09-25 · feat/container-no-button · 自动柜号 revised (CR #158) · C edited X1 Orders / Portal zones as integrator

- **Why.** The lead wants the client to SEE a randomly generated 柜号 on the form and be free to change it — not a silent internal number.
- **Orders (X1), edited:** `Services/OrderImportService.php` — `nextContainerNumber()` is now a random `CTN-XXXXXX` (unique against imports and containers, no sequence / lock), `GENERATED_CONTAINER_PATTERN`, `withContainerNumber()` keeps what was submitted and replaces only a blank or a taken generated number.
- **Portal (X1), edited:** `Http/Controllers/PortalInboundImportController.php` (`create()` / `manualDefaults()` prefill, `nextContainerNo()` JSON), `routes.php` (`asns.imports.container_no`), `views/asns/imports/partials/context-fields.blade.php` (editable box + 重新生成 button + fetch script), `lang/zh/portal.php`.
- **Tests.** `PortalContainerNumberTest` rewritten; `PortalManualInboundTest` / `PortalInboundImportTest` / `PortalOrderTypeImportTest` back to asserting the input (with its `id`).
- **Keep on rebase:** the box must stay editable and prefilled; the generated pattern lives in `OrderImportService::GENERATED_CONTAINER_PATTERN` only.
