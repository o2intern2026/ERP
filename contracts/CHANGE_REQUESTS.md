# Contract change requests

Append only. Seat C processes this list at each checkpoint (see COLLAB_PLAN.md §5.3).

| # | Date | Seat | Task | File | Request | Decision (C) |
|---|---|---|---|---|---|---|
| 1 | 2026-09-07 | C | A0 | composer.json, AGENTS.md §Stack, COLLAB_PLAN §0, ERP_PLAN §8.1 | Laravel 11 left security support 2026-03-12; every 11.x release is blocked by Composer security advisories | **Laravel 12** (`^12.0`), PHP 8.2 platform lock kept (approved by project lead 2026-09-07). Plan wording updated to Laravel 12 in COLLAB_PLAN, ERP_PLAN, CODEX_PLAN, AGENTS, README (2026-09-07) |
| 2 | 2026-09-07 | C | A0 | config/queue.php, db-schema.md §0 | Laravel's queue table is named `jobs`, colliding with the business Job table (§1.6, A27) | Queue table renamed `queue_jobs` (`DB_QUEUE_TABLE`); the business table keeps `jobs` |
| 3 | 2026-09-07 | C | A0 / A24 | enums.md §6 | §6.3 `default_uom` list lacks `label` and `pickface_week`, but §6.2 rule 2 / §6.4 bill per label and per pickface·week (34-row import, 2026-09-07) | Added `label`, `pickface_week` to `charge_codes.default_uom` |
| 4 | 2026-09-07 | C | A0 / A24 | enums.md §6 | `charge_rules.quantity_source` has no value for the per-order despatch fee | Added `orders` |
| 5 | 2026-09-07 | C | A24 | charge-codes.md #20 / #21 | Edward card: "Order process – urgent despatch $15, apply to urgent order beyond cut-off" — unclear whether it replaces or adds to the $5 standard fee | ~~Contract: **replaces** (one order-processing charge per order). Default accepted 2026-09-07~~ → **Project lead decided 2026-09-08: adds.** An urgent order is charged the $5 standard fee plus the $15 urgent fee. Rule / docs / test changed in #48 |
| 6 | 2026-09-07 | C | A24 | charge-codes.md #32 | "Non standard pallet purchase" has no data field to detect it (`pallet_source` has four values, none = non-standard) | Manual charge with reason (`charges.is_manual`) until a field is agreed |
| 7 | 2026-09-07 | C | A24 / B5 | charge-codes.md §1 | The two Cartage rows are "TMS freight items", but no trigger exists for port → warehouse container cartage (it happens before the ASN) | Priced as `own_fleet` / `fixed` rate items, charged at `shipment.quote_confirmed` of a cartage shipment. Default accepted by project lead 2026-09-07; X2 defines the cartage shipment flow in B5 |
| 8 | 2026-09-07 | C | A0 | enums.md (※ items) | Plan describes but does not name: role slugs, `tailgate_reason`, `exceptions.type/status/source_module`, `documents.type`, `delivery_runs`/`run_stops` status, outbox status, `credit_notes.status`, `clients.leg_type`, import / wave / stocktake statuses | Named by C, marked ※; objections via this file before M1 |
| 9 | 2026-09-07 | C | A0 / A17 | db-schema.md §3 | `client_addresses`: §8.2 groups it with the MasterData tables (C) but §3.3 lists it in the OMS model and A17 (address book) is X1's task | Owner **X1 / Orders** (module-section reading, kickoff rule); MasterData reads through the model |
| 10 | 2026-09-07 | C | A0 | db-schema.md §3, §6 | COLLAB §1.2 lists `holds`, `return_requests` and "customer_quotes 的订单侧" as X1 tables; §3.3 says holds reuse `exceptions` (type = hold), returns are `orders.order_type = return`, §6.3 puts `customer_quotes` in Billing | No `holds` / `return_requests` tables. `customer_quotes*` owned by C / Billing; X1's A7b creates quotes through Billing's quote service + RateService |
| 11 | 2026-09-07 | C | A0 / A11 / A8b | events.md | `return.financial_decision` publisher is ambiguous (§3.4: "Billing: credit note or none"; §6.4 lists it as a Billing trigger) | Publisher **Orders (X1)** records Finance's decision on the return order; Billing consumes it and issues the credit note. Accepted by project lead 2026-09-07 |
| 12 | 2026-09-07 | C | A0 | ERP_PLAN §8.3 layout | PSR-4 needs `Seeders/` (capital S) for autoloaded seeders; §8.3 writes `seeders/` | `Seeders/`; `views/` and `migrations/` stay lowercase (path-loaded) |
| 13 | 2026-09-07 | C | A0 | .github/workflows/ci.yml | Plan says "MySQL 8": CI image is `mysql:8.0`, seat C's machine runs 8.4 LTS (brew `mysql@8.0` is deprecated) | Both are MySQL 8; no dialect difference expected for this schema. Info only |
| 14 | 2026-09-07 | C | A0 | repository | Repo is public; COLLAB §8.1 says private; `data/` holds the Edward rate card and the seeder will carry real rates | Project lead decided 2026-09-07: keep public, commit per plan |
| 15 | 2026-09-07 | C | A0 | public/css/app.css | Kickoff names `resources/css/app.css` "served statically"; without a build step only `public/` is web-served | File lives at `public/css/app.css` (≤ 100 lines); `resources/css` removed with the Vite scaffolding |
| 16 | 2026-09-07 | C | A0 | data/ | §8.3 says the repo copy of 需派送货物清单.xlsx is a **de-identified** sample; the source file on hand is the raw client list | Rate card committed as `data/Storage_rate_27022026.xlsx`; the manifest is **not** committed until a de-identified copy is produced (see M0 report) |
| 17 | 2026-09-07 | C | A31 | events.md, app/Support/Outbox | Seats need a code-level way to consume events | Added `EventConsumer` interface + `ConsumerRegistry` (register in your provider's boot); dispatcher = `outbox:dispatch` every minute via scheduler instead of a queue worker (same cron-only constraint, simpler) |
| 18 | 2026-09-07 | C | A1 / OMS-11 | services.md §6 | Orders / Transport need to check "active financial hold" without reading the shared `exceptions` table | Added `ExceptionService::hasActiveHold(holdType, clientId?, orderId?)` (additive) |
| 19 | 2026-09-07 | C | A1 | tests/Feature/{Orders,Portal,Reports,Transport}/*PlaceholderTest.php | M1 puts every module behind `auth` (routes.md), which broke the M0 placeholder tests owned by X1 / X2 | C updated those four files to authenticate via `Tests\Support\CreatesUsers` (staff / clientUser helpers). One-off; from now on X1 / X2 own them |
| 20 | 2026-09-07 | C | A1 | PlatformSeeder | Demo needs one user per role (§7 step 1) | Seeded `<role>@erp.local` users, password from `SEED_DEMO_PASSWORD` (default `password`) — local development only; production seeds must set a real value |
| 21 | 2026-09-07 | C | A27 | services.md §5 | Cost / margin must never reach client users | `JobService::summarize` omits the four cost/margin keys when the global client scope is active |
| 22 | 2026-09-07 | C | B5e | carriers.md, services.md §3, app/Support/Contracts/CarrierAdapter.php | B5e Go / No-Go from public docs | **Transdirect GO (conditional)**: quote, book, cancel, label, tracking by polling an HTML table; no POD / webhooks via API. **EIZ NO-GO** for phase 1: no public or partner API. `CarrierAdapter` interface fixed for X2's B5c. Open action items: Transdirect account + API key, payment arrangement, unit check on first demo call |
| 23 | 2026-09-07 | C | B1 / B2 | db-schema.md §4 | "上架后才可用" (§4.3 rule 2) needs a flag on the unit; the plan's model has none | Added `stock_units.putaway_completed` (bool), `asns.unplanned_confirmed`, `asns.putaway_completed_at`, `asns.notes`, `warehouse_tasks.notes` — additive owner columns |
| 24 | 2026-09-07 | C | B1 | services.md §1 | Real `StockService` merged in M2 | `FakeStockService` retired from `FakeServicesProvider` (class kept for unit tests). The stock events are emitted by the Warehouse consumers of the order events, which hold the correlation context |
| 25 | 2026-09-07 | C | B2b | AsnImportService | The shared `ManifestParser` is X1's A4 (M3) | Until M3 the ASN import runs on `FakeManifestParser` (two fixed rows); §4.7 #17 is verified after M3 |
| 26 | 2026-09-07 | C | B12 | TaskService, /warehouse/tasks | Devanning / unload / labour billing needs `task.completed` from the first WMS task (COLLAB §4: billing events from day one) | Task core (create / complete / `task.completed`) delivered in M2, ahead of B12's M4 slot; pick / pack / scan_records tasks follow in M4 |
| 27 | 2026-09-07 | C | B10a / B10b / B11 | db-schema.md §4 | Quarantine needs a reason on the unit; snapshots need pickface occupancy and reserved qty; serial scans need the scanner | Additive owner columns: `stock_units.condition_reason`, `condition_changed_at`; `stock_snapshots.job_id`, `asn_line_id`, `location_type`, `qty_reserved`; `stocktake_lines.scanned`, `counted_by`; `scan_records.scanned_by`. Phone-camera scanning uses the browser's native BarcodeDetector (no CDN library) |
| 28 | 2026-09-07 | C | A19 / A20 / A23 | db-schema.md §1, enums.md §1 | Approval centre, audit log and webhooks need tables the plan only named | Added `approvals`, `webhook_endpoints`, `webhook_deliveries`, `activity_log` (spatie) with enums `approvals.type/status`, `webhook_deliveries.status` |
| 29 | 2026-09-07 | C | A23 | app/Support/Outbox/ConsumerRegistry | Webhook pusher must see every event without listing them | `ConsumerRegistry::register('*', …)` wildcard (additive) |
| 30 | 2026-09-07 | C | A28 / A29 / A30 | services.md §8 | Shared UI needs shared services | `ExceptionService::assign/start` (additive), `SearchRegistry` (modules register searchers), `DocumentDownloader` (single visibility rule for portal downloads) |
| 31 | 2026-09-07 | C | A24 / A5 | enums.md §6, charge-codes.md | Seeding the real card needed a few values the plan left implicit | Added `pickface_week` UOM, `pallets_warehouse_plain` / `one` quantity sources, `manual` trigger, `percent` pricing mode (fuel surcharge), `void` invoice status (reserved), `cancelled` credit-note status |
| 32 | 2026-09-07 | C | A6a | services.md §9 | Cancel / redo must "produce a reversal, never delete" but uninvoiced reversals were double-counting | Uninvoiced original + its twin are both `reversed` (audit only); an invoiced original keeps `invoiced` and its negative twin is billable — net effect: charged once (§6.8 #9) |
| 33 | 2026-09-07 | C | A6b | StorageBillingService | Weekly storage needs the daily snapshots (B10a, block/c3-wms-outbound) | `block/c4-billing` is built on top of `block/c3-wms-outbound`; the billing PR is opened as a draft and merges at M6 after M4 as planned |
| 34 | 2026-09-07 | C | A6a / B5d | events.md shipment.quote_confirmed | Drift check: Billing's freight rules read `charge_code` / `client_price_cents`, which are not in the contract payload (X2 followed the contract correctly) | Billing fixed to `cost_cents` / `customer_price_cents`; additive nullable key `cartage_container_size` ("20" \| "40") distinguishes container cartage from a normal delivery. X2: set it when the cartage shipment flow lands (CHANGE_REQUESTS #7); absent = normal delivery |
| 35 | 2026-09-07 | C | B4 | events.md, db-schema.md §4 | §4.3 rule 5 wants `packed` and `dispatched` recorded separately, but events.md had no dispatch event and no table for the handover | Added `outbound.dispatched` (additive; Orders → `dispatched`, Transport informational) and `outbound_dispatches` (one row per fulfilment). Load-out billing stays on `task.completed` (`load`, pallets), so Billing needs no new consumer. Numbers #31–#34 are used by `block/c4-billing` (merges at M6) |
| 36 | 2026-09-07 | C | B4 / B13 | db-schema.md §4, enums.md | Outbound and returns need columns the plan only implied | Additive owner columns: `warehouse_tasks.wave_id`, `warehouse_task_lines.order_line_id`, `waves.notes`, `return_receipts.receipt_no/client_id/notes`, `return_receipt_lines.description/received_at/inspected_at`. `return_receipts.status` follows enums.md (`expected → received → inspected`; `closed` reserved for the financial decision). Named ※: `outbound_dispatches.handed_to`, `packages.package_type` |
| 37 | 2026-09-07 | C | B2c / A4 | services.md, FakeServicesProvider, OrdersServiceProvider | M3 merged the real `OrderService` and `ManifestParser`; the Fakes were still bound under `USE_FAKE_SERVICES` | `FakeOrderService` / `FakeManifestParser` retired from the provider (classes kept for unit tests); `OrdersServiceProvider` binds the real services unconditionally (C edited X1's provider as integrator — one line). The ASN import page test now runs the real workbook (135 rows, 10 reported errors) |
| 38 | 2026-09-08 | X1 (C's agent) | A11 | db-schema.md §3 | Return chain needs the return order linked to its original and Finance's decision stored once (`return.financial_decision` is the only credit trigger) | Additive owner columns (migration `2026_09_08_700000`): `orders.original_order_id`, `orders.return_inspected_at`, `orders.return_decision` (credit \| no_credit), `orders.return_decided_by`, `orders.return_decided_at`, `orders.return_decision_note`; `order_lines.original_order_line_id`. Please list them in db-schema.md §3 |
| 39 | 2026-09-08 | X1 (C's agent) | A4b | routes.md, db-schema.md §3 | routes.md reserves `/api/orders` (token auth) but every route must sit under a module prefix (RoutesContractTest); the API needs a token table and an idempotency ledger | Implemented as `POST /orders/api/orders` (`orders.api.orders.store`, `withoutMiddleware(auth, client.scope, CSRF)`). New X1 tables (migration `2026_09_08_700010`): `order_api_tokens` (client_id, name, token_hash, last_used_at, revoked_at, created_by), `order_api_idempotency_keys` (client_id, idempotency_key, order_id; unique per client). Please replace the reserved `/api/orders` row in routes.md with `/orders/api/orders` and add the two tables to db-schema.md §3 |
| 40 | 2026-09-08 | X1 (C's agent) | A13 | Warehouse OutboundService::dispatch, Transport booking | OMS-11: an active financial hold must refuse booking / dispatch at the action. Orders now refuses `transitionOperational('dispatched')` (OrderRuleViolation) and exposes `Order::hasActiveFinancialHold()`; but Warehouse's dispatch page still hands goods over, so the `outbound.dispatched` consumer marks the batch dispatched, leaves the order at `packed` with a timeline note, and the release re-syncs it | Request: `OutboundService::dispatch()` (C) and the Transport booking step (X2) call `ExceptionService::hasActiveHold('financial', client_id, order_id)` and refuse with a message before the handover / booking |
| 41 | 2026-09-08 | X1 (C's agent) | A9-p | bootstrap/app.php (middleware priority), ClientScope | Implicit route-model binding (`SubstituteBindings`) runs before the route-level `client.scope` middleware sets the tenant, so `Order $order` in a `/portal/**` action resolved another client's order (200 instead of 404) | Portal actions resolve models inside the action (`Order::query()->findOrFail()` after the scope is set). Request: prepend `ClientScope` to the middleware priority (before `SubstituteBindings`) so every module's implicit bindings are tenant-filtered |
| 42 | 2026-09-08 | X1 (C's agent) | A11 / A12 | enums.md §3, events.md | Decisions taken without a contract change: (a) a return order runs on the original order's Job (job_type `return` unused) and is `confirmed` at request time without emitting `order.confirmed` (Warehouse must not reserve stock for it); (b) e-mail uploads (.eml / .txt) use `orders.source = pdf`; (c) `order.reduced` only lists lines with `asn_line_id`, and Orders shrinks its own fulfilment lines because Warehouse's consumer re-reserves without re-emitting `stock.reserved`; (d) after picking starts, cancel / quantity change is `admin \| warehouse_supervisor` only (ERP_PLAN §3.4 says "Coordinator 或主管") | Please confirm or adjust at M6 |
| 45 | 2026-09-08 | C | M5 integration | TransportServiceProvider, FakeServicesProvider, config/services.php | M5 merged the real `TransportOptionService`; the Fake was still bound under `USE_FAKE_SERVICES`, and Transdirect settings were read with `env()` inside the provider (empty once `config:cache` runs) | `FakeTransportOptionService` retired (class kept for unit tests); `TransportServiceProvider` binds the real service unconditionally and no longer calls `env()` — `services.transdirect` (api_key, base_url) lives in `config/services.php`. Integrator edits in X2's provider, no behaviour change for Manual / own-fleet paths. #38–#44 are reserved for `block/x1-portal-reports` |
| 46 | 2026-09-08 | C | A27 / B4 / A13 | Platform consumers, OutboundService | events.md says Platform consumes `shipment.booked` (Job cost `estimated`) and X2's HANDOFF asked for Job cost to come from `carrier_costs`; §3.8 #7 says a financial hold blocks booking and dispatch | Added `JobCostConsumer` (`shipment.booked` → estimated cost from `carrier_costs`, `delivery.pod_captured` → actual cost, `confirmed` when every outbound shipment of the Job is delivered). Warehouse `OutboundService::dispatch` now refuses while `ExceptionService::hasActiveHold('financial', client, order)` is true. **X2 follow-up:** `ShipmentBookingService` should apply the same check before booking (Transport zone, not edited by C) |
| 47 | 2026-09-08 | C | M6 integration | FakeServicesProvider, tests/Feature/Transport/{B5c,M4Integration}, tests/Feature/Orders/QueueAndBatchTest | With Billing merged the last Fake (`FakeRateService`) leaves the provider; two X2 tests asserted the Fake was bound and used its `withRate()` test helper, and X1's batch page test expected "revenue pending" because the `charges` table did not exist | `FakeServicesProvider` binds nothing (flag kept, all five Fakes retired; classes remain as contract documentation). Integrator edits: the two Transport tests pin `new FakeRateService` via `app()->instance()` so their freight prices stay deterministic; the Orders batch test now expects a revenue figure (0.00 AUD until charges land). Real own-fleet pricing goes through Billing's `RateService` (`TR-DELIVERY-BASE`; a missing rate raises `missing_rate`, never zero) |
| 48 | 2026-09-08 | C | A24 / A6a | charge-codes.md #20 #21, BillingSeeder RULES, ChargeEngineTest | Implements the #5 decision (adds) | `WH-ORDER-DESPATCH` rule condition removed (fires for every `outbound.packed`); `WH-ORDER-DESPATCH-URGENT` unchanged (`is_urgent=true`). Seeder `updateOrCreate` corrects existing databases on the next seed. Rate card figures re-checked against `Desktop/ERP/Edward Storage rate 27022026.xlsx` (byte-identical to `data/Storage_rate_27022026.xlsx`): all 34 rates match the seeded Edward card |
| 49 | 2026-09-08 | C (covering X2) | B5c / B5e | enums.md §5, carriers.md, Transport adapters, config/services.php | Project lead 2026-09-08: no Transdirect account for now — use an open-source API instead | Added `carrier_services.source = karrio` and `App\Modules\Transport\Adapters\KarrioAdapter` (Karrio, open-source self-hosted multi-carrier API; `docker/karrio`). Registered in `TransportServiceProvider`, seeded as carrier `KARRIO` with standard / express / same_day services, label `Karrio 开源网关`. Idle without `KARRIO_API_KEY`, so Manual / own-fleet flows are unchanged. Transdirect adapter kept dormant. HANDOFF: C edits X2's Transport zone for this task only |
| 50 | 2026-09-08 | X1 (C's agent) | A7b | services.md §9 `QuoteService`, db-schema.md §6 `customer_quote_lines` | `QuoteService::create` prices every line through `RateService`, so Transport's preliminary freight — already a customer price in `transport_quotes.customer_price_cents` — cannot be stored as a quote line: `TR-DELIVERY-BASE` has no standard-card rate (missing rate) and a `cost_plus` item would need `cost_cents` in the line context, which lands in `customer_quote_lines.assumptions` (a client-visible row). Workaround: the customer quote holds the warehouse service lines; the estimate shows the freight live from `transport_quotes` (customer price only) and the chosen `transport_quote_id` + price are written into `customer_quotes.notes` | Request: accept a pre-priced line `{charge_code, qty, amount_cents, transport_quote_id}` that skips `RateService` and records `assumptions.priced_by = transport`, so A7b can persist the freight line properly |
| 51 | 2026-09-08 | X1 (C's agent) | A7b | ERP_PLAN §3.7 A7b, §3.9 Q3 | Estimate rules decided without a contract change (`OrderEstimateService`): (a) urgent = `service_level = same_day` entered after `clients.dispatch_cutoff_time` (no cut-off → every same-day order), mirroring Warehouse's `outbound.packed.is_urgent`, and `WH-ORDER-DESPATCH-URGENT` adds to `WH-ORDER-DESPATCH` (#5); (b) pick unit: stock units on the linked ASN line decide (pallet units → `WH-PICK-PLT`, pallets = ceil(cartons ÷ cartons per pallet unit)); without stock a pallet-like `package_type` (pallet / plt / 托) counts its quantity as pallets, everything else as cartons; (c) per-carton weight = line `actual_weight_kg` ÷ cartons (line total, as TailgateRule), else ASN line weight ÷ cartons, else unknown → lightest band and `weight_assumed`; the band is found by probing the three carton codes through `RateService` with `weight_kg` (rate-item bands), contract edges only as fallback; (d) `WH-LABEL-OUT` = pieces (cartons + pallets), `WH-LOAD-PLT` = pallet picks only (a carton-only order shows no load-out line); (e) `pickup_deliver` orders have no warehouse lines; (f) freight = Transport quote of stage `preliminary` (`final` for pickup_deliver), selected > recommended > cheapest > lowest price, read-only; (g) re-estimate = new quote, previous set `expired` through `QuoteService::setStatus`; (h) staff roles admin / customer_service / dispatcher / finance, client users via `POST /portal/orders/{order}/estimate`; estimable while `received … packed`, never for return orders. ERP_PLAN A7b says "调 TransportOptionService": Orders reads Transport's quotes instead of calling `quote()` (there is no shipment before confirmation and `quote()` writes X2's tables) | Please confirm or adjust at M7 |
| 52 | 2026-09-08 | X1 (C's agent) | A21 | routes.md | `ClientScope` aborts any client-user request outside `/portal/**`, so the A21 client view cannot be served under `/reports/**` to client users | Client users: `GET /portal/reports` + `GET /portal/reports/export/{table}` (Portal module, renders `reports::client`, client id from the user). Staff: `GET /reports` (老板视角, admin \| finance), `/reports/export/{table}`, `/reports/client?client_id=` + `/reports/client/export/{table}` (admin \| finance \| customer_service \| dispatcher). Please add the portal rows to routes.md |
| 53 | 2026-09-08 | X1 (C's agent) | A21 | ERP_PLAN §2 A21, db-schema.md §3 | Report definitions decided without a contract change (`ReportService`): orders = `orders.created_at` in the period; on-time = orders whose `delivered` transition (`order_events`) falls in the period, on time when `date(created_at) ≤ requested_date`, plus the current overdue-open count; inbound = ASNs with `coalesce(arrived_at, created_at)` in the period, cartons from `asn_lines.received_cartons`, pallets = pallet `stock_units` of those ASNs; outbound = `packages` (packed) and `outbound_dispatches` (dispatched) by date; financials = Jobs opened in the period from the cached `jobs.*_cents`, margin per `JobService::summarize` (actual once `cost_status = confirmed`, else estimated); open exceptions as of now (not period-filtered), client view limited to `discrepancy` / `pick_short` / `delivery_failed`; the client view never selects cost / margin / estimated_jobs and always carries an explicit `client_id` predicate. Additive X1 indexes (migration `2026_09_08_700020`): `orders.created_at`, `orders(client_id, created_at)`, `order_events(dimension, to_status, created_at)` | Please confirm at M7 |
| 54 | 2026-09-08 | X1 (C's agent) | A22 | db-schema.md (Reports), AGENTS.md scheduling | Scheduled client reports need a send log so cron re-runs never mail a period twice | New X1 / Reports table `report_deliveries` (migration `2026_09_08_710000`): client_id, frequency (weekly \| monthly), period_start, period_end, email, status (sent \| skipped \| failed), error, sent_at, timestamps. `reports:client-weekly` Monday 07:00 and `reports:client-monthly` 1st 07:30 (Australia/Melbourne) registered in `ReportsServiceProvider` via `callAfterResolving(Schedule::class)`; recipient `clients.billing_email` else `contact_email`, no address → skipped (recorded once); a period already `sent` is never re-mailed unless `--force`; one CSV per table attached in memory — no `documents` row (the report is regenerable, not a business document). Please add the table to db-schema.md (new §7 Reports, or under §3) |
| 55 | 2026-09-08 | C | B2b / A6b | AsnImportService, InvoiceService | Found by the §7 demo run: (a) the ASN import dropped the suburb the shared parser extracts, so `OrderService::createFromAsn` blocked every mark as `incomplete_delivery`; (b) a Job service invoice swallowed the storage-category charges, leaving nothing for the weekly storage invoice | (a) `asn_lines.deliver_to_suburb` now filled from the parser; (b) `draftForJob` / `draftMonthly` exclude `category = storage` — storage, pallet rental and pickface fees bill only on the weekly storage invoice (§6.8 #4, #11). #50–#54 are reserved for `block/x1-quotes-reports` |
| 56 | 2026-09-08 | C | §7 | database/seeders/DemoFlowSeeder.php, tests/Feature/DemoFlowTest.php | The plan's end-to-end script needed data that exercises every module in one go | `DemoFlowSeeder` (run on demand, not part of `db:seed`) builds the §7 story through the modules' own services; `DemoFlowTest` runs it in CI and asserts the cross-module outcomes. Without `KARRIO_API_KEY` the third-party leg uses a manual quote |
| 57 | 2026-09-08 | C | B14 / B5c | db-schema.md §4 warehouses | Carrier gateways (Karrio, Transdirect) refuse to quote without a complete pickup address; `warehouses` had only address + state, so third-party quotes never appeared for real orders | Additive `warehouses.suburb`, `warehouses.postcode` (seeded MEL = Dandenong South 3175, shown on the warehouse form). X2's `ShipmentQuoteRequestFactory` already reads them |
| 58 | 2026-09-08 | C (covering X2) | B5c | ShipmentBookingService | `book()` built the consignment request only for `transdirect`, so the Karrio adapter received an empty request and every Karrio booking failed with `request_failed` | Request is built for every gateway source (`transdirect`, `karrio`); manual / own-fleet unchanged. One-line integrator edit in X2's service |
| 65 | 2026-09-08 | C | A24 / A6a / B12 | events.md, charge-codes.md #9 #26 #27 ※, BillingSeeder RULES, TaskService | Acceptance audit (docs/acceptance/2026-09-08): `delivery.extra_charge` never billed (rule read `hours_business`, condition `failed_delivery`, key `{delivery_id}` absent from the payload); `WH-UNLOAD-PLT` fired for every receiving task; orders never reached `billing_status = billed` and Jobs never showed invoiced revenue | Rules fixed (conditions `waiting` / `redelivery` / `failed`, quantity from `qty`, key `extra:{shipment_id}:{charge_type}:{occurred_at}`); `task.completed` now carries `asn.{asn_no, inbound_type}` and the unload rule requires `loose_truck`. New event **`invoice.issued`** (Billing → Orders `billed`, Platform Job revenue) with `JobRevenueConsumer`; Orders' consumer is requested from X1 (block/x1-quotes-reports) |
| 66 | 2026-09-08 | C | A6b | Billing PerJobInvoiceConsumer | §7 step 4 / §6.8 #12: per_job clients should see the service invoice draft when the final quote is confirmed; Finance had to draft by hand | `PerJobInvoiceConsumer` on `shipment.quote_confirmed` drafts (never issues) one service invoice per Job for `invoice_mode = per_job`; monthly clients keep pooling. Finance reviews and issues |
| 67 | 2026-09-08 | C | A27 / A20 / A5 | JobController, RateItem | §2.5 #6 Job page panels were M0 placeholders; §2.5 #4 rate item edits were not in the audit log | Job page shows ASNs, stock totals, orders, shipments, documents and invoices of the Job (read-only cross-module queries under the client scope); `RateItem` logs every priced field (log name `rate_card`) |
| 68 | 2026-09-08 | C | A18 / A7b | services.md §7 QuoteService | Decision on #50: X1's estimate needs the customer freight price from Transport on the customer quote without re-pricing it from a rate card | `QuoteService::create` accepts a pre-priced line `{charge_code, qty, amount_cents, transport_quote_id}` (assumptions record `pre_priced`); X1 turns the live freight figure into a real quote line in block/x1-quotes-reports. #52 and #54 acknowledged: `/portal/reports` added to routes.md, `report_deliveries` to db-schema.md §3 |
| 70 | 2026-09-08 | X1 (C's agent) | A7b / B5d (§5.7 #2, §7 step 4) | routes.md (Portal), services.md §3 `QuoteSelectionService` | Portal quote confirmation: `POST /portal/orders/{order}/quotes/{quote}/confirm` (`portal.orders.quotes.confirm`) → `QuoteSelectionService::select($shipment, $quote, 'client', userId)`. Decisions: the order page lists every outbound shipment with `fulfilment_id` set (one per packed batch, §3.8 #8) with its `final` quotes (`quoted` = confirmable, `selected` = the confirmed choice), falling back to the preliminary shipment when no batch shipment exists yet; the client may confirm or re-select only while the shipment is `quoted` (Transport's rule; its `DomainException` is shown as a form error); a quote of another client's shipment is a 404 (Shipment client scope + explicit client predicate), staff get 403 on the portal action; only `customer_price_cents`, `eta_days`, `service_level`, carrier name and the Recommended / Cheapest / Fastest flags are selected — never `cost_cents` / `markup_percent`; no `charges` are written here (Billing bills the freight from `shipment.quote_confirmed`) | Please add the route to routes.md and confirm the per-batch listing |
| 71 | 2026-09-08 | X1 (C's agent) | §7 step 8 / §3.8 #5 (audit gap #2) | routes.md (Portal); read-only use of Billing `invoices` and Warehouse `stock_units` / `asn_lines` / `asns` / `locations` | `GET /portal/invoices` (`portal.invoices.index`) lists the client's non-draft invoices (invoice_no, type, period, issued_at, due_at, subtotal, GST, total, paid, status, overdue flag, outstanding total); `GET /portal/invoices/{invoice}/download` (`portal.invoices.download`) serves `pdf_document_id` through `DocumentDownloader` (draft / foreign invoice / no PDF → 404). `GET /portal/stock` (`portal.stock.index`) groups stock by goods line (mark + description + ASN no) × location type × condition × putaway_completed: pallets, on hand, reserved, available (= on hand − reserved only for put-away `good` stock, §4.3 rules 1–2, otherwise 0), inbound, totals; units with 0 on hand and 0 inbound are hidden; search on mark / description / ASN no / FBA ref. Reads use `DB::table()` with the signed-in user's `client_id` as an explicit predicate (the A21 client-view pattern, #53); Invoice / Document go through the client scope. Portal nav gains 我的库存 / 我的账单 | Please add the three routes to routes.md |
| 72 | 2026-09-08 | X1 (C's agent) | §3.8 #3 billing line (audit gap #7, event from #65) | events.md `invoice.issued` | `App\Modules\Orders\Consumers\InvoiceIssuedConsumer` (registered in `OrdersServiceProvider::boot`): every `order_ids` entry moves `unbilled` / `partially_billed` → `billed` through `OrderStatusService::transitionBilling` (system actor; the timeline note names the invoice no and type); orders already `billed` / `credited` are skipped, so a redelivered event changes nothing (`paid` is an invoice status, not an order billing status); `job_ids` are ignored — Billing lists only the orders it could attribute a line to; an order id that does not exist or belongs to another client throws, so the event is retried and dead-lettered (integration exception) instead of moving a foreign order; storage / manual invoices with an empty `order_ids` are no-ops. Verified on the real path (`OutboundService::pack` → `outbound.packed` charges → `InvoiceService::issue`) | Please confirm at M7 |
| 73 | 2026-09-08 | X1 (C's agent) | A7b (closes #50 via #68) | services.md §7 `QuoteService`, db-schema.md §6 `customer_quote_lines` | `OrderEstimateService` now stores Transport's freight as a pre-priced line `{charge_code: TR-DELIVERY-BASE, qty: 1, amount_cents: customer_price_cents, transport_quote_id, description: 运费 · carrier · service level, context: quote_stage / shipment_no}`; `customer_quotes.notes` only says 待运输报价 when no Transport quote exists (the notes snapshot is gone). The estimate panel is a snapshot of the stored quote: the freight amount comes from the line, carrier / ETA / flags from the referenced `transport_quotes` row (customer columns only), 待运输报价 when there is no freight line — staff / client re-estimate once Transport has quoted. Totals: services subtotal (ex GST) / freight / total ex GST / GST / total inc GST; `customer_quotes.subtotal_cents` and `gst_cents` include the freight line (GST per `TR-DELIVERY-BASE.tax_treatment`). The timeline note still reports the services subtotal | #50 closed by #68; please confirm the totals layout at M7 |
| 74 | 2026-09-08 | X1 (C's agent) | A13 / X2 handoff (#40, #46 delivered) | — | Interim branch removed: `FulfilmentService::stepOperational` no longer catches `OrderRuleViolation` — an `outbound.dispatched` arriving under a financial hold now fails the consumer (retry with backoff → dead + integration exception) because Warehouse refuses the handover itself; `OrderHoldService::release` no longer re-syncs the operational status (`resyncOperationalStatus` stays for A11 quantity reductions). `orders::show` gained the staff-only link to `transport.orders.margin` (admin / customer_service / dispatcher / transport_operator / finance — the roles X2's `OrderMarginController` accepts); the portal never renders it (PortalOrdersTest) | FYI |
| 75 | 2026-09-08 | C (review of PR #18) | A7b / A21 | OrderEstimateService, Portal routes | Adversarial review before M7: (a) pallet-pick estimate divided the cartons still on hand by every pallet unit, so a re-estimate after picking quoted 40 pallet picks instead of 2; (b) `isUrgent` keyed on service_level / created_at and treated a missing cut-off as urgent, unlike Warehouse's `outbound.packed.is_urgent`; (c) the client report route was named `portal.reports`, contract says `portal.reports.index` | (a) capacity = `asn_lines.received_cartons` ÷ pallet units (fallback: average of pallets still holding stock); (b) urgent iff cut-off exists, requested_date = today and now > cut-off; (c) renamed. Open for X1 (coverage, not defects): tests for the hold-race consumer path, mailer failure branch, pickup_deliver / unknown-weight / not-estimable estimate cases, portal quote fallback for shipments without a batch |
| 85 | 2026-09-08 | C | A30 | SearchRegistry | Tester feedback #3: search should behave like a search engine (several keywords, any order, no need to type dashes) | `SearchRegistry::search` runs the phrase and each keyword through every module searcher, merges by URL and ranks by keywords matched; searchers unchanged |
| 86 | 2026-09-08 | C | A6b / A2 | InvoiceService, clients | Tester feedback #4: invoices for a week / two weeks / any range, grouped by order or by Job, per client preference | `InvoiceService::draftPeriod(client, from, to, scope service\|storage\|all, group_by)`; `invoices.group_by`, `invoice_lines.order_id` (resolved from the charge source), `clients.invoice_period` / `invoice_grouping` as defaults; un-invoiced page gets one period form per client with presets. Scheduled auto-drafting per client period is a follow-up |
| 76 | 2026-09-08 | X1 (C's agent) | Tester feedback item 5 (order forms) | enums.md §3 (new row `order_lines.package_type` / `declared_packages.package_type`) | The free-text package type on the staff and portal order forms became a dropdown: `OrderEnums::PACKAGE_TYPES = carton \| satchel \| pallet \| crate \| tube \| flat_pack \| skid`, labels `orders.package_types.*` (纸箱 carton … 栈板 skid), default `carton`, `Rule::in` on goods lines and declared packages. Warehouse's `packages.package_type` (carton \| pallet \| satchel \| crate, enums.md §4) is a subset — please add the Orders list to enums.md §3 (or tell X1 to shrink it). API (`POST /orders/api/orders`), Excel, PDF drafts and ASN-generated lines still accept / carry free text on purpose, so both order pages render unknown values raw (`OrderEnums::packageTypeLabel()`); `OrderEstimateService` treats `skid` as pallet-like | |
| 77 | 2026-09-08 | X1 (C's agent) | Tester feedback item 6 (OMS-13 / A16) | services.md (OrderCreationService note), events.md `order.confirmed` (no payload change) | The order forms carry a 尾板车 checkbox, auto-ticked client side from the heaviest single piece (line weight ÷ cartons, declared package weight) against `RateService::thresholds(client, 'TR-TAILGATE')['tailgate_weight_kg']` (default 25) or a residential address type. Contract note: when the person changed the box (`tailgate_manual = 1`), `OrderCreationService::create()` stores `tailgate_required` as given with `tailgate_reason = manual` at creation (timeline note, no free-text reason) and `TailgateRule::apply()` leaves the order alone at entry and at confirmation — the same semantics as the existing `POST /orders/{order}/tailgate` override — so `order.confirmed` may carry `tailgate_reason = manual` straight from entry. An untouched box keeps the automatic rule (heavy_item / residential_address) | |
| 78 | 2026-09-08 | X1 (C's agent) | Tester feedback item 3b | — (scope decision) | Address suggestions on both order forms come only from the client's own history: `client_addresses` (book first) + distinct `orders.deliver_to_*` snapshots (most recent first), `LIKE %q%` on address / suburb / name, ≥ 2 characters, max 8, deduplicated, never another client's rows. Deliberately left out ("如果很难就放弃"): street-level autocomplete for brand-new addresses — it needs an external address provider (Google Places / AddressFinder / Mapbox: API key, per-request cost, entry in dependencies.md); the helper text tells users to type a new address in full. Decide at a later checkpoint whether to buy one | |
| 79 | 2026-09-08 | X1 (C's agent) | Tester feedback item 3b | routes.md (Orders, Portal) | Please add two rows: `GET /orders/addresses/suggest?q=&client_id=` (`orders.addresses.suggest`, JSON `[{label, name, phone, address, suburb, state, postcode, address_type, source}]`, roles admin / customer_service / dispatcher, 422 without `client_id`) and `GET /portal/addresses/suggest?q=` (`portal.addresses.suggest`, client from the session, staff 403). Both sit inside the module prefixes (RoutesContractTest green) | |
| 80 | 2026-09-08 | X1 (C's agent) | Tester feedback items 1, 2, 7 | public/css/app.css (C's file, ≤ 100 lines) | Three small style blocks are inline in X1 views for now: `dl.kv` / `dl.kv.kv-2` label-value grids (portal order page), `form.filter-row` one-line filter bar (portal stock), `table.form-rows` dense input rows + `.suggest-list` dropdown (order forms). Warehouse / Billing pages have the same stacked `<dl>` — if C wants the classes shared, move them into app.css and X1 drops the inline copies; otherwise close as FYI | |
| 87 | 2026-09-08 | C | Tester feedback item 9 (invoice "$") | Billing views, `app/Modules/Orders/views/batches/index.blade.php` (X1 zone, integrator edit) | Every amount on invoices (PDF + screen), receivables, quotes, charges, rate cards and the batch revenue figure renders through `Money::cents()->format()` → `$1,234.56`; bare `number_format(cents / 100, 2)` is no longer used for money. | Decided: `App\Support\Money::format()` is the one money renderer (contracts/services.md §Money); qty / weight stay plain `number_format`. |
| 88 | 2026-09-08 | C (covering X1) | Tester feedback item 10 (portal 先估价再提交) | routes.md (Portal row), `PortalOrderController`, `portal::orders.create`, `OrderEstimateService::preview()` (Orders zone) | The portal order form's submit becomes two steps: `POST /portal/orders/preview` prices the validated form as a transient Order (same lines as A7b, priced through RateService, nothing saved; freight is quoted after confirmation) and re-renders the form with the estimate + a "确认提交订单" button (`formaction` → `portal.orders.store`); any edit hides the confirm button again. `store()` now also runs `OrderEstimateService::estimate()` so the confirmed estimate travels with the order (best-effort, never blocks). | Decided: implemented on `main` as integrator (X1 out of quota); `OrderEstimateService::lines()` unchanged, `preview()` is additive. HANDOFF row added. |
| 89 | 2026-09-08 | C | Tester feedback item 8 (客户细分 + 注册) | routes.md (Platform `/register`, MasterData approve), enums.md (`clients.status` gains `pending`), `config/erp.php` (`allow_signup`), lang platform/masterdata | Public `/register` creates the Client (status `pending`, own code from the company name, standard rate card, prepaid terms, contact + address = invoice Bill-to) and its first client-role user (`is_active = false`). Login with the right password on a pending account says "审核中". Staff (admin / customer_service / finance) approve from `/admin/clients` (pending rows first, approve button, or status → active on the edit form) which activates the client's users. Data isolation is the existing ClientScope (one client never sees another's rows). | Decided: `pending` applies to clients only (`Enums::CLIENT_STATUSES`); suppliers/carriers keep `MASTER_STATUSES`. Registration can be switched off with `ALLOW_SIGNUP=false`. |
| 90 | 2026-09-08 | C | Tester feedback round 3 item 2 (入库单) | db-schema.md §4 (`goods_receipts`, `goods_receipt_lines`, `stock_units.goods_receipt_id`, `asns.receiving_completed_at`), enums.md (`goods_receipts.status`, `documents.type` + `goods_receipt`), routes.md (Warehouse rows), `lang/zh/warehouse.php` `receipts.*`, `lang/zh/platform.php` `documents.types.goods_receipt` | New Warehouse entity 入库单 (goods receipt): every receiving batch of an ASN is its own document — `GoodsReceiptService::openFor()` opens (or reuses) the ASN's open batch inside `ReceivingService::receiveLine()` (ASN row locked so two operators cannot open two batches), each received line is snapshotted into `goods_receipt_lines` and its stock units (incl. the `-DMG` unit) carry `goods_receipt_id`; the operator ends the batch with 入库完成 (`complete()`: totals, dompdf A4 PDF stored at `receipts/Y/m/{receipt_no}.pdf` and attached as a client-visible `goods_receipt` document on the ASN, `asns.receiving_completed_at` once every line is received). Pages: 收货 worklist `/warehouse/receiving`, 入库单 list / page / live PDF `/warehouse/receipts…`, 入库单 section + one-line roll-up + 入库完成 badge on the ASN page, receipt hint on the receiving form. `receiveLine()` gains an optional trailing `?int $actorId` (controllers and DemoFlowSeeder pass it); its return stays `list<StockUnit>` (the receipt is reachable via `AsnLine::receiptLine()->receipt`) so existing `foreach` consumers keep working. A line is still received once; partial per-line receiving across batches stays out of scope. | Decided by the lead 2026-09-08: all three readings wanted (printable PDF, one-screen 无预报收货, explicit 入库完成); **numbering `receipt_no = {asn_no}-R{batch_no}`** — the 入库单 reuses the ASN number, one ASN may have several batches, each batch is its own 入库单, all batches roll up to the ASN. No new ASN status. |
| 91 | 2026-09-08 | C | Tester feedback round 3 item 2 (无预报收货) | routes.md (Warehouse rows), `UnplannedReceivingController`, `GoodsReceiptService::receiveUnplanned()`, lang `warehouse.receiving.unplanned.*` | One-screen walk-in receipt for goods with no prior ASN: 客户 / 仓库 / 入库类型 (default loose_truck) / 送货参考 / 收货库位 + goods rows (唛头, 品名, 实收, 破损, 单位 pallet \| carton, 托盘数, 总重, 差异原因). The service creates an **unplanned ASN** through `AsnService::create()` (`unplanned = true`, `expected_date = today`, `reference` = 送货参考), adds the lines with `expected_cartons = received + damaged` (nothing was pre-advised), receives every row through `ReceivingService::receiveLine()` with auto-generated units (cartons and weight split evenly across the row's units, remainder onto the first units) and completes the 入库单 at once (one screen = one delivery). | Decided by the lead 2026-09-08. The Coordinator rule is unchanged: `PutawayService` still refuses the unplanned ASN until `AsnService::confirmUnplanned()` (button 确认无预报到货 on the ASN page); the success flash says so. |
| 92 | 2026-09-08 | C (integrator for the X1-zone lang keys) | Tester feedback round 3 item 2 (terminology) | `lang/zh/warehouse.php`, `lang/zh/platform.php`, `lang/zh/orders.php`, `lang/zh/portal.php`, `lang/zh/reports.php`, `docs/demo-guide.zh.md` | **ASN = 预报单 (ASN); 入库单 is reserved for the goods receipt document — never mixed.** Warehouse keys changed: `nav_asns` 入库 ASN → 预报单 (ASN); `asns.title` → 预报单 (ASN); `asns.create` 新建 ASN → 新建预报单 (ASN); `asns.created` → 预报单 :asn_no 已创建。; `asns.asn_no` ASN 号 → 预报单号 (ASN); `asns.unplanned` → 无预报到货(系统自动生成临时预报单;需 Coordinator 确认后才能上架); `asns.unplanned_confirmed` → 无预报到货已确认。; `asns.confirm_unplanned` → 确认无预报到货; `asns.empty` → 还没有预报单 (ASN)。; `asns.generate_orders` → 从预报单 (ASN) 生成派送订单; `tasks.asn` ASN → 预报单 (ASN); `scan.hint` …对应的入库 ASN → 对应的预报单 (ASN); `scan.found_asn_line` 唛头 :label 的入库单 → 唛头 :label 的预报单. Platform keys: `documents.related_types.asn` 入库 ASN → 预报单 (ASN); `search.types.asn` ASN → 预报单 (ASN) (+ `search.types.goods_receipt` 入库单). **X1-zone keys edited by C as integrator (X1 out of quota):** `orders.batches.title` 按柜号 / 入库单号查订单 → 按柜号 / 预报单号 (ASN) 查订单; `orders.batches.asn` 入库单 → 预报单 (ASN); `portal.stock.search` …入库单号… → …预报单号 (ASN)…; `portal.stock.fields.asn_no` 入库单号 → 预报单号 (ASN); `reports.hints.inbound` (期间内到仓的入库单 → 预报单 (ASN)); `reports.…asns` 入库单数 → 预报单 (ASN) 数. 入库 stays for the process (Job workbench panel 入库, 入库类型, 入库批次 nav in Orders). | Decided by the lead 2026-09-08 (decision 1). Lang-only edits; no route, view or code identifiers renamed. |
| 93 | 2026-09-10 | C (integrator) | Tester feedback: 403 → Chinese no-permission page naming the allowed roles | `app/Support/Auth/{RequiredRoles,RoleRequiredException}.php` (new), `resources/views/errors/403.blade.php`, lang platform.errors.*, and a mechanical edit in X1/X2 controllers (Orders, Reports, Transport): `abort_unless($user->hasAnyRole([...]), 403[, msg])` → `RequiredRoles::requireAny([...][, msg])` (still a 403; carries the roles). | Every 403 now renders errors/403 with the user's roles, the roles that ARE allowed (from the helper, Spatie's role middleware exception, or the route's `role:` middleware) and any custom reason message; JSON requests keep a plain 403. | Decided: helper lives in app/Support (C zone); X1/X2 keep using `RequiredRoles::requireAny()` for code-level role checks from now on. Portal `isClientUser()` checks untouched (their zh message shows as the reason). |
| 94 | 2026-09-10 | C | Tester feedback #4 (入库单只能 Excel 导入、无法手动填) + receiving form blocker | routes.md (Warehouse rows), `ReceivingController`, `GoodsReceiptService::receiveLines()`, `warehouse::receiving.bulk`, `asns/show` | (1) New one-screen 手动填写入库单 for an existing ASN: tick lines, enter 实收 / 破损 / 单元 / 托盘数 / 差异原因, optionally 入库完成 at once; all rows go into the ASN's open batch. (2) ASN page: 入库单 section gains the call-to-action + explanation, Excel/manual cards retitled 预报货物行 so they are not mistaken for the 入库单. (3) Per-line receiving form: spare unit rows are disabled until revealed and pruned server-side before validation — the `units.1.carton_qty is required` blocker every warehouse role hit on 2026-09-10. | Decided: no schema change; receipts numbering unchanged. |
| 95 | 2026-09-10 | C (integrator for the Orders view) | Tester feedback #6 (仓库任务意义不明) | enums.md (`VAS_TASK_TYPES`, `SYSTEM_TASK_TYPES`), routes.md (`tasks.cancel`), `TaskController`, `TaskService::cancel()`, `warehouse::tasks.*`, nav label, `orders::show` (one role-gated link, X1 zone) | (A) 新建作业登记 offers only the six billable VAS types; receiving/putaway/move/count/pick/pack/load/return_inspection can no longer be created by hand. (B) System-written tasks are read-only records on the list (link to the wave / outbound page); the generic 完成 refuses them — it used to bypass pick confirmation; hand-made records can be 取消 without a billing event. (C) Menu renamed 作业登记(VAS); a task may bind an order instead of an ASN (source_type `order` → WH-WRAP-OUT-PLT / labour rules), with a shortcut on the ASN page and the order page. | Decided by the lead 2026-09-10 ("abc都做"). No schema change; `task.completed` payload unchanged. |
| 105 | 2026-09-10 | C | Tester feedback: 确认订单后要等一两分钟才出现在出库页 | events.md §rules, `config/erp.php` (`outbox_dispatch_now`, `_hops`, `_seconds`), `DatabaseOutboxPublisher`, `OutboxDispatcher::dispatchNow()`, `routes/console.php` | (1) Screen-facing events (`order.confirmed` for now) are delivered right after the business transaction commits, in the same request, following the Job's follow-up events up to 3 hops / 2 s; failures are logged and left to cron. (2) `outbox:dispatch` runs every ten seconds instead of every minute (Laravel sub-minute schedule; production cron unchanged). Reservation logic, event payloads and module boundaries unchanged; row lock + consumed_events keep delivery at-most-once even when cron and the request overlap. | Decided by the lead 2026-09-10 ("可以改"). |
| 96 | 2026-09-10 | C | Audit 2026-09-10 (Warehouse minors) | routes.md unchanged; `lang/zh/warehouse.php` (`nav_locations`, `receiving.no_receiving_location*`, `receiving.units_required`, `locations.hint` / `no_receiving`, `stocktakes.not_counted` / `close_blocked` / `errors.*`, `returns.errors.*`, `outbound.errors.financial_hold`), `resources/views/layouts/nav/warehouse.blade.php`, `App\Support\Exceptions\RuleViolation` (new, C zone) | Blocking-bug audit: (a) stocktake 记录 form, 入库单 links (ASN page + global search) offered to roles whose route refuses them; (b) 收货 impossible in a warehouse with no active 收货区 (empty required select, 库位配置 linked from nowhere); (c) `required_if:received_cartons,>,0` never fired — 10 cartons booked with no stock units; (d) 完成收货 / 验收完成 / 关闭盘点 / 发运交接 offered before their preconditions with English refusals; (e) 新建仓库 / 新建库位 / 移库 / 上架 / 盘点扫码 forms lost the typed value on a refusal | Decided: no route or role change. Count form and receipt links follow the route role lists (dispatcher sees the 入库单 number as text; the goods_receipt search branch is role-filtered). New nav entry 库位配置 (admin / warehouse_supervisor / warehouse_operator); receiving forms show a 收货区 notice + link instead of an empty select; 无预报收货 disables the submit when the chosen warehouse has none. `units` rule is a closure: required iff `received_cartons > 0`. Completion buttons are `disabled` with the missing step named; the board marks financially held batches (`ExceptionService::hasActiveHold`). **`RuleViolation`** (extends InvalidArgumentException) carries a lang key: services keep their English message, controllers show `RuleViolation::display($e)` in Chinese. Forms use `old()` (`PutawayController` flashes `putaway_unit`, stock cards carry `_form`). |
| 97 | 2026-09-10 | C | Audit 2026-09-10 (Platform minors) | `SearchRegistry::register(module, searcher, roles = [])` / `search(query, ?user)` (App\Support, additive), `ExceptionController::sourceUrl(record, ?user)`, `lang/zh/platform.php` (`approvals.awaiting_second_person` / `errors.*`, `documents.client_required` / `client_hint` / `no_client`, `users.role_placeholder`) | Audit: Job workbench linked CS / dispatcher into `/billing` (admin \| finance) and transport_operator into `/warehouse/**`; Exception Centre 来源 links 403'd for roles outside the target route; Approval Centre offered 批准 / 驳回 to the requester (PLT-7 refusal in English); a 客户可见 document without a 客户 was visible to nobody and reported success; the user form pre-selected 管理员 | Decided: searchers declare the roles of the routes they link to and are skipped for other users (masterdata: admin \| customer_service \| finance; warehouse: its read group); `sourceUrl` checks `Route::has()` **and** the reader's role; Job page guards its cross-module links the same way. Requester sees 等待第二人审批 instead of the buttons; `ApprovalService` refusals are `RuleViolation`s. `DocumentController::store` derives `client_id` from `job_id` and refuses `client_visible` without a client (`visibility()` too; the list hides 设为可见 for such rows). User form gets a placeholder option so `required` + `Rule::in` force an explicit role. |
| 98 | 2026-09-10 | C | Audit 2026-09-10 (MasterData minor) | `MasterDataServiceProvider` searcher | Global search showed 客户 hits (with payment terms / invoice mode in the meta) to all seven staff roles; four of them 403 on `/admin/clients/{id}/edit` | Decided: searcher registered with the master-data roles; meta shows the client status, never billing terms. |
| 99 | 2026-09-10 | C | Audit 2026-09-10 (Billing minors) | services.md §4 RateService note (additive: percent items need `context.base_cents`, otherwise `is_poa = true`), `QuoteService` context gains `base_cents` from the quote form (`lines.*.base`), `InvoiceController::show` (`creditNoteApproved`, `creditNotePending`), `unbilled` entries (`period_from`, `period_to`, `default_scope`), `lang/zh/billing.php` (`credit_notes.approved_badge` / `pending_hint` / `no_approval_hint` / `errors.*`, `quotes.line_base`) | Audit: credit-note 开出 always refused on first click (English, no approval state shown); rate-card / credit-note forms lost everything on a validation error and the `<details>` re-collapsed; TR-FUEL (percent) quoted as a priced-looking $0.00 with no POA flag; 按账期生成草稿 defaulted to 本月 + 服务费 regardless of the pool | Decided: `RateService::price` returns `is_poa = true` (snapshot `reason = no_base_cents`) for a percent item with no base — never $0 (契约 "never 0"; ChargeEngine already passes `customer_price_cents` as the base). Quote form gains 基数金额 per line. Invoice page shows 待审批 (link to 审批中心) / 已批准 per note and disables 开出 until approved; `CreditNoteService` refusals are `RuleViolation`s. Forms use `old()` and reopen on error. Period form defaults to the pool's min / max `charge_date` and to 全部费用 / 仓储费 / 服务费 by what the pool holds. |
| 100 | 2026-09-10 | C (integrator, X1 zone) | 2026-09-10 audit lane B — Orders | `OrderController`, `OrderLineController`, `OrderImportController`, `DraftOrderController`, `ClientAddressController`, `ApiTokenController`, `BatchController`, `OrderFormRows`, `OrderValidation` (new), `OrderCreationService`, `orders::{show,form,imports.create,drafts.create,api-tokens.index}`, `orders::partials.{asn-line-select,goods-lines,goods-line-row}`, `lang/zh/orders.php` | Behaviour changes, no contract change: (a) `GET /orders/batches` without `?ref` no longer 500s; (b) a received from_stock order always offers the ASN goods-line picker (also when the client has no ASN lines yet) and 确认 names the unlinked lines; (c) 新增货物行 keeps the chosen 包装类型 (carton only as fallback); (d) a Job of another client is a `job_id` field error with the form kept, Job pickers filtered by client and labelled `JOB — 客户名`; (e) the hidden 提货 fieldset submits nothing and `declared_packages` are ignored unless `order_type = pickup_deliver`; (f) a goods row carrying only 箱数 reaches validation instead of vanishing; (g) every Orders validator passes Chinese attribute names / row-aware messages (`orders.validation.attributes|messages`); (h) an API order without `package_type` defaults to carton and the usage example includes it; (i) 确认订单 button only for admin / customer_service / dispatcher (hint `orders.actions.confirm_needs_role` otherwise); (j) PDF-draft 服务等级 and the API-token form keep `old()` input. Generic per-rule Chinese text still depends on `lang/zh/validation.php` (lane A). | — |
| 101 | 2026-09-10 | C (integrator, X1 zone) | 2026-09-10 audit lane B — Portal | `PortalOrderController`, `PortalReturnController`, `PortalReportController`, `PortalValidation` (new), `PortalTransportQuotes`, `portal::orders.{create,show}`, `lang/zh/portal.php` | Behaviour changes, no contract change: (a) hidden 提货包裹 rows are disabled client side and pruned server side, so a stale row never blocks 获取估价 / 确认提交; (b) 申请退货 is not offered on line-less 纯运输 orders (`portal.returns.pure_transport`; staff twin in `orders::show`), `quantities` optional so the service's Chinese message answers an empty selection; (c) expired final quotes show an 已过期 badge and no 确认此方案 button, `portal.quotes.expired_hint` when every quote is stale; (d) every goods row (incl. added rows) carries `required` on 包装类型 / 箱数, `requested_date` has `min=today`; (e) a rejected 申请退货 keeps reason / quantities (`withInput`, panel stays open); (f) Portal validators pass Chinese attributes / messages (`PortalValidation` = Orders set + `portal.validation.*`), the duplicate error list on the order form is gone; (g) portal report filter: Chinese messages, `old()` kept, 366-day cap validated (see #103). | — |
| 102 | 2026-09-10 | C (integrator, X2 zone) | 2026-09-10 audit lane B — Transport | `layouts/nav/transport`, `ShipmentController`, `ShipmentLabelController`, `ManualQuoteController`, `QuoteSelectionController`, `ShipmentBookingController`, `RedeliveryController`, `CarrierPodController`, `ExtraChargeController`, `OwnFleetCostController`, `RunStopController`, `DriverController`, `DeliveryRunController`, `CarrierInvoiceController`, `OrderMarginController`, `TransportValidation` (new), `ShipmentLabelService::available()` (new), `ManualQuoteService::MAX_MARKUP_PERCENT`, `transport::{shipments.show,runs.show,driver,orders.margin}`, `lang/zh/transport.php` | Behaviour changes, no contract change (routes / events / enums untouched): (a) nav links 班次 / 承运商对账 / 司机任务 gated by the controllers' role lists; (b) every action form on the shipment page is wrapped in the role its controller requires; (c) 打印运单 only when `ShipmentLabelService::available()` (final + selected quote, adapter declares `label`), otherwise a hint; label failures redirect with `errors.label` instead of a 422 page (B6 test updated); (d) 人工报价 markup > 999.99 % refused with `transport.manual_quote.markup_too_high` (input kept) instead of a 500; (e) 手工报价 offers only the stages whose booking request can be built, otherwise `manual_quote.details_unavailable_hint`; (f) run stop reorder: one changed number = move that stop, several stops on one number → Chinese `runs.duplicate_positions` with input kept; (g) driver POD without a signature shows a visible alert (inert `required` dropped); (h) quote selection flashes `status` (was `success`, never rendered; B5d test updated); (i) all Transport validators pass Chinese attributes / messages (`TransportValidation`), business-rule branches `withInput()`; (j) `GET /transport/orders/{id}/margin` renders an empty state (`transport.costs.order_no_shipments`) for an order without shipments and 404s only when neither an order row nor a shipment exists. | — |
| 103 | 2026-09-10 | C (integrator, X1 zone) | 2026-09-10 audit lane B — Reports | `ReportPeriod::rules()`, `Rules\MaxPeriodSpan` (new), `Http\ReportValidation` (new), `BossReportController`, `ClientReportController`, `PortalReportController` (Portal), `reports::partials.filters`, `lang/zh/reports.php` (`filters.span_hint`, `validation.*`) | Behaviour changes, no contract change: (a) the filter partial redisplays `old('client_id' / 'from' / 'to')` after a rejected GET so the client picker and dates survive the redirect to the bare page; (b) the three report validators pass Chinese attribute names (开始日期 / 结束日期 / 客户) and messages (`结束日期不能早于开始日期。`, `请先选择客户。`, module-scoped generic sentences) so Reports never prints Laravel's English default; (c) a range longer than `ReportPeriod::MAX_DAYS` (366) is rejected with `reports.validation.messages.max_days` naming the latest allowed 到 date instead of being silently clamped on the page and the CSV (`MaxPeriodSpan`, DataAwareRule); the clamp in `fromInput()` stays only as a defence for the mail commands; the filter bar states the cap (`filters.span_hint`). | — |
| 104 | 2026-09-10 | C (integrator) | 2026-09-10 audit lane B — cross-module note for lane A / seat C | `lang/zh/validation.php` (not created by this lane), `resources/views/layouts/partials/flash.blade.php` (unchanged) | The X1 / X2 modules now each carry a `<Module>Validation::{messages,attributes}()` helper (Orders, Portal, Transport, Reports) fed from `lang/zh/<module>.php` → `validation.attributes|messages`. Transport and Reports include a module-scoped set of generic per-rule sentences; Orders / Portal only translate the row-aware / business keys and still fall back to Laravel's English sentence (with the Chinese attribute name) for scalar rules until lane A lands `lang/zh/validation.php`. Request: when that file lands, keep the per-module `attributes` arrays authoritative (they are passed explicitly, so they win) and consider dropping the duplicated generic sentences from `transport.validation.messages` / `reports.validation.messages`. No `contracts/*.md` row changed in this lane; additive lang keys only. | — |
| 106 | 2026-09-10 | C (integrator for Orders) | Tester feedback item 4 B/C/D (出库) | enums.md (`exceptions.type` +`stock_shortage`), services.md (`ExceptionService::resolveOpen`, additive), `OutboundService` messages, `OutboundController`, `outbound::index`, Warehouse `OrderConfirmedConsumer`; Orders zone: `FulfilmentService::allocateBackordersFromPutaway`, `OrderController::show`, `orders::show`, lang orders | (B) the eight outbound refusals are Chinese. (C) A reservation shortfall raises a `stock_shortage` exception (Warehouse), the 出库 board lists confirmed-but-unallocated orders with need / available / short per line and the ASN link, the order page shows the same banner; the exception auto-resolves when a later putaway fills the backorder. (D) The release form has the 客户 / 要求日期 filters the controller already accepted. | Decided by the lead 2026-09-10 ("改第四项b,c,d"). No schema change. |
| 111 | 2026-09-10 | C (integrator for Orders / Portal) | Lead request: client-side cancellation by stage | enums.md (`exceptions.type` +`cancel_request`), routes.md (Portal cancel / cancel-request, Orders reject), `OrderChangeService` (cancelByClient, requestCancel, rejectCancelRequest; staff cancel resolves open requests), `PortalOrderCancelController` (new), portal order page 可用操作 block, staff order page banner | Stage 1 (received / confirmed / allocated): the client cancels directly with a reason — same transaction and `order.cancelled` event as staff, timeline 客户取消. Stage 2 (picking / packed): the client files 申请取消 → `cancel_request` exception in the coordinator queue; staff execute (existing cancel, which closes the request) or reject with a reason the client sees. Stage 3 (shipped): only 申请退货, now a prominent button. | Decided by the lead 2026-09-10 ("阶段一直接取消,别的按你的方案来"). No schema change. |
| 112 | 2026-09-10 | C (integrator for Orders) | Lead request: staff have no prompt for client cancel / return requests | routes.md (`orders.requests.index`), `ClientRequestService` + `ClientRequestController` + `orders::requests.index` (new), `layouts/nav/orders` (客户请求 link with pending badge), `ExceptionRecord` relations creator / resolver / order (Platform) | A 客户请求 inbox lists open cancel requests (execute / reject inline, same endpoints as the order page) and portal return requests not yet collected, plus a 30-day history; the Orders menu shows the pending count. No new write paths, no schema change. | Decided by the lead 2026-09-10. |
| 107 | 2026-09-10 | C | i18n/zh sweep lane A (A1 platform/support): 「整个系统的每一处报错都用中文」 | frozen zone: `bootstrap/app.php` (exception renderer), `resources/views/errors/{404,419,429,500,503}.blade.php` + `partials/card.blade.php`, `lang/zh/{auth,passwords,pagination}.php` (new), `lang/zh/platform.php` | (1) Framework error pages 404 / 419 / 429 / 500 / 503 render Chinese cards modelled on the existing 403 (what happened, what to do, 返回 / 首页); a custom `abort()` reason is shown only when it is Chinese, Laravel's English defaults never render; 419 asks to retry the form, 429 shows Retry-After, 503 distinguishes maintenance mode. (2) `lang/zh/auth.php`, `passwords.php`, `pagination.php` created — paginator links were still Previous / Next on every index page. (3) Safety net in `bootstrap/app.php`: an uncaught `App\Support\Exceptions\RuleViolation` on a form POST goes back to the form with its Chinese `userMessage()` under the `rule` error key instead of a 500 page; GET / JSON requests keep Laravel's rendering. (4) `platform.integration.job` for the one literal `<th>` in the integration monitor. Tests: `tests/Feature/Platform/ErrorPagesTest`, `ValidationLocaleTest`. No enum, event or service-signature change | Accepted (C, same day). Contract: user-visible refusals are thrown as `RuleViolation(englishDevMessage, langKey, replacements)` and controllers render `RuleViolation::display($e)`; `InvalidArgumentException` remains the catch type so X1 / X2 code is unaffected. X1 / X2 may adopt the same class from their own catch blocks without a further CR |
| 108 | 2026-09-10 | C | i18n/zh sweep lane A (A2 warehouse, A3 billing / masterdata): service refusals and exception texts in Chinese | Warehouse: `PutawayService`, `MoveService`, `QuarantineService`, `StocktakeService`, `ReturnService`, `AsnOrderGeneration`, `StockLedger`, every Warehouse controller catch; `lang/zh/warehouse.php` (+`stock.errors`, `asns.errors`, `putaway.errors`, `moves.errors`, `stocktakes.errors`, `returns.errors`). Billing: `RateCardService`, `ChargeEngine`, `RateCardController`, `ChargeController`, `InvoiceController`, `rate_cards/show`, `charges/index`; `lang/zh/billing.php` (+`rate_cards.errors`, `rate_cards.approval_note`, `rate_cards.band_min/band_max`, `charges.errors.review_only`, `exceptions.missing_rate`). MasterData: no change needed | (A2) 14 Warehouse `InvalidArgumentException` refusals become `RuleViolation` with lang keys; every Warehouse controller catch renders `RuleViolation::display($e)`. (A3) 5 rate-card refusals (active card immutable, draft-only add item / submit / activate, second-person approval) and the review-queue refusal become `RuleViolation`; the `missing_rate` exception `message` on the exceptions board and the default rate-card approval `request_note` on the approvals page come from lang; literal `POA` cells and the weight-band `min` / `max` placeholders go through lang; `credit_notes.title` was English. Deliberately English (internal, never surfaced — guarded by `Rule::in` / `Rule::exists` / regex validation before the call): `StockLedger` unknown movement_type, `ReturnService` unknown condition / disposition, `QuarantineService` condition guard, `ReceivingService` receiving-location guard, `InvoiceService` unknown scope, `Client::dueDateFor` unknown payment_terms, `ChargeEngine` reversal reason in `calculation_snapshot_json`, artisan command descriptions. NOT translated on purpose: `billing::invoices.pdf` (legal document for Australian clients; strings: Bill to, Invoice date, Due, Type, Code, Description, Qty, UOM, Amount (ex GST), GST, Subtotal (ex GST), Total AUD, footer sentence) — lead to decide whether a bilingual PDF is wanted. Tests: `tests/Feature/Warehouse/Audit20260910MinorsTest`, `tests/Feature/Billing/Audit20260910MinorsTest::test_billing_refusals_render_in_chinese` (assertSee lang text, assertDontSee developer message). Wording only — behaviour identical | Accepted (C, same day). No enum, event, table or service-signature change; `contracts/` untouched apart from this row |
| 109 | 2026-09-10 | C (integrator, X1 zone) | 2026-09-10 i18n sweep lane B — Orders / Portal / Reports (整个系统的每一处报错都用中文) | `OrderHoldService::release()` (one message), `PortalOrderController::index()` + `PortalStockController::index()` (filter `validate()` calls pass the existing `PortalValidation::messages()/attributes()`; `PortalValidation.php` unchanged), `orders::partials.asn-line-select` (one option label), `portal::orders.create` (estimate uom cell), `lang/zh/orders.php`, `lang/zh/portal.php` | Wording only, no contract change (enum keys, events, routes untouched): (a) `orders.customer_statuses.*` (staff order page + portal) were the PRD English labels → Chinese; (b) the stale-hold refusal flashed by `HoldController::release()` → new key `orders.holds.messages.not_active`; (c) portal estimate preview printed the raw charge-code uom key → rendered through the pre-existing `orders.estimate.uoms.*` (raw key only when no label exists; no lang change); (d) portal order-list / stock filters pass `PortalValidation::messages()/attributes()` and `portal.validation.attributes` gained `q` / `status` / `from` / `to` / `condition` / `availability`, so a hand-edited `?condition=…` never names the raw query key; (e) `portal.messages.client_only` reworded to cover every portal page; `fba_reference` = FBA 货件编号（Shipment ID） in `orders.fields`, `orders.validation.attributes`, `orders.imports.fields`, `portal.fields`, and FBA 引用 → FBA 货件编号 in `orders.imports.hint`, `orders.imports.errors.inconsistent_group`, `portal.stock.search`; (f) polish: `orders.fulfilments.fields.backordered` 'Backorder' → 缺货箱数, `orders.fulfilments.batches_title` → 分批履约与缺货, hardcoded `箱` in the ASN goods-line option → new key `orders.drafts.asn_option_cartons`. Tests extended: `FinancialLockTest`, `OrderManagementTest`, `FulfilmentAllocationTest`, `OrderImportTest`, `PortalQuoteConfirmationTest`, `PortalInvoicesAndStockTest`, `ReportsTest` assert the Chinese text and the absence of the old English. Left English on purpose: consumer / model guards no browser user reaches, `ClientReportMailer` frequency guard, console summary keys, carrier tracking status text (third-party data), unit symbols, acronyms / proper nouns, the system-wide terms `Job` / `Coordinator` (same in lane A files), `token` on the admin API page. **For lane A:** `lang/zh/validation.php` `attributes.fba_reference` and `lang/zh/warehouse.php` `blocked_reasons.inconsistent_delivery_or_fba` still say 'FBA Shipment ID' / 'FBA 引用' — align to FBA 货件编号. | **Applied** on `i18n/zh-sweep-x` (C as integrator); X1 rebases when it lands on `main`. |
| 110 | 2026-09-10 | C (integrator, X2 zone) | 2026-09-10 i18n sweep lane B — Transport | `ManualCarrierAdapter::book()` (one message), `ShipmentStatusMachine::transition()` (one message), `ShipmentBookingService` (`NOT_BOOKED_STATUSES` / `DETAIL_KEYS` constants, `carrierReason()` / `carrierDetail()` new, private), `lang/zh/transport.php` (new: `booking.manual_reference_required`, `booking.missing_reference`, `booking.carrier_status_reason`, `booking.carrier_detail`, `booking.carrier_statuses.{new,pending_payment,pending_review,request_failed,cancelled}`, `booking.carrier_status_other`; reworded: `booking.hold_hint`, `labels.print_waybill`, `labels.waybill_unavailable`, `labels.manual_unavailable`, `extra_charges.reported`) | Wording only, no contract change (`CarrierAdapter` interface, events, enums untouched): (a) the manual carrier's missing-reference refusal (caught by the booking service and flashed as `booking.failed`) → Chinese; (b) the status machine's `DomainException` message → the existing key `tracking.invalid_transition` (the one `ShipmentProgressService` already uses; production one-liner, `B5ShipmentTest` +1 `expectExceptionMessage`); (c) a carrier / gateway booking that comes back not booked now reads 承运商预订失败：<Chinese status label>（<raw carrier status>），承运商返回：<gateway sentence, ≤ 200 chars> instead of the bare code — the same text goes into the `manual_transport` exception; a non-failure status without a booking reference reads 承运商未返回预订参考号（<raw status>） (`booking.missing_reference`) rather than a "not booked" label; the gateway sentence is looked up as `message`, then `detail`, then `error` at each level before recursing, so a Karrio `error` code never displaces its `message`; (d) polish: Finance → 财务, Waybill → 运单, Billing → 计费模块 in the keys listed. Tests: `B5ShipmentTest` asserts the Chinese status-machine message, `B9aCarrierCostMarginTest` +1 test (manual refusal, `request_failed` with an `error` + `message` body, `booked` without reference — all render on the shipment page in Chinese). Left as is: consignment note / own-fleet label / waybill PDFs (third-party documents, English — `labels.{own_fleet,ship_to,shipment,package,weight,dimensions}` on the own-fleet label PDF), POD PDF and POD mail (already Chinese), model enum guards, `SyncTrackingCommand` description, carrier tracking / gateway text (third-party data), `Log::*`. | **Applied** on `i18n/zh-sweep-x` (C as integrator); X2 rebases when it lands on `main`. |
| 113 | 2026-09-11 | C | Lead request: automatic demo story (`php artisan demo:run`) | `app/Modules/Platform/Console/DemoRunCommand.php` + `app/Modules/Platform/Services/DemoStory.php` (new), `PlatformServiceProvider` (command registration), `lang/zh/demo.php` (new), `tests/Feature/Platform/DemoRunCommandTest.php`, `docs/demo-guide.zh.md` §3.1 | Additive, repeatable end-to-end story on the CURRENT database through the modules public services (the same calls DemoFlowSeeder makes — never truncates, migrates or seeds): container ASN → 登记到货 → devanning + receiving (one line short, one carton damaged → discrepancy) → 入库完成 PDF → putaway → orders by consignment mark → confirm (stock reserved) → wave → pick (first order one short → pick_short) → pack (measured packages) → quotes / booking for two orders (own fleet + driver POD when the client has a delivery rate, else Karrio when it quoted, else manual quote + carrier POD) → dispatch → POD → per-job service invoice issued. `--client --warehouse --lines --orders --tag --until --json`; `--until` stops at a stage so testers finish by hand (`putaway` leaves the last unit); every identifier carries the tag; refuses a missing client / warehouse / role account in Chinese (exit 1). No contract change: no new tables, events, routes or enums. | Decided by the lead 2026-09-11. |
| 114 | 2026-09-11 | C (Transport PDF templates as integrator, X2 zone) | Lead request: every client-facing PDF in English, the screens stay Chinese (「所有的 receipt / invoice / label 等给客户生成的 PDF 全部改为英语，系统还是保留中文」) | `lang/en/pdf.php` (new — the only lang/en file), `warehouse::receipts.pdf`, `warehouse::labels.{units,locations}`, `billing::invoices.pdf`, `transport::shipments.{consignment-note,label}`, `transport::driver.pod-pdf`, `AGENTS.md` (i18n rule), `tests/Feature/Platform/PdfEnglishTest.php` (new) | The seven PDF templates draw every word from `pdf.*` (goods receipt, tax invoice, unit / location labels, consignment note, own-fleet label, POD); dates print as `d M Y`; enum values on documents (inbound / unit / location / package types, service levels, invoice types, payment terms `net_N`, billing UOMs) map to English there too. No locale switch at render time: the app locale stays zh and `pdf.*` resolves through `fallback_locale = en`, so `lang/zh` is untouched (the old `warehouse.receipts.pdf.*`, `transport.labels.*`, `transport.consignment_note.*`, `transport.driver.*` keys stay for the screens and error messages). Data fields (marks, goods descriptions, names) print as entered. `PdfEnglishTest` fails the suite when a PDF template uses a non-`pdf.` key, carries a Chinese literal or references a key missing from `lang/en/pdf.php`, and when `lang/en` grows a second file | Accepted 2026-09-11 (lead decision); rule added to AGENTS.md |
| 115 | 2026-09-11 | C | Lead request: 「点"从预报单生成派送订单"后显示收件信息不完整，该用哪个账号去哪里补全」— there was nowhere: the manual add-line form had no address / suburb / state and an existing goods line could not be edited | routes.md (+2 Warehouse routes `warehouse.asns.lines.delivery.{edit,update}`), `AsnService::updateLineDelivery(AsnLine, array, bool $applyToMark): int` (new), `AsnLine::isOnOrder()` / `hasCompleteDelivery()` (new), `AsnController::editDelivery/updateDelivery`, `warehouse::asns.delivery` (new page), `warehouse::asns.show` (per-line 编辑收件信息 link + 收件信息不完整 flag, blocked list links to the lines, add-line form with phone / address / suburb / state / postcode), `lang/zh/warehouse.php` (`asns.edit_delivery`, `delivery_*`, `blocked_fix`, `errors.delivery_locked`), `tests/Feature/Warehouse/AsnLineDeliveryTest.php` (new) | The edit page requires the five fields OMS checks (name, address, suburb, state, postcode; phone / FBA / mark optional), defaults to copying the address to the ASN's other not-yet-ordered lines under the same mark (one mark = one order, so they must agree), and is refused with a Chinese message once the line has an `order_line_id` — the order carries the address from then on. `hasCompleteDelivery()` mirrors Orders' `AsnOrderService::completeDelivery` for the flag only; the decision stays in OMS. No schema change | Accepted 2026-09-11 (lead: 按你的建议修复) |
| 116 | 2026-09-11 | C (Portal zone as integrator; Warehouse own) | Lead request: 客户自助 / 对接 (CartonCloud / Extensiv pattern) — the client uploads its packing list with container no. and ETA in the portal and the ASN is created at once, waiting for customer service to confirm; integrated clients push the same through an API. Existing staff flows untouched, no warehouse rule changed | Warehouse: migration `asns.client_confirmed_at / client_confirmed_by`, `Asn::isClientSubmitted() / isPendingClientConfirmation() / createdBy() / clientConfirmedBy()`, `AsnService::confirmClientSubmission(Asn, ?int) / clearDraftLines(Asn): int / pendingClientConfirmationCount(): int`, route `warehouse.asns.confirm_client` (admin / customer_service), ASN list `?pending=1` filter + badge, ASN page banner + 确认预报单 button, 预报单 nav badge (admin / customer_service), `lang/zh/warehouse.php`. Portal: migration `portal_asn_submissions` (new Portal-owned table), `PortalAsnSubmission` model, `PortalAsnService` (submitFromPortal / submitFromApi / replacePackingList), `PortalAsnController` (index / create / store / show / import / template), `PortalAsnApiController` (`POST /portal/api/asns`), views `portal::asns.{index,create,show}`, `layouts/nav/portal` link, `lang/zh/portal.php` `asns.*` + validation attributes / messages, routes.md, db-schema.md, `tests/Feature/Portal/PortalAsnTest.php` (new) | The client's submission is an ordinary ASN (created_by_type = `client`, already in enums.md) created through Warehouse's own AsnService + AsnImportService — the same parser, pre-checks and Job creation as the coordinator form — plus a Portal-owned log row. "待客服确认" is a flag (`client_confirmed_at`), shown on the ASN list / page / nav badge and in the portal; it never gates receiving, putaway or order generation ("不动业务规则"). 补传装箱单 replaces draft lines only while unconfirmed, booked and untouched (AsnService guard, refused in Chinese otherwise). The API reuses the client API token of `/orders/api/orders` (one credential per client system) and the same Idempotency-Key contract. Cost / margin fields do not exist on these tables; another client's ASN is a 404 through the client scope | Accepted 2026-09-11 (lead) |
| 117 | 2026-09-11 | C (Warehouse + Platform own; Orders and Portal zones as integrator) | Lead decision: an ASN follows the client's order (采购单 / 客户订单 / 调拨单 pattern) — staff generate it from orders as the main flow, the coordinator's manual ASN stays as the exception, and clients do not create ASNs (reverses the client-side part of #116) | services.md (+§10 `InboundService`, `JobService::cancelIfEmpty`), routes.md (Orders +`orders.inbound.{index,store}`; Portal asns rows reduced to index / show, API row removed), `App\Support\Contracts\InboundService` (new) implemented by `AsnService::createAsnFromOrderLines`, `JobService::cancelIfEmpty` (Platform + Fake), Orders: `OrderInboundService` / `OrderInboundController` / `orders::inbound.index` / nav 待建预报 badge / order-page 生成预报单 button / `lang/zh/orders.php` `inbound.*`, Portal: `PortalAsnController` index / show only, `PortalAsnApiController` + `PortalAsnService` + `PortalAsnSubmission` + `portal::asns.create` deleted, `lang/zh/portal.php` `asns.*` trimmed, `tests/Feature/Orders/OrderInboundTest.php` (new), `tests/Feature/Portal/PortalAsnTest.php` (read-only) | Staff (admin / customer_service / warehouse_supervisor) pick the from_stock orders that arrive together (status received / confirmed, ≥ 1 goods line without an ASN line) and one ASN is created under the first order's Job — one ASN line per order line with the order's consignee / FBA / mark, linked both ways. Other picked orders are merged into that Job when their own Job carries no ASN; the emptied per-order loose Jobs are cancelled (note names the shipment Job). Already-confirmed orders get their unlinked quantities booked as backorder quietly (no 缺货 exception — the goods are announced); `asn.putaway_completed` then allocates them through the unchanged FulfilmentService path. No warehouse rule changes. Retained from #116: `asns.client_confirmed_at / by`, `warehouse.asns.confirm_client` and the pending badges (only for `created_by_type = client`, which no screen sets today), the empty `portal_asn_submissions` table (migration already merged; unused), the read-only portal 预报入库 pages with 入库单 PDF download. Excel-import auto-generation deliberately not built (lead) | Accepted 2026-09-11 (lead: 全部按你的建议来，Excel 导入不用做) |
| 118 | 2026-09-11 | C (Transport zone as integrator; Orders + Portal zones as integrator) | Lead request: the client, not the dispatcher, decides the transport plan; the transport options' estimates show together with the warehouse 估价 | services.md §3 (`TransportOptionService::estimate()` new), db-schema.md (`orders.transport_preference` json), `TransportOptionService` (estimate; candidates / pricing keyed by client id; preliminary stage selects the client's option; final stage confirms the *same option* within tolerance instead of the recommended one; reference falls back to the client's choice), `ShipmentQuoteRequestFactory` (sender from the goods' warehouse before packing, parcels from order lines when no declared packages, public `itemsFromLines` / `partyForWarehouse` / `completeParty`), `FakeTransportOptionService::estimate`, shipment page 客户下单时选择 row, `lang/zh/transport.php`; Orders: migration `orders.transport_preference`, `Order` cast, `OrderEstimateService::freightFromPreference` (freight line from the client's choice until Transport quotes; `freightLine` accepts a null quote id), order page line, `lang/zh/orders.php`; Portal: `PortalTransportEstimate` (new), `PortalOrderController` preview / store / rules (`transport_choice`), `portal::orders.create` (options table with radios, totals follow the choice), `portal::orders.show` (choice line, 您的选择 badge), `lang/zh/portal.php`; `tests/Feature/Portal/PortalTransportChoiceTest.php` (new) | 获取估价 now prices the transport options for the order (same adapters, customer price only) and the client ticks one; the choice is stored on the order, the estimate carries it as the freight line, `order.confirmed` selects the matching preliminary quote as the client's decision and `outbound.packed` confirms the final quote for that option automatically while within `variance_tolerance_percent`; beyond it, or when the option is gone, the client re-confirms in the portal (existing page, its choice marked) and the coordinator's button is 代客确认. Preliminary quotes for from_stock orders now work (warehouse sender + line parcels) — before, the request could not be built and a `manual_transport` exception was raised at every confirmation. Cost and markup never reach the portal (estimate() returns customer fields only) | Accepted 2026-09-11 (lead) |
| 119 | 2026-09-11 | C (Orders zone as integrator; Warehouse own) | Lead request: an ASN generated from orders must not need goods lines typed by hand — the ASN page imports the client's pending orders (consignee, phone, address, postcode, FBA, cartons) directly; manual add-line demoted to the exception; each line shows its source order | services.md (§2 `OrderService::awaitingAsn` / `attachOrdersToAsn` new, §10 `InboundService::addOrderLinesToAsn` new), routes.md (Warehouse +`warehouse.asns.import_orders`), `App\Support\Contracts\InboundService::addOrderLinesToAsn` implemented by `AsnService`, `App\Support\Contracts\OrderService::awaitingAsn` / `attachOrdersToAsn` implemented by Orders (`AsnOrderService`, `OrderInboundService`) + Fake, Warehouse: `routes.php` (`POST /warehouse/asns/{asn}/import-orders`, roles admin / customer_service / warehouse_supervisor), `AsnController::importOrders`, `warehouse::asns.show` (从订单导入货物行 block with the candidate table and `order_ids[]` checkboxes, manual add-line form inside a collapsed `<details>`, 来源订单 column + 由订单生成 badge per line), `lang/zh/warehouse.php` `asns.import_orders_*` / `orders_imported` / `manual_add_summary` / `from_orders_badge` / `order_col` / `errors.import_orders_closed`; Orders: `OrderInboundService`, `AsnOrderService`, `lang/zh/orders.php` `inbound.errors.asn_other_client`; `tests/Feature/Warehouse/AsnImportOrdersTest.php` (new); Platform: `JobService::moveOrder` (new contract method, services.md §5 — the merged order's shipment / quotes / holds / tasks / documents follow it into the ASN Job so `cancelIfEmpty` can close the per-order Job; explicit table list, the one sanctioned cross-module `job_id` write, db-schema.md ownership note), `cancelIfEmpty` also waits for pending / failed `outbox_events` of the Job. Review round (same day): `attachOrdersToAsn` gained an optional 4th parameter `?string $containerNo = null` (the import form's `container_no`, required when the ASN has > 1 container; `InboundService::addOrderLinesToAsn` refuses floating lines on a multi-container ASN with `warehouse.asns.errors.import_orders_pick_container`), refuses an order whose Job still has undelivered events (`orders.inbound.errors.events_pending`), the ASN row is locked for the whole import (and by `PutawayService` before the flip to putaway), the 由订单生成 badge follows provenance (order line older than the ASN line) instead of any link | On a booked / arrived / receiving ASN the page lists the client's from_stock orders in received / confirmed with ≥ 1 goods line not yet on an ASN (oldest first: order no, Job, mark, consignee, requested date, unlinked / total lines, cartons, status). Staff tick the ones arriving in this shipment and one goods line per unlinked order line is appended to the ASN with the order's consignee / phone / address / suburb / state / postcode / FBA / mark / description / cartons / weight — no typing; on a one-container ASN the lines land on that container. Links are written both ways (`asn_lines.order_line_id`, `order_lines.asn_line_id`); the orders are merged into the ASN's Job, their emptied loose Jobs cancelled (flash names the shipment Job, the merged orders and the closed Jobs); already-confirmed orders get their quantities booked as backorder quietly so putaway allocates them through the unchanged chain. Candidate rows come unticked (全选 header box; a failed submit keeps the selection; empty submit → 请至少勾选一张订单); rows whose Job already carries another ASN render disabled with Job 已有预报单，不能并入; a multi-container ASN asks which container the lines go onto. Refused: another client's order (`orders.inbound.errors.asn_other_client`), an order whose Job already carries an ASN (`job_has_asn`) or still has undelivered events (`events_pending`), an ASN past receiving (`warehouse.asns.errors.import_orders_closed`), a floating line on a multi-container ASN (`import_orders_pick_container`); operators and dispatchers cannot import. The import card sits right under the 货物行 table (before 入库单) with a header button and the empty-state / 入库单 hints pointing at it; the manual form re-opens with its errors when its own validation fails. The manual add-line form and Excel import stay for goods that are on no order, collapsed under 手工添加预报货物行（例外情况）; 待建预报 (#117) keeps working and stops listing the imported orders | Accepted 2026-09-11 (lead) |
| 120 | 2026-09-14 | C (Transport, Orders, Portal zones as integrator) | Lead request: "orders of type pickup_deliver (提货直送: the 3PL collects at the client's pickup address and delivers; no warehouse stock, no fulfilment, no `outbound.packed`) must be billed the SAME handling standards as from_stock outbound orders, and the estimate must show them" — until now a pure-transport order paid freight only and the 获取估价 showed freight alone | `App\Support\PackageUnits` + `App\Support\UrgentDespatch` (new, shared by Orders / Transport / Billing), `BillingSeeder` (`RULES` is now a list of [code, trigger, condition, source, key] so a code carries one rule per trigger; +8 `pickup_deliver` rules; +`TR-PICKUP` code and manual rule), `contracts/events.md` (`shipment.quote_confirmed` +`order_type`, and for pickup_deliver `is_urgent`, `lines`, `pallet_count`, `carton_count`, `label_count`), `contracts/charge-codes.md` (#20–26, #28 trigger column; §6 `TR-PICKUP`), Transport `shipment.quote_confirmed` publisher (payload additions; `QuoteSelectionService::select(..., ?DateTimeInterface $confirmedAt = null)` and the additive `TransportOptionService::quote(..., ?DateTimeInterface $triggeredAt = null)` so the automatic final confirmation is dated the order's `confirmed_at` — `is_urgent` follows the client's decision time, not outbox processing time), Orders `OrderEstimateService::lines()` (pickup_deliver lines from the declared packages; `declared_packages.*.weight_kg` required for pickup_deliver), Portal 获取估价 / order page estimate, `tests/Feature/Billing/ChargeEngineTest`, `tests/Feature/Platform/SupportHelpersTest` (new), `ERP_PLAN.md` notes at OMS-6 / §5.3 / §6 trigger table | Defaults (按默认): (1) billing moment = the FINAL `shipment.quote_confirmed` — the event that already bills TR-DELIVERY-BASE / TR-TAILGATE / TR-FUEL and on which `PerJobInvoiceConsumer` drafts the per_job service invoice; quantities from the client's DECLARED packages (`orders.declared_packages`: package_type, qty, weight_kg per piece, dims — the plan's final basis for pure transport); unique key `order:{order_id}`, version 1, so a reconfirmation never re-bills. (2) Same codes, same rates: WH-ORDER-DESPATCH ×1; WH-ORDER-DESPATCH-URGENT when urgent (requested_date = the confirmation day and confirmed after `clients.dispatch_cutoff_time` — `UrgentDespatch::isUrgent`, the existing Orders rule); WH-PICK-PLT and WH-LOAD-PLT per declared pallet / skid piece (`PackageUnits::unitType`); WH-PICK-CTN-LT22 / 22-45 / GE45 per carton-type piece banded on its declared weight_kg (null → 0 → LT22, as the engine already does); WH-LABEL-OUT per declared piece (Σ qty). Rule condition `order_type = pickup_deliver` (string compare — a payload without `order_type` matches nothing, so `outbound.packed` and every existing fixture are byte-identical). (3) New code `TR-PICKUP` 上门提货附加费 (transport / delivery / "Collection at sender (surcharge)") with a MANUAL-trigger rule only, no Edward row — never auto-billed until priced. (4) Estimate: `OrderEstimateService::lines()` returns the same lines for pickup_deliver from the declared packages so the portal 获取估价 and the order page show them next to freight. Engine quantity sources reused unchanged: `orders` (always 1), `pallets` (`pallet_count`), `labels` (`label_count`), `cartons` (`lines` with unit_type carton by `unit_weight_kg`); no `ChargeEngine` change. Enum values unchanged (`charge_rules.trigger_event`, `quantity_source`, `charges.source_type` = shipment for these rows) | Accepted 2026-09-14 (lead: 按默认). |
| 121 | 2026-09-14 | C (Orders zone as integrator for the estimate) | Lead question 2026-09-14: 「一个托盘上十几个货，一个一个出，这个解决了吗」— operationally yes (reservations and picks take any quantity off a pallet unit), but Billing typed every pick line by the STOCK UNIT's type, so one carton taken off a pallet was billed as a whole-pallet pick (WH-PICK-PLT) with the pallet's weight, and the estimate rounded any partial pallet up to a pallet pick | `contracts/events.md` (`outbound.packed` lines: unit_type = how the line was picked), `app/Modules/Warehouse/Services/OutboundService.php` (`pickBilling()`), `app/Modules/Orders/Services/OrderEstimateService.php` (`pickUnit()` → whole pallets + loose cartons), `tests/Feature/Warehouse/PartialPalletPickTest.php` (new) | Rule (lead: 先改): a pallet unit is a pallet pick only when the line took everything the pallet still held (the pick movement ends at 0); taking part of a pallet is carton picks banded on the pallet weight spread over the cartons it held when received (fallback: the ASN line's per-carton weight); carton units unchanged. The estimate uses the same arithmetic: whole pallets = floor(cartons / cartons-per-pallet) → WH-PICK-PLT (+ WH-LOAD-PLT), the remainder → carton band. No schema or rate change; from_stock whole-pallet orders bill exactly as before | Accepted 2026-09-14 (lead) |
| 122 | 2026-09-14 | C (Warehouse, Billing, Platform, Support own; no X1 / X2 file touched) | Lead request (design brief 2026-09-14, approved "7个点都先按默认执行"): 拼柜 / LCL — several clients' ASNs share one physical container; the coordinator needs a screen to link them, the devanning fee (and cartage / sideloader) must be split over the clients' Jobs, a sideloader flag is needed, and the 20-line / 22.5 t caps of the card were never enforced by the real RateService (only by the Fake) | `contracts/db-schema.md` §4 (`physical_containers` new; `containers` +physical_container_id / devanning_basis / devanning_basis_qty / devanning_share / devanning_basis_provisional; `warehouse_tasks` job_id / client_id nullable for source_type physical_container, +physical_container_id), `contracts/enums.md` (source_type +physical_container; physical_containers.consolidation / allocation_basis / status; trigger_event +physical_container.arrived; quantity_source +allocated; charges.source_type container = physical_containers.id), `contracts/events.md` (task.completed additive box block: job / client null, physical_container, members[] with share, allocation_basis, basis_provisional, activity_version; new `physical_container.arrived`), `contracts/charge-codes.md` (§1 cartage trigger → physical_container.arrived allocated, §2 rows 3–8 `allocated`, §6 +TR-SIDELOADER, §7 caps enforced + `allocated` context), `contracts/services.md` (§4 RateService thresholds enforced / `allocated`; §9 ChargeEngine allocated; new §11 PhysicalContainerService), `contracts/routes.md` (+`/warehouse/physical-containers*`, tasks.store `physical_container_id`), `App\Support\Enums` (+TASK_SOURCE_TYPES physical_container, QUANTITY_SOURCES allocated, TRIGGER_EVENTS physical_container.arrived, CONSOLIDATIONS, ALLOCATION_BASES, PHYSICAL_CONTAINER_STATUSES), `App\Support\Ui\StatusBadge` (two tone maps), `App\Support\Fakes\FakeRateService` (allocated skips min qty). Warehouse: migrations `2026_09_14_200000/200010/200020`, models `PhysicalContainer` (new) / `Container` / `WarehouseTask`, `Events\PhysicalContainerArrived` (new), `Services\PhysicalContainerService` (new: create / link / unlink / shares / markArrived / registerDevanning / completeDevanning / recompute / candidates), `TaskService` (box branch, public static `eventPayload`), `TaskController` (physical_container_id, container-on-ASN check, linked-row refusal), `PhysicalContainerController` (new) + views `physical_containers/{index,create,show}`, `tasks/{create,index}`, `asns/show` (物理柜 column + FCL/LCL badge + 关联物理柜 shortcut + box tasks), `routes.php`, nav include (物理柜 / 拼柜), global search by box number, `lang/zh/warehouse.php` `physical_containers.*` / `tasks.*` / `asns.*`, `lang/zh/platform.php` source_types.physical_container. Billing: `ChargeEngine` (quantity_source `allocated`: per-tuple client / Job / key suffix / snapshot, per-member condition, whole-box reversal on a higher version, source container), `RateService` (max_line_count / max_gross_weight_kg → is_poa reason; allocated skips min_billable_qty / min_charge_cents), `BillingSeeder` (TR-SIDELOADER code; WH-DEVAN-* → allocated; TR-CARTAGE-20/40 + TR-SIDELOADER rules on physical_container.arrived; the shipment.quote_confirmed cartage rules kept dormant), `BillingChargeConsumer` (+physical_container.arrived). Tests: `tests/Feature/Warehouse/PhysicalContainerTest.php`, `tests/Feature/Billing/DevanningAllocationTest.php`, `tests/Feature/Billing/RateThresholdTest.php` (new). ERP_PLAN §0.2 rule 1, §4.2, §4.8, §6.4; docs/demo-guide.zh.md | **Accepted 2026-09-14 (lead), all seven defaults:** (1) allocation basis per box — `cartons_received` by default, `pallets` when the box unpack_mode = pallet, `equal` when the basis total is 0; the coordinator may pick cbm / lines / equal; while any member ASN is still receiving the split is provisional on the pre-advised cartons. (2) Shares computed by Warehouse at devanning-task completion (and at 登记到港 for cartage), stored on the member rows, published in the event; 重算分摊 re-emits with activity_version + 1 and the engine reverses the older split — including a member unlinked since. (3) FCL / single-client containers keep the per-ASN path untouched: linking is optional, an unlinked row's devanning task bills byte-identically (`allocated` degrades to billable_qty without members). (4) Sideloader = separate surcharge code `TR-SIDELOADER` (transport / delivery), rule on physical_container.arrived with condition sideloader_required = true, allocated, NO Edward rate row → Missing Rate per member the first time a flagged box is billed; Finance prices it (or 0) on the client card. (5) Cartage TR-CARTAGE-20/40 for boxes with cartage_by_us = true is allocated by the same share on the new Warehouse event `physical_container.arrived`; the never-built cartage shipment of **#7 / #34 is superseded** — their `shipment.quote_confirmed` rules stay seeded and dormant, X2 owes nothing. (6) The devanning rule is matched per MEMBER's own unpack_mode (a palletised member pays PLT × share, a loose member LOOSE × share, mixed → POA); the 20-line cap and the 22.5 t cap are evaluated on the WHOLE box and are now enforced by the real `RateService` (`max_line_count` / `max_gross_weight_kg` → is_poa → needs_review), Fake aligned; minimum quantity / minimum charge never apply to a member's fraction. (7) The portal shows only the ASN's own container_no — never the consolidation badge, never other members (negative test); `seal_no` dropped. Rounding: shares 4 dp (Σ = 1.0000 exactly), each member's amount rounded independently, so Σ members may differ from the box price by cents when the shares are not exact quarters / fifths — open point for the lead. Trial server, run once: `php artisan db:seed --class="App\Modules\Billing\Seeders\BillingSeeder"` (after `migrate`) |
| 123 | 2026-09-14 | C (Orders + Portal zones as integrator; contracts / docs own) | Lead request 2026-09-14: 「帮我加回入库提交csv功能，要保证能够准确的转换客户订单信息。在后台做」— bring back the client's CSV inbound submission that #116 built and #117 removed, without re-opening #117: clients still do not create ASNs, so the upload converts the client's list into the client's ORDERS (source `portal`, status received), which appear in the staff 待建预报 queue where customer service generates the ASN exactly as today (#117 / #119). Accuracy of the conversion is the point | Portal (X1 zone): `routes.php` (+`portal.asns.imports.{index,create,template,store,show,confirm}`), `Http/Controllers/PortalInboundImportController.php` (new), `views/asns/imports/{index,create,show}.blade.php` (new), `views/asns/index.blade.php` + `views/orders/index.blade.php` (上传清单 buttons), `lang/zh/portal.php` (`inbound.*`, validation attributes / messages). Orders (X1 zone): `Services/SpreadsheetManifestParser.php` (rewritten in place — format sniffing, encodings, delimiter, full-width, header aliases, units, phone / postcode / state normalisation, row-level Chinese messages with the sheet's own column label, `label` key on entries, `deliver_to_suburb` / `requested_date` / `service_level` row keys), `Services/OrderImportService.php` (`source` / `client_visible` / `inbound` context, nullable `job_id` with one loose Job opened at confirm, per-group 要求送达日 / 服务等级, declared-package weight = line total ÷ 箱数, `JobService` dependency), `Http/Controllers/OrderImportController.php` (errors CSV prints the column label), `Http/Controllers/OrderInboundController.php` (portal submissions per client), `views/inbound/index.blade.php` (客户提交的入库信息 card + 选中并填入 pre-fill), `views/imports/{index,show}.blade.php` (来源 badge, file name, inbound card, read-only for portal source), `lang/zh/orders.php` (`imports.columns / warnings / inbound / portal_note / job_note`, row-aware error messages, `xls_unsupported` …). Contracts: `enums.md` (`order_imports.source` +`portal`), `routes.md` (Portal row), `services.md` §8 notes. Tests: `tests/Feature/Portal/PortalInboundImportTest.php`, `tests/Feature/Orders/ManifestParserAccuracyTest.php` (new) + fixtures `tests/Fixtures/imports/*` | **Flow:** 预报入库 → 上传清单 (CSV / Excel) with optional 柜号 / 柜型 / 预计到港日 / 参考号 / 备注 → preview (every mapped field per mark: 收件人 / 电话 / 地址 / 城区 / 州 / 邮编 / FBA / 品名 / 包装 / 箱数 / 重量 / 尺寸, hard errors on top with row + column, warnings, blocked groups explained) → 确认提交 creates the orders (all ready groups, no address-book save) under ONE new loose Job whose reference is the client's 参考号 / 柜号 → the page lists the order numbers (portal links) and says customer service builds the ASN. 要求送达日 defaults to 预计到港日 + 7 (today + 7 without one), 服务等级 standard; a 要求送达日 / 服务等级 column on the sheet wins per mark. The inbound context lives in `order_imports.errors.context.inbound` (no migration); orders ↔ import through `errors.result.created` (exists). 待建预报 shows the context per client with 选中并填入 (ticks the batch, fills container_no / size / expected_date / notes); the staff import list labels portal rows 客户门户 and the page is read-only for them. Tenancy: `order_imports` global scope (before route binding) + explicit owner / source check → another client's import is 404; the upload's Document is `client_visible` for the owner only. **Parser accuracy rules (additive to §8):** format from the bytes (zip → XLSX, OLE → 旧版 XLS 另存为 message, else text); CSV encoding UTF-8 BOM / UTF-16 LE / BE (BOM or NUL pattern) / valid UTF-8 / else GB18030, with a warning naming the encoding; delimiter comma / semicolon / tab sniffed on the first lines; full-width digits / letters / punctuation → half-width, NBSP / zero-width stripped; header aliases (收件人 / 收货人 / Consignee / Receiver, 电话 / 联系电话 / 手机 / Phone / Tel, 地址 / 收件地址 / 收货地址 / Address, 城区 / 城市 / Suburb / City, 邮编 / 邮政编码 / Postcode / Zip, 州 / State / 省份, FBA / FBA参考号 / Reference / 参考号, 箱数 / Cartons / CTNS, 毛重 / 重量 / 总重 / Weight (kg), 长 / 宽 / 高 with (cm) / (mm), 要求送达日 / Requested date, 服务等级 …) case- and punctuation-insensitive; `No.` / `Qty` / `数量` / `件数` count cartons only when no 箱数-type column exists (else 数量 / 件数 / Qty = unit count); title rows above the header skipped, row numbers stay the sheet's own (XLSX uses the row's `r`); dimensions ×10 when the header names cm; 单件重量-type header × 箱数 → line total with one file-level warning; 箱数 accepts "10箱" / "3 ctns", refuses decimals; phones kept as text — spaces / dashes / brackets removed, +61 / 61 → 0, a 9-digit number padded to 10 with a warning, scientific notation or a float export refused with 请把电话列设为文本; postcodes 4 digits inside an Australia Post range, a 3-digit value padded (800 → 0800) with a warning, others refused; state codes / full names / lower case / dotted / Chinese (维多利亚 新南威尔士 昆士兰 南澳 西澳 塔斯马尼亚 北领地 首都领地 + 维州 / 新州 / 昆州 / 塔州) → code, unknown refused, missing state derived from the postcode range with a warning, state / postcode mismatch warned; package words (纸箱 / 托盘 / 快递袋 / 木箱 …) → `OrderEnums::PACKAGE_TYPES` code; identical rows warned as 重复行; every message `第 :row 行「<column as in the sheet>」…` (fixes the raw `orders.fields.deliver_to_state` key the old required message printed). Staff side unchanged in behaviour except: the errors CSV prints the column label, declared packages from an import carry 单件重量 = line total ÷ 箱数 (before: the line total, which over-flagged 尾板车) | Accepted 2026-09-14 (lead request) |
| 124 | 2026-09-14 | C (Warehouse, Platform, Support, contracts own; Transport (X2) and Orders / Portal (X1) zones as integrator) | Lead decision 2026-09-14 (design brief, "按这个方式改"): a client who wants goods brought INTO our warehouse must not use 现场提货直送 (a customer delivery order: no consignee here, goods must enter stock, bills outbound handling). Industry practice (CartonCloud / Extensiv): the inbound leg belongs to the inbound document — the 预报单 gains a 到仓方式 (客户自送 default \| 我方上门提货) and Transport collects at the pickup address and delivers to the warehouse | events.md (+`asn.collection_requested`, +`asn.collection_cancelled`; `shipment.quote_confirmed` / `shipment.booked` / `delivery.pod_captured` / `delivery.failed` +`asn_id` +`shipment_type`, `order_id` nullable; Warehouse consumes those four), db-schema.md (`asns` +11 collection columns; `shipments.order_id` nullable, +`asn_id`, +`asn_activity_version`), enums.md (`asns.inbound_transport`, `asns.collection_status`; `shipments.shipment_type` +`inbound_collection` with the outbound lifecycle), routes.md (+`PUT` / `DELETE /warehouse/asns/{asn}/collection`), services.md (§3 intake `fromAsnCollection` / `cancelAsnCollection`; §10 `AsnService::setCollection` / `clearCollection` / `recordCollectionProgress`), charge-codes.md §6 (collection bills TR-* only, on the ASN's Job). `App\Support\Enums` (+ASN_INBOUND_TRANSPORTS, ASN_COLLECTION_STATUSES, COLLECTION_PACKAGE_TYPES, ADDRESS_TYPES), `StatusBadge` (collection tone map). Warehouse: migration `2026_09_14_300000_add_collection_to_asns`, `Asn` model, `Events\AsnCollectionRequested` / `AsnCollectionCancelled`, `AsnService`, `Consumers\AsnCollectionProgressConsumer` (the four Transport events), `AsnController` (create / updateCollection / destroyCollection), `routes.php`, views `asns/create`, `asns/show`, `asns/partials/collection-fields` + `collection-card`, `lang/zh/warehouse.php` `asns.collection.*`. Transport (X2 zone, HANDOFF): migration `2026_09_14_300010_add_asn_collection_to_shipments`, `TransportEnums` (+`inbound_collection`, `DELIVERY_TYPES`, `isDelivery()`), `ShipmentStatusMachine`, `Shipment` (+asn_id, `isCollection()`), `Consumers\AsnCollectionRequestedConsumer` / `AsnCollectionCancelledConsumer` + registry, `ShipmentIntakeService::fromAsnCollection` / `cancelAsnCollection`, `ShipmentQuoteRequestFactory::buildCollection` / `collectionItems`, `TransportOptionService` (`sender` in `_quote_request`; no client preference without an order), `QuoteSelectionService` (type check, +asn_id, order context null-safe), `ShipmentBookingService` / `DriverPodService` / `CarrierPodService` / `TrackingSyncService` (payloads, type checks), `ShipmentProgressService` / `DeliveryRunService` / `DeliveryRunController` / `RedeliveryService` (delivery types), `PackageManifest::forCollection`, `ConsignmentNotePdf` + `consignment-note.blade` (ASN + collect-from / deliver-to), `ShipmentController` / `IndexController` + `shipments/show`, `index`, `driver` views, `lang/zh/transport.php`, `lang/en/pdf.php`. Orders (X1): `DeliveryPodCapturedConsumer` returns early on a null `order_id`. Portal (X1): `views/asns/partials/collection.blade.php` (new, read-only, client price only) + one `@include` in `asns/show`, `lang/zh/portal.php` `asns.collection`. Platform: `JobCostConsumer` counts both delivery types. Billing: `ChargeEngine::applyEvent` — a newer `activity_version` now also reverses the older charges of a rule's key when the rule no longer matches (a re-quoted collection whose tailgate dropped); `QuoteSelectionService` carries `activity_version` (= `asns.collection_version`) for collections only. Tests: `tests/Feature/Warehouse/AsnCollectionTest.php`, `tests/Feature/Transport/InboundCollectionShipmentTest.php`, `tests/Feature/Portal/PortalAsnCollectionTest.php` (new); B5d / B7 / B9a payload key lists extended. Docs: HANDOFF.md, MERGELOG.md, docs/demo-guide.zh.md, ERP_PLAN §4 / §5 notes | **Accepted 2026-09-14 (lead).** v1 boundaries: no client choice of the collection plan (the dispatcher / customer service confirms on the shipment page; the portal card is read-only, client price only); edits and cancellation after booking go through the dispatcher on the shipment page (no cancel-booking button exists yet — the ASN page refuses with a Chinese message); `collection_status` `quoted` / `collected` are reserved (no event sets them); containers (FCL / LCL cartage) stay on `physical_containers` (#122) — this is for loose goods / pallets only. No Billing seeder change: the existing `shipment.quote_confirmed` TR-* rules bill the collection on the ASN's Job (`order_type` null → no handling code). Server steps after merge: deploy runs `migrate --force` (two migrations); nothing else |
| 125 | 2026-09-15 | C (Warehouse, Support, contracts own; Orders / Portal (X1) and Transport (X2) zones as integrator) | Lead decision 2026-09-15 (Jimmy): 「保留客服审核，帮我按建议补」 — the approved proposal: (1) on the portal 入库清单 upload the client ticks 需要我们上门提货 and gives the pickup address, contact and ready date; the packages come from the list rows (箱数 / 重量 / 尺寸), never typed twice; (2) the same flow shows the collection plans with CLIENT prices and the client ticks one (the CR #118 idea); (3) customer service reviews in 待建预报: the submission is flagged 客户申请上门提货, generating the ASN pre-fills the pickup data and opens the collection shipment (CR #124 chain); the final quote for the client's option is confirmed automatically within the client's tolerance, outside it the client re-confirms in the portal (or CS on the shipment page); (4) the customer-service review stays: nothing reaches Transport until CS generates the ASN, and clients still never create ASNs (CR #117) | events.md (`asn.collection_requested` +`client_preference` +`requested_via`, confirmation paragraph), db-schema.md (`asns` +`collection_preference` / `collection_requested_via` / `collection_import_id`), enums.md (+`asns.collection_requested_via`), services.md (§2 `OrderService::awaitingAsn` rows +`collection_requested`; §3 collection client choice, `CollectionTailgate::required`, `ShipmentQuoteRequestFactory::collectionSender` / `collectionRequest`; §10 +`InboundService::requestCollection`, `setCollection` optional `client_preference` / `requested_via` / `import_id`, `clearCollection` clears them), routes.md (Portal imports store / show / confirm fields, +`POST /portal/asns/{asn}/collection/quotes/{quote}/confirm`, `orders.inbound.store` fields). Code — Warehouse (C): migration `2026_09_15_100000_add_collection_preference_to_asns`, `Asn` (fillable / casts), `AsnService` (`PREFERENCE_KEYS`, `requestCollection`, `collectionOrigin`, payload keys, `clearCollection`), `views/asns/partials/collection-card` (客户选择的方案 · 客户价 · 选择时间, 来源 客户门户申请 #import, re-confirm note on the edit form), `views/asns/show` (warning badge on 从订单导入货物行 rows), `lang/zh/warehouse.php`. Support (C): `Contracts\InboundService::requestCollection`, `Enums::ASN_COLLECTION_REQUESTED_VIA`. Transport (X2, HANDOFF): `Support/CollectionTailgate.php` (new — the pickup tailgate rule extracted from `ShipmentIntakeService`), `ShipmentIntakeService::fromAsnCollection` (calls it), `ShipmentQuoteRequestFactory` (`buildCollection` through the new public static `collectionSender` / `collectionRequest`, behaviour unchanged), `TransportOptionService::clientPreference` (reads `asns.collection_preference` for a shipment with `asn_id`). Orders (X1, HANDOFF): `OrderImportService::recordInboundCollection` (new), `OrderInboundService` (`generate` → `requestCollection` in the same transaction, preference read server side from the import, `awaitingAsn` +`collection_requested`, `collectionRequests`, public static `asnLineDescription`), `AsnOrderService` (docblock), `OrderInboundController` (store rules, `portalSubmissions` +`collection`), `views/inbound/index` (到仓方式 column / badge, data-* on 选中并填入, 到仓方式 fieldset + JS, error block), `lang/zh/orders.php`. Portal (X1, HANDOFF): `PortalTransportEstimate` (`SNAPSHOT` public, `collectionOptions`, `snapshot`, public static `defaultWarehouse`), `PortalTransportQuotes::forAsnCollection` (new), `PortalInboundImportController` (create / store / show / confirm), `PortalAsnController::show` (+`collectionQuotes`), `PortalQuoteController::confirmCollection` (new), `routes.php` (+1), views `asns/imports/{create,show,index}`, `asns/partials/collection`, `asns/show` (status line), `lang/zh/portal.php`. Tests: `tests/Feature/Portal/PortalCollectionRequestTest.php`, `tests/Feature/Orders/InboundCollectionReviewTest.php`, `tests/Feature/Transport/ClientChosenCollectionPlanTest.php` (new), `tests/Support/StubCarrierAdapter.php` + `PortalCollectionFlow.php` (new helpers). Docs: HANDOFF.md, MERGELOG.md, docs/demo-guide.zh.md, ERP_PLAN §5.3 note | **Upload:** 到仓方式 card on 上传入库清单 — 我们自己送到仓库 (default) \| 需要你们上门提货 with 送达仓库 (hidden when only one active warehouse; default = the client's latest ASN warehouse, else the first active by code), 提货联系人 / 电话 / 地址 / 城区 / 州 / 邮编 (4 digits) / 地址类型, 可提货日期 (≥ today), 提货备注; the fieldset is hidden AND disabled otherwise and the server drops those fields (`exclude_unless`); stored at `order_imports.errors.context.inbound.collection` {warehouse_id, address, ready_date, notes} — no migration on Orders. **Preview:** 上门提货申请 card (pickup, contact, ready date, warehouse, packages derived per row: qty = 箱数, 单件重量 = line total ÷ 箱数, dims; rows without all dimensions named as not priced) and, while pending, the plans (carrier, service level, ETA, 客户价, 推荐 / 最低价 / 最快; recommended pre-ticked) inside the confirm form; no plan → the reason + 客服建立预报单后会确认运费, confirm still allowed. The estimate request is built by the SAME code as Transport's `buildCollection` for the ASN customer service will generate from these rows (pickup → warehouse, the rows as the ASN goods lines they become incl. description, zone = pickup postcode, `CollectionTailgate`), so it equals it key for key except `description` (asserted). **Confirm:** a plan key is required when plans exist; the server re-prices and trusts only the key (unknown → 提货方案或价格已更新，请重新选择, nothing imported); the snapshot (SNAPSHOT keys + chosen_at / chosen_by, never cost / markup) and the packages are recorded (`recordInboundCollection`, row-locked, pending only) before the orders are created. After confirm: 您选择的方案 + 客户价, or 待客服确认运费; 已提交清单 badge 申请上门提货. **待建预报:** 客户申请上门提货 badge, suburb / state / postcode, ready date, chosen plan + 客户价; 选中并填入 sets 我方上门提货 and fills every pickup field, the warehouse and `collection_import_id` (staff may edit). Generate with we_collect → `InboundService::requestCollection` inside the generate transaction: packages [] (Transport prices the ASN goods lines), `client_preference` read server side from the import (portal, same client, imported — else `orders.inbound.errors.collection_import_invalid`), `requested_via` client + `import_id`; without an import `requested_via` staff, no preference. Any refusal (address incomplete, ready date past, nothing priceable, warehouse without address) rolls the whole generation back: no ASN, orders untouched, Chinese error, input kept. **Transport:** a shipment with `asn_id` takes `asns.collection_preference` as the reference: same option within `variance_tolerance_percent` → selected as the client (still-active chooser) else system, dated the request; beyond it → `quoted` awaiting re-confirmation; a re-request re-runs it; a staff edit on the ASN page keeps the preference. **Portal 预报入库:** when the collection shipment is `quoted` with nothing selected and the client had chosen → 提货运费有变化，请确认方案 (carrier, service level, ETA, 客户价, expiry, 您当时的选择 badge, 确认此方案 per row; expired rows not confirmable); once confirmed → plan + 客户价 + 确认方 (客户 / 客服 / 系统). **ASN page:** 客户选择的方案 · 客户价 · 选择时间, 来源 客户门户申请 #import (link), a re-confirmation note on the edit form; 从订单导入货物行 rows from such imports carry 客户申请了上门提货，导入后请在到仓方式中设置 (`awaitingAsn` +`collection_requested`). No Billing change (the #124 TR-* rules bill it on the ASN's Job). Server steps after merge: deploy runs `migrate --force` (one migration); no seeder. **Review fixes (2026-09-15):** a re-request compares against the latest explicit decision on the shipment (the client's portal re-confirmation or a staff confirmation), not the upload-time plan, and automatic confirmations never move that reference; the portal re-confirm route 404s for a staff-requested collection; 待建预报 refuses orders the import did not create (`collection_import_orders`) and carries the plan only when the ASN takes all of the import's orders; the portal refuses a collection request with no row carrying weight + L/W/H (`errors.no_items`), which Warehouse could not collect by | Accepted 2026-09-15 (lead) |
| 126 | 2026-09-15 | C (Warehouse, Billing, Support, contracts own; Orders / Portal (X1) zones as integrator) | Lead request 2026-09-15 (Jimmy): expensive goods go on the bottom rack level (less likely to be knocked), storage there costs more, the supervisor still chooses the putaway location, the client side declares it. Lead answers: (1) two tiers only — `standard` (default) and `bottom`; locations also record the rack level so a middle tier can be added later; (2) the surcharge arises only when the pallet's REQUIRED tier is `bottom` AND the location it occupies is `bottom`; (3) pallets only — loose carton units are never tier-checked or tier-charged; (4) declared goods value may PRE-FILL `bottom` (per-client threshold on the rate card) and never prices anything — the stored declaration is what counts; (5) no dedicated / reserved locations; (6) a partly picked pallet still in a bottom location keeps paying the full weekly surcharge — the supervisor gets a list to consolidate; (7) the surcharge is 10 %–20 % above the pallet's normal storage price, following the Melbourne or Sydney warehouse's price standard; (8) no tier check on moves and no mid-week calculation — the last-snapshot-of-the-week rule stands, MoveService untouched. **Three plan rules amended on the lead's instruction:** ERP_PLAN §3 Q3 (declared unit value may pre-fill the storage tier; still never priced), §4.1.10 (上架建议: one hint only — the first free bottom location for a declared-bottom pallet), §4.3 rule 3 (one putaway rule check: a declared-bottom good pallet into a non-bottom storage location needs a reason) | db-schema.md (`locations` +storage_tier +rack_level; `order_lines` +storage_tier +storage_tier_source; `asn_lines` +storage_tier +storage_tier_source; `stock_units` +required_storage_tier +storage_tier_override_reason; `stock_snapshots` +location_storage_tier +required_storage_tier; `rate_items` +warehouse_id), enums.md §4 (storage tier, tier source), charge-codes.md §3 (+`WH-STORAGE-TIER-PLT-WK`) and §7 (`min_percent`, `max_percent`, `tier_value_threshold_cents`), services.md §4 (context `warehouse_id`, `storage_tier`; warehouse specificity) and §8 (Row `storage_tier`, `storage_tier_declared`), events.md `snapshot.weekly` units[] (documentation, additive), routes.md (bulk locations, ASN line tier, stock filter, putaway reason, portal stock / template). Support: `Enums::STORAGE_TIERS` / `STORAGE_TIER_SOURCES`, `RuleViolation::langKey()`, `Contracts\ManifestParser` Row doc. Warehouse: migrations `2026_09_15_100000_add_storage_tier_to_locations`, `2026_09_15_100010_add_storage_tier_to_stock_tables`; `Location` (LogsActivity), `AsnLine`, `StockUnit`, `StockSnapshot`; `LocationController` (store + `bulk`), `PutawayService` (`?string $reason`, `tierMismatch` / `standardIntoBottom`), `PutawayController` (hint, reason input), `StockController` (`bottom_leftover`), `SnapshotService` (tier columns, summary counts), `ReceivingService` / `ReturnService` (required tier), `AsnService` (tier normalised in `addLines`, order-line copy, `setLineStorageTier`), `AsnImportService`, `AsnController` (`storeLine` tier, `updateStorageTier`), `routes.php`, views `locations/index`, `labels/locations`, `putaway/index`, `stock/index`, `snapshots/index`, `asns/show`; `WarehouseSeeder` (A-*-01 bottom level 1, -02..-04 levels 2..4); `lang/zh/warehouse.php`, `lang/en/pdf.php` (`location_label.level`, `storage_tiers`). Billing: migration `2026_09_15_100030_add_warehouse_to_rate_items`; `RateItem`; `RateService::matchItem` (warehouse +16; a context without `warehouse_id` never matches a warehouse row — review 2026-09-15, so `thresholds()` and callers without a warehouse read the all-warehouse row); `StorageBillingService` (warehouse_id on every weekly line — storage, quarantine, rental, carton / CBM, pickface — and the surcharge); `RateCardController` + `rate_cards/show` (warehouse select / column, percent band check); `BillingSeeder` (code, documentation rule, `TIER_ITEM` added idempotently to the active / draft standard cards); `lang/zh/billing.php`. Orders (X1, HANDOFF): migration `2026_09_15_100020_add_storage_tier_to_order_lines`, `OrderLine`, `OrderCreationService` line whitelist, `OrderImportService` (+`RateService`, `storageTiers()` pre-fill), `SpreadsheetManifestParser` (header aliases, `STORAGE_TIER_VALUES`, `convertRow`), `OrderInboundService::linePayload`, `views/imports/show` (存储等级 column), `lang/zh/orders.php`. Portal (X1, HANDOFF): `PortalInboundImportController` (template column + sample cells, `tierSurcharge`), `PortalStockController` (`l.storage_tier`), views `asns/imports/create` (hint), `asns/imports/show` (column + surcharge line), `stock/index` (column), `lang/zh/portal.php`. Tests: `tests/Feature/Warehouse/LocationTierTest.php`, `PutawayTierTest.php`, `tests/Feature/Billing/StorageTierBillingTest.php`, `tests/Feature/Orders/DeclaredStorageTierTest.php` (new); `BillingPagesTest` standard card items 34 → 35. Docs: HANDOFF.md, MERGELOG.md, docs/demo-guide.zh.md, ERP_PLAN §3 Q3 / §4.1.10 / §4.3 rule 3 / §6.2 notes | **Accepted 2026-09-15 (lead).** v1 boundaries: no location reservation; no tier check on MoveService, stocktake or restore from quarantine; no middle tier (rack_level is recorded only); cartons, pickface and quarantine never tier-checked or charged; the surcharge is priced on the same pallet's base storage charge of the same week under the same key `unit:{id}:week:{week}` — a missing or POA base passes no `base_cents`, so the surcharge is POA / needs review, never a priced $0; snapshot rows written before this CR (NULL tiers) never produce a surcharge; the value pre-fill needs `tier_value_threshold_cents` on the client's card (no default); client changes to a declared tier go through customer service (staff ASN line tier edit — the portal stays read-only). **Card versioning:** BillingSeeder adds ONE line for the new code (percent 10, band 10–20, all warehouses) to each standard card still active or draft that has no line for it; no existing rate, charge snapshot or superseded version changes; keyed on (card, code) so a re-run adds nothing; the next 复制为新版本 copies it; per-warehouse rows (MEL / SYD) are added by Finance through a new version + approval. Server steps after merge: deploy runs `migrate --force` (four migrations); run `php artisan db:seed --class="App\Modules\Billing\Seeders\BillingSeeder"` once by hand; the supervisor marks bottom locations for MEL and SYD (库位配置 → 批量设置层位 / 存储等级) |
| 128 | 2026-09-17 | C (contracts / docs own; Orders / Portal (X1) zones as integrator) | Lead request 2026-09-17 (Jimmy): 「只建立了CSV自动导入, 不能从客户订单界面手动导入」— a manual path next to the CSV upload; decision **以订单为准**: when an already-placed order is brought into a manual list, the ORDER's consignee / goods data is the truth — the list only adds the inbound context (柜号 / 柜型 / 预计到港 / 参考号 / 备注) and, if ticked, the 上门提货 request. Approved proposal: (1) Portal 预报入库 gets 手工建立入库清单 — rows typed on the page (the CSV template's fields), add / remove rows; (2) the same page ticks the client's existing orders that still await an ASN and brings them into the list (order data wins); (3) inbound context + optional 上门提货 with plan choice exactly as the CSV upload (#125); (4) submit → the SAME preview → confirm as the CSV: typed rows become orders, ticked orders are attached to the same submission; customer service sees ONE 待建预报 card, 选中并填入 ticks all, collection review / auto-confirm unchanged; (5) drafts | services.md §8 (`ManifestParser::fromRows`, additive), routes.md (Portal row: `portal.asns.imports.manual.{create,store,edit}` + `action`), enums.md (`order_imports.status` +`draft`), db-schema.md (`order_imports` source `portal`, audit keys `context.manual`, `result.attached`). Support (C): `Contracts\ManifestParser::fromRows`, `Fakes\FakeManifestParser::fromRows`. Orders (X1, HANDOFF): `SpreadsheetManifestParser` (`FORM_FIELDS`, `fromRows`, `carryDown` / `scalar` extracted), `OrderImportService` (constructor +`OrderInboundService`; `preview()` refactored onto the shared private `record()`; new `previewRows`, `saveDraft`, `attachableOrders`; `confirm()` re-checks and row-locks the attached ids and writes `result.attached`, status imported when created ∪ attached ≠ ∅; job note for a manual list), `Models/OrderImport` (`orderIds`, `isManual`, `manualAttachedIds`), `OrderInboundService` (`collectionRequests` and the collection subset rule read `orderIds()`), `OrderInboundController::portalSubmissions` (`orderIds()`, `manual`, `attached_nos`), `OrderImportController::show` (+`attachedOrders`), views `inbound/index` (来源 / 文件名 column, attached orders under the card), `imports/index` (手工录入), `imports/show` (typed rows + attached orders read-only, draft note), `lang/zh/orders.php`. Portal (X1, HANDOFF): `routes.php` (+3), `PortalInboundImportController` (`manualCreate` / `manualEdit` / `manualStore`, shared `inboundRules` / `collectionRules($draft)` / `submissionContext` / `collectionContext`, `show` / `confirm` with attached orders and `estimateRows`), `PortalTransportEstimate::collectionOptions` (a row `label` travels to its package / unpriced entry), views `asns/imports/manual` (new), `partials/{context-fields,collection-fields,manual-rows,manual-row}` (new; `create` now includes the first two — no behaviour change), `asns/imports/{index,show}`, `asns/index`, `lang/zh/portal.php`. Tests: `tests/Feature/Portal/PortalManualInboundTest.php`, `tests/Feature/Orders/InboundManualSubmissionReviewTest.php` (new). Docs: HANDOFF.md, MERGELOG.md, docs/demo-guide.zh.md, ERP_PLAN §3 note | **Accepted 2026-09-17 (lead).** **Form:** `GET /portal/asns/imports/manual` (or `?draft={id}` — the client's own draft, else 404) — rows table (唛头, 中文品名, 英文品名, 包装类型, 箱数, 单件重量 helper, 重量 (kg, 整行合计), 长 / 宽 / 高 (mm), 收件人, 电话, 地址, 城区, 州, 邮编, FBA 参考号, 存储等级, 要求送达日 optional; add / remove by JS, numbered by position), 带入已有订单 (checkbox list of `OrderImportService::attachableOrders` = the client's 待建预报 candidates minus orders already in ANY other portal submission — created / attached by a confirmed one, or ticked on a draft / pending manual list — with the note 以订单信息为准，清单只补入库信息), the inbound context and the #125 到仓方式 fieldset (shared partials), buttons 保存草稿 / 提交预览 (`action`). **Preview (`action = preview`):** at least one row or one ticked order; every row through `ManifestParser::fromRows` — exactly parse()'s normalisation and validation (carry-down of blank consignee cells under the same 唛头, duplicate-row warning, phone / postcode / state / package / date / 存储等级 rules), Chinese `第 N 行「列名」…` messages keyed `rows.{i}.{column}` back on the form with the input kept and the cell marked; then `OrderImportService::previewRows` — the same pipeline as the upload (storage tiers, `groups()` with sha256 = hash of the normalised rows + sorted attached ids, no duplicate-file warning), audit +`context.manual = {rows, attached_order_ids}`, `original_name` null, `document_id` null; a ticked id that is not attachable is refused in Chinese by order number (or by id when it is not the client's) and nothing is stored. The preview page shows 手工录入 instead of a file name (no download), the typed groups as today, and the attached orders in a separate read-only block built from the ORDER (order no, 唛头, consignee, address, FBA, lines with cartons / weight / dims, requested date, status) — no form fields; the collection estimate prices the typed rows PLUS the attached orders' unlinked lines shaped like import rows (`label` = order no in the packages table), so it equals what Transport prices on the ASN. **Confirm:** unchanged for typed groups (one loose Job); the attached ids are row-locked and re-checked inside the transaction (still the client's, still waiting for an ASN, in no other submission — else the whole confirm rolls back with a Chinese message) and recorded as `result.attached = [{order_id, order_no}]`; status imported when created ∪ attached ≠ ∅; the collection packages recorded by `recordInboundCollection` include the attached orders' lines; the flash counts both. **以订单为准 enforced by construction:** no code path reads an attached order into a group or writes to it — the list carries its id only (`context.manual.attached_order_ids` → `result.attached`); posting consignee fields for it changes nothing (`PortalManualInboundTest::test_typed_rows_and_an_attached_order_preview_from_the_order_and_confirm_never_changes_the_attached_order` compares the order's and its lines' full attribute sets before / after preview and confirm). **Drafts (`action = draft`):** rows, ticks and context kept as posted under `status = draft` (format rules only, nothing required, no parsing, no groups / result); `GET /portal/asns/imports/{import}/edit` reopens the client's own draft (else 404) with rows, ticks and context filled and the pickup fieldset open; the submitted draft is the SAME row moved to pending; `show` of a draft redirects to the form; listing pages label it 草稿 (继续编辑 link in the portal). **Staff:** `OrderImport::orderIds()` = created ∪ attached is what 待建预报 (`portalSubmissions`), `collectionRequests` / `awaitingAsn.collection_requested`, the collection subset rule (whole-list check over created + attached) and the portal index / show / flash read — one card per submission, 来源 手工录入 / 文件名, both order sets named, 选中并填入 ticks all; `orders.imports.show` shows the typed rows (raw) and the attached orders read-only. Tenancy: `order_imports` global scope + owner check (draft / submission of another client → 404), attached ids checked against the client's own orders (another client's id → 订单 #:id 不存在或不属于贵公司). **No migration, no seeder:** `draft` is a new value of the existing `status` column; everything else lives in `order_imports.errors`. Server steps after merge: deploy runs `migrate --force` (nothing to migrate) |
| 129 | 2026-09-17 | C (contracts / docs own; Orders / Portal (X1) zones as integrator) | Lead request 2026-09-17 (Jimmy): clients cannot choose where their goods are stored when they place an order. Approved proposal (「按你的建议来做」): the client chooses the STORAGE TIER (标准 \| 底层) per goods line when placing an order — never a physical bin; the supervisor still picks the location at putaway (#126 hint / refusal unchanged). The same field goes onto the staff manual order form, the order API and the draft line add / edit, and the order pages show it | No migration (`order_lines.storage_tier` / `storage_tier_source` exist since #126) and no service contract change; enums.md unchanged (values exist). Orders (X1, HANDOFF): `views/partials/goods-line-row` (存储等级 select after 包装类型, shared by the portal and staff forms, `old()` kept), `views/partials/goods-lines` (header cell, footer colspan 12 → 13 / 10 → 11, one hint line under the table: what the tier means + the client's own 底层 percent per warehouse when priced), new `views/partials/storage-tier-select`; `Http/OrderFormRows::withStorageTierSource()` (`LINE_CONTENT` unchanged — a tier alone is not content, the spare row stays pruned); `OrderController` (rule `lines.*.storage_tier` nullable in `Enums::STORAGE_TIERS`, source `staff`, `tierSurcharge` priced server side for the client the form came back with — no JS pricing), `OrderLineController` (same rule on add / edit, source `staff`; a request without the field leaves the line's tier alone; the existing timeline line note covers the change), `OrderApiController` (rule, source `client`; a body without the field carries no tier and no source), `views/show` (存储等级 column: badge 底层 warn / 标准 muted + source 客户选择 / 员工设置 / 按单价预选; the select on the line add / edit forms; editor colspan 7 → 8), `views/api-tokens/index` (JSON example +`storage_tier`, one hint line), `lang/zh/orders.php` (`lines.storage_tier`, `lines.storage_tiers`, `lines.storage_tier_sources`, `lines.storage_tier_hint`, `lines.storage_tier_surcharge`, validation attributes / messages for `lines.*.storage_tier` and `storage_tier`, `api.storage_tier_hint`). Portal (X1, HANDOFF): new `Services/PortalStorageTier` (`surchargeByWarehouse(int $clientId): array<code, "10%">` — the same rule as #126's private inbound helper: customer price through `RateService::price`, percent mode only, missing / POA / min-charge rows skipped; static `summary()` for display), `PortalOrderController` (rule, source `client` on preview and store, `tierSurcharge` view key), `views/orders/show` (存储等级 column, badge only), `lang/zh/portal.php` (`fields.storage_tier`). routes.md (one row: portal preview / store, staff store, draft line add / edit, API accept `storage_tier`). Tests: `tests/Feature/Portal/PortalOrderStorageTierTest.php`, `tests/Feature/Orders/OrderStorageTierTest.php` (new). Docs: HANDOFF 2026-09-17, MERGELOG, demo-guide FAQ 客户下单时怎么选底层, ERP_PLAN §3 Q3 note | **Accepted 2026-09-17 (lead).** Decision: a tier, not a bin — the client never names a location; the supervisor's putaway choice, the #126 refusal rule and the weekly surcharge are unchanged (order line → ASN line → stock unit already worked once `order_lines.storage_tier` was set). Source `client` from the portal form and the API (the 标准 the client leaves selected is still the client's declaration, exactly as an explicit 标准 cell on the inbound list), `staff` from the manual form and the draft line forms; an API body without the field carries no tier and no source (standard on the ASN, as before). The form hint shows only the client's own percent (never cost / margin) and only when its card prices `WH-STORAGE-TIER-PLT-WK` as a percent; the staff form prices it for the selected client on a server-side re-render only. The order-time 估价 has no storage line (unchanged: the surcharge arises from the weekly snapshot). `PortalInboundImportController::tierSurcharge()` stays as it is (that controller is in CR #128's diff) and can switch to `PortalStorageTier` in a later CR. No server step after deploy |
| 130 | 2026-09-17 | C (contracts / docs own; Transport (X2) zone as integrator; one role token each in Orders (X1) `views/show` and Platform `jobs/show`) | Lead decision 2026-09-17 (Jimmy): drivers (role `transport_operator`) must no longer plan their own work. Until now they could create delivery runs for any driver, add and reorder stops, see every run, open 承运商对账 (carrier invoices) and the shipment board / shipment pages that show carrier cost and quotes. Industry practice: the dispatcher plans, the driver executes | No migration, no seeder, no enum / event / service change. Permission matrix (before → after): `runs.create` / `runs.store` / `runs.stops.store` / `runs.stops.reorder` admin \| customer_service \| dispatcher \| transport_operator → admin \| customer_service \| dispatcher; `runs.index` / `runs.show` same four roles → same four roles, but a transport_operator without a planner role sees only runs whose `driver_id` is itself (another driver's run → 404) and the run page is read-only (no add-stop / reorder forms, no 新建班次, shipment numbers as text); `transport.index` (board), `shipments.show`, `shipments.consignment-note` any signed-in staff → every staff role except transport_operator (`ShipmentController::VIEWER_ROLES`); `shipments.label`, `shipments.book`, `shipments.pod.store`, `shipments.extra-charges.store`, `shipments.quotes.manual`, `shipments.redelivery.store`, `shipments.quotes.select` admin \| customer_service \| dispatcher \| transport_operator → admin \| customer_service \| dispatcher; `shipments.own-fleet-cost.store` admin \| dispatcher \| transport_operator \| finance → admin \| dispatcher \| finance; `orders.margin` admin \| customer_service \| dispatcher \| transport_operator \| finance → admin \| customer_service \| dispatcher \| finance; `carrier-invoices.*` admin \| transport_operator \| finance → admin \| finance; `driver`, `driver.stops.deliver`, `driver.stops.fail` transport_operator (own stops) — unchanged. Enforced in the controllers (`RequiredRoles`; the Transport routes file uses no role middleware). Views: nav include hides 运输 and 承运商对账 for transport_operator (班次 → own runs, 司机任务 stay); `runs/index` hides 新建班次, `runs/show` wraps the add-stop form, the reorder form / sequence inputs / save button and the shipment links in `@role('admin\|customer_service\|dispatcher')`; `shipments/show` drops transport_operator from its existing `@role` lists; Orders `views/show` and Platform `jobs/show` stop offering the 收付毛利 / shipment links to the driver (no 403 behind a click). Tests: `tests/Feature/Transport/DriverPermissionsTest.php` (new, 4 tests); the existing Transport tests that planned runs or acted on shipments as a transport_operator now use a dispatcher (the run's driver stays a transport_operator). Docs: routes.md Transport row, HANDOFF 2026-09-17 司机权限收紧, MERGELOG, demo-guide role table + page map + FAQ | **Accepted 2026-09-17 (lead).** The driver executes: driver page + own runs read-only; everything that plans, prices, books, costs or reconciles stays with admin \| customer_service \| dispatcher (finance keeps exactly the read / cost access it had). Left as is, on purpose: warehouse_supervisor / warehouse_operator keep their read access to the board and the shipment page (Warehouse's ASN collection card and the Job page link there); 承运商对账 stays admin \| finance — the dispatcher was never on it and the 2026-09-10 nav audit keeps it off the dispatcher's menu (add `dispatcher` to `CarrierInvoiceController` + the nav include if the lead wants the transport office reconciling); no route middleware added because the Transport routes file has none. Deploy only — no migration, no seeder, no manual step |
| 134 | 2026-09-22 | C (own zones only: MasterData, Billing model, Platform registration, contracts / docs) | Audit item A13 (GAP-01): clients created by staff through `GET /admin/clients/create` → `POST /admin/clients` were saved without `standard_rate_card_id` (only the self-registration path and the BillingSeeder back-fill bound the standard card), so every charge for such a client fell to Missing Rate. Lead decision 2026-09-22 (Jimmy): "remove the staff 'new client' feature altogether; clients are created ONLY through self-registration (/register, RegistrationController, which already binds the standard rate card and creates the pending client + its login), and staff approve, edit and manage them afterwards." | routes.md (MasterData: two routes removed, `masterdata.clients.bind_standard_card` added); `app/Modules/MasterData` (routes, ClientController, `views/clients/index` + `form` — edit only, `Models/Client` `creating` hook + `standardRateCard()` / `activeOwnRateCard()`), `app/Modules/Billing/Models/RateCard` (`activeStandard()` / `activeStandardId()`), `app/Modules/Platform/Http/Controllers/RegistrationController` (uses the helper), `lang/zh/masterdata.php`, `tests/Support/CreatesUsers.php`; no migration, no seeder change (the BillingSeeder back-fill stays), no enum / event / service-contract change | Remove `masterdata.clients.create` / `.store` and the 新建客户 button; the clients index explains that companies register themselves at `/register` and appear as 待审核 (links the registration page while `erp.allow_signup` is true — the config default; when it is false the hint says signup is closed and how to reopen it); the client form becomes edit-only and shows read-only 标准价目表 name · version and 专属价目表 有 / 无, with a warning + admin-only 修复 (bind the active standard card) when the standard card is null — the same 修复 sits on the index row. Safety net: `Client::creating` fills a null `standard_rate_card_id` with the active standard card on EVERY create path (registration, seeders, tests); a client created before any standard card exists (MasterDataSeeder runs before BillingSeeder) is still back-filled by the seeder. | Decided as requested. `clients.status` keeps `pending → active` via approve / edit; suppliers and carriers keep their staff create forms (they never carry a rate card). The test helper `CreatesUsers::client(['standard_rate_card_id' => null])` still yields an unpriced client for the Missing Rate tests (unbinds after create). `docs/acceptance/2026-09-10-blocking-bug-audit.md` keeps its historical `/admin/clients/create` step as written. |
| 133 | 2026-09-22 | C (contracts / docs own; Transport (X2) zone as integrator, branch `fix/audit-transport`) | Audit blockers A7 (TMS-01) and A8 (TMS-02), lead approval 2026-09-22. (A7) A delivery run could not be corrected: a stop could be added but never removed, date / driver / vehicle never changed, a run never cancelled, and the driver could act only while `run_date` was exactly today — so a wrong shipment on the truck, a sick driver, a run created for the wrong day (the form defaults to today and accepts any date) or two drops left at midnight locked those shipments to a dead run with no UI path (`delivery_run_id` was only ever set, never nulled; peer TMS let the allocator unassign a consignment or roll a run). (A8) Quotes expire 24 h after creation and were produced only by the event consumers (order confirmed / packed / ASN collection); an expired quote still read 待选择 with `expires_at` shown nowhere and no re-quote action anywhere, so a shipment packed on Friday had no selectable option on Monday — own fleet became impossible for it (人工报价 only takes manual services) | No migration, no seeder, no enum / event / service-contract change (`delivery_runs.status` `cancelled` and `transport_quotes.status` `expired` already exist; nothing new is published — a stop leaving a run informs no other module, see HANDOFF 2026-09-22). routes.md: +4 Transport routes (`runs.update`, `runs.stops.destroy`, `runs.cancel`, `shipments.requote`, planner roles per #130). Rules: 移出班次 for a pending stop of an open (planned / dispatched) run — the shipment keeps `booked` with no run and is re-plannable, the remaining stops renumbered, a dispatched run with nothing open completes; 修改班次 (date / driver / vehicle, run number unchanged) and 取消班次 (releases every pending stop, status `cancelled`) only while no stop is delivered — a failed stop stays as the record of the attempt and its shipment goes through 重新派送 as before; the driver acts on any of its planned / dispatched runs dated on or before today until the run completes (all stops delivered / failed → `completed`, as before), `/driver` lists them oldest first with an earlier date flagged. 重新报价 (`ShipmentRequoteService`, Transport-internal): quoting \| quoted \| quote_confirmed-not-booked, inbound collections included; expired `quoted` rows → `expired`, a confirmed final selection → `requoted` with the shipment back to `quoted`, then `TransportOptionService::quote()` for the shipment's latest stage (final once a final quote exists, for a collection or once packed; else preliminary; no quote at all → pickup_deliver final, from_stock preliminary) re-runs the same client-preference / variance rule as the event path, so a client's chosen option within tolerance is confirmed again in its name and a re-confirmation publishes `shipment.quote_confirmed` again (Billing's unique key means no second freight charge — unchanged from the packed re-quote path). Quotes table: 有效至 dd/mm HH:mm, 已过期 badge for dead rows; selecting an expired quote stays refused. Activity log: `delivery_run` 修改班次 / 移出班次 / 取消班次 and `shipment` 重新报价 |
| 131 | 2026-09-22 | C (Warehouse, contracts own) | Audit 2026-09-22 INBOUND-01 / INBOUND-14 (lead approval 2026-09-22): the unit-label barcode — Code 128 of the 24-character `label_code` — measured 147 mm (the -DMG code 152 mm) on a 100 × 150 mm label whose printable width is 88 mm, so the bars ran off the page and nothing could scan it; a 13-character location code (94 mm) clipped too, and location labels printed a blank page after every label (138 mm + 20 mm padding > 138 mm printable). The unit label's largest line was the location at print time (the receiving dock), Chinese in 品名 rendered as boxes, and the declared 底层 tier was not printed | **Approved.** (1) The barcode encodes a short scan token — `U<stock_unit_id>` on a unit label, `L<location_id>` on a location label (`App\Modules\Warehouse\Services\ScanCodes`; ≤ 65 mm for any id below a trillion) — while the printed text keeps the full `label_code` / `full_code`. (2) Every scan input accepts BOTH forms, case-insensitively: 扫码 page (`ScanResolver`), 上架 `location_code`, 移库 / 解除隔离 `location_code`, 盘点 open `location_code` and count `code` — via the model scopes `StockUnit::scanCode()` / `Location::scanCode()` (services.md §12, routes.md Warehouse row). (3) Location labels: `.label` 116 mm + 20 mm padding = one page each. (4) Unit label content, English only (lead instruction 全英文, no CJK font): largest line = consignment mark + carton count ("M2 · 10 ctn"), a boxed **BOTTOM LEVEL** when `required_storage_tier = bottom`, the description only when it has no CJK characters (else omitted), the location as small print "Received at …" (`lang/en/pdf.php` `unit_label.received_at`). `LabelService::barcodeWidthPx()` + `PRINTABLE_WIDTH_PX` let `LabelScanCodeTest` assert the fit for the longest possible token and that PDF page count = label count. No migration, no seeder; labels already printed keep scanning by their full code |
| 138 | 2026-09-22 | C (Warehouse own) | Audit 2026-09-22 blockers INBOUND-02 / INBOUND-03 / INBOUND-08 / OUTBOUND-01 / OUTBOUND-02 (lead approval 2026-09-22): a received ASN line could be received again (double stock); an ASN with a line received as 0 never reached `putaway` (no putaway / label charges, no 生成派送订单); 登记拆柜 from the ASN page saved the devanning task without its container (no WH-DEVAN-* fee, no exception); the pack form was six fixed rows; a cancelled order could still be released, picked, packed and dispatched and its pick task never closed | **Approved, one new route.** `POST /warehouse/outbound/tasks/{task}/close` (`warehouse.outbound.tasks.close`, admin \| warehouse_supervisor — routes.md row): 关闭任务 on the wave page for the started pick task of a CANCELLED order (task `cancelled`, the order's remaining active reservations released → `stock.released`, wave completes). No other contract change: `ReceivingService::receiveLine` refuses a received line / a non-receivable ASN (`RuleViolation`, existing lang keys), `PutawayService::completeIfDone` is public and uses the 入库单 rule (a line received with 0 counts), also run at 入库完成 and when a line is received with no units (`asn:reevaluate-putaway` for ASNs already stuck — HANDOFF); `POST /warehouse/tasks` requires `container_id` for a devanning task when the ASN has containers; `POST /warehouse/outbound/pack/{fulfilment}` rows carry `qty` (件数, 1–200, expanded server side, ≤ 200 packages per pack); 待释放 / `releaseWave` skip `operational_status = cancelled`, `confirmPick` / `pack` / `dispatch` refuse it, and `OrderCancelledConsumer` also cancels the order's not-yet-started pick tasks. No order note is written back to Orders (no note hook in services.md) |
| 132 | 2026-09-22 | C (Billing own; `contracts/services.md` §4 / §6 / §9 and `charge-codes.md` §6 notes) | Audit 2026-09-22 FIN-03 (lead approval 2026-09-22, "a billing rule C may decide"): a missing rate only raised an exception and returned nothing — the charge row was lost, nothing was replayed when the rate was added later, and neither the unbilled pool nor the draft invoice warned; the demo's first invoice was short $310 of confirmed freight and the review-queue hint claimed 缺费率 rows appear there (true only for manual charges) | `ChargeEngine::charge` (missing rate, non-banded code): raises the `missing_rate` exception as before AND writes the charge row with status `needs_review`, `amount_cents` 0, no rate item / card, `calculation_snapshot_json {missing_rate: true, suggested_cents, exception_id}` under the same idempotency key `(source_activity_id, charge_code, activity_version)`; it stays OUT of the unbilled pool and of every draft (`InvoiceService::unbilled()` takes pending / approved only), so "never $0" holds. `suggested_cents` = the payload's `customer_price_cents` for **`TR-DELIVERY-BASE` only** — on `shipment.quote_confirmed` that price IS the freight line; pre-filling it on TR-FUEL / TR-TAILGATE / TR-REMOTE would suggest the whole freight for a surcharge — null for every other code and event. Same-version replay: a still-unpriced placeholder is re-priced IN PLACE (the key is unique per version): rate now on a card → the same row becomes pending (or POA needs_review) with the priced snapshot + `repriced_from_missing_rate`, and its exception is closed by the system (`resolved_by` null, note `billing.exceptions.missing_rate_priced`); still missing → the row and its open exception stand, no second exception. Higher `activity_version`: the placeholder is marked `reversed` WITHOUT a negative twin (a $0 row never entered the pool) and its exception closed (`missing_rate_superseded`); the new version raises its own. `ChargeEngine::review` (review page, now `?client_id=` filterable, 缺费率 badge, suggested amount pre-filled) → approved + closes the linked exception with the note; `ChargeEngine::reverse` of a placeholder: reversed, no twin. Unbilled pool: per client 「缺费率 N · 待复核 N」 badges → `/admin/exceptions?type=missing_rate&client_id=` / `/billing/charges/review?client_id=`, plus a 「只有未定价费用的客户」 list for clients with no pooled charge; draft invoice page: a warning box above 开出发票 listing needs_review rows and open missing-rate exceptions of the same Job(s) (per-Job draft) or client + period (period drafts) — a warning, not a block. `ExceptionService::resolve` widened to `?int $resolvedBy` (system closes; callers unchanged; a non-hold resolved with a note keeps the note on `message`). Review-queue hint rewritten. Tests: `tests/Feature/Billing/AuditBillingBlockersTest.php` (missing rate → placeholder with suggested cents, not in pool / draft, warning box, review page pre-fill, pricing closes the exception, replay idempotent; add rate + replay heals in place, newer version supersedes without a twin). Re-pinned assertions of the old "no row" behaviour: `ChargeEngineTest` (task 502 → placeholder), `DevanningAllocationTest` (unpriced member / sideloader placeholders counted, pending-only where the old assertion listed cards / keys), `StorageTierBillingTest` (missing base storage rate = placeholder, not null) | **Decided 2026-09-22 (C, billing rule).** Open question for the lead: should unresolved missing-rate / needs_review items BLOCK 开出发票 for that Job / period instead of warning? Implemented as a warning — Finance may legitimately issue what is priced now and add a supplementary invoice later; a block would be one guard in `InvoiceService::issue()` (say the word). No migration, no seeder, no server step |
| 139 | 2026-09-22 | C (frozen zone: `config/erp.php`, `.env.example`; Billing own; `lang/en/pdf.php`) | Audit 2026-09-22 FIN-01 / GAP-04: the tax invoice PDF (stored at issue, client-visible in the portal) named no seller, no ABN, no address, no bank / remittance details and no payment reference — under Australian GST rules the client cannot claim the GST credit and has nowhere to pay; it also had no rate column, printed the internal charge id instead of the source document, and no paid / balance figures | Lead instruction 2026-09-22: PLACEHOLDER company details for now. `config/erp.php` `company` block (`name, abn, address, phone, email, bank_name, bsb, account_no, account_name`) read from `COMPANY_*` in `.env` with built-in defaults ("Demo Logistics Pty Ltd", ABN "12 345 678 901", "1 Warehouse Road, Moorebank NSW 2170", "+61 2 0000 0000", accounts@example.com, "Demo Bank", BSB 000-000, account 12345678, account name "Demo Logistics Pty Ltd") so the trial server needs no configuration; `.env.example` lists the keys with a replace-before-go-live comment. `InvoiceService::issue()` refuses (zh `billing.errors.company_abn_missing`) while `company.abn` is blank (an operator may blank it later), like GoodsReceiptService without the CJK font. PDF (`billing::invoices.pdf`, `InvoiceService::pdfData`): seller top-left with the ABN prominent, TAX INVOICE + Bill to on the right, a meta strip (invoice no / date / due + terms / type · period), a Rate column from `charges.rate_snapshot_cents` (— when the charge has none), the source document after the description instead of `#charge_id` (`InvoiceService::sourceRefs`: order no + mark, ASN no, container no, or the stock unit's ASN + mark — read-only lookups on orders / shipments / warehouse_tasks / asns / containers / physical_containers / stock_units), Less credit notes / Paid / Balance due under the totals whenever anything was credited or paid, and a "How to pay" box (bank, BSB, account no / name, Payment reference = invoice no). Every word from `lang/en/pdf.php` (PdfEnglishTest unchanged). No credit-note PDF exists in the codebase, so there is nothing to add there. Tests: `AuditBillingBlockersTest` (rendered HTML carries the seller block, payment reference, rate, ASN / order / mark references, Paid + Balance due when part-paid; issue refused in Chinese with `config(['erp.company.abn' => ''])`, allowed again once set) | **Accepted 2026-09-22 (lead: placeholders).** Server after merge: none — the defaults apply; before go-live replace every `COMPANY_*` value in `.env`. Invoices issued BEFORE this merge keep their stored PDF (frozen at issue); the staff 下载 PDF button re-renders live, so their client copy would need a re-issue via credit note + new invoice if the seller block matters for them |
| 140 | 2026-09-22 | C (Billing own; Platform `ApprovalController` / `approvals/index` / `jobs/show` link; `contracts/routes.md`, `services.md` §9, `db-schema.md` §6) | Audit 2026-09-22 Part B billing items, lead approval 2026-09-22 ("fix exactly as the report suggested"). **FIN-16** the charges list offered 冲销 on 已开票 rows: `ChargeEngine::reverse` leaves an invoiced original and writes a billable negative twin for the next period, so credit note + 冲销 on the same charge refunded the client twice with no link between the two. **FIN-13** the payment amount box was empty (Finance retyped the 未结 figure shown above it), any amount above the balance was accepted and flipped the invoice to 已收款, and a payment could not be voided (7561.40 typed for 756.14 = SQL). **FIN-08** `RateCardService::updateItem` / `addItem` only checked `status = draft`, so the requester could change any rate between the second person's approval and 生效; the approval centre printed `rate_card #5` / `credit_note #3` with no link and no diff against the live version. **FIN-09** the manual-charge code select had no blank option (silently the alphabetically first code, TR-CARTAGE-20 = $1,230.60 with the amount left blank), `charge_date` was always today (a September labour fee entered on 2 Oct missed the 上月 period), no `?job_id=` prefill and nothing on the Job page led there. **FIN-10** a draft is a snapshot, yet the hint said the opposite; a Job already on an open draft still offered 按此 Job 开票 (a second fragment), the /billing header 待开票 counted drafted rows the pool did not show, and a charge could never be traced to its invoice | **Decided 2026-09-22 (C).** (FIN-16) `charges/index`: new 发票 column (draft number + 草稿中 badge, or the issued number, or —); an invoiced row shows 去开冲减单 → `invoices/{invoice}?credit_line={invoice_line_id}#credit-note`, which opens the credit-note form with that line's remaining amount pre-filled (`InvoiceController::creditedByLine`, also the per-line `max`); a row on a draft shows a hint; 冲销 stays for pooled rows only, and `ChargeController::reverse` refuses a charge with `invoice_line_id` or status `invoiced` (`billing.charges.errors.on_draft` / `invoiced_use_credit_note`) — the engine's negative-twin path is kept for version supersedes only. (FIN-13) migration `2026_09_22_100000_add_void_columns_to_payments` (`payments.note`, `payments.void_of_payment_id` → payments); `InvoiceService::recordPayment` refuses `amount > outstandingCents()` (`billing.errors.payment_exceeds_outstanding`); new `voidPayment(Payment, reason, ?by)` writes the offsetting negative row (same method / reference, `note` = reason) — never a delete — and `applyPaymentTotals()` recomputes paid / status (`paid` → `part_paid` → `issued`) / `is_overdue` from Σ rows for both paths; route `POST /billing/payments/{payment}/void` (`billing.payments.void`, 作废收款 with a required reason on every un-voided receipt; a void row or a second void is refused in Chinese); the amount box pre-fills and caps at the balance. (FIN-08) `RateCardService::lockedForApproval` — `updateItem` / `addItem` refused (`billing.rate_cards.errors.locked_for_approval`) while a `rate_card_change` approval is pending **or approved**; the card page hides the edit / add forms and says so (a rejected / cancelled request unlocks); `previousVersion` + `diffAgainstPrevious` (items matched on code + pallet class + band + zone + service level + warehouse + carrier; rate / minimum / POA / mode / percent / thresholds compared) drive the draft page: 「与 vN 相比:新增 · 修改 · 删除」, 已修改 / 新增 badges, old → new in the cell, a 只看改动 checkbox (inline JS), a table of removed rows; `ApprovalController::subjectLinks` links `rate_card` → the card page and `credit_note` → its invoice (`platform.approvals.subjects.*`). Found on the way and fixed: `newVersion` copied `getAttributes()` through the `array` cast, so every copied version stored `threshold_json` double-encoded and read it back as a string (pick bands, container caps, pallet sizes lost on v2+) — items are now `replicate()`d and `RateItem` uses the tolerant `Casts\ThresholdJson` (decodes a double-encoded row, writes back clean), so rows already broken on the server heal on read without a data fix. (FIN-09) blank first option `billing.charges.select_code` (code required), `<x-date-field name="charge_date">` default today with `max` today, validated `date` ≤ today and passed to `ChargeEngine::manual(..., ?CarbonInterface $chargeDate)`; `GET /billing/charges/manual?job_id=&client_id=` pre-selects the Job (added to the list even when older than the latest 300) and narrows the list to the client; the Job page 费用与利润 header carries 手工加费 for admin / finance (also on a Job with no charge yet). (FIN-10) `InvoiceService::appendJobCharges(draft, jobId)` + route `POST /billing/invoices/{invoice}/append-job/{job}` (`billing.invoices.append_job`); the pool row of a Job on an open non-storage draft shows 「已有草稿 DR-… ($)」 + 并入该草稿 instead of 按此 Job 开票 (`InvoiceController::openDraftsByJob`, per-Job draft preferred over a period draft); the draft page lists 「JOB 另有 N 条新服务费未并入」 + 并入本草稿 and its hint now says a draft is a snapshot; the /billing header reads 待开票 = pending / approved with no invoice line (exactly the pool) plus 草稿中 N. Tests: `tests/Feature/Billing/AuditPartBBillingTest.php` (5, one per item; the FIN-08 test also pins the threshold copy and the legacy heal). No seeder, no enum / event change | **Accepted 2026-09-22 (C, own zone).** Server after merge: deploy runs `migrate --force` (one additive migration on `payments`); nothing else. Open question for the lead: should 冲销 be removed for drafted rows too (it is refused now with a hint to 放弃草稿) or should a draft line be removable one by one? Not built — say the word |
