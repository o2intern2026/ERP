# contracts/routes.md — URL prefixes per module and seat

`routes/web.php` (frozen zone) does nothing except `require` each module's `routes.php`. Every module's `routes.php` must wrap **all** of its routes in the URL prefix(es) and route-name prefix below. `tests/Feature/Platform/RoutesContractTest.php` fails the build if any registered route falls outside this table.

| Module | Seat | URL prefix(es) | Route name prefix | Blade view namespace | lang file |
|---|---|---|---|---|---|
| Platform | C | `/` (redirects to where login lands: `/jobs`, `/driver` for a driver-only user, `/portal` for a client — CR #137), `/login`, `/register`, `/logout`, `/account/**` (the signed-in user's own password, CR #137), `/admin/**` (users, roles, exceptions, documents, search, approvals, audit, integration monitor), `/jobs/**` (Job workbench) | `platform.` | `platform::` | `lang/zh/platform.php` |
| MasterData | C | `/admin/clients/**`, `/admin/suppliers/**`, `/admin/carriers/**` | `masterdata.` | `masterdata::` | `lang/zh/masterdata.php` |
| Warehouse | C | `/warehouse/**` | `warehouse.` | `warehouse::` | `lang/zh/warehouse.php` |
| Billing | C | `/billing/**` | `billing.` | `billing::` | `lang/zh/billing.php` |
| Orders | X1 | `/orders/**` | `orders.` | `orders::` | `lang/zh/orders.php` |
| Portal | X1 | `/portal/**` | `portal.` | `portal::` | `lang/zh/portal.php` |
| Reports | X1 | `/reports/**` | `reports.` | `reports::` | `lang/zh/reports.php` |
| Transport | X2 | `/transport/**`, `/driver/**` (driver mobile pages) | `transport.` | `transport::` | `lang/zh/transport.php` |

## Rules
- Middleware: every route except `/login`, `/register` and `/logout` sits behind `auth`. The global client scope (M1/A1) is enforced in the data layer, not per route.
- Client-role users may reach only `/portal/**`, `/logout`, `/account/**` (own password, CR #137) and the client views Portal links to; enforced server side (M1), never by hiding links. A client user whose client is not `active` is signed out by the client scope on the next request (CR #137, audit GAP-03).
- `/admin/**` is Platform's namespace; MasterData owns exactly the three sub-paths above and nothing else under `/admin`.
- Unauthenticated pages: `/login` and `/register` (client self-registration, tester feedback #8 / CHANGE_REQUESTS #89; hidden when `erp.allow_signup` is false). The only API route in phase 1 is A4b's `POST /orders/api/orders` (X1, bearer token from `order_api_tokens`, `Idempotency-Key` header; no session, CSRF or client scope — the token names the client). It sits under the Orders prefix so the prefix rule holds (CHANGE_REQUESTS #39).
- Nav: `resources/views/layouts/nav.blade.php` includes `layouts/nav/<module>.blade.php` for every module; each module edits only its own include.

## M0 placeholders (one per module; Feature test in `tests/Feature/<Module>/`)
| Module | Route | Name | View |
|---|---|---|---|
| Platform | `GET /jobs` | `platform.index` | `platform::index` |
| MasterData | `GET /admin/clients` | `masterdata.index` | `masterdata::index` |
| Warehouse | `GET /warehouse` | `warehouse.index` | `warehouse::index` |
| Billing | `GET /billing` | `billing.index` | `billing::index` |
| Orders | `GET /orders` | `orders.index` | `orders::index` |
| Portal | `GET /portal` | `portal.index` | `portal::index` |
| Portal | `GET /portal/reports` (client view of A21, client users only — ClientScope confines them to `/portal/**`, CHANGE_REQUESTS #52) | `portal.reports.index` | `reports::client` |
| Orders | `POST /orders/{order}/estimate` (A7b 估价 → Billing QuoteService) | `orders.estimate` | — |
| Portal | `POST /portal/orders/{order}/estimate` (client estimate, 客户价 only) | `portal.orders.estimate` | — |
| Portal | `POST /portal/orders/{order}/quotes/{quote}/confirm` (client confirms the final transport quote → QuoteSelectionService, §5.7 #2) | `portal.orders.quotes.confirm` | — |
| Orders | `GET /orders/addresses/suggest?q=&client_id=` (JSON address suggestions from the client's address book + past orders) | `orders.addresses.suggest` | — |
| Portal | `GET /portal/addresses/suggest?q=` (same, for the signed-in client) | `portal.addresses.suggest` | — |
| Portal | `POST /portal/orders/preview` (获取估价: validates and prices the unsaved order form, nothing persisted; the form then offers 确认提交订单 → `portal.orders.store`; tester feedback #10, CHANGE_REQUESTS #88) | `portal.orders.preview` | `portal::orders.create` |
| Portal | `POST /portal/orders/{order}/cancel` (stage 1: client cancels a received / confirmed / allocated order directly → OrderChangeService::cancelByClient, same `order.cancelled` event) and `POST /portal/orders/{order}/cancel-request` (stage 2: picking / packed → `cancel_request` exception for the coordinator) — CHANGE_REQUESTS #111 | `portal.orders.cancel`, `portal.orders.cancel_request` | `portal::orders.show` |
| Orders | `POST /orders/{order}/cancel-request/reject` (coordinator declines the client's request; reason shown in the portal) | `orders.cancel_request.reject` | — |
| Orders | `GET /orders/requests` (客户请求 inbox: open cancel requests + portal return requests + 30-day history; staff roles admin \| customer_service \| dispatcher \| warehouse_supervisor \| finance; CHANGE_REQUESTS #112) | `orders.requests.index` | `orders::requests.index` |
| Orders | `GET /orders/inbound`, `POST /orders/inbound` (从订单生成预报单, CHANGE_REQUESTS #117: worklist of from_stock orders whose goods have no ASN line yet, grouped by client; a picked set + warehouse / inbound type / container / ETA → OrderInboundService::generate → Warehouse InboundService::createAsnFromOrderLines; orders merged into the first order's Job, emptied Jobs cancelled via JobService::cancelIfEmpty; roles admin / customer_service / warehouse_supervisor. CHANGE_REQUESTS #125: the portal-submission card flags 客户申请上门提货 (pickup suburb / state / postcode, ready date, the client's chosen plan + 客户价) and 选中并填入 also fills the 到仓方式 fields; `POST` accepts `inbound_transport` client_delivers (default) \| we_collect with `collection[name\|phone\|address\|suburb\|state\|postcode\|type]`, `collection_ready_date`, `collection_notes`, `collection_import_id` (all dropped unless we_collect; name / address / suburb / state / postcode 4 digits / ready date required_if we_collect) → `InboundService::requestCollection` in the same transaction — the client's plan is read server side from that import (a portal import of the same client, status imported, else `orders.inbound.errors.collection_import_invalid`); every ticked order must have been created by that import (else `orders.inbound.errors.collection_import_orders`), and the plan is carried only when the ASN takes ALL of the import's orders — a partial generation keeps `requested_via` client + the import id but no preference (review 2026-09-15); any refusal rolls the generation back, errors under `inbound`) | `orders.inbound.index`, `orders.inbound.store` | `orders::inbound.index` |
| Platform | `GET /register`, `POST /register` (client self-registration → Client `pending` + inactive client-role user; tester feedback #8, CHANGE_REQUESTS #89) | `platform.register`, `platform.register.store` | `platform::auth.register` |
| MasterData | `POST /admin/clients/{client}/approve` (pending → active and activates the client's users; admin \| customer_service \| finance) | `masterdata.clients.approve` | — |
| Platform | CHANGE_REQUESTS #137 (audit Part B platform items, lead approval 2026-09-22) — **new** `GET /account/password`, `PUT /account/password` (修改密码 for the signed-in user: `current_password`, `password` + confirmation, `Password::min(8)`; `auth` + client scope, every role incl. client; linked from the user name in the nav). **Changed** `GET /` (`platform.home`): no longer a fixed redirect to `/jobs` — a guest → `/jobs` (→ `/login`), a client user → `/portal`, a user whose only staff role is `transport_operator` → `/driver`, other staff → `/jobs`; `POST /login` lands the same way (`LoginController::landingUrl`), is throttled 5 attempts / minute per email + IP (Chinese refusal, `RateLimiter`), and answers a right password on a deactivated login — or a login whose client is `inactive` — with 账号已停用，请联系客服 instead of 密码错误 (audit GAP-03). **Role middleware added:** `jobs.create` / `jobs.store` → `role:admin\|customer_service\|dispatcher\|warehouse_supervisor` (the 新建 Job button follows); `documents.store` / `documents.visibility` → `role:admin\|customer_service\|finance\|warehouse_supervisor` (the upload form and the 客户可见 buttons follow; `documents.index` / `documents.download` stay open to every staff role). `JobService::summarize` drops `estimated_cost_cents` / `actual_cost_cents` / `margin_cents` / `margin_is_estimate` for a signed-in user outside `JobService::COST_ROLES` (admin \| finance \| customer_service \| dispatcher — OrderMarginController's list); the Job page hides the cost card, the invoices panel and the charges panel for other roles. **Form change** `POST /admin/documents`: fields `file`, `type`, `document_no` (ONE 单号 — `JOB-` / `ASN-` / `ORD-` / `SHP-` prefix or an invoice number, resolved server side by `DocumentNumberResolver` to `related_type` / `related_id` / `job_id` / `client_id`; unknown → Chinese refusal, nothing stored), `client_visible`, optional `back=1` (return to the posting page); the old `related_type` / `related_id` / `job_id` / `client_id` inputs are gone. `GET /admin/documents` gains `job_no=` (the Job page's 打开单据中心 link passes it). Partial `platform::documents.upload-details` (`['documentNo' => …]`) renders the upload `<details>` on the Job page and, via one `@include` in Orders `views/show` (HANDOFF 2026-09-22), on the order page. **Admin banner + command** (audit ADMIN-12): `layouts/partials/health` (frozen zone) shows admins on every page when the oldest `pending` outbox event is older than `config('erp.outbox_stale_minutes')` (default 10, env `OUTBOX_STALE_MINUTES`; `HealthCheck::staleBanner`, cached 60 s); `php artisan erp:health [--json]` prints pending count, oldest age, failed / dead, last `stock_snapshots.snapshot_date`, `queue_jobs` size and exits 1 when stale or dead > 0 | `platform.password.edit`, `platform.password.update` (new); `platform.home`, `platform.login.store`, `platform.jobs.create`, `platform.jobs.store`, `platform.documents.index`, `platform.documents.store`, `platform.documents.visibility` (changed) | `platform::auth.password` (new), `platform::documents.upload-details` (new partial), `platform::documents.index`, `platform::jobs.show`, `platform::jobs.index`, `layouts.partials.health` |
| MasterData | CHANGE_REQUESTS #137 (audit GAP-03): `PUT /admin/clients/{client}` — when `status` changes to `inactive` every user of that client is set `is_active = false` in the same transaction (symmetrical to approve / the existing `active` branch, which reactivates them); the edit form explains it next to the status select. No new route | `masterdata.clients.update` (behaviour) | `masterdata::clients.form` |
| MasterData | CHANGE_REQUESTS #134 (lead decision 2026-09-22, audit A13 / GAP-01): `GET /admin/clients/create` and `POST /admin/clients` (`masterdata.clients.create` / `masterdata.clients.store`, the staff 新建客户 form) are **removed** — clients are created only by self-registration at `/register` (standard rate card bound, status `pending`); staff approve (`clients.approve`), edit (`clients.edit` / `clients.update`) and manage them afterwards, and `Client::creating` binds the active standard card on every remaining create path. New `POST /admin/clients/{client}/bind-standard-card` (修复: binds the active standard card to a client whose `standard_rate_card_id` is null; **admin only**, offered on the clients index row and the edit page; refused with a message while no standard card is active) | `masterdata.clients.bind_standard_card` | — |
| Portal | `GET /portal/reports/export/{table}` (CSV of the client report) | `portal.reports.export` | — |
| Portal | `GET /portal/invoices`, `GET /portal/invoices/{invoice}/download` (own issued invoices, PDF via DocumentDownloader) | `portal.invoices.index`, `portal.invoices.download` | `portal::invoices.index` |
| Portal | `GET /portal/stock` (own stock, read-only; shows the declared 存储等级 per goods line — never location codes or prices, CHANGE_REQUESTS #126) | `portal.stock.index` | `portal::stock.index` |
| Portal | `GET /portal/asns`, `GET /portal/asns/{asn}` (预报入库, read-only: the client's own ASNs with progress, goods lines, packing-list parse results and the 入库单 PDFs via DocumentDownloader; CHANGE_REQUESTS #116 → #117 — clients no longer create ASNs) | `portal.asns.index`, `portal.asns.show` | `portal::asns.index`, `portal::asns.show` |
| Portal | `GET /portal/asns/imports` (已提交清单: the client's own uploads with rows / status / orders / ASN once generated), `GET /portal/asns/imports/create` (上传入库清单 form: file `.csv .xlsx .xls` + optional 柜号 / 柜型 / 预计到港日 / 参考号 / 备注), `GET /portal/asns/imports/template` (CSV template, UTF-8 with BOM, the Chinese headers the parser accepts, two sample rows), `POST /portal/asns/imports` (→ `OrderImportService::preview`, source `portal`, no Job yet, inbound context on the import), `GET /portal/asns/imports/{import}` (preview: every mapped field per 唛头, hard errors with row + column, warnings, blocked groups; after confirm the order numbers and the 客服建预报单 note), `POST /portal/asns/imports/{import}/confirm` (all ready groups → the client's orders, source `portal`, status received, ONE loose Job per submission; no address-book save). Client-role only (ClientScope + controller check); `order_imports` client scope + owner check → another client's import is a 404. The orders then wait in Orders' 待建预报 (`orders.inbound.index`, which shows the submitted 柜号 / ETA and pre-fills the ASN header) — CHANGE_REQUESTS #123. CHANGE_REQUESTS #125: `store` accepts `inbound_transport` client_delivers (default) \| we_collect with `warehouse_id` (active), `collection[name\|phone\|address\|suburb\|state\|postcode\|type]`, `collection_ready_date` (≥ today), `collection_notes` (every pickup field required_if we_collect, dropped otherwise) → `errors.context.inbound.collection`; `show` lists the collection plans (客户价 only, `PortalTransportEstimate::collectionOptions`) and the packages derived from the rows; `confirm` requires `collection_choice` (a plan key) when plans exist — re-priced server side, an unknown key is refused with 提货方案或价格已更新，请重新选择 — and records the snapshot + packages through `OrderImportService::recordInboundCollection` before creating the orders; a collection request whose ready rows have no row with cartons + weight + length / width / height (reason `no_items`) is refused (`portal.inbound.collection.errors.no_items`, no confirm button, nothing imported — the client re-uploads) because Warehouse could not collect by anything (review 2026-09-15) | `portal.asns.imports.index`, `.create`, `.template`, `.store`, `.show`, `.confirm` | `portal::asns.imports.index`, `.create`, `.show` |
| Portal | `POST /portal/asns/{asn}/collection/quotes/{quote}/confirm` (客户重新确认提货方案, CHANGE_REQUESTS #125: the client confirms a final quote of its own ASN's `inbound_collection` shipment when the price moved beyond the tolerance of the plan it chose — ASN through the client-scoped model and client-requested with a plan (`asns.collection_requested_via` client and `collection_preference` set; a staff-requested collection is a 404 — review 2026-09-15), quote must be `final` on a shipment with `asn_id` = the ASN, `shipment_type` inbound_collection, `client_id` = the user's client, else 404 → `QuoteSelectionService::select(…, 'client', userId)`; client users only (staff 403); redirect `portal.asns.show#collection`, refusals under `quote`) | `portal.asns.collection.quotes.confirm` | — (redirect to `portal.asns.show`) |
| Portal | CHANGE_REQUESTS #128 手工建立入库清单 (以订单为准): `GET /portal/asns/imports/manual` (the manual form — typed rows table, 带入已有订单 checkbox list of `OrderImportService::attachableOrders`, the inbound context and the #125 到仓方式 fieldset; `?draft={id}` reopens the client's own draft, else 404), `POST /portal/asns/imports/manual` (`action` = `draft` \| `preview`; fields `draft_id` (own draft, else 404), `rows[i][consignment_mark \| description_cn \| description_en \| package_type \| carton_qty \| actual_weight_kg \| length_mm \| width_mm \| height_mm \| deliver_to_name \| deliver_to_phone \| deliver_to_address \| deliver_to_suburb \| deliver_to_state \| deliver_to_postcode \| fba_reference \| storage_tier \| requested_date]`, `attached_order_ids[]`, the #123 context fields and the #125 collection fields (required_if we_collect for `preview`, format-only for `draft`); `draft` → `OrderImportService::saveDraft` (status `draft`, redirect to `.manual.edit`), `preview` → at least one row or ticked order, rows through `ManifestParser::fromRows` (row errors `rows.{i}.{column}` in Chinese back on the form, input kept), then `OrderImportService::previewRows` (a non-attachable id is refused in Chinese, nothing stored) → redirect `portal.asns.imports.show`), `GET /portal/asns/imports/{import}/edit` (continue a draft — own, status draft, else 404). `GET /portal/asns/imports/{import}` for a manual list: 手工录入 instead of a file name, no download, the attached orders in a read-only block from the ORDER, the collection estimate over typed rows + attached orders' lines; a draft redirects to `.manual.edit`. `POST …/confirm` unchanged in shape: confirms when ready groups or attached orders exist; the attached ids are re-checked under a row lock (refusal in Chinese, nothing created) and recorded as `result.attached`; the flash counts created + attached. Index: 来源 / 文件名 column (手工录入), drafts with 继续编辑, attached orders in the 订单 column. Entry buttons 手工建立入库清单 on `portal.asns.index` and `portal.asns.imports.index`. Client-role only; `order_imports` client scope + owner check | `portal.asns.imports.manual.create`, `.manual.store`, `.manual.edit` (+ `.show`, `.confirm`, `.index` behaviour) | `portal::asns.imports.manual`, `portal::asns.imports.partials.{context-fields,collection-fields,manual-rows,manual-row}` (the first two also used by `.create`), `.show`, `.index` |
| Portal | CHANGE_REQUESTS #126 on the #123 import pages: `GET /portal/asns/imports/template` adds a 存储等级 column (sample rows 标准 / 底层) and `GET /portal/asns/imports/create` one hint line; `GET /portal/asns/imports/{import}` shows a 存储等级 column per row (value + 按单价预选 badge for a `value_rule` pre-fill) and, when the client's card prices `WH-STORAGE-TIER-PLT-WK` as a percent, one line 底层库位：比标准仓储高 X% per warehouse (the client's own percent, never cost); `GET /portal/stock` shows the declared tier per row (no location codes). No new route | `portal.asns.imports.template`, `.create`, `.show`, `portal.stock.index` | `portal::asns.imports.create`, `.show`, `portal::stock.index` |
| Portal | CHANGE_REQUESTS #136 (audit PORTAL-05 / PORTAL-07) on the #123 / #128 import pages, no new route. **Skipped rows need a second confirmation:** `GET /portal/asns/imports/{import}` — a 唛头 with a row the parser refused is `blocked` (唛头 X 的第 N 行未读入…, `error_rows` on the group); when any row of the list will not become an order (refused, or a blocked / duplicate / ASN-matched mark) the button reads 只提交可生成的 N 张（跳过 M 行） with a required tick 我已知道跳过的行不会生成订单; `POST /portal/asns/imports/{import}/confirm` then requires `skip_acknowledged=1` (else refused in Chinese under `skip_acknowledged`, nothing created). The staff page mirrors it: `POST /orders/imports/{import}/confirm` requires `skip_acknowledged=1` under the same condition, its button reads 只生成可生成的 N 张（跳过 M 行）. **地址类型:** `GET /portal/asns/imports/template` gains the column 地址类型 after 邮编 (three sample rows: FBA / 商业 / 住宅) and `.create` one hint line; `POST /portal/asns/imports/manual` accepts `rows[i][deliver_to_address_type]` = `business` \| `fba` \| `residential` \| empty (a select per row, empty = 自动判断); the preview shows the type per mark (a residential mark carries the badge 住宅 · 按尾板车规则处理) and the order is created with the group's `deliver_to_address_type` (column → address book → FBA reference → business), so OMS-13 sets `tailgate_required` on a residential consignee exactly as for a typed order | `portal.asns.imports.show`, `.confirm`, `.template`, `.create`, `.manual.create`, `.manual.store`; `orders.imports.show`, `.confirm` | `portal::asns.imports.show`, `.create`, `.partials.manual-row`, `.partials.manual-rows`; `orders::imports.show` |
| Reports | `GET /reports` (boss view, admin \| finance), `GET /reports/client?client_id=` (staff client view), CSV exports | `reports.index`, `reports.client`, `reports.*.export` | `reports::index`, `reports::client` |
| Reports | `GET /reports` | `reports.index` | `reports::index` |
| Transport | `GET /transport` and `GET /driver` | `transport.index`, `transport.driver` | `transport::index`, `transport::driver` |
| Transport | CHANGE_REQUESTS #130 (lead 2026-09-17: the dispatcher plans, the driver executes) — roles per route, enforced in the controllers through `RequiredRoles` (the Transport routes file carries no role middleware; a refused role gets the 403 page listing who may): `GET /transport` (shipment board), `GET /transport/{shipment}`, `GET /transport/{shipment}/consignment-note` — every staff role except the driver (admin \| customer_service \| dispatcher \| warehouse_supervisor \| warehouse_operator \| finance, `ShipmentController::VIEWER_ROLES`); `GET /transport/{shipment}/label`, `POST /transport/{shipment}/book`, `POST /transport/{shipment}/pod`, `POST /transport/{shipment}/extra-charges`, `POST /transport/{shipment}/quotes/manual`, `POST /transport/{shipment}/redelivery`, `POST /transport/{shipment}/quotes/{quote}/select`, `GET /transport/runs/create`, `POST /transport/runs`, `POST /transport/runs/{run}/stops`, `PATCH /transport/runs/{run}/stops/order` — admin \| customer_service \| dispatcher; `POST /transport/{shipment}/own-fleet-cost` — admin \| dispatcher \| finance; `GET /transport/orders/{order}/margin` — admin \| customer_service \| dispatcher \| finance; `/transport/carrier-invoices/**` (index, store, show, differences CSV) — admin \| finance; `GET /transport/runs`, `GET /transport/runs/{run}` — admin \| customer_service \| dispatcher see every run, transport_operator only runs whose `driver_id` is the signed-in user (another driver's run is a 404, not a 403; the run page is read-only for the driver — no add-stop / reorder forms, shipment numbers as text); `/driver/**` — transport_operator only, own stops only (unchanged). Nav include: 运输 and 承运商对账 hidden for transport_operator, 班次 stays (own runs), 司机任务 stays | `transport.*` (see `app/Modules/Transport/routes.php`) | — |
| Transport | CHANGE_REQUESTS #133 (audit 2026-09-22, blockers A7 TMS-01 / A8 TMS-02; lead approval 2026-09-22) — `PATCH /transport/runs/{run}` (修改班次: `run_date` `Y-m-d`, `driver_id` an active transport_operator, `vehicle`; only while the run is planned / dispatched and no stop is delivered; the run number keeps the date it was created with), `DELETE /transport/runs/{run}/stops/{stop}` (移出班次: a pending stop of an open run leaves it — the run_stop row is deleted, `shipments.delivery_run_id` → null, the shipment stays `booked` and is offered on other runs again, the remaining stops are renumbered 1..n; a dispatched run with nothing left open completes; a stop of another run is a 404; a delivered / failed stop is refused), `POST /transport/runs/{run}/cancel` (取消班次: every pending stop released the same way, status `cancelled`; refused once a stop was delivered, or when completed / cancelled), `POST /transport/{shipment}/requote` (重新报价: status quoting \| quoted \| quote_confirmed — not booked — an inbound collection (#124) included; expired `quoted` rows → `expired`, a confirmed-but-unbooked final selection → `requoted` with the shipment back to `quoted`, then `TransportOptionService::quote($shipment, <latest stage>)` re-runs the client-preference / variance rule exactly as the event path does; no automatic option → the manual_transport exception and a Chinese refusal). Roles admin \| customer_service \| dispatcher (the driver executes only, #130); refusals in Chinese under `run` / `driver_id` / `stop` / `requote`; activity log `delivery_run` (修改班次 / 移出班次 / 取消班次, causer = the planner) and `shipment` (重新报价). Driver side: `GET /driver` lists the driver's planned / dispatched runs dated on or before today, oldest first, an earlier date flagged; `driver.stops.deliver` / `fail` accept a stop of any such run (was: today only), and a run whose stops are all delivered / failed completes as before. Shipment page: 有效至 (dd/mm HH:mm) column, a dead quote reads 已过期 instead of 待选择 (selecting it stays refused) | `transport.runs.update`, `transport.runs.stops.destroy`, `transport.runs.cancel`, `transport.shipments.requote` | — (redirect to the run page / back to the shipment page) |
| Transport | CHANGE_REQUESTS #135 (audit 2026-09-22 Part B — TMS-03 / CS-19, TMS-07, TMS-09, TMS-10, TMS-11, TMS-12, GAP-05; lead approval 2026-09-22) — no new route; field and behaviour changes on existing ones. **Money in dollars** (was cents): `POST /transport/{shipment}/quotes/manual` takes `cost` + `customer_price` (AUD, `numeric`, ≤ 2 decimals, ≥ 0.01; were `cost_cents` / `customer_price_cents`), `POST /transport/{shipment}/own-fleet-cost` takes `actual_cost` (was `cost_cents`), `POST /transport/{shipment}/extra-charges` takes `carrier_cost` (nullable; was `cost_cents`), `POST /transport/carrier-invoices` takes `total` (was `total_cents`); the controllers convert with `Money::fromDecimal` and every flash echoes the parsed amount (客户价 $185.50) — rows, services and event payloads stay integer cents. **Extra charges** (`POST /transport/{shipment}/extra-charges`): only while the shipment is `booked` \| `dispatched` \| `in_transit` \| `delivered` \| `failed` (`ExtraChargeService::REPORTABLE_STATUSES`, Chinese refusal otherwise and no form before booking); every report writes a `shipment_extra_charges` row with the `delivery.extra_charge` `event_id` in the same transaction and is listed under the form; the same `charge_type` reported before on the shipment is refused unless `confirm_repeat=1` (确认再次上报) is posted. **Carrier POD** (`POST /transport/{shipment}/pod`): +`delivered_at` (required, `<x-date-field time>` value `Y-m-d\TH:i` or `Y-m-d`, not after now, not before the shipment was created) → `pods.delivered_at`, `shipments.delivered_at` and the `delivery.pod_captured` `delivered_at`; `pod_file` accepts `pdf,jpg,jpeg,png` (content-sniffed: `%PDF-`, JPEG SOI, PNG signature) and is archived as the `pod` document with its own mime. **Driver** (`POST /driver/stops/{stop}/fail`): +`failure_note` (≤ 1000, required by the service when `failure_reason = other`), +`photos[]` (0–5, same rules as the POD photos, stored on the same path as `photo` documents, ids in `pods.photo_document_ids`); `fail` and `deliver` refuse a stop that is already `failed` (Chinese, names reason + time) or `delivered`; the driver page shows the stop status badge on every card and the failed state (reason, time, note, photo count) instead of the forms; posts carry `stop_id` so `old()` restores only that stop's name / signature / note; photos are downscaled on the phone (1600 px JPEG 0.8) by the inline script before the post. **Redelivery** (`POST /transport/{shipment}/redelivery`): refused once a redelivery exists (`shipments.redelivery_of_shipment_id` = this id; the page links it instead of the button); number `<原单号>-R1` / `-R2`… (root = the number without any `-R<n>`); redirect flashes `redelivery_hint` = the original id and the new page links the original's `#extra-charges` form for TR-REDELIVERY. **Consignment note** (`GET /transport/{shipment}/consignment-note`): From / Deliver to for EVERY shipment (`ShipmentQuoteRequestFactory::parties`: live order / ASN + warehouse data, the quote snapshot filling gaps), order number instead of the id, tracking number, delivery instructions, tailgate flag, "Received in good order — Name / Signature / Date" box; strings in `lang/en/pdf.php` only | `transport.shipments.quotes.manual`, `transport.shipments.own-fleet-cost.store`, `transport.shipments.extra-charges.store`, `transport.carrier-invoices.store`, `transport.shipments.pod.store`, `transport.driver.stops.fail`, `transport.driver.stops.deliver`, `transport.shipments.redelivery.store`, `transport.shipments.consignment-note` (all existing) | — |
| Warehouse | `GET /warehouse/receiving` (待收货 worklist: every ASN line still awaiting receipt; admin \| warehouse_supervisor \| warehouse_operator \| customer_service — the 收货 button only for the three warehouse roles; CHANGE_REQUESTS #90) | `warehouse.receiving.index` | `warehouse::receiving.index` |
| Warehouse | `GET /warehouse/receiving/unplanned`, `POST /warehouse/receiving/unplanned` (无预报收货: one screen → unplanned ASN + lines + units + completed 入库单; admin \| warehouse_supervisor \| warehouse_operator; #91) | `warehouse.receiving.unplanned.form`, `warehouse.receiving.unplanned.store` | `warehouse::receiving.unplanned` |
| Warehouse | `GET /warehouse/asns/{asn}/receive`, `POST /warehouse/asns/{asn}/receive` (手动填写入库单: every not-yet-received line of the ASN on one screen → GoodsReceiptService::receiveLines, optional 入库完成 in the same submit; tester feedback #4 2026-09-10, CHANGE_REQUESTS #94) | `warehouse.receiving.bulk_form`, `warehouse.receiving.bulk_store` | `warehouse::receiving.bulk` |
| Warehouse | `GET /warehouse/asns/{asn}/lines/{line}/delivery`, `POST /warehouse/asns/{asn}/lines/{line}/delivery` (编辑收件信息: the consignee fields 从预报单生成派送订单 needs, one goods line at a time, optionally copied to the ASN's other lines under the same mark → AsnService::updateLineDelivery; refused once the line is on an order; roles admin / warehouse_supervisor / warehouse_operator / customer_service; lead request 2026-09-11, CHANGE_REQUESTS #115) | `warehouse.asns.lines.delivery.edit`, `warehouse.asns.lines.delivery.update` | `warehouse::asns.delivery` |
| Warehouse | `POST /warehouse/asns/{asn}/confirm-client` (确认客户预报: customer service signs off a portal / API-submitted ASN → AsnService::confirmClientSubmission; roles admin / customer_service; a flag for the screens and the portal, never a gate on receiving or putaway; `GET /warehouse/asns?pending=1` lists the ones still waiting; CHANGE_REQUESTS #116) | `warehouse.asns.confirm_client` | — (redirect back) |
| Warehouse | `POST /warehouse/asns/{asn}/import-orders` (从订单导入货物行: on a booked / arrived / receiving ASN the staff tick the client's pending from_stock orders — received / confirmed, ≥ 1 goods line without an ASN line, listed by OrderService::awaitingAsn — and OrderService::attachOrdersToAsn → InboundService::addOrderLinesToAsn adds one goods line per order line with the order's consignee / phone / address / suburb / state / postcode / FBA / mark / description / cartons, linked both ways; the orders are merged into the ASN's Job and the emptied Jobs cancelled; form `order_ids[]` (integers, min 1); errors under `import_orders`; roles admin / customer_service / warehouse_supervisor; the manual add-line form on the same page is the collapsed exception; lead request 2026-09-11, CHANGE_REQUESTS #119) | `warehouse.asns.import_orders` | — (redirect to `warehouse.asns.show`) |
| Warehouse | `PUT /warehouse/asns/{asn}/collection` (到仓方式 我方上门提货, CHANGE_REQUESTS #124: request or re-request the collection — pickup address `collection[name\|phone\|address\|suburb\|state\|postcode\|type]`, `collection_ready_date` ≥ today, `collection_notes`, `collection_packages[][package_type\|qty\|weight_kg\|length_mm\|width_mm\|height_mm]` (required unless the ASN has goods lines with weight + dims) → `AsnService::setCollection` → `asn.collection_requested`; only while the ASN is `booked` and the collection is not yet booked by Transport; errors under `collection`) and `DELETE /warehouse/asns/{asn}/collection` (改为客户自送 → `AsnService::clearCollection` → `asn.collection_cancelled`; refused once booked — the dispatcher cancels on the shipment page). The create form (`POST /warehouse/asns`) accepts the same fields with `inbound_transport = we_collect`. Roles admin \| customer_service \| warehouse_supervisor | `warehouse.asns.collection.update`, `warehouse.asns.collection.destroy` | — (redirect to `warehouse.asns.show#collection`) |
| Warehouse | `POST /warehouse/tasks/{task}/cancel` (cancels a hand-made 作业登记 record without a billing event; system tasks pick/pack/load/return_inspection refuse) | `warehouse.tasks.cancel` | — |
| Warehouse | `GET /warehouse/receipts` (入库单 list with client / status / date filters), `GET /warehouse/receipts/{receipt}` (page with the ASN roll-up); admin \| warehouse_supervisor \| warehouse_operator \| customer_service \| finance (#90) | `warehouse.receipts.index`, `warehouse.receipts.show` | `warehouse::receipts.index`, `warehouse::receipts.show` |
| Warehouse | `GET /warehouse/receipts/{receipt}/pdf` (rendered live from the DB, open → 草稿 label, `application/pdf` inline `{receipt_no}.pdf`; the file stored at completion is what the document centre / portal serve) | `warehouse.receipts.pdf` | `warehouse::receipts.pdf` |
| Warehouse | `POST /warehouse/receipts/{receipt}/complete` (入库完成, optional `notes`; open receipt with ≥ 1 line; admin \| warehouse_supervisor \| warehouse_operator) | `warehouse.receipts.complete` | — |
| Warehouse | `GET /warehouse/physical-containers` (物理柜 / 拼柜 list: filters container_no / status / consolidation / sideloader, current warehouse), `GET /warehouse/physical-containers/create` (pre-fillable from the ASN page's 关联物理柜 shortcut: container_no, warehouse_id, size, unpack_mode, gross_weight_kg, eta_date), `POST /warehouse/physical-containers` (→ PhysicalContainerService::create), `GET /warehouse/physical-containers/{box}` (header, members with quantities and shares, the unlinked-row picker `?q=`, the box's tasks, the allocated charges per Job for admin \| finance \| customer_service \| dispatcher); roles admin \| customer_service \| warehouse_supervisor; **never reachable from the portal** — CHANGE_REQUESTS #122 | `warehouse.physical_containers.index`, `.create`, `.store`, `.show` | `warehouse::physical_containers.index`, `.create`, `.show` |
| Warehouse | `POST /warehouse/physical-containers/{box}/link` (`container_ids[]` → link), `POST /warehouse/physical-containers/{box}/unlink/{container}` (refused once devanned), `POST /warehouse/physical-containers/{box}/arrive` (登记到港 → `physical_container.arrived`), `POST /warehouse/physical-containers/{box}/recompute` (重算分摊 → activity_version + 1); errors under `link` / `arrive` / `recompute`; same roles (#122) | `warehouse.physical_containers.link`, `.unlink`, `.arrive`, `.recompute` | — (redirect to `.show`) |
| Warehouse | `POST /warehouse/tasks` now also accepts `physical_container_id` (xor `asn_id` / `order_id`; `task_type` must be `devanning` → PhysicalContainerService::registerDevanning, the ONE box task, redirect to the box page) and refuses a devanning task on a container row that is linked to a box (`warehouse.tasks.container_linked_use_box`) or a `container_id` that is not the ASN's (`container_not_on_asn`); `GET /warehouse/tasks/create?physical_container_id=` pre-selects the box (#122) | `warehouse.tasks.store`, `warehouse.tasks.create` | `warehouse::tasks.create` |
| Warehouse | `POST /warehouse/config/locations/bulk` (批量设置层位 / 存储等级, CHANGE_REQUESTS #126: `warehouse_id` required; optional filters `zone`, `aisle_from` / `aisle_to`, `bin_from` / `bin_to` — numeric codes compare as numbers; sets `set_rack_level` and / or `set_storage_tier` on that warehouse's STORAGE locations only; every change logged; flash names changed / matched; roles admin \| warehouse_supervisor). `POST /warehouse/config/locations` also accepts `rack_level` and `storage_tier` (tier kept only for type storage) | `warehouse.locations.bulk` | — (redirect back) |
| Warehouse | `PATCH /warehouse/asns/{asn}/lines/{line}/storage-tier` (存储等级 of one goods line: `storage_tier` standard \| bottom → `AsnService::setLineStorageTier`, source staff, the line's existing stock units get the same `required_storage_tier`, activitylog `asn_line`; roles admin \| customer_service \| warehouse_supervisor; `POST /warehouse/asns/{asn}/lines` accepts `storage_tier` too — #126) | `warehouse.asns.lines.storage_tier.update` | — (redirect to `warehouse.asns.show#lines`) |
| Warehouse | Label barcodes and scan inputs (CHANGE_REQUESTS #131): `GET /warehouse/labels/units?ids[]=` / `GET /warehouse/labels/asn/{asn}` encode `U<stock_unit_id>` in the barcode, `GET /warehouse/labels/locations?warehouse_id=&type=` encodes `L<location_id>`; the printed text keeps the full code. Every scan input accepts the token OR the full code, any case: `GET /warehouse/scan/resolve?code=`, `POST /warehouse/putaway/{unit}` `location_code`, `POST /warehouse/stock/{unit}/move` and `…/restore` `location_code`, `POST /warehouse/stocktakes` `location_code`, `POST /warehouse/stocktakes/{stocktake}/scan` `code` (services.md §12) | `warehouse.labels.units`, `.asn`, `.locations`, `warehouse.scan.resolve`, `warehouse.putaway.store`, `warehouse.stock.move`, `.restore`, `warehouse.stocktakes.store`, `.scan` | `warehouse::labels.units`, `.locations` |
| Warehouse | `POST /warehouse/outbound/tasks/{task}/close` (关闭任务, CHANGE_REQUESTS #138: the started pick task of a CANCELLED order → `OutboundService::closeCancelledTask` — task `cancelled`, remaining active reservations of the order released + `stock.released`, wave completed when nothing else is open; refuses a live order (`warehouse.outbound.errors.order_not_cancelled`), a non-pick task, a done / cancelled task); roles admin \| warehouse_supervisor (route middleware — the operator gets 403). Also #138: `POST /warehouse/outbound/pack/{fulfilment}` rows accept `packages[i][qty]` (1–200, expanded server side into that many Package rows, ≤ 200 per pack); `POST /warehouse/tasks` refuses a devanning task without `container_id` when the ASN has containers (`warehouse.tasks.container_required`); `GET /warehouse/tasks/create?asn_id=&container_id=&task_type=` preselects the container (the ASN's only unlinked one by default) | `warehouse.outbound.tasks.close`, `warehouse.outbound.pack`, `warehouse.tasks.store`, `warehouse.tasks.create` | `warehouse::outbound.wave`, `warehouse::outbound.pack` (+ `_package-row`), `warehouse::tasks.create` |
| Warehouse | `GET /warehouse?bottom_leftover=1` (底层库位剩货托盘 card on the stock list: pallet units in a bottom storage location holding fewer cartons than their first `receipt` ledger movement, lowest remaining share first); `POST /warehouse/putaway/{unit}` accepts `tier_reason` (required when a declared-bottom good pallet goes to a non-bottom storage location — refused otherwise with `warehouse.putaway.errors.tier_mismatch`, the row re-opens with the reason input) — #126 | `warehouse.index`, `warehouse.putaway.store` | `warehouse::stock.index`, `warehouse::putaway.index` |
| Warehouse | Audit 2026-09-22 Part B warehouse items (CR #141). New routes: `PATCH /warehouse/stock/{unit}` (`warehouse.stock.update`, admin \| warehouse_supervisor — 修改单元信息: `pallet_source`, `length_mm` / `width_mm` / `height_mm`, `weight_kg`, optional `pallet_class` (blank = re-suggested by `RateService::suggestPalletClass`), `reason` required → `UnitCorrectionService::correct`, activitylog `stock_unit` / `unit_corrected`; refused for a carton unit's pallet fields and when nothing changes); `GET /warehouse/physical-containers/{box}/edit` + `PUT /warehouse/physical-containers/{box}` (every header field while `arrived_at` is null and nothing was emitted; the warehouse only while no member is linked; same duplicate rules as create → `PhysicalContainerService::update`), `DELETE /warehouse/physical-containers/{box}` (only with no members, no non-cancelled task, nothing emitted → `::delete`), `GET /warehouse/physical-containers/{box}/arrive` (`warehouse.physical_containers.arrive_confirm`: the cartage / sideloader lines the event will raise per member share, priced read-only through `RateService::price` — the existing `POST …/arrive` is unchanged and now reached from this page); same roles as the box pages. Changed rules: `POST /warehouse/asns/{asn}/lines/{line}/receive` requires Σ `units[*][carton_qty]` = `received_cartons` (`warehouse.receiving.units_sum_mismatch`) and `variance_reason` when received + damaged ≠ expected or damaged > 0 (the bulk form's rule); `POST /warehouse/asns/{asn}/receive` and `POST /warehouse/receiving/unplanned` rows accept `rows[i][pallet_source]` (PALLET_SOURCES, default client_own); `POST /warehouse/tasks/{task}/complete` refuses a VAS record without its billable figure (wrap / waste `billable_qty` > 0, labour / vas_other hours > 0, scanning `scan_count` > 0 or `serials`; devanning defaults to 1 container) and `POST /warehouse/tasks` accepts `complete_now` + the same completion fields (create + complete in one transaction); `POST /warehouse/asns` validates `job_id` against `client_id` (`warehouse.asns.job_not_of_client`); `POST /warehouse/outbound/picks/{line}` requires `short_reason` (out_of_stock \| damaged \| not_found \| other, `OutboundService::SHORT_REASONS`) + optional `short_note` when `picked_qty` < required — both go into the pick_short exception message (Chinese); `POST /warehouse/outbound/pack/{fulfilment}` requires `package_type` and all three dims on every row that carries a weight (errors `packages.{i}.package_type` / `.length_mm`, row-numbered); `POST /warehouse/outbound/waves` requires `order_ids[]` (min 1, `warehouse.outbound.errors.select_orders`); `POST /warehouse/outbound/dispatch/{fulfilment}` refuses a `shipment_id` that is not the batch's shipment (`shipments.fulfilment_id`, read-only) — the board prefills it for a `booked` shipment and shows shipment no · carrier · status per row (read-only `shipments` / `carriers` / `transport_quotes`, Transport exposes no shipment read service). Labels: `GET /warehouse/labels/asn/{asn}`, `…/units?ids[]=`, `…/locations?warehouse_id=` take `batch` (1-based, 40 labels per PDF, `LabelService::BATCH_SIZE`); without `batch` a set over 40 answers an HTML page of batch links (`warehouse::labels.batches`); `…/locations` also filters `type`, `zone`, `aisle_from` / `aisle_to` (numeric codes compare as numbers, `Location::codeBetween`); every barcode is one PNG `<img>` | `warehouse.stock.update`, `warehouse.physical_containers.edit`, `.update`, `.destroy`, `.arrive_confirm`, `warehouse.receiving.store`, `.bulk_store`, `.unplanned.store`, `warehouse.tasks.complete`, `.store`, `warehouse.asns.store`, `warehouse.outbound.pick`, `.pack`, `.waves.release`, `.dispatch`, `warehouse.labels.asn`, `.units`, `.locations` | `warehouse::stock.show`, `warehouse::physical_containers.edit`, `.arrive`, `warehouse::receiving.form` (+ `_unit-row`), `.bulk`, `.unplanned` (+ `_unplanned-row`), `warehouse::tasks.create`, `.index`, `warehouse::asns.create`, `warehouse::outbound.wave`, `.pack`, `.index`, `warehouse::labels.batches`, `warehouse::locations.index` |
| Portal / Orders | CHANGE_REQUESTS #129 — 存储等级 chosen when the order is placed (a TIER, never a bin; the supervisor still picks the location, #126 unchanged): `POST /portal/orders/preview` and `POST /portal/orders` accept `lines[*].storage_tier` standard \| bottom (nullable, `Enums::STORAGE_TIERS`; every submitted line gets `storage_tier_source = client`); `GET /portal/orders/create` shows the select per goods-line row and, when the client's card prices `WH-STORAGE-TIER-PLT-WK` as a percent, one hint line 底层库位：比标准仓储高 X% per warehouse (`PortalStorageTier`, the client's own percent — never cost); `GET /portal/orders/{order}` shows a 存储等级 column (badge only). `POST /orders` (staff manual form: same field, source `staff`; the form re-rendered after a validation error prices the hint for the selected client server side), `POST /orders/{order}/lines` / `PATCH /orders/{order}/lines/{line}` (`storage_tier`, source `staff`; absent = line unchanged), `POST /orders/api/orders` (`lines[*].storage_tier`, source `client`; absent = no tier, standard once on the ASN); `GET /orders/{order}` shows the column with the source (客户选择 / 员工设置 / 按单价预选) and the select on the draft line add / edit forms. No new route | `portal.orders.preview`, `portal.orders.store`, `portal.orders.show`, `orders.store`, `orders.lines.store`, `orders.lines.update`, `orders.api.orders.store`, `orders.show` | `portal::orders.create`, `portal::orders.show`, `orders::form`, `orders::show` |
| Billing | CHANGE_REQUESTS #140 (audit 2026-09-22 Part B, lead approval 2026-09-22): `POST /billing/payments/{payment}/void` (作废收款, `reason` required → `InvoiceService::voidPayment` — an offsetting negative `payments` row with `void_of_payment_id` + `note`, never a delete; paid amount / status / overdue recomputed from Σ rows; a void row or an already-voided payment is refused in Chinese) and `POST /billing/invoices/{invoice}/append-job/{job}` (并入该草稿 on the pool row / 并入本草稿 on the draft page → `InvoiceService::appendJobCharges`: the Job's unbilled service charges join the open draft, totals recomputed; refused for a non-draft, a storage draft, another client's Job or nothing to add); both admin \| finance. Behaviour changes on existing routes, same CR: `POST /billing/charges/{charge}/reverse` refuses a charge on a draft or an issued invoice (`billing.charges.errors.on_draft` / `invoiced_use_credit_note`) — the list offers 去开冲减单 → `GET /billing/invoices/{invoice}?credit_line={invoice_line_id}#credit-note`, which opens the credit-note form with that line's remaining amount pre-filled; `POST /billing/invoices/{invoice}/payments` refuses `amount` above the outstanding balance (`billing.errors.payment_exceeds_outstanding`), the form pre-fills the balance; `GET /billing/charges/manual?job_id=&client_id=` pre-selects the Job / narrows the list to the client (linked from the Job page 费用与利润 · 手工加费), `POST /billing/charges/manual` takes optional `charge_date` (date ≤ today, default today) and `charge_code` has a blank first option; `POST /billing/rate-items/{item}` and `POST /billing/rate-cards/{card}/items` are refused (`billing.rate_cards.errors.locked_for_approval`) while a `rate_card_change` approval for the card is pending or approved; `GET /billing/rate-cards/{card}` on a draft shows the diff against the previous version (added / changed / removed, old → new, 只看改动); `GET /admin/approvals` (Platform) links each `rate_card` subject to its card and each `credit_note` subject to its invoice | `billing.payments.void`, `billing.invoices.append_job` | — (redirect back / to the invoice) |
| Warehouse | CHANGE_REQUESTS #142 (lead decision 2026-09-22, decision 1 — follows CR #141 OUTBOUND-08). Behaviour change on `POST /warehouse/outbound/picks/{line}`: `short_reason = not_found` no longer returns the shortfall to available stock — `OutboundService::confirmPick` freezes it on the unit (`stock_units.qty_frozen`, excluded from every 可用 figure: unit page, stock list `available_only`, `StockService::onHand` / `reserve`, client portal), appends the unit to the warehouse's open 差异盘点 (`StocktakeService::addDiscrepancyLine` — `stocktakes.kind = discrepancy`, one open per warehouse, created on first use, one line per unit, expected = the unit's on hand) and raises the pick_short exception with `source_type = stocktake` / `source_id` = that stocktake (the exception centre links there) and the stocktake number in the message; the flash names the stocktake; the wave page's confirm() says what 找不到 does. Other reasons unchanged (release to available, source fulfilment). `POST /warehouse/stocktakes/{stocktake}/lines/{line}` (count) now also releases the unit's frozen cartons when counted ≥ expected; `POST /warehouse/stocktakes/{stocktake}/close` adjusts the variance as before and clears any remaining frozen quantity. Pages: `GET /warehouse` and `GET /warehouse/stock/{unit}` show 冻结 N; `GET /warehouse/stocktakes` / `…/{stocktake}` show the kind (差异盘点 badge + hint) and 冻结 N per line. Decision 2 (INBOUND-05): no guard on `PATCH /warehouse/stock/{unit}` — the card's hint states that a correction takes effect from the next snapshot / weekly run and billed weeks are never re-run | `warehouse.outbound.pick`, `warehouse.stocktakes.count`, `.close`, `warehouse.index`, `warehouse.stock.show`, `warehouse.stocktakes.index`, `.show` (changed) | `warehouse::outbound.wave`, `warehouse::stock.index`, `.show`, `warehouse::stocktakes.index`, `.show` |
| Billing | CHANGE_REQUESTS #142 (lead decisions 2026-09-22, decisions 3–4). **Changed** `POST /billing/invoices/{invoice}/issue`: refused (`billing.errors.unpriced_block`, zh) while `InvoiceService::unpricedItems()` finds needs_review charges or open missing-rate exceptions in the draft's Job(s) / period — the CR #132 warning box is now a block — unless the form sends `unpriced_override = 1` (checkbox 仍然开票(未定价费用留到下期) shown only when such items exist); the override is written to `invoices.notes` and the activity log and the issued invoice page shows the badge 开票时有未定价费用 + the note (never on the client PDF); an empty draft is refused (`billing.errors.empty_draft`, the button is disabled). **New** `DELETE /billing/invoices/{invoice}/lines/{line}` (`billing.invoices.lines.destroy`, admin \| finance): 移出草稿 button per line on a DRAFT → `InvoiceService::removeLine` — the charge returns to the unbilled pool (`invoice_line_id` null, status untouched), the line is deleted, the Job detached when it has no line left, totals recomputed; an emptied draft stays with a hint; a line of another invoice → 404; an issued invoice → `billing.errors.only_drafts_remove_line` (credit note instead) | `billing.invoices.issue` (changed), `billing.invoices.lines.destroy` (new) | `billing::invoices.show` |
| Platform | CHANGE_REQUESTS #142 (lead decision 2026-09-22, decision 5): `GET /admin/approvals`, `POST /admin/approvals/{approval}/cancel` moved under `role:admin\|finance` (approve / reject already were) — the approval centre is for the roles that approve rate-card changes and credit notes, who are also the only requesters; every other staff role gets 403 and the 审批 nav entry is hidden (`layouts/nav/platform`). A requester follows their own request on the item's page: rate card (审批中 / 已批准 badges, locked hint → centre), invoice (credit note 审批中 → 审批中心 / 已批准) — both already there, verified by `ApprovalsVisibilityTest` | `platform.approvals.index`, `.cancel` (changed middleware) | `platform::approvals.index`, `layouts.nav.platform` |

Until M1 lands authentication, placeholders are reachable without login; M1 puts them behind `auth`.
| Portal | CHANGE_REQUESTS #143 on the #123 / #128 import pages (no new route): `POST /portal/asns/imports` and `POST /portal/asns/imports/manual` (and the staff `POST /orders/imports/preview`) accept `group_by` = `mark` (default) or `recipient` and `address_type_default` = `auto` (default) / `residential` / `business`; the preview and the staff import page show the rule used and the resulting order count. | — | `portal::asns.imports.partials.import-options` |
| Portal | CHANGE_REQUESTS #144 on the #123 upload page (no new route): `GET /portal/asns/imports/create?order_type=from_stock|pickup_deliver` preselects the order type (anything else = from_stock, as 预报入库's button); `POST /portal/asns/imports` accepts `order_type` — with `pickup_deliver` the inbound / 到仓方式 fields are ignored and `pickup[name|phone|address|suburb|state|postcode]` (state ∈ STATES, postcode 4 digits, phone optional) + `requested_date` (≥ today) are required, with `from_stock` (or no value) the #123 / #125 fields apply as before; `GET /portal/orders/create` (新建订单) carries a 批量导入 card with one link per type; `GET /portal/asns/imports` and `GET /portal/asns/imports/{import}` name the type, the preview / result of a 提货直送 list shows the pickup party instead of the inbound context. | — | `portal::asns.imports.partials.pickup-fields` |
| Orders (API) | CHANGE_REQUESTS #145 自动导入 — `POST /orders/api/imports` (multipart; bearer token from `order_api_tokens`, no session / CSRF / client scope, the token names the client; 403 `client_inactive` unless the client is active): `manifest` (CSV / XLSX / XLS / TXT ≤ 10 MB, required), `order_type` from_stock (default) \| pickup_deliver, `group_by`, `address_type_default`, `auto_confirm`, `force` (booleans), from_stock `container_no` / `container_size` / `expected_date` / `reference` / `notes`, `requested_date`, pickup_deliver `pickup[name\|phone\|address\|suburb\|state\|postcode]` (required for that type) → JSON summary (`import_id`, `status`, `auto_confirmed`, `replayed`, `row_count`, `error_count`, `groups{total,ready,blocked,imported}`, `orders[]`, `blocked[]`, `issues[]`, `warnings_count`, `review_url`), 201 imported / 202 pending / 200 replay or nothing created / 422 validation / 401; `GET /orders/api/imports/{import}` the same summary for one of the client's own submissions (portal / api / inbox; 404 otherwise). Admin page `GET /orders/api-tokens` documents both. | `orders.api.imports.store`, `orders.api.imports.show` | — |
| MasterData / Portal | CHANGE_REQUESTS #145 (no new route): `PUT /admin/clients/{client}` accepts `import_defaults[group_by\|address_type_default\|auto_confirm\|inbox_enabled\|notify_email]` (the fieldset 自动导入 on the client form; absent = untouched); `GET /portal/asns/imports` and `GET /portal/asns/imports/{import}` (+ confirm) cover `order_imports.source` portal / api / inbox — an API push or an inbox file is the client's submission, badged API 推送 / 收件夹自动导入. Cron: `imports:inbox` every five minutes (`routes/console.php`). | — | — |
| Portal | CHANGE_REQUESTS #146: `GET /portal/asns/imports/template.xlsx` — the Excel template = the client's consolidation list one to one (`data/inbound-list-template.xlsx`: 27 columns incl. the unread sender / country / street / battery / email / tracking ones, widths and frozen panes kept, three fictional sample cartons; client users only, 403 for staff); `GET /portal/asns/imports/template` (CSV) carries the same rows (group row, English header row, samples) instead of the Chinese-header template; `GET /portal/asns/imports/create` links both and lists every header with its meaning (`portal.inbound.template_columns`). | `portal.asns.imports.template_xlsx` | — |
| Warehouse | CHANGE_REQUESTS #148 (no new route): `POST /warehouse/asns/{asn}/receive` also accepts `rows_json` — a JSON object of the same `rows[i][…]` shape (the page packs every ticked row into it on submit and disables the row inputs, so a 286-line ASN posts a handful of variables instead of ~2,600 and never trips PHP's `max_input_vars`); when present it replaces `rows` before the existing filter and validation. | — | — |
