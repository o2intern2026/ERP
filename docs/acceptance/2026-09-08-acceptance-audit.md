# Acceptance audit — 2026-09-08

Audit of `main` at tag **M6b** (worktree branch `block/c11-acceptance-audit`, commit `e19efca`) against the numbered acceptance criteria in `ERP_PLAN.md` §2.5, §3.8, §4.7, §5.7, §6.8 and the §7 end-to-end script. Read-only: no application code, tests or contracts were changed.

## Test totals

| Run | Result |
|---|---|
| Full suite `DB_DATABASE=erp_test_audit php artisan test` | **214 passed, 1 skipped, 1892 assertions**, 24.6 s. Skipped: `Tests\Feature\Transport\KarrioLiveTest` (needs `KARRIO_LIVE=1` + `KARRIO_API_KEY`) |
| `--filter 'Tests\Feature\Platform'` | 53 passed (309 assertions) |
| `--filter 'Tests\Feature\MasterData'` | 6 passed (38) |
| `--filter 'Tests\Feature\Orders'` | 39 passed (515) |
| `--filter 'Tests\Feature\Portal'` | 5 passed (62) |
| `--filter 'Tests\Feature\Reports'` | 1 passed (2) — placeholder only |
| `--filter 'Tests\Feature\Warehouse'` | 41 passed (361) |
| `--filter 'Tests\Feature\Transport'` | 52 passed, 1 skipped (419) |
| `--filter 'Tests\Feature\Billing'` | 13 passed (169) |
| `Tests\Unit\Support\MoneyTest` | 4 passed |

Every test cited below passed in these runs. Test names are `File::test_method` under `tests/Feature/`; code references are `Class::method` under `app/`.

**Status key.** `covered` = an automated test proves the behaviour · `partial` = implemented but only part of the criterion is tested, or a stated detail is missing (said in Notes) · `gap` = not implemented / no evidence · `manual-only` = needs a human, hardware or an external system.

**Status counts.** §2.5: 4 covered · 3 partial · 1 gap. §3.8: 7 covered · 4 partial. §4.7: 13 covered · 10 partial · 1 manual-only. §5.7: 2 covered · 5 partial. §6.8: 10 covered · 3 partial. Total 63 items: 36 covered, 25 partial, 1 gap, 1 manual-only.

---

## §2.5 Platform / portal / reports

| # | Criterion (short) | Status | Evidence (test / code) | Notes / gap |
|---|---|---|---|---|
| 1 | 客户账号登录后,任何页面、导出、API 都拿不到别家数据 | covered | `Platform/AuthTest::test_client_users_land_on_the_portal_and_cannot_leave_it`, `::test_global_client_scope_limits_rows_to_the_current_client`; `Platform/ClientScopeBindingTest`; `Portal/PortalOrdersTest::test_client_sees_only_its_own_orders_and_another_clients_order_is_a_404`, `::test_client_downloads_only_its_own_pod_and_never_sees_internal_notes`; `Platform/JobWorkbenchTest::test_client_scope_hides_other_clients_jobs`; `Orders/OrderApiTest::test_valid_token_creates_the_order_for_its_client_and_validates_the_body`; `Transport/B9aCarrierCostMarginTest::test_carrier_cost_table_matches_contract_and_client_requests_cannot_query_costs`; `Platform/DocumentCentreTest::test_downloader_lets_clients_see_only_their_visible_documents`. Code: `App\Support\Tenancy\ClientGlobalScope`, `ClientScope` middleware (priority before `SubstituteBindings`, CR #41) | The only exports are staff-only CSVs (`orders.imports.errors`, `transport.carrier-invoices.differences`); there is no client-facing export to test. API: the body's `client_id` is ignored, the token names the client |
| 2 | 仓库操作员打开财务页面被拒绝 | covered | `Billing/BillingPagesTest::test_per_job_invoice_from_the_unbilled_pool_to_pdf_payment_and_receivables` (warehouse_operator → `/billing` 403; client → `/billing/invoices` 403); `MasterData/ClientCrudTest::test_master_data_is_for_admin_customer_service_and_finance_only`. Code: `role:admin|finance` group in `app/Modules/Billing/routes.php` | — |
| 3 | 价目表变更提交后进入审批中心,批准后才生效 | covered | `Billing/BillingPagesTest::test_rate_card_versions_are_approval_gated_and_history_keeps_its_snapshot` (activate refused before approval; requester cannot approve; second finance user approves; v2 active, v1 superseded, clients follow); `Platform/SearchApprovalActivityTest::test_approvals_need_a_second_person`. Code: `RateCardService::requestActivation/activate`, `ApprovalService::isApproved` | — |
| 4 | 审计日志能查到"谁在什么时候改了哪张价目表的哪一项" | partial | `Platform/SearchApprovalActivityTest::test_changes_are_audited_without_passwords` (Client and User changes with causer/old/new on `/admin/activity`). Code: `RateCard` uses `LogsActivity` (`logOnly status, version, effective_from, effective_to, approved_by`) | `RateItem` has no `LogsActivity` and `RateCardService::updateItem/addItem` write no activity row → "哪一项" (which item / which rate) is not in the audit log; only card-level status/version changes are. No test asserts a rate-card audit entry |
| 5 | 老板报表显示单量、准时率、毛利;客户报表只含该客户数据 | gap | `Reports/ReportsPlaceholderTest` only. `app/Modules/Reports/routes.php` has the single M0 placeholder route; `Reports/Services` and `Models` are `.gitkeep` | A21 / A22 remain with X1 (HANDOFF.md). Margin data exists per Job (`JobService::summarize`) but there are no report pages |
| 6 | 从任一 Job 页能看到该 Job 的订单、入库、库存、发运、单据、费用、成本与毛利 | partial | `Platform/JobWorkbenchTest::test_staff_create_a_job_from_the_form_and_see_it` (margin label), `::test_summary_hides_cost_and_margin_from_client_users`; `Platform/JobCostConsumerTest::test_job_cost_rolls_up_from_carrier_costs`; `Billing/BillingPagesTest::test_manual_charges_review_queue_quotes_and_charge_codes_pages` (charge panel on `/jobs/{job}`). Code: `platform::jobs.show`, `JobService::summarize` | The page shows revenue / cost / margin and the Billing charge list. The ASN, stock, orders, shipments and documents panels are still the M0 placeholders (`platform.jobs.panel_pending`: "该面板由 :module 模块在 :checkpoint 检查点提供", `show.blade.php` lines 33–40); `JobController::show` loads only `client`, `creator` and the summary. Blocks §7 step 9 |
| 7 | 收货差异、配送失败、Missing Rate 三类异常出现在同一个异常中心列表 | covered | All three types are written through `ExceptionService::raise` into `exceptions` and listed by `/admin/exceptions`: discrepancy — `Warehouse/StockCoreTest::test_damaged_stock_is_quarantined_and_never_available`, `Warehouse/InboundPagesTest::test_receive_then_put_away_by_scanned_location_code_then_query_stock`; delivery_failed — `Transport/B8DeliveryTrackingTest::test_driver_failure_updates_status_and_enters_the_shared_exception_centre`; missing_rate — `Billing/ChargeEngineTest::test_mixed_devanning_is_poa_and_a_missing_rate_raises_an_exception_without_a_charge`. Centre page: `Platform/ExceptionCentreTest::test_staff_work_an_exception_from_the_centre` (list, type filter, assign / start / resolve, client 403) | Composite evidence; no single test renders all three types on the page at once (`Enums::EXCEPTION_TYPES` has all of them) |
| 8 | 人为让 Billing 的事件消费失败一次,重试后费用仍正确生成,失败记录可在集成监控中看到 | partial | Generic mechanism: `Platform/OutboxTest::test_a_failing_consumer_backs_off_and_does_not_block_the_others`, `::test_after_five_failures_the_event_is_dead_and_an_integration_exception_is_raised`, `::test_integration_monitor_lists_events_and_retries_from_the_page`. Billing idempotency on redelivery: `Billing/ChargeEngineTest::test_devanning_and_putaway_each_produce_exactly_one_charge_at_the_card_price` (`dispatchDue()` twice, no double charge) | No test makes `BillingChargeConsumer` itself fail once and asserts the charges after the retry. Demo needs a manual way to force the failure (see manual checks) |

## §3.8 OMS

| # | Criterion (short) | Status | Evidence (test / code) | Notes / gap |
|---|---|---|---|---|
| 1 | 导入真实《需派送货物清单》→ 按唛头 + 地址 + FBA 生成正确张数的订单;地址不一致行阻断;FBA 引用;可读错误 | covered | `Orders/OrderImportTest::test_real_workbook_creates_the_expected_grouped_orders_and_keeps_fba_reference` (real `data/需派送货物清单.xlsx` → 30 ready groups, 5 error rows, 30 orders / 135 lines, FBA reference kept), `::test_preview_groups_valid_lines_blocks_inconsistent_mark_and_confirms_one_order` ("地址或 FBA 引用不一致" blocker, preview page), `::test_retransmitted_file_warns_duplicate_and_never_creates_a_second_order` (errors CSV); `Orders/SpreadsheetManifestParserTest` | — |
| 2 | 同一份文件重传 → 提示重复,不产生重复订单 | covered | `Orders/OrderImportTest::test_retransmitted_file_warns_duplicate_and_never_creates_a_second_order` | — |
| 3 | received → delivered 与 unbilled → billed 两条线分别验收;时间线记录每次变化和操作人 | partial | `Orders/OrderManagementTest::test_operational_and_billing_statuses_progress_independently_with_a_complete_timeline` (7 operational + 1 billing events, actor on all 8, page shows them in order); real event-driven walk: `Orders/OutboundEventConsumersTest::test_real_pick_pack_dispatch_and_pod_events_walk_the_order_to_delivered` | The billing line only moves through `OrderStatusService::transitionBilling` called directly. Nothing in Billing (`InvoiceService::issue`) or an Orders consumer sets `orders.billing_status` when an invoice is issued (no `billing_status` writer outside `OrderStatusService` / `OrderCreationService`) → in a demo the order stays `unbilled` after invoicing unless changed by hand |
| 4 | 已发运的订单无法被编辑,只能走退货 | covered | `Orders/OrderManagementTest::test_a_dispatched_order_cannot_be_edited`; `Orders/OrderChangeAndReturnTest::test_shipped_orders_cannot_be_edited_cancelled_or_reduced_only_returned`, `::test_return_chain_from_request_through_inspection_to_the_financial_decision` | Return assumptions recorded in CR #42 (return order shares the original Job, no `order.confirmed` for returns) |
| 5 | 客户门户只看自己的订单,能下单、查状态、下载 POD | covered | `Portal/PortalOrdersTest` — 4 tests (list / search / status filter / other client 404; create with address book, `client_id` in body ignored; POD download own-only, hidden document 403, no internal notes or margin link; return request) | Portal has no invoice list and no stock view (§7 step 8) |
| 6 | 纯运输订单不经过仓库环节,直接出现在 TMS 待派清单里 | partial | `Orders/TailgateAndTransportOrderTest::test_a_pure_transport_order_needs_no_goods_lines_opens_its_own_job_and_confirms_straight_to_transport` (no lines, no fulfilment, `order.confirmed` with pickup + declared packages); `Warehouse/OrderEventsConsumerTest::test_pure_transport_orders_do_not_touch_stock`; `Orders/OutboundEventConsumersTest::test_pod_on_a_pure_transport_order_moves_it_from_confirmed_to_delivered`. Code: `Transport\Consumers\OrderConfirmedConsumer` → `ShipmentIntakeService` (final-stage quotes for `pickup_deliver`), `/transport` lists all shipments | No test asserts that a `pickup_deliver` order's shipment appears in Transport (`Transport/M4TransportIntegrationTest` does this for `from_stock` only) |
| 7 | 财务锁:可确认、配货、拣货、打包,预订 / 发运被拒;放行后继续;时间线记录放行人 | covered | `Orders/FinancialLockTest` — 3 tests (confirm → packed under the hold, dispatch refused, release needs a note, placer and releaser in the timeline; Warehouse handover refused then flows after release; finance/admin only, list badge); `Warehouse/OutboundFlowTest::test_dispatch_is_refused_while_a_financial_hold_is_active`; `Transport/B9aCarrierCostMarginTest::test_only_a_manual_financial_hold_blocks_booking` | CR #40 / #46 delivered (Warehouse dispatch and Transport booking both check `ExceptionService::hasActiveHold('financial', …)`) |
| 8 | 分两次发货:客户看到一张订单 + 两个发货批次;全部送达后才已送达 | covered | `Orders/FulfilmentAllocationTest::test_partial_stock_is_split_into_two_fulfilments_and_order_closes_only_after_both_are_delivered`; `Orders/OutboundEventConsumersTest::test_order_is_packed_and_dispatched_only_when_every_batch_is` | Portal detail renders the batches (`portal::orders.show` "fulfilments" section) but no test asserts the client view of two batches |
| 9 | 按柜号或入库单号查询:全部订单、发货进度与合计毛利 | partial | `Orders/QueueAndBatchTest::test_batch_page_lists_every_order_generated_from_one_container` (by container no and ASN no; orders; cartons / shipped; revenue figure). Code: `OrderBatchService` | Revenue only — `OrderBatchService` sums `charges`; there is no cost side, so "合计毛利" is not shown |
| 10 | 25 kg → 自动勾选尾板;预订后 charges 一条且仅一条尾板费;人工取消需填原因写入时间线 | partial | Orders: `Orders/TailgateAndTransportOrderTest::test_a_25kg_piece_ticks_tailgate_automatically_and_the_confirmed_event_carries_it` (25 kg piece, 4×10 kg not, residential yes), `::test_a_person_can_override_the_tailgate_decision_with_a_reason_that_lands_in_the_timeline` (reason required, timeline). Billing: `Billing/ChargeEngineTest::test_quote_confirmed_charges_freight_tailgate_and_fuel_once_from_a_client_card` (`TR-TAILGATE` once, second dispatch no duplicate) | Not chained: no test drives order → `shipments.tailgate_required` → `shipment.quote_confirmed` → `TR-TAILGATE`. `TR-TAILGATE` has no Edward seed rate (charge-codes.md §6) → on seeded data the fee is a Missing Rate exception unless the demo client card carries it |
| 11 | 第二次向同一 FBA 仓下单,地址一键带入,固定备注自动填好 | covered | `Orders/ClientAddressBookTest::test_second_order_to_the_same_fba_address_uses_the_saved_snapshot_and_fixed_notes`, `::test_address_book_is_sorted_by_frequency_and_address_snapshots_do_not_change`; `Portal/PortalOrdersTest::test_client_creates_an_order_from_the_portal_with_the_address_book_and_cannot_pick_another_client` | — |

## §4.7 WMS

| # | Criterion (short) | Status | Evidence (test / code) | Notes / gap |
|---|---|---|---|---|
| 1 | 40ft 拆柜 ASN,收货录 5 箱短少与 2 箱破损并填原因,上架后可用库存正确 | covered | Short + reason + discrepancy exception: `Warehouse/InboundPagesTest::test_receive_then_put_away_by_scanned_location_code_then_query_stock` (10 of 12, "short shipped"); damaged + reason → quarantine, never available: `Warehouse/StockCoreTest::test_damaged_stock_is_quarantined_and_never_available` (8 good, 2 damaged "crushed"); available only after putaway with ledger balances: `StockCoreTest::test_stock_is_available_only_after_putaway_and_balances_follow_the_ledger` | Composite — not one 40ft scenario with both 5 short and 2 damaged |
| 2 | `stock:reconcile` 输出差异为零 | covered | `StockCoreTest::test_reconcile_detects_and_fixes_a_drifted_balance` (fails on drift, `--fix` repairs); `stock:reconcile` asserted successful at the end of 8 flow tests. Scheduled daily 02:00 (`routes/console.php`) | — |
| 3 | 客户 A 的货绝不会被分配给客户 B 的订单 | covered | `StockCoreTest::test_client_a_stock_is_never_reserved_for_client_b` (reserved 0, shortfall 5) | — |
| 4 | 释放 20 张订单的波次,拣货任务按库位路径排序 | partial | `Warehouse/OutboundFlowTest::test_wave_picks_packs_and_dispatches_a_confirmed_order` (task lines ordered `MEL-A-01-01`, `MEL-PF-01-01`; a second wave cannot re-pick the same fulfilment); `Warehouse/OutboundPagesTest::test_outbound_board_release_pick_pack_dispatch`. Code: `OutboundService::releaseWave` | Ordering proven on a one-order / two-line wave; a 20-order wave is not exercised |
| 5 | 打包录 25 kg 包裹后,尺寸重量传给 TMS 并触发尾板判定 | partial | `OutboundFlowTest` (25 kg package with dims in `outbound.packed`); `Transport/M4TransportIntegrationTest::test_real_packed_packages_drive_final_manual_quote_booking_cost_and_dispatch` (packed dims / weight reach the final quote request) | Transport takes `tailgate_required` from the order (`ShipmentIntakeService` line 49, `ShipmentQuoteRequestFactory` line 52); measured package weight does not re-evaluate it. Consistent with OMS-13 (rule at order confirmation) but the criterion's "触发尾板判定" at packing is not implemented |
| 6 | 隔离的损坏货不出现在可用库存 | covered | `StockCoreTest::test_damaged_stock_is_quarantined_and_never_available`; `Warehouse/StocktakeMoveQuarantineTest::test_quarantined_stock_leaves_available_stock_and_needs_no_reservations`, `::test_restore_from_quarantine_makes_stock_available_again`; `Warehouse/OutboundPrepPagesTest::test_move_quarantine_and_restore_from_the_unit_page` | — |
| 7 | 每日快照自动生成,可回查任意一天、按托盘类型分类 | covered | `Warehouse/SnapshotTest::test_snapshot_classifies_pallets_sources_and_pickface_slots_and_can_be_re_run` (by class / source / pickface, re-run replaces); `OutboundPrepPagesTest::test_snapshot_page_shows_the_day` (`/warehouse/snapshots?date=`). `stock:snapshot` scheduled 23:55 daily (`routes/console.php`) | "自动" depends on cron / `schedule:work` — manual check |
| 8 | 实收少于预报仍能完成收货,Coordinator 队列出现该差异 | covered | `InboundPagesTest::test_receive_then_put_away_by_scanned_location_code_then_query_stock` (10 < 12, discrepancy exception, putaway proceeds); `Platform/ExceptionCentreTest` (discrepancy listed) | The receiving discrepancy carries no `order_id`, so it appears in the shared Exception Centre (`/admin/exceptions`), not in X1's order queue `exceptions` preset (which filters by order) |
| 9 | 订单确认后库存显示已预留;取消后预留自动释放,流水有记录 | covered | `Warehouse/OrderEventsConsumerTest` (reserve / shortfall / cancel → release with ledger row); `Orders/OrderChangeAndReturnTest::test_before_picking_a_coordinator_cancels_freely_and_warehouse_releases_the_reservation`; `StockCoreTest::test_reservation_is_fifo_partial_and_released_with_ledger_rows` | Reserved qty on the stock page is not asserted by a test |
| 10 | 拣不到足量 → Pick Short 异常;Coordinator 选"部分发"后订单进入履约批次 | partial | `OutboundFlowTest::test_wave_picks_packs_and_dispatches_a_confirmed_order` (4 of 6 → `pick_short` exception, shortfall released, batch packs) | No Coordinator decision step exists: `OutboundService::confirmPick` releases the shortfall automatically and the fulfilment continues to pack (partial ship is implicit; the code comment says "so the coordinator can decide" but no action / UI implements backorder / partial / cancel) |
| 11 | 打包完成为 packed,交接后为 dispatched,两者时间戳不同 | covered | `OutboundFlowTest` (`outbound.packed` and `outbound.dispatched` separate events, `dispatched_at` ≥ packed time, load task); `OutboundPagesTest::test_outbound_board_release_pick_pack_dispatch` | — |
| 12 | 收货用扫码枪扫箱标、拣货用手机扫码页校验,两种方式都能完成同一作业 | manual-only | Keyboard (scan-gun) input paths: putaway by scanned location code `InboundPagesTest::test_receive_then_put_away…`; stocktake scan `OutboundPrepPagesTest::test_stocktake_flow_through_the_pages`; phone camera resolver `::test_scan_resolves_units_locations_and_marks` (`/warehouse/scan`, native `BarcodeDetector`) | The pick page (`warehouse::outbound.wave`) only takes a picked quantity — there is no label-code verification on picking, and camera scanning exists only on `/warehouse/scan`. Hardware behaviour needs a human |
| 13 | 同一唛头下三行货物入库后是三个库存单元,可分别预留与拣货 | covered | `OutboundFlowTest` (two `M1` lines → separate units, separate pick lines, separate reservations); `Warehouse/ReturnReceiptTest` (two `R1` lines); `Warehouse/AsnOrderGenerationTest` (two same-mark lines → two order lines each linked to its ASN line) | Proven with two lines per mark, not three |
| 14 | 散货卡车 ASN 无 containers,整柜有;拆柜费只对后者产生 | partial | `Warehouse/InboundTest::test_container_asn_gets_a_job_containers_and_line_counts` (loose_truck → 0 containers; container ASN → containers, line_count); devanning charge from a container task: `Billing/ChargeEngineTest::test_devanning_and_putaway_each_produce_exactly_one_charge_at_the_card_price` | No negative test that a loose-truck ASN produces no devanning charge (the rule needs `task_type = devanning` + `container.size`, so none arises by construction) |
| 15 | 完成"人工时"VAS 任务后 charges 出现对应费用行且可溯源到该任务 | partial | Traceability: `ChargeEngineTest::test_devanning_and_putaway…` (`source_type = task`, `source_activity_id = task:{id}`); labour event fields: `InboundTest::test_completing_tasks_emits_task_completed_with_billing_fields` (hours_business 2, hours_after_hours 1); rate: `Billing/RateServiceTest::test_edward_standard_card_prices_every_uom` (`VAS-LABOUR-HR-AH` 2 h = 110.00). Rules `VAS-LABOUR-HR` / `-AH` exist in `BillingSeeder::RULES` | No test asserts that a labour `task.completed` event produces the two labour charge rows (the only labour case in `ChargeEngineTest` is the missing-rate path) |
| 16 | 司机标记退回后库存不变;仓库退货验收完成后库存才增加 | covered | `Warehouse/ReturnReceiptTest::test_return_is_received_then_inspected_and_only_inspection_adds_stock`; `OutboundPagesTest::test_return_receipt_pages`; `Orders/OrderChangeAndReturnTest::test_return_chain_from_request_through_inspection_to_the_financial_decision` | Driver failure (`Transport/B7DriverPodTest`, `B8DeliveryTrackingTest`) never touches stock by construction |
| 17 | 真实清单导入为 ASN,上架后一键生成订单:订单数 = 唛头数,行与库存单元一一对应,不再需要在 OMS 重新导入 | partial | `InboundPagesTest::test_import_creates_lines_from_the_real_manifest_parser_and_records_the_import` (real workbook → 135 ASN lines, 10 reported errors, packing_list document); `AsnOrderGenerationTest::test_orders_are_generated_once_per_consignment_mark_and_linked_back_to_the_asn_lines` (orders = marks, every ASN line gets `order_line_id`, qty from received cartons, FBA address type); `OutboundPagesTest::test_generate_orders_button_on_the_asn_page`; `Orders/AsnOrderServiceTest` | The real workbook is not driven through receive → putaway → generate in one test (generation is proven on 3 synthetic lines); "订单数等于唛头数" for the real file (30 groups per `OrderImportTest`) is inferred, not asserted |
| 18 | 对已生成订单的 ASN 再点一次不产生重复订单 | covered | `AsnOrderGenerationTest` (second `generate()` → no orders); `Orders/AsnOrderServiceTest::test_received_asn_lines_are_grouped_and_a_second_call_creates_nothing` | — |
| 19 | 1200×1200×1600 / 600 kg → oversize_high;1200×1200×1400 / 900 kg → overweight 并进 POA 待报价;改阈值后新收货按新阈值,已收货不变 | partial | `InboundTest::test_receiving_suggests_pallet_class_from_the_client_thresholds` (oversize_high, overweight, override with reason); `RateServiceTest::test_pallet_class_suggestion_uses_the_card_thresholds` (all bands, beyond-all → null). Code: `ReceivingService::receiveLine` reads `RateService::suggestPalletClass` from the active card and stores `pallet_class` on the unit | (a) "进 POA 待报价": receiving only stores `pallet_class = overweight`; nothing is queued until the weekly storage charge is priced (`WH-STORAGE-PLT-OVERWEIGHT-WK` is POA → `needs_review`), and no test bills an overweight pallet. (b) Threshold change: follows by construction (class stored at receiving), but no test changes the thresholds and receives again |
| 20 | warehouse_plain / CHEP 托盘周快照带来源,Billing 据此产生托盘租赁费;客户自带不产生 | covered | `Billing/StorageBillingTest::test_a_week_of_snapshots_bills_each_unit_once_by_class_source_and_pickface_slot` (2 CHEP → `WH-PALLET-RENT-POOL-WK` ×2, 1 plain → `-PLAIN-WK`, client_own none); `Warehouse/SnapshotTest` (source per snapshot row) | — |
| 21 | pickface 库位有货的周,按占用格数计 pickface 周费,不按托盘数 | covered | `StorageBillingTest` (two units in one pickface slot → one `WH-STORAGE-PICKFACE-WK`, qty 1); `SnapshotTest` (`pickface_slots = 1`) | — |
| 22 | 拣一整托与三箱(10/30/50 kg)后 outbound.packed 逐行带单箱重量;Billing 一条整托拣货费 + 三档各一条 | covered | `OutboundFlowTest` (lines: pallet qty 5 / 250 kg, carton qty 4 / 10 kg); `ChargeEngineTest::test_outbound_packed_produces_despatch_and_banded_pick_charges_and_a_redo_reverses` (`WH-PICK-PLT` + `LT22` / `22-45` / `GE45` one charge each) | — |
| 23 | 卡车散货 ASN 收货 6 托、上架 6 托、装车 6 托,三个事件各带托盘数,产生卸货 / 上架 / 装车三条费用 | partial | Events: `InboundPagesTest::test_receive_then_put_away…` (receiving task `billable_qty` = unloaded pallets), `InboundTest::test_putaway_of_the_last_unit_emits_asn_putaway_completed_with_the_billing_payload` (`pallet_count`), `OutboundFlowTest` (load task `billable_qty` = loaded pallets). Billing: only `WH-PUTAWAY-PLT` asserted (`ChargeEngineTest`); rules `WH-UNLOAD-PLT` / `WH-LOAD-PLT` exist in `BillingSeeder::RULES` | No Billing test for unload / load charges. The unload rule condition is `task_type = receiving` only — the contract adds `asn.inbound_type = loose_truck`, which the receiving `task.completed` payload does not carry, so container receipts with `unloaded_pallets` would also bill unload |
| 24 | labour 班内 2 h / 班外 1 h → 两条 code;扫 5 序列号 → scan_records 5、费用 qty 5、序列号印在 packing list;进 / 出库缠膜两条 code;废弃物 0.5 CBM 按 1 CBM | partial | Labour hours in the event: `InboundTest::test_completing_tasks_emits_task_completed_with_billing_fields`; scanning: `StocktakeMoveQuarantineTest::test_scanning_task_stores_serial_numbers_and_bills_per_scan` (5 `scan_records`, `billable_qty` 5, `scan_count` 5, blanks / duplicates dropped); waste minimum: `RateServiceTest` (0.5 → 1.0 CBM, 80.00). Rules for `VAS-LABOUR-HR(-AH)`, `VAS-SCAN`, `WH-WRAP-IN/OUT-PLT`, `VAS-WASTE-CBM` exist; `wrap` / `waste` are valid task types (`Enums::TASK_TYPES`) | No Billing test produces the labour / scan / wrap / waste charge rows from events. "序列号印在 packing list 上": no packing list document exists (Warehouse PDFs are unit / location / ASN labels; Transport's consignment note lists packages, not serials) → gap |

## §5.7 TMS

| # | Criterion (short) | Status | Evidence (test / code) | Notes / gap |
|---|---|---|---|---|
| 1 | 向所有合格 Transport Source 请求报价,展示全部方案并标记 Recommended / Cheapest / Fastest;无方案时进 Manual Transport Exception 不阻塞 | covered | `Transport/B5cTransportOptionServiceTest::test_service_asks_each_active_source_and_persists_qualified_quotes`, `::test_no_automatic_option_raises_manual_transport_exception_without_throwing`; `Transport/B5dQuoteSelectionTest::test_flags_respect_delivery_date_and_own_fleet_preference`; `Transport/B5ShipmentTest::test_quote_candidates_display_all_sources_prices_eta_and_acceptance_flags`; `Transport/M4TransportIntegrationTest` (manual quote flagged recommended / cheapest) | Sources live today: `own_fleet`, `manual`, `karrio` (idle without key), `transdirect` (dormant, CR #49) |
| 2 | 客户在门户确认或改选;确认后 charges 出现运费(客户价),预订后 carrier_costs 出现成本;客户看不到成本;预订不受收款状态限制;仅财务锁时不可预订 | partial | Coordinator confirmation + exact event: `B5dQuoteSelectionTest::test_coordinator_confirms_a_final_quote_and_emits_the_exact_event`; client tenancy of selection: `::test_client_selection_entry_point_enforces_client_tenancy` (service level; `/transport/{shipment}` 403 for clients); freight charge on confirmation: `Billing/ChargeEngineTest::test_quote_confirmed_charges_freight_tailgate_and_fuel_once_from_a_client_card`; `carrier_costs` on booking + costs hidden from clients: `Transport/B9aCarrierCostMarginTest::test_third_party_booking_records_quote_cost_and_exact_booked_event_once`, `::test_carrier_cost_table_matches_contract_and_client_requests_cannot_query_costs`; hold gate: `::test_only_a_manual_financial_hold_blocks_booking`. Code: `ShipmentBookingService` checks only `hasActiveHold('financial')` (no invoice / payment check) | No client-facing page to confirm or change the quote: Portal routes are orders / returns / documents only (A7b pending with X1). "预订不受收款状态限制" holds by construction but is not asserted with an unpaid / overdue invoice |
| 3 | 第三方预订后持有 tracking number 与 waybill,轨迹无人工干预自动更新 | partial | `B9aCarrierCostMarginTest::test_third_party_booking_records_quote_cost_and_exact_booked_event_once` (tracking_number stored); `Transport/B6ShipmentLabelTest::test_third_party_prints_and_reuses_the_platform_waybill_verbatim`; `Transport/B8DeliveryTrackingTest::test_tracking_table_matches_the_contract_and_polling_is_idempotent` (poll → `tracking_events`, delivered); `transport:sync-tracking` every 30 min (`TransportServiceProvider`); adapter contract: `Transport/KarrioAdapterTest`, `B5cTransportOptionServiceTest::test_transdirect_adapter_uses_v4_contract_and_parses_tracking_html` | All against stub adapters / `Http::fake`. Live path needs a running Karrio with a carrier connection (`KarrioLiveTest` skipped without `KARRIO_LIVE=1`); Transdirect has no account. Manual on demo day |
| 4 | 自派 shipment 编入班次,司机手机签收后:订单已送达、客户收到附 POD 邮件、运费可结算同时发生 | partial | Driver POD → shipment delivered + mail with attachment: `B8DeliveryTrackingTest::test_driver_delivery_updates_transport_status_and_emails_the_pod`; POD PDF + exact event: `Transport/B7DriverPodTest::test_driver_signature_and_photos_create_a_pod_pdf_and_exact_event`; order → `delivered` from the event: `Orders/OutboundEventConsumersTest::test_real_pick_pack_dispatch_and_pod_events_walk_the_order_to_delivered` (test event); Job cost confirmed: `Platform/JobCostConsumerTest` | Not chained in one test (driver page → order status). "运费可结算" is not modelled: Billing has no `delivery.pod_captured` consumer (`BillingChargeConsumer::EVENTS` = task.completed, asn.putaway_completed, outbound.packed, shipment.quote_confirmed, delivery.extra_charge) and charges have no settleable state — freight charges are `pending` from quote confirmation and nothing changes at POD. Real e-mail delivery → manual |
| 5 | 第三方送达后,平台 POD 文件挂到订单并随邮件发给客户 | covered | `B8DeliveryTrackingTest::test_third_party_pod_is_archived_emitted_and_emailed` (operator uploads the carrier POD → client-visible document, `delivery.pod_captured` with `captured_by_type = carrier_api`, mail sent); client download: `Portal/PortalOrdersTest::test_client_downloads_only_its_own_pod_and_never_sees_internal_notes` | POD arrives by manual upload (no carrier API POD — CR #22) |
| 6 | 配送失败进异常列表可重派;等候费经 `delivery.extra_charge` 进入 Billing | partial | Transport: `B8DeliveryTrackingTest::test_driver_failure_updates_status_and_enters_the_shared_exception_centre` (exception + redelivery shipment), `::test_carrier_failure_updates_status_and_emits_delivery_failed`, `::test_extra_charge_form_emits_the_contract_payload_for_billing` (waiting 1.5 man_hour); `B7DriverPodTest::test_driver_reports_a_failed_attempt_with_the_exact_event`. Billing: `BillingChargeConsumer` is registered for `delivery.extra_charge`, rules `TR-WAITING` / `TR-REDELIVERY` / `TR-FAILED` in `BillingSeeder::RULES` | **Billing side does not work against the contract payload:** `TR-WAITING` has `quantity_source = hours_business` but the event carries `qty` / `uom` → qty 0 → skipped (`ChargeEngine::applyEvent`: `if ($qty <= 0) continue`); `TR-FAILED` condition is `charge_type = failed_delivery` while the enum / event value is `failed`; the idempotency template `extra:{delivery_id}:{charge_type}` names a key absent from the payload, so it renders `extra::redelivery` for every shipment (second redelivery charge returns the first). No test covers the Billing side. Waiting fees never reach `charges` today |
| 7 | 订单详情页显示"收 − 付 = 毛利" | partial | `B9aCarrierCostMarginTest::test_order_margin_uses_actual_cost_when_known_and_expected_cost_until_then` (`/transport/orders/{id}/margin`, client 403), `::test_own_fleet_is_booked_when_assigned_and_its_cost_is_entered_manually` (shipment page formula) | The order detail page (`orders::show`, X1) has no link or panel to the margin page (no `margin` reference in `app/Modules/Orders/views`; HANDOFF B9a note still open) |

## §6.8 Billing

| # | Criterion (short) | Status | Evidence (test / code) | Notes / gap |
|---|---|---|---|---|
| 1 | 真实价目表:九种计费单位可配置算对;34 行导入(Cartage 落运费项);POA 进待报价;托盘阈值 / 22–45 kg 分档 / 20 行上限 / 22.5 t 门槛可改,改后新费用按新参数、旧费用不变 | partial | `Billing/RateServiceTest::test_edward_standard_card_prices_every_uom` (container_40, pallet, carton bands, cbm with 1 CBM minimum, man_hour, POA, missing rate, `max_line_count` threshold); `Billing/BillingPagesTest::test_rate_card_versions_are_approval_gated_and_history_keeps_its_snapshot` (34 items per version; edit only on drafts; old charge snapshot unchanged); POA → review queue: `ChargeEngineTest::test_mixed_devanning_is_poa_and_a_missing_rate_raises_an_exception_without_a_charge`, `BillingPagesTest::test_manual_charges_review_queue_quotes_and_charge_codes_pages`. Code: `RateCardController::updateItem` accepts `threshold_json`, `weight_band_min/max`, `min_charge`, `is_poa`, `pallet_class`; Cartage as freight items = CR #7 (`TR-CARTAGE-20/40` on `shipment.quote_confirmed`); rates re-checked against the workbook (CR #48) | Not tested: pricing of every UOM (label, pickface_week, order, scan, container_20 not priced in a test — though charged in flow tests), editing pallet thresholds / bands / 20-line cap / 22.5 t via the page and pricing after the change, `TR-CARTAGE-*` weight POA |
| 2 | 40ft 拆柜:拆柜任务完成一条拆柜费,上架完成一条上架费,金额与价目表一致 | covered | `ChargeEngineTest::test_devanning_and_putaway_each_produce_exactly_one_charge_at_the_card_price` (280.00; 2 × 4.50; second dispatch run adds nothing) | — |
| 3 | 打包后订单处理费 + 分档拣货费;预订后运费(自派固定 / 第三方成本 × markup)与尾板费(仅一条) | covered | `ChargeEngineTest::test_outbound_packed_produces_despatch_and_banded_pick_charges_and_a_redo_reverses` (despatch + bands; urgent adds 15.00 per CR #5 / #48), `::test_quote_confirmed_charges_freight_tailgate_and_fuel_once_from_a_client_card` (cost_plus 100.00 × 1.2, tailgate once); `RateServiceTest::test_client_card_overrides_the_standard_card_and_prices_cost_plus` (default markup and carrier / service override) | Freight and tailgate arise at `shipment.quote_confirmed` (events.md), which in flow option B precedes booking; `shipment.booked` creates no charge |
| 4 | 周仓储费按托盘类型正确出账,可回查依据的每日快照 | covered | `Billing/StorageBillingTest` (standard / high classes, one charge per unit per ISO week from three daily snapshots, `source_activity_id = unit:{id}:week:2026-W37`, re-run idempotent, next week bills again, `billing:storage-weekly --week=`); `OutboundPrepPagesTest::test_snapshot_page_shows_the_day` | Charge → snapshot link is the week key, not snapshot row ids |
| 5 | 改价目表后历史账单金额不变 | covered | `BillingPagesTest::test_rate_card_versions_are_approval_gated_and_history_keeps_its_snapshot` (`rate_snapshot_cents` 450 / `amount_cents` 900 unchanged after v2 = 5.00) | — |
| 6 | 账单 PDF 含 GST,每行可点回源单据 | partial | `BillingPagesTest::test_per_job_invoice_from_the_unbilled_pool_to_pdf_payment_and_receivables` (PDF served, GST 2896 on 28960); `billing::invoices.pdf` prints per-line GST and subtotal / GST / total | "每行可点回源单据": `invoices/show.blade.php` line 44 links each line to `/billing?job_no=` (the charge list) and prints `source_type #source_id` as text — no link to the task / ASN / order / shipment page |
| 7 | 部分收款后客户未结余额正确 | covered | `BillingPagesTest::test_per_job_invoice…` (100.00 → `part_paid`, receivables 218.56; balance → `paid`, receivables empty) | — |
| 8 | 一个 `shipment.quote_confirmed` 同时产生基础运费、燃油、尾板三条费用行;`shipment.booked` 不产生费用 | covered | `ChargeEngineTest::test_quote_confirmed_charges_freight_tailgate_and_fuel_once_from_a_client_card` (`TR-DELIVERY-BASE` 12000, `TR-TAILGATE` 4500, `TR-FUEL` 1200; dispatched twice) | `shipment.booked` has no Billing rule or consumer, so no charge by construction (not asserted) |
| 9 | 缺 code 用绑定标准表兜底;标准表也缺或已解绑进 Missing Rate,不产生 $0;先打包、撤销、再打包只一条有效打包费 + 一条冲销 | covered | `RateServiceTest::test_client_card_overrides_the_standard_card_and_prices_cost_plus` (fallback; unbind → missing); `ChargeEngineTest::test_mixed_devanning_is_poa_and_a_missing_rate_raises_an_exception_without_a_charge` (exception, no row); `::test_outbound_packed_produces_despatch_and_banded_pick_charges_and_a_redo_reverses` (activity_version 2 → originals reversed + audit twins, net charged once) | Reversal semantics changed by CR #32: uninvoiced original and its twin are both `reversed`; only an invoiced original keeps a billable negative twin |
| 10 | 改价后旧费用行的费率快照不变 | covered | as #5 | — |
| 11 | 整柜客户一个 Job 单独一张发票;尾程客户月末多个 Job 合并一张,内按 Job 分组 | covered | `BillingPagesTest::test_per_job_invoice…` (draft for one Job); `::test_monthly_client_consolidates_jobs_and_credit_notes_need_a_second_person` (2 Jobs → `invoice_jobs` = 2, 6 lines, `invoice_type = monthly`) | Two Jobs merged, not three |
| 12 | per_job:打包实测后客户确认最终报价,系统按已存在费用一次生成服务发票;monthly 进未开票池月底汇总;三种账期到期日;过期标红;未收款不影响预订;财务锁;第 3 周三张周仓储发票;复称高出报价生成补充发票 | partial | Per-Job invoice from existing charges and monthly pool: `BillingPagesTest` (both tests); due dates: `MasterData/ClientCrudTest::test_due_dates_follow_payment_terms` (prepaid = issue day, eom = month end, net_30) + `BillingPagesTest` (net_14, eom on issue); only a manual hold blocks booking: `Transport/B9aCarrierCostMarginTest::test_only_a_manual_financial_hold_blocks_booking` (`ShipmentBookingService` has no invoice check). Code: `billing:flag-overdue` + `is_overdue` danger badge (`invoices/index.blade.php`); `InvoiceService::draftStorageWeek` + `POST /billing/invoices/storage` | (a) No automatic service invoice at final-quote confirmation — `InvoiceService::draftForJob` is a manual Finance action and nothing in Billing reacts to `shipment.quote_confirmed` other than charge rules. (b) Overdue flag: command and badge exist, no test. (c) Weekly storage invoices: drafting exists, no test, no automation — "第 3 周已有三张" needs three manual drafts. (d) Supplementary invoice after carrier re-weigh: `Transport/B9bCarrierInvoiceTest` updates `carrier_costs.actual / variance` only; no customer-side charge or invoice is generated (Finance would add a manual charge, then draft the Job again — the `draftForJob` docblock calls that "supplementary"). (e) prepaid / eom / net_14 as three issued invoices in one scenario — unit-level only |
| 13 | CHEP 2 托存 3 周 → 3 条租赁费(qty 2);仓库木托上架一次购买费;客户自带两者都不产生 | covered | `StorageBillingTest` (2 CHEP → 2 rental charges per week, billed again the next week; plain 1; client_own none); `ChargeEngineTest::test_devanning_and_putaway…` (`VAS-PALLET-PURCHASE` 25.00 for one warehouse_plain pallet at putaway; client_own none) | Granularity differs from the wording: one charge per pallet (qty 1 each) rather than one line qty 2; two weeks run, not three |

---

## §7 End-to-end script — what runs through code today

| Step | Executable through code? | Chained by | Where the chain breaks |
|---|---|---|---|
| 1 管理员登录,八角色与客户隔离 | yes | `PlatformSeeder` seeds `<role>@erp.local` for all 8 roles (CR #20); `Platform/AuthTest`, `Platform/UserManagementTest`, `Portal/PortalOrdersTest` | — (showing eight logins is manual) |
| 2 真实清单导入为 40ft 拆柜 ASN 货物行(含 FBA) | yes | `Warehouse/InboundPagesTest::test_import_creates_lines_from_the_real_manifest_parser_and_records_the_import` (135 lines; the workbook's 5 incomplete rows show as 10 errors and the import status is `failed` although lines are created) | Explain the error rows in the demo |
| 3 收货(录差异)→ 上架 → 库存与流水 → 一键按唛头生成订单 | yes | `InboundPagesTest::test_receive_then_put_away…`, `Warehouse/AsnOrderGenerationTest`, `Warehouse/OutboundPagesTest::test_generate_orders_button_on_the_asn_page` | Real-file chain not tested end to end (§4.7 #17) |
| 4 确认 → 关联批次 → 波次 → 拣货 → 打包实测 → 最终报价(对比初步)→ 客户确认 → 服务发票 → 收款 | up to the final quote | `Warehouse/OutboundFlowTest`, `Transport/M4TransportIntegrationTest` (real packages → final own-fleet + manual quote → select → book → dispatch), `Transport/B5dQuoteSelectionTest::test_final_quote_above_tolerance_waits_for_reconfirmation` / `…within_tolerance…`; payment: `Billing/BillingPagesTest` | Client confirmation exists only at service level (no portal page, A7b); the service invoice is drafted manually by Finance, not generated by the confirmation (§6.8 #12a). `M4TransportIntegrationTest` pins `FakeRateService` for the own-fleet price — on the seeded Edward card `TR-DELIVERY-BASE` is a Missing Rate until a demo client card carries it (CR #47) |
| 5 预订(25 kg 自动尾板,尾板费在报价确认时一次)→ 打 label → 编班次 → 司机签收;monthly 客户只进未开票池;第三个客户财务锁 → 不能预订 → 放行 | yes, with seed prerequisite | `Transport/B9aCarrierCostMarginTest` (booking; hold blocks / release), `Transport/B6ShipmentLabelTest`, `Transport/B5bDeliveryRunTest`, `Transport/B7DriverPodTest`, `Billing/ChargeEngineTest` (tailgate once), `BillingPagesTest` (monthly pool) | Needs a client rate card with `TR-DELIVERY-BASE` / `TR-TAILGATE` / `TR-FUEL` (no Edward rows); tailgate chain not tested end to end (§3.8 #10) |
| 6 客户自动收到附 POD 的邮件 | yes (mail faked) | `Transport/B8DeliveryTrackingTest::test_driver_delivery_updates_transport_status_and_emails_the_pod` (`DeliveryPodMail` to `clients.contact_email`, one attachment) | Real mail transport → manual |
| 7 charges 逐条可溯源:拆柜、上架、订单处理、分档拣货、运费、尾板、周仓储 | yes | `Billing/ChargeEngineTest`, `Billing/StorageBillingTest`, Job page charge panel (`BillingPagesTest::test_manual_charges…`) | Traceability is data (`source_type / source_id / source_activity_id`); pages show it as text, not links (§6.8 #6) |
| 8 客户门户下载含 GST 的服务发票与本周仓储发票 PDF,自查订单与库存 | no | Invoice PDFs are stored as client-visible documents (`BillingPagesTest`) and `GET /portal/documents/{id}` would serve them via `DocumentDownloader` | Portal has no invoice list and no stock view (`app/Modules/Portal/routes.php`: orders, returns, documents only); weekly storage invoice drafting untested (§6.8 #12c) |
| 9 该柜的 Job 工作台:全部订单、发货进度、单据、费用、成本与毛利一页 | partly | `Platform/JobWorkbenchTest`, `Platform/JobCostConsumerTest`, charge panel | Orders / ASN / stock / shipments / documents panels are placeholders (§2.5 #6) |
| 10 整柜客户按 Job 开票;尾程客户三个 Job 合并;复称高出报价生成补充发票;人为事件消费失败重试 | partly | `BillingPagesTest` (per Job; monthly merge of 2 Jobs); `Platform/OutboxTest` (failure, backoff, dead, retry from monitor) | Supplementary invoice is manual only (§6.8 #12d); no Billing-specific failure / retry test (§2.5 #8) |
| 11 异常中心、审计日志与审批记录 | yes | `Platform/ExceptionCentreTest`, `Platform/SearchApprovalActivityTest` (`/admin/activity`, `/admin/approvals`) | Rate-item audit missing (§2.5 #4) |

---

## Gaps and partials (ordered by what blocks a demo first)

| # | Gap / partial | Criteria | Suggested fix (one line) | Seat |
|---|---|---|---|---|
| 1 | Job workbench panels (orders, ASN, stock, shipments, documents) are M0 placeholders | §2.5 #6, §7 step 9 | `JobController::show` loads the module models by `job_id` (Orders, Asns, StockUnits, Shipments, Documents) and `platform::jobs.show` renders them with links | C |
| 2 | Portal has no invoice list / storage-invoice download and no stock self-check | §7 step 8, §3.8 #5 | Add `/portal/invoices` (client-visible invoice documents through `DocumentDownloader`) and `/portal/stock` (client-scoped `stock_units` / `StockService::onHand`) | X1 |
| 3 | Reports module is a placeholder | §2.5 #5 | A21 boss report (order count, on-time % from `requested_date` vs `delivered_at`, margin from `jobs`) + client report under `ClientScope`; A22 scheduled send | X1 |
| 4 | `delivery.extra_charge` never bills: `TR-WAITING` reads `hours_business` (payload has `qty`), `TR-FAILED` condition `failed_delivery` ≠ `failed`, idempotency template `{delivery_id}` not in payload | §5.7 #6 | Fix `BillingSeeder::RULES` (quantity source for `qty` / `uom = man_hour`, condition `failed`, template `extra:{shipment_id}:{charge_type}`) and add a `ChargeEngineTest` for the three codes | C |
| 5 | No client-facing quote confirmation page | §5.7 #2, §7 step 4 | A7b portal page listing final quotes → `QuoteSelectionService::select(..., 'client', userId)` | X1 (service by X2 exists) |
| 6 | Service invoice is not generated when the client confirms the final quote (per_job) | §6.8 #12, §7 step 4 | Billing consumer on `shipment.quote_confirmed` for `invoice_mode = per_job` → `InvoiceService::draftForJob` (draft; Finance issues), or record that Finance drafts manually | C |
| 7 | Order `billing_status` never moves when an invoice is issued | §3.8 #3 | Emit an `invoice.issued` event (contract change) consumed by Orders → `transitionBilling('billed')` for the invoiced Jobs' orders | C (contract) + X1 |
| 8 | No TR-* rates in seed data (`TR-DELIVERY-BASE`, `TR-TAILGATE`, `TR-FUEL` are Missing Rate on the Edward card) | §3.8 #10, §6.8 #3, §7 steps 4–5 | Demo seeder: a client rate card with these items (cost_plus / fixed / percent) for the demo clients | C |
| 9 | Supplementary invoice after carrier re-weigh is manual only | §6.8 #12, §7 step 10 | On `carrier_invoice_lines.variance_cents > 0` for a cost_plus shipment emit an extra-charge event (`charge_type = other`) so Billing creates a `needs_review` adjustment and Finance drafts the Job again | X2 emit / C bill |
| 10 | Rate item changes are not in the audit log | §2.5 #4 | `LogsActivity` on `RateItem` (rate_cents, min_charge_cents, threshold_json, weight bands, is_poa) or `activity()` in `RateCardService::updateItem/addItem` | C |
| 11 | Pick short has no Coordinator decision (auto partial ship) | §4.7 #10 | Hold the fulfilment at the `pick_short` exception until the coordinator chooses partial / backorder / cancel (or document the automatic partial rule in the plan) | C |
| 12 | Weekly storage invoices untested and not automated | §6.8 #12c, §7 step 8 | Test `InvoiceService::draftStorageWeek`; optionally schedule a Monday draft per client after `billing:storage-weekly` | C |
| 13 | Unload / load / labour / scan / wrap / waste charges have rules but no Billing test; unload rule lacks the `loose_truck` condition | §4.7 #15 #23 #24 | One `ChargeEngineTest` per rule from the real `task.completed` payloads; add `inbound_type` to the receiving task payload and the rule condition | C |
| 14 | No packing list document, so serial numbers are not printed | §4.7 #24 | Warehouse packing-list PDF per fulfilment listing packages and `scan_records` serials | C |
| 15 | Order detail page has no "收 − 付 = 毛利" link / panel | §5.7 #7 | Staff-only link to `route('transport.orders.margin', $order->id)` on `orders::show` | X1 |
| 16 | Batch view (by container / ASN) shows revenue only, no margin | §3.8 #9 | Subtract `carrier_costs` (actual else expected) of the orders' shipments, staff only | X1 |
| 17 | Overweight / beyond-band pallets are not queued for quoting at receiving | §4.7 #19 | Raise a `billing_hold` (or POA) exception / `needs_review` quote line when `pallet_class` is `overweight` or null | C |
| 18 | Pure-transport order → Transport list not asserted | §3.8 #6 | Test: `pickup_deliver` order confirmed → `shipments` row `quoting`, visible on `/transport` | X2 |
| 19 | Billing-specific consumer failure / retry not tested | §2.5 #8 | Test: make `BillingChargeConsumer` throw once, retry from `/admin/integration`, assert exactly one charge | C |
| 20 | Tailgate not re-evaluated from measured package weights | §4.7 #5 | Either document OMS-13 (decision at order confirmation) as the accepted reading or re-check on `outbound.packed` in Transport | X2 |
| 21 | Real workbook not driven through ASN → orders in one test; 20-order wave not exercised | §4.7 #17, #4 | Extend `AsnOrderGenerationTest` with the real workbook (30 marks) and a multi-order wave | C |

## Manual checks for demo day

1. **Cron / scheduler** — nothing moves without `php artisan schedule:work` (or cron `schedule:run`): `outbox:dispatch` every minute carries every cross-module event; `stock:snapshot` 23:55; `billing:storage-weekly` Monday 01:00; `billing:flag-overdue` 06:00; `stock:reconcile` 02:00; `transport:sync-tracking` every 30 min. For a same-day demo run `outbox:dispatch`, `stock:snapshot --date=…`, `billing:storage-weekly --week=…` by hand.
2. **Seed data** — demo users `<role>@erp.local` (password from `SEED_DEMO_PASSWORD`); a per_job container client, a monthly last-mile client and a third client for the financial hold; a client rate card carrying `TR-DELIVERY-BASE` (cost_plus), `TR-TAILGATE` (fixed), `TR-FUEL` (percent) — without it freight and tailgate become Missing Rate exceptions.
3. **Scan gun** on receiving / putaway / stocktake forms (keyboard wedge into the `.scan` inputs) and **phone camera** on `/warehouse/scan` (native `BarcodeDetector` — Chrome on Android; not available in iOS Safari). Picking has no label verification scan.
4. **Driver mobile page** (`/driver`) on a real phone: signature canvas, photo upload, tailgate warning; then the client's POD e-mail arrives (needs `MAIL_*` config and `clients.contact_email`).
5. **Karrio** — for live third-party quotes / labels / tracking start `docker/karrio`, set `KARRIO_API_KEY` and a carrier connection, run `KARRIO_LIVE=1 php artisan test --filter KarrioLiveTest`; otherwise only Manual and own-fleet paths.
6. **Forced Billing consumer failure** (§2.5 #8): e.g. temporarily deactivate the client's standard card binding so the consumer raises Missing Rate, or mark the outbox row `failed` and use the retry button on `/admin/integration`; confirm one charge and one `integration_failed` / `missing_rate` exception.
7. **Real workbook import** shows 10 errors on 5 incomplete rows and the import status `failed` while 135 lines are created — decide how to present it.
8. **Exception centre** with all three types visible together (receiving discrepancy, delivery failure, Missing Rate) — create each once before the walkthrough.
9. **PDFs** (dompdf) — invoice, consignment note, labels, POD: check Chinese text rendering and printer output; Pico.css loads from CDN, so the demo machine needs internet.
10. **Portal walk-through** as a client user: orders list / detail / POD download work; invoices and stock are not reachable from the portal (gap #2).


## Follow-up (C, 2026-09-08, block/c13-audit-fixes)

Delivered after this audit: Job workbench panels (gap #1), `delivery.extra_charge` billing rules (#4), per_job service invoice draft at final-quote confirmation (#6), `invoice.issued` event + Platform Job revenue consumer (#7 — Orders' `billed` consumer requested from X1), unload rule limited to loose trucks (#14), `RateItem` audit log (#10). Demo data for #8 comes from `DemoFlowSeeder` (Edward client card with TR-* items). Open for X1: portal quote confirmation, portal invoices / stock, reports (block/x1-quotes-reports). Open for X2/C: supplementary invoice from carrier re-weigh variance (#9), pick-short coordinator decision (#11).
